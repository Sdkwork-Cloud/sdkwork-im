# Deferred PostgreSQL migrations

SQL files here are **not** applied by `pnpm db:postgres:migrate`.

Phase 0 uses single-table truth; partitioning (`013`) is Phase 2 expand-contract work.

See `docs/superpowers/specs/2026-06-16-im-phase0-approach-a-design.md`.
