apiVersion: apps/v1
kind: Deployment
metadata:
  name: streaming-service
  namespace: sdkwork-im
  labels:
    app.kubernetes.io/name: streaming-service
    app.kubernetes.io/component: streaming
    app.kubernetes.io/part-of: sdkwork-im
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: streaming-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: streaming-service
        app.kubernetes.io/component: streaming
    spec:
      containers:
        - name: streaming-service
          image: ghcr.io/sdkwork/streaming-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 18084
          envFrom:
            - configMapRef:
                name: streaming-service-runtime
            - secretRef:
                name: streaming-service-secrets
                optional: true
          env:
            - name: SDKWORK_IM_ENVIRONMENT
              value: production
            - name: SDKWORK_IM_DEPLOYMENT_PROFILE
              value: cloud
            - name: OTEL_SERVICE_NAME
              value: sdkwork-im-streaming-service
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              cpu: 200m
              memory: 384Mi
            limits:
              cpu: "1"
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: streaming-service
  namespace: sdkwork-im
  labels:
    app.kubernetes.io/name: streaming-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: streaming-service
  ports:
    - name: http
      port: 18084
      targetPort: http
