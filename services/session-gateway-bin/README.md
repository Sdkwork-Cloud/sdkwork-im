# session-gateway-bin

Thin binary runner for `session-gateway`. The library crate owns handlers and domain routers; this package wires `sdkwork-routes-im-realtime-open-api` for web-framework wrapping at process startup.
