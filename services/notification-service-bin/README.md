# notification-service-bin

Thin binary runner for `notification-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-router-im-notification-app-api` for web-framework wrapping at process startup.
