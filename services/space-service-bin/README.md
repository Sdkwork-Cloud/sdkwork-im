# space-service-bin

Thin binary runner for `space-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-router-im-space-open-api` for web-framework wrapping at process startup.
