# audit-service-bin

Thin binary runner for `audit-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-routes-im-audit-backend-api` for web-framework wrapping at process startup.
