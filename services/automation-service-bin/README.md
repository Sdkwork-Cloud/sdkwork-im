# automation-service-bin

Thin binary runner for `automation-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-router-im-automation-app-api` for web-framework wrapping at process startup.
