# media-service-bin

Thin binary runner for `media-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-router-im-media-app-api` for web-framework wrapping at process startup.
