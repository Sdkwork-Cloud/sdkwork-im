# comms-social-service-bin

Thin binary runner for `social-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-router-im-social-*` route crates for web-framework wrapping at process startup.
