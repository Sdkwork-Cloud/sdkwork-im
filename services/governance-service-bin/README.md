# governance-service-bin

Thin binary runner for `governance-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-router-im-governance-backend-api` for web-framework wrapping at process startup.
