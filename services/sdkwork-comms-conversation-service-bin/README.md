# sdkwork-comms-conversation-service-bin

Thin binary runner for `sdkwork-comms-conversation-service`. The library crate owns handlers and domain routers; this package wires `sdkwork-routes-im-chat-open-api` for web-framework wrapping at process startup.
