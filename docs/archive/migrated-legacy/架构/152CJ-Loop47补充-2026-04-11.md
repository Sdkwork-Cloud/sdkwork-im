# 152CJ Loop47补充 - 2026-04-11

- `conversation-runtime` 新增 `shared-external history runtime truth`
  - `history_visibility = shared` 已进入真实 domain/runtime/HTTP 合同
  - external-linked 成员通过显式 shared/external 锚点进入 `MembershipState::Linked`
  - `Linked` 成员可读历史，但不被视为 generic active member
- As-Built 边界明确为：
  - `joined / world_readable / invited / shared` 都已是当前 runtime truth
  - 其中 `shared` 只覆盖“显式锚点 + 历史读取”这条最小语义
  - `shared_channel_policy -> runtime` 自动投影、shared fanout / rebuild / retention 仍是 future truth
- `S07` 当前口径保持 `not_closed / local_closure`
  - 已闭合：`reaction / pin`、`direct_chat -> conversation`、`thread minimal runtime truth`、`invited history visibility`、`shared-external history`
  - 仍待闭合：`retention enforcement`、`thread subscription / notification`
