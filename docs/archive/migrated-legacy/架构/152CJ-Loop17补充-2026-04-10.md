# 152CJ Loop-17 补充 - 2026-04-10

## As-Built 增量
- conversation 聚合已具备 `ConversationPolicy / policy_epoch`
- runtime 已新增 `conversation.policy_applied`
- policy 可经 HTTP 建会请求直接绑定
- `capability_flags` 已真实约束 `message.reaction / message.pin`

## 未闭环项
- 当前没有稳定历史消息 `GET` 读面
- `history_visibility` 尚未进入真实读路径
- `retention_policy_ref` 尚未进入 archive/restore/retention owner 闭环

## 架构判断
- `S09` 获得更强的 `local_closure`
- `S09` 仍不是 `step_closure`
- `S10` 阻塞不变，但阻塞原因已更精确：
  - 不只是“缺 S09”
  - 而是“缺读路径 policy enforcement 与 retention owner 闭环”
