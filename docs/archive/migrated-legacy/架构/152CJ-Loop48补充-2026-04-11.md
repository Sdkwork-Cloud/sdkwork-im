# 152CJ Loop48补充 - 2026-04-11

- `conversation-runtime` 新增 `retention enforcement runtime truth`
  - `retention_policy_ref` 不再只是 payload 字段
  - `conversation.policy_applied` 会把 `tenant.compliance` 之类 policy ref 派生为 commit `retention_class = compliance`
  - policy 生效后的 `conversation/message mutation` commit 与 replay 后新消息会沿用同一 retention class
- As-Built 边界明确为：
  - 当前只闭合 `runtime commit metadata propagation`，不等于 `archive / restore / legal-hold owner` 已落地
  - `retention_policy_ref -> archive / restore / retention owner` 仍属 `S13` 边界
- `S07` 当前口径保持 `not_closed / local_closure`
  - 已闭合：`reaction / pin`、`direct_chat -> conversation`、`thread minimal runtime truth`、`invited history visibility`、`shared-external history`、`retention enforcement runtime truth`
  - 仍待闭合：`thread subscription / notification`、`shared fanout / rebuild`

