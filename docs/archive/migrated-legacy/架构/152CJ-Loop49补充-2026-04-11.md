# 152CJ Loop49补充 - 2026-04-11

- `conversation-runtime` 新增 `thread root-author auto-subscription runtime truth`
  - thread create path 不再只写 creator owner
  - 当 root message author 与 creator 不同且仍是 parent active member 时，runtime 会把 root author 一并 materialize 为 thread active member
  - auto-subscribed member 会保留 `parentConversationId / rootMessageId / threadRole = root_author` 并拥有默认 read cursor
  - recovery replay 后仍保留该 member truth，使现有 projection / notification fanout 可以直接消费 thread active principals
- As-Built 边界明确为：
  - 当前只闭合 `root author subscription -> active member -> notification target` 最小链路
  - 不等于 `im_thread_subscription` durable model、follow/unfollow、per-user unread / notification level 已落地
- `S07` 当前口径保持 `not_closed / local_closure`
  - 已闭合：`reaction / pin`、`direct_chat -> conversation`、`thread minimal runtime truth`、`thread root-author subscription / notification runtime truth`、`invited history visibility`、`shared-external history`、`retention enforcement runtime truth`
  - 仍待闭合：`shared fanout / rebuild`
  - 更强 `im_thread_subscription` durable model / per-user unread-notification level 继续 deferred
