# S05 Loop33补充 - 2026-04-10

## 1. 本轮主任务
- 在 runtime-dir 模式落地 social startup journal replay，并用 snapshot missing TDD 锁定恢复行为。

## 2. 已兑现事实
- `SocialControlRuntime::from_runtime_dir(...)` 启动时先 load snapshot，再 replay `state/social-commit-journal.json`。
- replay 对已存在 `event_id` 幂等跳过；snapshot 缺失、落后或 load 失败且 journal 可读时，会重建 truth 并回写 `social-state.json`。
- replay 已覆盖当前已落地 social event：`friend_request.submitted`、`friendship.activated`、`user_block.blocked`、`direct_chat.bound`、`external_connection.established`、`external_member_link.bound`、`shared_channel_policy.applied`。
- `friend_request.requestMessage` 与 `user_block.expiresAt` 已补入 journal payload；旧 journal 缺字段时 replay 降级为 `None`。
- TDD 新增并保持通过：
  - `test_control_plane_social_file_runtime_replays_friend_request_when_snapshot_is_missing`
  - `test_control_plane_social_file_runtime_replays_direct_chat_pair_guard_when_snapshot_is_missing`

## 3. 仍未关闭
- 无 `tx boundary`
- 无 `crash consistency / replay-based repair`
- `S05 = not_closed / local_closure`

## 4. 当前结论
- Loop33 已关闭“无 journal replay”缺口；`S05` 下一唯一主缺口收敛为事务边界与修复链路。

