# Step 05 CP05-4 local-minimal-node device registration owner 执行补充 - 2026-04-07

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么继续做这个子项
  - `session-gateway` 的 device registration seam 已经从 `AppState/lib.rs` 收口，但 repo 级 `CP05-4` 仍有一条真实残余 seam 留在 `services/local-minimal-node/src/node/access.rs`。
  - 只要 `access.rs` 还同时承担 access/auth 与 device registration / route bind 编排 owner，`local-minimal-node` 的 session、message、projection、effects 路径就仍然停留在 edge glue，`CP05-4` 不能闭环。
- 本轮实际完成
  - 新增 `services/local-minimal-node/src/node/device_registration.rs`
    - `LocalNodeDeviceRegistration`
    - `new(...)`
    - `ensure_registered_device(...)`
    - `bind_registered_device(...)`
    - `ensure_route_session_current(...)`
  - `services/local-minimal-node/src/node.rs`
    - 新增 `mod device_registration;`
    - `AppState` 持有 `device_registration: LocalNodeDeviceRegistration`
    - `AppState` 新增 `require_registered_device_binding(...)` 与 `bind_device_registration(...)` 委托边界
  - `services/local-minimal-node/src/node/build.rs`
    - `build_app_with_dependencies_and_runtime_and_journal(...)` 改为装配共享 `node_id / realtime_cluster / session_presence_runtime / realtime_runtime / projection_service`
  - `services/local-minimal-node/src/node/access.rs`
    - 移除 `ensure_route_session_current(...)` 与 `bind_registered_device(...)` 的事实 owner 实现
    - `ensure_registered_device(...)` 退化为薄委托
  - `services/local-minimal-node/src/node/session.rs`
    - session/device 入口改为消费 `AppState::bind_device_registration(...)`
- 本轮验证
  - Red
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_device_registration_owner_moves_out_of_access_impl --offline --target-dir target-cp054o-red-device-registration`
  - Green / Regression
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_device_registration_owner_moves_out_of_access_impl --offline --target-dir target-cp054o-green-device-registration`
    - `cargo test -p local-minimal-node --test lib_structure_test --offline --target-dir target-cp054o-green-local-structure-full`
    - `cargo test -p local-minimal-node --offline --target-dir target-cp054o-reg-local-minimal-node`
    - `rustfmt --edition 2024 services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/build.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/src/node/device_registration.rs`
    - `rustfmt --edition 2024 --check services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/build.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/src/node/device_registration.rs`
- 当前判断
  - 本轮是 `CP05-4` 的第十五个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞
  - repo 级搜索仍显示 `services/session-gateway/src/lib.rs`、`services/session-gateway/src/session.rs`、`services/session-gateway/src/websocket_route.rs` 保留重复 `ensure_route_session_current(...) + register_device(...)` 入口 glue，属于下一条高概率 blocker
