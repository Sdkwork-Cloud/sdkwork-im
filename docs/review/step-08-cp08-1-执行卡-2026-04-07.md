# Step 08 / CP08-1 执行卡 - 2026-04-07

## 当前上下文
- 当前波次：`Wave C`
- 当前 step：`Step 08`
- 当前子任务：`CP08-1`
- 前置状态：
  - `Step 07` 已闭环，`Wave C / 93` 仍等待 `Step 08 / Step 09`
  - `Step 08` 尚无已闭环检查点
  - `crates/craw-chat-contract-agent` 与 `crates/craw-chat-contract-iot` 仅有执行记录 / twin 存储契约，尚未形成统一主体模型

## 本轮为什么做这个子任务
- `docs/step/08-AI-Agent-IoT统一扩展层落地.md` 将 `CP08-1` 定义为 `agent` / `device` 契约与主体模型落地。
- `docs/架构/134-AI-Agent-IoT统一实时通信模型设计-2026-04-06.md` 明确要求 `user / agent / device / bot / system` 进入统一 actor / sender 结构。
- 如果 `agent` / `device` 仍停留在各自孤立契约里，后续 `CP08-2` 的 Agent 流式输出与 `CP08-3` 的设备上行/下行链路都会继续绕开统一 sender 语义。

## 本轮实际完成

### 1. 补齐 Agent 主体契约
- 在 `craw-chat-contract-agent` 新增：
  - `AgentSubject`
  - `AgentSubjectRecord`
  - `AgentSubjectStore`
- `AgentSubject::sender(...)` 现在可以把 Agent 主体物化为统一 `Sender` 快照，稳定输出：
  - `id`
  - `kind = "agent"`
  - `member_id`
  - `session_id`
  - `metadata`

### 2. 补齐 Device 主体契约
- 在 `craw-chat-contract-iot` 新增：
  - `DeviceSubject`
  - `DeviceSubjectRecord`
  - `DeviceSubjectStore`
- `DeviceSubject::sender(...)` 现在可以把设备主体物化为统一 `Sender` 快照，稳定输出：
  - `id = device_id`
  - `kind = "device"`
  - `member_id`
  - `device_id`
  - `session_id`
  - `metadata`

### 3. 平台契约 facade 同步导出
- `im-platform-contracts` 已重新导出：
  - `AgentSubject`
  - `AgentSubjectRecord`
  - `AgentSubjectStore`
  - `DeviceSubject`
  - `DeviceSubjectRecord`
  - `DeviceSubjectStore`
- 这让后续 Step 08 的平台层验证可以继续复用统一 facade，而不需要直接绑死到 crate 内部路径。

### 4. 为 `CP08-1` 增加红绿验证
- 新增测试：
  - `crates/im-platform-contracts/tests/agent_device_subject_model_test.rs`
- 测试覆盖：
  - `AgentSubject::sender(...)` 的统一 sender 物化
  - `DeviceSubject::sender(...)` 的统一 sender 物化
  - agent/device in-memory subject store 的 save/load 语义
  - `im-platform-contracts` facade re-export 是否完整

## 改动范围
- 代码：
  - `crates/craw-chat-contract-agent/src/lib.rs`
  - `crates/craw-chat-contract-iot/Cargo.toml`
  - `crates/craw-chat-contract-iot/src/lib.rs`
  - `crates/im-platform-contracts/src/lib.rs`
- 测试：
  - `crates/im-platform-contracts/tests/agent_device_subject_model_test.rs`

## TDD 证据

### Red
- `cargo test -p im-platform-contracts --test agent_device_subject_model_test --offline --target-dir target-step08-cp081-red-subject-model`

失败与预期一致：
- `AgentSubject`
- `AgentSubjectRecord`
- `AgentSubjectStore`
- `DeviceSubject`
- `DeviceSubjectRecord`
- `DeviceSubjectStore`
- `im-platform-contracts` 对应 re-export

在本轮实现前均不存在。

### Green
- `cargo test -p im-platform-contracts --test agent_device_subject_model_test --offline --target-dir target-step08-cp081-green-subject-model`

## 回归验证
- `cargo test -p craw-chat-contract-agent --offline --target-dir target-step08-cp081-contract-agent`
- `cargo test -p craw-chat-contract-iot --offline --target-dir target-step08-cp081-contract-iot`
- `cargo test -p im-platform-contracts --offline --target-dir target-step08-cp081-platform`
- `cargo fmt -p craw-chat-contract-agent -p craw-chat-contract-iot -p im-platform-contracts -- --check`

## 结论
- `CP08-1`：本轮已闭环
- `Step 08`：仍未闭环，后续必须继续完成：
  - `CP08-2` Agent 流式输出与 tool call 生命周期
  - `CP08-3` 设备接入与 telemetry / command 主路径
  - `CP08-4` Agent / Device 对统一 sender / actor / capability 机制的复用收口

## 当前还差什么
- `agent` 还未进入流式输出、tool call、handoff 事件生命周期
- `device` 还未进入 session / presence / telemetry / command 主链路
- `Step 08` 级别的 `91 / 95 / 97` 不能提前判定通过
- `Wave C / 93` 继续阻塞于 `Step 08 / Step 09`

## 下一轮继续做什么
1. 进入 `Step 08 / CP08-2`
2. 先用最小红灯测试冻结 Agent 流式输出与 tool call 生命周期
3. 继续要求 Agent 主路径复用本轮落地的统一主体 / sender 语义，而不是再造独立事件主体模型
