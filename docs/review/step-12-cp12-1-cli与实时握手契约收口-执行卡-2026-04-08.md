# Step 12 / CP12-1 CLI 与实时握手契约收�?执行�?- 2026-04-08

## 当前上下�?
- 当前波次：`Wave D`
- 当前 step：`Step 12`
- 当前子任务：`CP12-1`
- 前置状态：
  - `Step 11` 已闭环，`self-hosted.split-services.development` �?quant / HA / DR / rollback 基线可复�?
  - `tools/chat-cli` �?HTTP authority model 文档�?contract test 已建�?
  - 当前真实阻塞收敛到：`watch` / `chat-session` �?public realtime 主链路上无法稳定拿到首帧 `realtime.connected`

## 本轮为什么做这个增量
- `docs/step/12-SDK-CLI与兼容矩阵收�?md` 明确要求 `CLI` 真实消费当前协议与权威字段模型，不能只停留在 help、HTTP 命令和静�?README�?
- 现有 `chat_cli_e2e_test` 暴露的不�?sleep 脆弱性，而是协议协商与运行时握手之间的真实漂移：
  - runtime `hello_ack` 默认只协�?`payload.json`
  - websocket 热路径却仍然强制等待 `session_resume`
  - 结果�?`watch` / `chat-session` �?public app 上卡死在 `realtime.connected` 之前

## 本轮实际完成

### 1. 已用更小的失败测试锁定真实根�?
- 新增 / 更新聚焦测试�?
  - `tools/chat-cli/src/realtime.rs`
    - `test_connect_realtime_socket_completes_session_resume_before_connected_frame`
    - `test_connect_realtime_socket_reads_connected_frame_from_local_public_app`
  - `services/session-gateway/tests/websocket_smoke_test.rs`
    - `test_realtime_websocket_skips_session_resume_when_capability_not_negotiated`
- 这些测试把问题从“CLI E2E 超时”收敛到单一协议边界�?
  - `hello_ack.capabilities` 未协�?`session.resume` 时，运行时不应继续阻�?`session_resume`

### 2. 已修�?runtime hello 协商�?websocket 握手的一致�?
- 更新：`services/session-gateway/src/websocket.rs`
- 当前行为已改为：
  - 仅当 `hello_ack.capabilities` 真实协商�?`session.resume` 时，才继续等�?`session_resume -> session_resumed`
  - 若当�?`effective snapshot` 未协商该能力，则直接�?checkpoint 进入 `realtime.connected`
- 这修复了控制面快照与热路径语义不一致的实际偏差�?

### 3. 已让 CLI 按协商消费恢复握手，而不是硬编码猜测
- 更新：`tools/chat-cli/src/realtime.rs`
- 当前行为已冻结为�?
  - `hello_ack` 协商�?`session.resume` 时，CLI 发�?`session_resume`
  - 未协商该能力时，CLI 直接等待首个 business frame
  - 同时保留 future capability path 的回归保�?

### 4. 已补�?CLI realtime 可诊断性与真实闭环验证
- 更新：`tools/chat-cli/src/lib.rs`
- `watch` 失败上下文现在会明确指出卡在�?
  - `realtime.connected`
  - `subscriptions.synced`
  - `event.window / events.acked`
- 当前 fresh 结果�?
  - `chat_cli_contract_test`：通过
  - `chat_cli_e2e_test`�? / 3 通过
  - `session-gateway websocket_smoke_test`�?0 / 10 通过

### 5. 已把 Step 12 CLI 指南回写为当前真实口�?
- 更新：`docs/部署/CLI聊天验证与兼容矩�?md`
- 新口径已明确�?
  - authority fields 仍只通过 bearer token �?`auth_bind`
  - `session_resume` 是“按协商启用”的能力，不再被文档误写成当前默认必经步�?
  - `services/session-gateway/tests/websocket_smoke_test.rs` �?`tools/chat-cli/src/realtime.rs` 现已纳入验证资产

## TDD / Red-Green 证据

### Red
- `cargo test -p sdkwork-im-cli --offline --test chat_cli_e2e_test test_chat_cli_can_drive_two_party_http_and_websocket_validation_flow -- --nocapture`
  - 初始失败：`failed while waiting for realtime.connected: timed out waiting for realtime frame`
- `cargo test -p sdkwork-im-cli --offline test_connect_realtime_socket_reads_connected_frame_from_local_public_app -- --nocapture`
  - 初始失败：public app 真实主链路无法返回首�?`realtime.connected`
- `cargo test -p session-gateway --offline test_realtime_websocket_skips_session_resume_when_capability_not_negotiated -- --nocapture`
  - 初始失败：websocket 在未协商 `session.resume` 时仍超时等待下一控制�?

### Green
- runtime 按协商条件化 `session_resume` 要求后，同一�?public app / CLI / wrapper 测试全部转绿
- 本轮没有通过加大 sleep 或跳�?ACK 伪造结果，而是修正了协议协商与热路径实现的真实边界

## Fresh 验证
- `cargo test -p session-gateway --offline --test websocket_smoke_test -- --nocapture`
- `cargo test -p sdkwork-im-cli --offline --test chat_cli_contract_test -- --nocapture`
- `cargo test -p sdkwork-im-cli --offline --test chat_cli_e2e_test -- --nocapture`
- `cargo fmt --all --check`

## 当前判断
- `CP12-1`：闭�?
- 已兑现：
  - CLI HTTP authority model �?realtime 主链路现在可共同通过真实 public app 验证
  - `watch` / `chat-session` / PowerShell wrapper 都已消费当前 `payload.json` 基线
  - future `session.resume` 路径已有回归保护，不再依赖临时猜�?
- 当前仍未兑现但不阻塞 `CP12-1` 关闭�?
  - `CP12-2` �?SDK README / facade 边界
  - `CP12-3` �?compatibility matrix 文档 / 测试 / 控制面映射整�?
  - `CP12-4` 的多终端聊天与流式验证脚本整步收�?

## 下一轮继续做什�?
1. 进入 `Wave D / Step 12 / CP12-2`
2. 收口 `sdks/sdkwork-im-sdk` �?`sdks/sdkwork-control-plane-sdk` �?README �?facade 边界
3. 继续保持 `CP12-1` 已冻结的 CLI / websocket 协商语义不漂�?
