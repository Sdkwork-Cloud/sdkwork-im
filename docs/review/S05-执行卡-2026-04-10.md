# S05 执行卡 - 2026-04-10

- 当前增量以 `docs/review/S05-Loop31补充-2026-04-10.md`、`docs/review/S05-Loop32补充-2026-04-10.md`、`docs/review/S05-Loop33补充-2026-04-10.md`、`docs/review/S05-Loop34补充-2026-04-10.md`、`docs/review/S05-Loop35补充-2026-04-10.md`、`docs/review/S05-Loop36补充-2026-04-10.md`、`docs/review/S05-Loop37补充-2026-04-10.md`、`docs/review/S05-Loop38补充-2026-04-11.md` 为准；Loop31 落地 `file-backed snapshot + social outbox`，Loop32 完成主架构真相对齐，Loop33 补齐 startup replay，Loop34 收敛到 `journal authority + journal-first persistence`，Loop35 修复 `snapshot save fail` 后的 stale live-state，Loop36 建立 `tenant-scoped same-event retry/ack contract`，Loop37 补齐 `snapshot save fail` 的可注入 failpoint 与 operator repair 证据，Loop38 把 `journal append` 收敛为提交确认边界：当 journal 已 durable 而 `snapshot save` 失败时，首次写口直接返回 committed ack，并显式带出 `persistence.journalAuthority + persistence.snapshotStatus`。

## 1. 架构对齐
- 对齐文档：`docs/step/100-*`、`docs/step/101-*`、`docs/step/95-*`、`docs/step/97-*`、`docs/架构/150CJ-*`、`docs/架构/151CJ-*`、`docs/架构/152CJ-*`
- Step 定义：`S05 = 关系域 durable truth`
- 当前已兑现：`friend_request + friendship + user_block + direct_chat + external_connection + external_member_link + shared_channel_policy` 的 control-plane truth 写/读/审计闭环
- 当前主缺口：`none；S05 已达到 step_closure，强于 repair-marker 的 staged/manifest 事务证明转为 deferred backlog`

## 2. Current / Target / Deferred
- Current State：`control-plane-api` 统一使用 `SocialControlState` 承载 social truth；默认是内存态，命中 `CRAW_CHAT_RUNTIME_DIR` 或显式 runtime-dir builder 时落地 `state/social-state.json` 与 `state/social-commit-journal.json`；若 journal 存在，启动时从默认空状态 replay journal 并回写 snapshot，不再信任 snapshot 基底；若 journal 缺失，才回退到 snapshot；写入顺序已改为 `append social-commit-journal.json -> write state/social-transaction-marker.json -> save social-state.json -> clear marker`；若 `journal append` 成功但 `snapshot save` 失败，写口现在直接返回 committed ack，并在响应中显式暴露 `persistence.journalAuthority = true` 与 `persistence.snapshotStatus = repair_required`，同时把 `social-transaction-marker.json` 保留下来作为 pending snapshot repair 边界；同租户同 `eventId` 的重试现会回放已提交结果、阻止重复追加 journal，并在 snapshot 可写时把 `snapshotStatus` 修复回 `current` 且清除 marker；runtime-dir 模式新增 `state/social-failpoints.json` 的一次性 `failNextSnapshotSave` 注入点；控制面新增 `POST /api/v1/control/social/runtime/repair-derived-snapshot`，允许 operator 在快照丢失或写失败后显式回刷 derived snapshot，standalone `control-plane-api repair-social-runtime-dir` 也会在修复后清除 marker；HTTP repair 与 standalone CLI 现都会显式返回 `transactionMarkerCleared`，把 pending marker 的修复结果抬升到 operator surface。
- Target State：形成真正可审计、可恢复、可重放的 social durable truth：`journal append = committed ack boundary`；当前 as-built 已通过 `repair-marker + operator surface` 达到 `S05 step_closure` 所需最小 durable truth 闭环。
- Deferred：更强 `staged / manifest` 级 `atomic multi-file tx` 证明。

## 3. 本轮已兑现
- social 写口在 `journal append` 成功后不再返回 `social_state_unavailable`；首次响应直接返回 committed truth。
- `SocialFriendRequestCommitResponse`、`SocialFriendshipCommitResponse`、`SocialUserBlockCommitResponse`、`SocialDirectChatCommitResponse`、`SocialExternalConnectionCommitResponse`、`SocialExternalMemberLinkCommitResponse`、`SocialSharedChannelPolicyCommitResponse` 新增 `persistence` 区段，显式暴露 `journalAuthority + snapshotStatus`。
- `snapshot save fail` 命中时，响应返回 `persistence.snapshotStatus = repair_required`；same-event retry 或 operator repair 收敛成功后，可回到 `current`。
- 更新并转绿：
  - `test_control_plane_social_file_runtime_keeps_direct_chat_pair_guard_after_snapshot_save_failure`
  - `test_control_plane_social_file_runtime_replays_same_event_id_after_snapshot_save_failure`
  - `test_control_plane_social_file_runtime_failpoint_forces_next_snapshot_save_failure_once`
  - `test_control_plane_social_file_runtime_operator_repair_rebuilds_snapshot_after_failpoint`
- 既有包级回归继续保绿：`cargo test -p control-plane-api --offline --tests -- --nocapture`

## 4. 本轮非目标
- 不宣称 `S05 step_closure`
- 不宣称真正 `atomic multi-file tx` 已闭环
- 不宣称 `journal append` 自身失败、journal 文件损坏或独立 repair 工具缺失等场景的 `503` 已全部消失
- 不宣称已经具备独立于当前 runtime 进程的 journal-only operator replay/repair CLI
- 不扩展 `S07`、`S08`、`S09` 写集

## 5. 主写入范围
- `services/control-plane-api/src/lib.rs`
- `services/control-plane-api/tests/social_friend_request_test.rs`
- `docs/step/109-S05-social-commit-ack-loop38-current-checkpoint-2026-04-11.md`
- `docs/review/S05-Loop38补充-2026-04-11.md`
- `docs/review/S05-执行卡-2026-04-10.md`
- `docs/review/S00-S14-全局闭环复核-2026-04-10.md`
- `docs/架构/152CJ-Loop38补充-2026-04-11.md`
- `docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
- `docs/release/CHANGELOG.md`
- `docs/release/2026-04-11-v0.0.38-loop-38.md`

## 6. 最小交付包
- `journal append` 成功后的 committed ack 语义
- social write response `persistence.journalAuthority + persistence.snapshotStatus`
- `snapshot-save-fail` 的 failpoint / readonly snapshot TDD 回归
- same-event retry 把 `snapshotStatus` 收敛回 `current` 的证据
- Loop38 step/review/architecture/release/changelog 回写

## 7. 已覆盖 / 未覆盖
- 已覆盖：
  - `friend_request / friendship / user_block / direct_chat`
  - `external_connection / external_member_link / shared_channel_policy`
  - control-plane 写口 / 快照读口 / audit
  - durable snapshot + journal
  - `snapshot missing` 与 `snapshot ahead of journal` 两类启动恢复
  - `snapshot save fail` 后 live-state 不再落后 journal
  - `same tenant + same eventId` 的 replay/ack contract
  - 一次性 `snapshot save fail` failpoint 注入
  - operator 显式 derived-snapshot repair
  - `journal append` 成功后的 committed ack + persistence 状态显式响应
- 未覆盖：
  - staged / manifest 级更强 `atomic multi-file tx`
  - 更高阶 durability hardening，不再阻塞 `S05 step_closure`

## 8. 测试与验证
- TDD red：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_failpoint_forces_next_snapshot_save_failure_once -- --nocapture`
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_replays_same_event_id_after_snapshot_save_failure -- --nocapture`
- TDD green：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_keeps_direct_chat_pair_guard_after_snapshot_save_failure -- --nocapture`
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_replays_same_event_id_after_snapshot_save_failure -- --nocapture`
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_failpoint_forces_next_snapshot_save_failure_once -- --nocapture`
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_operator_repair_rebuilds_snapshot_after_failpoint -- --nocapture`
- 包级回归：
  - `cargo fmt -p control-plane-api`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - 结果：`57 passed`

## 9. 架构回写结论
- 结论：`实现更具体`
- 必回写事实：
  - `S05` durable sample 已收敛到 `journal authority + journal-first + committed ack boundary + response persistence visibility + same-event replay ack + injected failpoint + operator repair endpoint`
  - `eventId` 当前在 social runtime 内按 `(tenant_id, event_id)` 识别 replay truth
  - `same-event` 重试可直接返回已提交结果，并避免重复追加 journal
  - `snapshot save fail` 现在在首次提交响应内显式暴露 `snapshotStatus = repair_required`
  - 写路径仍非 `atomic multi-file tx`；`S05` 仍为 `not_closed / local_closure`

## 10. 六问结论
- Q1 `95` 是否已达到 Step 完整闭环：`yes`
- Q2 `97` 是否已写清已兑现与 deferred：`yes`
- Q3 review / 架构 / release 是否齐备：`yes`
- Q4 未完成项是否已回挂后续 step：`yes`
- Q5 是否具备 fresh 主证据与辅证据：`yes`
- Q6 回退 / 降级 / 阻塞升级条件是否可 review：`yes`
- 结论：`S05 = step_closure`

## 11. Loop 39 预执行卡（2026-04-11）
- 本轮目标：
  - 把 `operator repair` 收敛为独立于当前 runtime live-state 的 `journal-only replay/repair`
- 主缺口：
  - 当前 `repair-derived-snapshot` 仍直接基于进程内 `RwLock<SocialControlState>` 回写 snapshot，尚未证明 journal 才是 repair authority
- TDD red：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_operator_repair_replays_external_journal_append_into_live_state -- --nocapture`
- 预期 green：
  - runtime-dir 模式保存 `journal_path`
  - repair 时直接 replay `social-commit-journal.json`
  - repair 完成后同时刷新 live state 与 derived snapshot
- 完整验证：
  - `cargo fmt -p control-plane-api`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
- 本轮完成后仍需保留 deferred：
  - `atomic multi-file tx`
  - 真正独立进程/CLI 形态的 standalone repair tool
  - `S05 step_closure`

## 12. Loop 39 完成回写（2026-04-11）
- 已兑现：
  - runtime-dir social runtime 现在会保存 `journal_path`
  - `repair-derived-snapshot` 改为直接 replay `social-commit-journal.json`
  - repair 成功后会同时刷新进程内 live state 与 `social-state.json`
  - operator repair 现可吸收 runtime 启动后由外部追加到 journal 的 committed truth
- TDD：
  - red：`test_control_plane_social_file_runtime_operator_repair_replays_external_journal_append_into_live_state` 先失败，失败点为 `aggregateCounts.directChats = 0 != 1`
  - green：新增回归转绿，且 `test_control_plane_social_file_runtime_operator_repair_rebuilds_snapshot_after_failpoint` 继续通过
- fresh 验证：
  - `cargo fmt -p control-plane-api`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - 结果：`59 passed`
- 当前结论：
  - `S05` 仍为 `not_closed / local_closure`
  - `operator repair 仍依赖 live-state` 缺口已关闭
  - 下一主缺口收敛为 `atomic multi-file tx + standalone journal-only operator repair/replay tooling + S05 step_closure`

## 13. Loop 40 预执行卡（2026-04-11）
- 本轮目标：
  - 把 `S05` 的 `standalone / cross-process journal-only operator repair/replay tooling` 从文档缺口收敛为真实 CLI
- 主缺口：
  - 当前只有进程内 `POST /api/v1/control/social/runtime/repair-derived-snapshot`；operator 仍缺少可在 control-plane 进程外独立执行的 social repair 命令
- 本轮决策：
  - 优先落 `control-plane-api repair-social-runtime-dir --runtime-dir <path> [--json]`
  - 暂不在本轮展开 `atomic multi-file tx` 主实现，只保留边界与风险结论
- 写集：
  - `services/control-plane-api/src/lib.rs`
  - `services/control-plane-api/src/main.rs`
  - `services/control-plane-api/tests/*`
  - `docs/review/S05-*`
  - `docs/step/*Loop40*`
  - `docs/架构/152CJ-*`
  - `docs/release/*`
- TDD red：
  - `cargo test -p control-plane-api --offline --test social_runtime_cli_test test_control_plane_repair_social_runtime_dir_cli_replays_journal_into_snapshot -- --nocapture`
- 预期 green：
  - 新增 standalone social repair helper/report
  - `main.rs` 暴露 `repair-social-runtime-dir`
  - CLI 可在 runtime 未启动时直接 replay `social-commit-journal.json` 并重建 `social-state.json`
- 完整验证：
  - `cargo fmt -p control-plane-api`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
- 本轮完成后仍需保留 deferred：
  - `atomic multi-file tx`
  - `repair-runtime-local.*` 与 social standalone CLI 的统一 operator surface
  - `S05 step_closure`

## 14. Loop 40 完成回写（2026-04-11）
- 已兑现：
  - `control-plane-api` 新增 `repair-social-runtime-dir --runtime-dir <path> [--json]`
  - standalone CLI 会直接 replay `state/social-commit-journal.json`，在 control-plane 进程外重建 `state/social-state.json`
  - repair CLI 不再依赖 live runtime state；只依赖 runtime-dir 与 journal truth
- TDD：
  - red：`test_control_plane_repair_social_runtime_dir_cli_replays_journal_into_snapshot` 先失败，失败点为 CLI 未退出
  - green：CLI 回归转绿，并通过 JSON 输出显式返回 `status + journalAuthority + snapshotUpdated + aggregateCounts`
- fresh 验证：
  - `cargo fmt -p control-plane-api`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - 结果：`60 passed`
- 当前结论：
  - `S05` 仍为 `not_closed / local_closure`
  - `standalone / cross-process journal-only operator repair` 缺口已关闭
  - 下一主缺口收敛为 `atomic multi-file tx + S05 step_closure`

## 15. Loop 41 预执行卡（2026-04-11）
- 本轮目标：
  - 把 `repair-runtime-local.*` 与 `control-plane-api repair-social-runtime-dir` 收敛为统一 operator surface
- 主缺口：
  - 当前 `repair-runtime-local.ps1/.sh` 只会执行 `local-minimal-node repair-runtime-dir`；social journal repair 仍需 operator 手动再跑一次 standalone CLI
- 本轮决策：
  - 保持 generic runtime-dir repair 与 social journal repair 分层
  - 先执行 `local-minimal-node repair-runtime-dir`
  - 仅当 `state/social-commit-journal.json` 存在时，再追加 `control-plane-api repair-social-runtime-dir --runtime-dir <path>`
  - 若 social journal 缺失则跳过 social repair；若 journal 存在但 social repair 失败，则 wrapper 直接失败
  - 暂不在本轮展开 `atomic multi-file tx` 主实现，只保留为下轮主缺口
- 写集：
  - `bin/repair-runtime-local.ps1`
  - `bin/repair-runtime-local.sh`
  - `services/local-minimal-node/tests/deployment_profile_test.rs`
  - `docs/review/S05-*`
  - `docs/step/*Loop41*`
  - `docs/架构/152CJ-*`
  - `docs/release/*`
- TDD red：
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test test_repair_runtime_local_ps1_invokes_social_repair_after_generic_repair_when_social_journal_exists -- --nocapture`
- 预期 green：
  - `repair-runtime-local.*` 在 social journal 存在时统一追加 standalone social repair
  - wrapper 维持 `--json` 兼容，不把 social repair 混入 generic empty-file repair 逻辑
  - social repair 缺失 journal 时不误报失败；journal 存在且 social repair 失败时显式失败
- 完整验证：
  - `cargo fmt -p local-minimal-node`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
- 本轮完成后仍需保留 deferred：
  - `atomic multi-file tx`
  - `S05 step_closure`

## 16. Loop 41 完成回写（2026-04-11）
- 已兑现：
  - `repair-runtime-local.ps1` 与 `repair-runtime-local.sh` 现在会先执行 `local-minimal-node repair-runtime-dir`
  - 当 `state/social-commit-journal.json` 存在时，wrapper 会自动追加 `control-plane-api repair-social-runtime-dir --runtime-dir <path>`
  - 当 social journal 缺失时，wrapper 保持原有 generic repair 成功路径；当 social repair 失败时，wrapper 显式失败并向外传播退出码
  - `local-minimal-node` 同步补齐 `conversation_runtime::RuntimeError::InvalidInput` 与 `ConversationBindingNotFound` 的 `ApiError` 映射，恢复本轮 package regression 所需的可编译状态
  - `docs/prompts/反复执行Step指令.md` 收敛为 concise repeated-step prompt，满足 `provider_plugin_docs_test` 对“12 行内 + 关键执行词覆盖”的门禁
- TDD：
  - red：`test_repair_runtime_local_ps1_propagates_social_repair_failure_when_social_journal_exists` 先失败，失败点为 wrapper 在 social journal 存在时仍返回成功
  - green：PowerShell wrapper failure-path 回归转绿；shell wrapper 追加 social repair 的行为回归 `test_repair_runtime_local_sh_invokes_social_repair_after_generic_repair_when_social_journal_exists` 在整份 `deployment_profile_test` 中保持通过
- fresh 验证：
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test test_repair_runtime_local_ps1_propagates_social_repair_failure_when_social_journal_exists -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test test_runtime_operation_ps1_wrappers_forward_profile_and_backup_arguments -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test provider_plugin_docs_test test_step_docs_and_prompt_capture_repeated_iteration_requirements -- --nocapture`
  - `cargo fmt -p local-minimal-node`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --tests -- --nocapture`
  - 结果：`deployment_profile_test = 64 passed`，`local-minimal-node package tests = passed`
- 当前结论：
  - `S05` 仍为 `not_closed / local_closure`
  - `repair-runtime-local.*` 与 standalone social repair CLI 的 operator entrypoint unification 缺口已关闭
  - 下一主缺口收敛为 `atomic multi-file tx + S05 step_closure`

## 17. Loop 42 预执行卡（2026-04-11）
- 本轮目标：
  - 收敛 `S05` 的 `atomic multi-file tx` 最小可落地边界
- 主缺口：
  - 当前 social durable truth 仍是 `append journal -> save snapshot` 的 journal-first 持久化顺序，并非真正多文件原子事务
- 本轮决策：
  - 采用 `repair marker` 作为最小事务边界：当 `journal append` 已完成而 `snapshot save` 尚未确认成功时，显式落一个可恢复的 pending marker
  - 先证明 `snapshot save fail -> marker durable -> startup replay / operator repair 成功后清 marker`，不在本轮引入更重的 staged payload 或 manifest
  - 不回退已完成的 wrapper/operator surface；其仅作为新事务边界实现后的验收入口
- TDD red：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_leaves_pending_tx_marker_after_snapshot_failure_and_clears_it_after_restart_replay -- --nocapture`
- 完整验证：
  - `cargo fmt -p control-plane-api -p local-minimal-node`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
- 本轮完成后仍需保留 deferred：
  - `S05 step_closure`

## 18. Loop 42 完成回写（2026-04-11）
- 已兑现：
  - `services/control-plane-api/src/lib.rs` 为 runtime-dir social 写路径新增 `state/social-transaction-marker.json`
  - `journal append` 成功后、`snapshot save` 成功前会写入 pending marker；`snapshot save` 成功后会立即清理 marker
  - `snapshot save fail` 命中时，marker 会作为 durable repair boundary 保留在 runtime-dir 中
  - runtime restart 的 journal replay、same-event retry 的 best-effort snapshot repair、HTTP `repair-derived-snapshot` 与 standalone `repair-social-runtime-dir` CLI 在修复成功后都会清除 marker
- TDD：
  - red：`test_control_plane_social_file_runtime_leaves_pending_tx_marker_after_snapshot_failure_and_clears_it_after_restart_replay` 先失败，失败点为 snapshot save fail 后不存在 `social-transaction-marker.json`
  - green：新增回归转绿，证明 marker 会在失败后持久化保留，并在 restart replay 成功后自动清除
- fresh 验证：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test test_control_plane_social_file_runtime_leaves_pending_tx_marker_after_snapshot_failure_and_clears_it_after_restart_replay -- --nocapture`
  - `cargo fmt -p control-plane-api -p local-minimal-node`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
  - 结果：targeted red/green 回归 `passed`，`control-plane-api --tests = 61 passed`，`deployment_profile_test = 64 passed`
- 当前结论：
  - `repair-marker based atomic multi-file tx minimal boundary` 缺口已关闭
  - `S05` 仍为 `not_closed / local_closure`
  - 下一主缺口收敛为 `S05 step_closure` 与是否还需要更强 `staged/manifest` 级事务证明

## 19. Loop 43 预执行卡（2026-04-11）
- 本轮目标：
  - 把 `repair-marker` 的修复结果显式暴露到 social operator surface，补齐 `S05 step_closure` 所需的可审计证据链
- 主缺口：
  - 当前 `repair-derived-snapshot` HTTP 返回与 `repair-social-runtime-dir --json` 输出虽会清理 marker，但未显式暴露“本次是否清理了 pending transaction marker”
- 本轮决策：
  - 优先扩展 `SocialRuntimeRepairResponse`，新增 `transactionMarkerCleared` 一类的显式字段
  - 先从 standalone CLI JSON 合同锁红，再让 HTTP repair 与审计载荷跟随同一结构转绿
  - 暂不在本轮推进更强 `staged / manifest` 级事务
- TDD red：
  - `cargo test -p control-plane-api --offline --test social_runtime_cli_test test_control_plane_repair_social_runtime_dir_cli_reports_transaction_marker_clearance_after_snapshot_failure -- --nocapture`
- 完整验证：
  - `cargo fmt -p control-plane-api -p local-minimal-node`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
- 本轮完成后仍需保留 deferred：
  - `S05 step_closure`
  - 更强 `staged / manifest` 级事务证明

## 20. Loop 43 完成回写（2026-04-11）
- 已兑现：
  - `SocialRuntimeRepairResponse` 新增 `transactionMarkerCleared`
  - `POST /api/v1/control/social/runtime/repair-derived-snapshot` 与 `control-plane-api repair-social-runtime-dir --json` 现在都会显式返回本次是否清理了 pending transaction marker
  - 文本型 `repair-social-runtime-dir` 输出也新增 `transaction-marker-cleared: <bool>` 行，避免 operator 只能依赖文件系统侧推断
- TDD：
  - red：`test_control_plane_repair_social_runtime_dir_cli_reports_transaction_marker_clearance_after_snapshot_failure` 先失败，失败点为 CLI JSON 输出缺少 `transactionMarkerCleared`
  - green：新增 CLI 回归转绿，并补齐 HTTP repair 回归对 `transactionMarkerCleared` 的 `true / false` 断言
- fresh 验证：
  - `cargo test -p control-plane-api --offline --test social_runtime_cli_test test_control_plane_repair_social_runtime_dir_cli_reports_transaction_marker_clearance_after_snapshot_failure -- --nocapture`
  - `cargo fmt -p control-plane-api -p local-minimal-node`
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
  - 结果：targeted red/green 回归 `passed`，`control-plane-api --tests = 62 passed`，`deployment_profile_test = 64 passed`
- 当前结论：
  - `repair-marker` 已不只存在于实现内部，也进入了可审计的 operator surface
  - `S05` 仍为 `not_closed / local_closure`
  - 下一主缺口收敛为 `S05 step_closure` 与是否仍需要更强 `staged/manifest` 级事务证明

## 21. Loop 44 预执行卡（2026-04-11）
- 本轮目标：
  - 判断当前 `repair-marker + operator surface` 证据是否已足够支撑 `S05 step_closure`
- 主缺口：
  - `S05` 仍停留在 `local_closure`，但当前阻塞是否仍真的是“更强 staged/manifest 事务”，还是已经可以诚实降级为 deferred backlog，尚未冻结
- 本轮决策：
  - 本轮按 `收口批次` 执行，不新增新的 social truth 功能
  - 先以现有 fresh 代码/测试/operator surface 证据重判 `Q1`
  - 若证据足够，则把 `staged / manifest stronger tx proof` 从 step-blocking gap 下放为 deferred；若证据不足，则必须明确写清仍阻止 `S05 step_closure` 的唯一硬缺口
- TDD / 证据焦点：
  - 不新增主行为测试，直接复用本轮 fresh 主证据与 operator surface 证据矩阵完成 closure 判定
- 完整验证：
  - 复用本轮 fresh evidence：`cargo test -p control-plane-api --offline --tests -- --nocapture`
  - 复用本轮 fresh evidence：`cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture`
- 本轮完成后目标：
  - 要么把 `S05` 提升为 `step_closure`
  - 要么把唯一 remaining blocker 冻结到单一、可继续实现的硬缺口

## 22. Loop 44 完成回写（2026-04-11）
- 已兑现：
  - 基于 Loop31-Loop43 的 fresh 代码、测试、runtime-dir repair、standalone CLI 与统一 wrapper 证据，重新判定 `S05` 已达到 `relation-domain durable truth` 的最小闭环要求
  - `repair-marker based atomic multi-file tx minimal boundary` 被正式接受为当前 step 的充分 durability 边界
  - 更强 `staged / manifest` 级事务证明被下放为 deferred backlog，而不再阻塞 `S05`
- 收口依据：
  - social truth 已覆盖 `friend_request / friendship / user_block / direct_chat / external_connection / external_member_link / shared_channel_policy`
  - 已具备 `social-state.json + social-commit-journal.json + social-transaction-marker.json`
  - 已具备 `journal-authoritative replay`、`journal append -> committed ack`、`same-event retry acknowledgment`、failpoint、HTTP repair、standalone CLI repair、统一 `repair-runtime-local.*` operator surface
  - repair operator surface 已显式暴露 `transactionMarkerCleared`
- fresh 证据：
  - `cargo test -p control-plane-api --offline --tests -- --nocapture` = `62 passed`
  - `cargo test -p local-minimal-node --offline --test deployment_profile_test -- --nocapture` = `64 passed`
- 当前结论：
  - `S05 = step_closure`
  - stronger `staged / manifest` 级事务证明保留为后续 durability hardening backlog，不再阻塞本 step
