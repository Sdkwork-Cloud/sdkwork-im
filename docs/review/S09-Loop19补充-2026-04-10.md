# S09 Loop-19 补充 - 2026-04-10

## As-Built 增量
- `ConversationPolicy::normalize()` 现在仅接受 `joined / world_readable`
- `businessPolicyVocabulary.historyVisibilityModes` 现在仅发布 `joined / world_readable`
- `POST /im/v3/api/chat/conversations` 明确拒绝 `invited/shared`
- runtime 读路径若遇到 legacy `invited/shared` 快照，返回显式 unsupported

## Fresh Evidence
- `cargo test -p im-domain-core --offline`
- `cargo test -p conversation-runtime --offline --tests`
- `cargo test -p control-plane-api --offline --tests`
- `cargo test -p craw-chat-ccp-registry --offline`
- `cargo test -p im-platform-contracts --offline`

## 闭环判断
- `S09.status = step_closure`
- `S09.step_closure = published history visibility vocabulary aligned with runtime truth`
- `S10 = 可准入`

## Deferred
- `invited` 真能力待 `S06/S07` 完成 invitation durable truth 后再由 `S09` 重发
- `shared` 真能力待 `S12` 完成 shared-channel / external durable truth 后再由 `S09` 重发
- `retention_policy_ref` 运行时/归档闭环继续归 `S13`

## 下一轮输入
- `next_mode = 实现批次`
- `next_step = S10`
- 优先收口 `stream / rtc / media artifact lifecycle`
