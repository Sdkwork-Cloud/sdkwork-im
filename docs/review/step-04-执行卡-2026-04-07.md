# Step 04 执行卡 - 2026-04-07

## 1. 当前判断

- 当前波次：`Wave B`
- 当前 Step：`04`
- 当前状态：`已启动，未闭环`

## 2. 本轮为什么做 Step 04

`Wave A / Step 03` 已闭环并通过 `93` 后，执行顺序必须进入 `Step 04`，不能停留在上一波次。

本轮选择的最小可验证 slice 是：

- 先落地 `runtime-link / runtime-route` 的真实 crate 骨架
- 先冻结一个可测试的连接状态模型和一个可测试的 route epoch / drain / migration 模型
- 暂不把整套 `session-gateway` 热路径一次性搬空，避免 Step 04 一开始就变成大范围重写

## 3. 本轮实际完成

### 3.1 先写 fail 的 Step 04 测试

新增：

- `services/session-gateway/tests/runtime_plane_split_test.rs`

Red 结论：

- 初始运行时因 `craw_chat_runtime_link / craw_chat_runtime_route` crate 不存在而失败

### 3.2 补齐 `runtime-link / runtime-route` 最小真实实现

新增：

- `crates/craw-chat-runtime-link`
- `crates/craw-chat-runtime-route`

当前已落地的最小能力：

- `runtime-link`
  - `LinkConnectionState`
  - `OutboundQueuePolicy`
  - `ResumeWindow`
  - `LinkSession`
- `runtime-route`
  - `RouteBindingRequest`
  - `RouteBinding`
  - `RouteNodeLifecycle`
  - `RouteMigrationResult`
  - `RouteRuntimeError`
  - `RouteDirectory`

### 3.3 让现有服务测试真实消费新 runtime crate

- `session-gateway` 已通过 dev-dependency 消费上述两个新 crate
- `runtime_plane_split_test` 已验证：
  - `LinkSession` 状态推进：`Connected -> HelloNegotiated -> Authenticated -> Active -> Draining`
  - `RouteDirectory` 的 route epoch、node draining、route migration

## 4. 本轮改动文件

### 4.1 代码

- `Cargo.toml`
- `crates/craw-chat-runtime-link/*`
- `crates/craw-chat-runtime-route/*`
- `services/session-gateway/Cargo.toml`
- `services/session-gateway/tests/runtime_plane_split_test.rs`

### 4.2 文档

- `docs/review/step-04-执行卡-2026-04-07.md`
- `docs/review/step-04-质量审计与复盘-2026-04-07.md`
- `docs/review/step-04-架构兑现与回写决议-2026-04-07.md`
- `docs/架构/09-实施计划.md`
- `docs/架构/131-连接管理与分层弹性扩容架构设计-2026-04-06.md`
- `docs/架构/136-关键业务链路与跨Plane时序设计-2026-04-06.md`

## 5. 本轮验证

### 5.1 Red

- `$env:CARGO_TARGET_DIR='target/local-minimal-step04-red'; cargo test -p session-gateway --test runtime_plane_split_test --offline`

失败原因：

- `craw_chat_runtime_link`
- `craw_chat_runtime_route`

两个 crate 尚不存在

### 5.2 Green

- `cargo fmt --check --package craw-chat-runtime-link --package craw-chat-runtime-route --package session-gateway`
- `$env:CARGO_TARGET_DIR='target/local-minimal-step04-green'; cargo test -p session-gateway --test runtime_plane_split_test --offline`
- `$env:CARGO_TARGET_DIR='target/local-minimal-step04-green'; cargo test -p session-gateway --test cluster_routing_test --offline`

结果：

- Step 04 最小 runtime skeleton 测试转绿
- 现有 route/drain cluster 回归 `12/12` 继续通过

## 6. 本轮对应架构能力

- `docs/架构/130-连接优先的AI时代即时通讯架构蓝图-2026-04-06.md`
- `docs/架构/131-连接管理与分层弹性扩容架构设计-2026-04-06.md`
- `docs/架构/136-关键业务链路与跨Plane时序设计-2026-04-06.md`
- `docs/架构/149-多Cell多Region协议升级与灾备兼容设计-2026-04-06.md`

本轮已兑现：

- `runtime-link / runtime-route` 不再只是 step 文档中的名字，已有真实 crate
- 连接状态推进模型已有最小可测试 owner
- route epoch / drain / migrate 模型已有最小可测试 owner

本轮尚未兑现：

- `session-gateway` 真实连接热路径还未抽到 `runtime-link`
- `session-gateway` 真实 cluster/route owner 还未切到 `runtime-route`
- `local-minimal-node` 还未明显收敛为 Link/Route runtime 装配层
- `149` 的跨 cell / region 兼容假设尚未进入真实实现

## 7. 当前 Step 04 还差什么

- `CP04-2` 尚未满足：连接热路径未从 `session-gateway` 真实抽离
- `CP04-3` 仅部分满足：route epoch / drain 模型已有 skeleton，但未接管现有 cluster bridge
- `CP04-4` 尚未满足：`local-minimal-node` 仍主要复用旧运行态实现
- `91 / 95 / 97` 对 `Step 04` 均未通过

## 8. 下一轮动作

1. 继续留在 `Step 04`
2. 把 `session-gateway` 中现有 route lifecycle / epoch / drain 公开模型开始迁到 `runtime-route`
3. 把 `session-gateway` 的连接状态与 resume window 公开模型开始迁到 `runtime-link`
4. 补 Step 04 的更多 smoke / E2E 证据，再判断是否能推进到更深抽离

## 9. 2026-04-07 增量推进（Route Owner 收口）

本节覆盖前文中“`session-gateway` 真实 cluster/route owner 尚未切到 `runtime-route`”的旧判断。

### 9.1 本轮为什么继续做 Step 04

- 对应 `docs/step/04-link-plane与route-plane运行时重构.md` 中的 `CP04-3`
- 如果 route ownership / epoch / drain 只停留在 skeleton crate，而不接入真实 `cluster bridge`，本 step 仍然只算“起步”，不能形成真实 owner 证据

### 9.2 本轮实际完成

- 先新增 fail-first 测试：`services/session-gateway/tests/cluster_routing_test.rs`
  - `test_cluster_bridge_public_route_models_use_runtime_route_owner_types`
- `crates/craw-chat-runtime-route/src/lib.rs` 已扩展真实 route 元数据：
  - `session_id`
  - `connection_kind`
  - `bound_at`
  - `route_epoch`
- `RouteBindingRequest` 已补 metadata builder，`RouteDirectory` 已补：
  - `activate_node`
  - `release`
  - `routes_for_node`
  - `node_lifecycle`
  - `migrate_routes_at`
- `services/session-gateway/src/cluster.rs` 已改为通过 `RouteDirectory` 持有 route owner / node lifecycle
- `session-gateway` 的公开 route 模型已收口到 `runtime-route`
  - `RealtimeDeviceRoute = RouteBinding`
  - `RealtimeNodeLifecycleView = RouteNodeLifecycle`
  - `RealtimeRouteMigrationResult = RouteMigrationResult`
- `services/session-gateway/Cargo.toml` 已把 `craw-chat-runtime-route` 升为正式依赖，不再只是测试期消费

### 9.3 本轮新增改动文件

- `crates/craw-chat-runtime-route/src/lib.rs`
- `services/session-gateway/Cargo.toml`
- `services/session-gateway/src/cluster.rs`
- `services/session-gateway/tests/cluster_routing_test.rs`
- `docs/review/step-04-执行卡-2026-04-07.md`
- `docs/review/step-04-质量审计与复盘-2026-04-07.md`
- `docs/review/step-04-架构兑现与回写决议-2026-04-07.md`
- `docs/架构/09-实施计划.md`
- `docs/架构/131-连接管理与分层弹性扩容架构设计-2026-04-06.md`
- `docs/架构/136-关键业务链路与跨Plane时序设计-2026-04-06.md`

### 9.4 本轮增量验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-route-red'; cargo test -p session-gateway --test cluster_routing_test test_cluster_bridge_public_route_models_use_runtime_route_owner_types --offline`
- Green：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-route-green'; cargo test -p session-gateway --test cluster_routing_test test_cluster_bridge_public_route_models_use_runtime_route_owner_types --offline`
  - `cargo fmt --check --package craw-chat-runtime-route --package session-gateway`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-route-green'; cargo test -p session-gateway --test runtime_plane_split_test --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-route-green'; cargo test -p session-gateway --test cluster_routing_test --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-route-green'; cargo test -p session-gateway --test websocket_smoke_test --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-route-green'; cargo test -p session-gateway --offline`

### 9.5 当前 Step 04 闭环判断更新

- `CP04-2`：仍未通过
  - `session-gateway/src/websocket.rs` 的真实连接热路径尚未迁到 `runtime-link`
- `CP04-3`：现已通过
  - route ownership / epoch / drain 模型已落地到 `runtime-route`
  - `session-gateway` 真实 cluster bridge 已开始消费该 owner
  - fail-first + 全量 `session-gateway` 回归均有证据
- `CP04-4`：仍未通过
  - `local-minimal-node` 还未明显收敛为 Link/Route runtime 装配层
- `91`：未整体通过
  - 当前仅能证明 Step 04 已推进，不足以证明整 step 质量闭环
- `95`：未整体通过
  - Link Plane 未闭环，Route Plane 仅完成 owner 收口起点
- `97`：部分完成
  - 已按本轮真实状态补充回写 `09 / 131 / 136`
- `93`：当前不触发
  - `Wave B` 尚未完成，不能做波次总验收

### 9.6 下一轮继续做什么

1. 继续留在 `Wave B / Step 04`
2. 把 `session-gateway/src/websocket.rs` 的连接状态、resume window、draining 状态继续迁向 `runtime-link`
3. 检查 `local-minimal-node` 如何改成 Link/Route runtime 的纯装配层
4. 补 `local-minimal-node` 侧的 Step 04 E2E 证据，再重新评估 `91 / 95 / 97`

## 10. 2026-04-07 增量推进（Link Owner 起点）

### 10.1 本轮为什么继续做 Step 04

- `CP04-3` 已推进后，当前最直接阻塞变为 `CP04-2`
- 如果 websocket 热路径仍完全不消费 `runtime-link`，Step 04 仍然只会停在“Route Plane 已起步，Link Plane 未接线”的半成品状态

### 10.2 本轮实际完成

- 先新增 fail-first 单测：
  - `websocket::tests::test_build_active_link_session_maps_checkpoint_into_runtime_link_owner`
- `services/session-gateway/src/websocket.rs` 已开始真实消费 `runtime-link`
  - 新增 `build_link_session`
  - 新增 `activate_link_session`
  - `serve_realtime_websocket` 现已在真实热路径中创建 `LinkSession`
  - `complete_ccp_handshake` 现已推进 `HelloNegotiated / Authenticated`
  - `RealtimeWindowCheckpoint` 现已映射到 `ResumeWindow`
  - 连接退出时已标记 `Draining`
- `services/session-gateway/Cargo.toml` 已把 `craw-chat-runtime-link` 升为正式依赖

### 10.3 本轮新增验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-link-red'; cargo test -p session-gateway websocket::tests::test_build_active_link_session_maps_checkpoint_into_runtime_link_owner --offline`
- Green：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-link-green'; cargo test -p session-gateway websocket::tests::test_build_active_link_session_maps_checkpoint_into_runtime_link_owner --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-link-green'; cargo test -p session-gateway --test websocket_smoke_test --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-link-green'; cargo test -p session-gateway --offline`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`

### 10.4 当前 Step 04 判断再更新

- `CP04-2`：仍未通过，但已有真实推进
  - `runtime-link` 已开始接入 websocket 热路径
  - 但连接 accept/upgrade、queue/backpressure、resume/reconnect 仍未完整抽离到 crate owner
- `CP04-3`：保持通过
- `CP04-4`：仍未通过
- `91 / 95 / 97`：整体仍不通过

## 11. 2026-04-07 增量推进（local-minimal-node 装配收口 + drain 迁移护栏）

### 11.1 本轮为什么继续做 Step 04

- 当前 `Wave B / Step 04` 的剩余阻塞已收敛到：
  - `CP04-2`：Link Plane 热路径尚未完整抽离
  - `CP04-4`：`local-minimal-node` 是否真的朝装配层收敛
- 本轮选择先补 `CP04-4`，因为：
  - `local-minimal-node` 仍在 `build.rs` 内部分裂构造 `cluster / runtime / presence`
  - drained 节点上的旧请求还能把 runtime state 偷回本地，说明本地节点仍在越权侵入 Route Runtime 边界

### 11.2 本轮实际完成

- 先新增 fail-first 结构测试：
  - `test_local_minimal_node_build_surface_assembles_realtime_plane_via_session_gateway_bundle`
- `services/session-gateway/src/assembly.rs` 新增统一装配 bundle：
  - `RealtimePlaneAssembly`
- `services/local-minimal-node/src/node/build.rs` 已改为通过 `RealtimePlaneAssembly` 统一装配：
  - `RealtimeClusterBridge`
  - `RealtimeDeliveryRuntime`
  - `SessionPresenceRuntime`
- 已删除本地节点旧的拆分 helper：
  - `build_local_minimal_realtime_cluster`
  - `build_local_minimal_realtime_runtime`
  - `build_local_minimal_presence_runtime`
- `services/local-minimal-node/src/node/access.rs` 已新增 owner locality 护栏：
  - 非 `allow_session_takeover` 场景下，若当前节点不是 route owner，则先拒绝，不再触发本地 rebind
- `services/session-gateway/src/cluster.rs` 已新增：
  - `ensure_device_route_local`
- 这次修复同时消除了一个真实 Step 04 bug：
  - drained 旧节点上的拉取请求此前会先把 runtime state 从新 owner 节点偷回旧节点，再因为本地 draining 报错
  - 现在改为先拒绝，route owner 与 runtime state 保持一致

### 11.3 本轮新增修改文件

- `services/session-gateway/src/assembly.rs`
- `services/session-gateway/src/lib.rs`
- `services/session-gateway/src/cluster.rs`
- `services/local-minimal-node/src/node.rs`
- `services/local-minimal-node/src/node/build.rs`
- `services/local-minimal-node/src/node/access.rs`
- `services/local-minimal-node/tests/lib_structure_test.rs`

### 11.4 本轮新增验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-node-red'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_build_surface_assembles_realtime_plane_via_session_gateway_bundle --offline`
- Green：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-node-green'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_build_surface_assembles_realtime_plane_via_session_gateway_bundle --offline`
  - `cargo fmt --check --package session-gateway --package local-minimal-node`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-node-final'; cargo test -p local-minimal-node --test cluster_drain_rebalance_e2e_test --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-node-final'; cargo test -p local-minimal-node --test cluster_realtime_routing_e2e_test --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-session-full'; cargo test -p session-gateway --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-node-full'; cargo test -p local-minimal-node --offline`

### 11.5 当前 Step 04 判断再更新

- `CP04-2`：仍未通过
  - `websocket.rs` 的完整 accept/upgrade、queue/backpressure、resume/reconnect owner 还未迁完
- `CP04-3`：保持通过
- `CP04-4`：现已通过
  - `local-minimal-node` 已通过统一 `RealtimePlaneAssembly` 明确装配 Link/Route runtime
  - drained 旧节点已不能越权把 runtime state 迁回本地
  - 结构门、路由迁移 E2E、跨节点路由 E2E、包级回归均已给出证据
- `91`：仍未整体通过
  - 原因已收敛到 `CP04-2`
- `95`：仍未整体通过
  - Link Plane 还未达到完整热路径抽离标准
- `97`：继续为部分完成
  - 本轮已具备补充 `09 / 131 / 136` 的新 as-built 依据
- `93`：仍不触发
  - `Wave B` 尚未完成

### 11.6 下一轮继续做什么

## 12. 2026-04-07 增量推进（runtime-link 默认队列 owner 回收）

### 12.1 为什么做这一步

- `Wave B / Step 04` 仍未闭环，当前主阻塞仍是 `CP04-2`
- 在 accept / upgrade / resume / reconnect 全量迁移之前，`queue / backpressure` 的默认策略 owner 仍留在 `session-gateway`
- 本轮先把“默认实时出站队列策略”的权威定义从服务侧收回到 `craw-chat-runtime-link`

### 12.2 本轮实际改动

- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增默认实时出站队列阈值常量
  - 新增 `OutboundQueuePolicy::realtime_default()`
  - 新增 owner 自测 `test_realtime_default_queue_policy_uses_owner_limits`
- `services/session-gateway/src/websocket.rs`
  - 删除服务侧私有默认队列常量
  - `build_link_session(...)` 改为直接消费 `OutboundQueuePolicy::realtime_default()`
  - 新增 fail-first/green 测试 `test_build_link_session_uses_runtime_link_default_queue_owner_policy`

### 12.3 验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-queue-red'; cargo test -p session-gateway test_build_link_session_uses_runtime_link_default_queue_owner_policy --offline`
- Green：
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-queue-green'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-queue-green'; cargo test -p session-gateway test_build_link_session_uses_runtime_link_default_queue_owner_policy --offline`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-queue-full'; cargo test -p session-gateway --offline`

### 12.4 当前判断

- `CP04-2`：仍未通过
  - 但 `queue / backpressure` 的默认 owner 已从 `session-gateway` 回收到 `runtime-link`
  - accept / upgrade / resume / reconnect 热路径仍待继续迁移
- `CP04-3`：保持通过
- `CP04-4`：保持通过
- `91 / 95 / 97`：整体仍未通过
- `93`：`Wave B` 仍不触发

1. 继续留在 `Wave B / Step 04`
2. 只围绕 `CP04-2` 推进 `session-gateway/src/websocket.rs` 的完整 Link Plane owner 抽离
3. 继续补 `resume / reconnect / queue / backpressure` 的 fail-first 证据
4. 在 `CP04-2` 通过后，再重新评估 `91 / 95 / 97` 是否可整体放行

## 13. 2026-04-07 增量推进（runtime-link auth owner 回收）

### 13.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3` 与 `CP04-4` 已通过，剩余唯一主阻塞仍是 `CP04-2`
- 按 `04-link-plane与route-plane运行时重构.md` 与 `131 / 136 / 147` 的边界要求，`auth_bind` 的连接身份 owner 不应继续停留在 `session-gateway/src/websocket.rs`

### 13.2 本轮实际完成

- 先补 fail-first 测试：
  - `websocket::tests::test_build_link_session_preserves_actor_identity_for_runtime_link_auth_owner`
- `crates/craw-chat-runtime-link/src/lib.rs`
  - `LinkSession` 新增 `actor_kind`
  - `LinkSession::new(...)` 新增 actor kind 入参
  - 新增 `LinkSession::matches_auth_bind(...)`
  - 新增 owner 自测：
    - `test_link_session_matches_auth_bind_identity`
    - `test_link_session_rejects_mismatched_auth_bind_identity`
- `services/session-gateway/src/websocket.rs`
  - `build_link_session(...)` 改为把 `auth.actor_kind` 一并收口到 `runtime-link`
  - `complete_ccp_handshake(...)` 不再依赖服务侧 `auth_bind_matches_context(...)`
  - 改为直接消费 `link_session.matches_auth_bind(...)`
  - `AuthOkFrame` 现在从 `LinkSession` 回填 tenant / principal / actor / device / session 身份
- `services/session-gateway/tests/runtime_plane_split_test.rs`
  - 跟进 `LinkSession::new(...)` 新签名，确保 Step 04 runtime plane smoke 继续对齐真实 owner

### 13.3 本轮验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-auth-owner-red'; cargo test -p session-gateway test_build_link_session_preserves_actor_identity_for_runtime_link_auth_owner --offline`
- Green：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-auth-owner-green'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-auth-owner-green'; cargo test -p session-gateway test_build_link_session_preserves_actor_identity_for_runtime_link_auth_owner --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-auth-owner-full'; cargo test -p session-gateway --offline`

### 13.4 当前判断更新

- `CP04-2`：仍未通过，但继续前推
  - `auth_bind` 身份校验 owner 已从服务侧条件判断继续收口到 `runtime-link`
  - `AuthOkFrame` 的身份回填已开始消费 `LinkSession`
  - 但 accept / upgrade、真实 queue/backpressure 执行、resume / reconnect 仍未完整迁出 `session-gateway`
- `CP04-3`：保持通过
- `CP04-4`：保持通过
- `91 / 95 / 97`：仍不得整体放行
- `93`：`Wave B` 仍未完成，不触发总验收

### 13.5 下一轮继续做什么

1. 继续停留在 `Wave B / Step 04`
2. 继续只围绕 `CP04-2`
3. 优先补下一条 fail-first：`hello / accept / upgrade` 或 `resume / reconnect` 的 owner 迁移
4. 每完成一个可判定 slice，都同步补 `docs/review` 与 `docs/架构` 回写，再重新评估 `91 / 95 / 97`

## 14. 2026-04-07 增量推进（runtime-link hello owner 回收）

### 14.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过后，`CP04-2` 继续是唯一主阻塞
- 本轮选择 `hello / accept` 这条最窄热路径：
  - 把 `hello` 的协议校验、绑定校验、capability 协商与 `HelloNegotiated` 状态推进继续收回 `runtime-link`

### 14.2 本轮实际完成

- 先补 fail-first 测试：
  - `websocket::tests::test_build_link_session_negotiates_hello_via_runtime_link_owner`
- `crates/craw-chat-runtime-link/Cargo.toml`
  - 新增对 `craw-chat-ccp-core / craw-chat-ccp-control` 的 owner 依赖
- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `LinkHelloError`
  - 新增 `LinkSession::negotiate_hello(...)`
  - 把 `hello` 的协议校验、绑定校验、capability 协商和 `HelloNegotiated` 状态推进收口到 runtime owner
  - 新增 owner 自测：
    - `test_link_session_negotiates_supported_hello`
    - `test_link_session_rejects_unsupported_hello_protocol`
    - `test_link_session_rejects_unsupported_hello_binding`
- `services/session-gateway/src/websocket.rs`
  - `complete_ccp_handshake(...)` 不再直接持有 `hello.protocol / hello.binding / negotiated_capabilities(...)` 判断
  - 改为直接消费 `link_session.negotiate_hello(...)`
  - 删除服务侧私有 `supported_capabilities / negotiated_capabilities`

### 14.3 本轮验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-hello-owner-red'; cargo test -p session-gateway test_build_link_session_negotiates_hello_via_runtime_link_owner --offline`
- Green：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-hello-owner-green'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-hello-owner-green'; cargo test -p session-gateway test_build_link_session_negotiates_hello_via_runtime_link_owner --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-hello-owner-full'; cargo test -p session-gateway --offline`

### 14.4 当前判断更新

- `CP04-2`：仍未通过，但继续前推
  - `hello` 协商 owner 已开始从 `session-gateway` 收口到 `runtime-link`
  - 现在 `runtime-link` 已同时持有：
    - 默认队列阈值 owner
    - `auth_bind` 身份匹配 owner
    - `hello` 协商 owner
  - 但 accept / upgrade 的接入层、真实 queue/backpressure 执行、resume / reconnect 仍未完整迁出 `session-gateway`
- `CP04-3`：保持通过
- `CP04-4`：保持通过
- `91 / 95 / 97`：仍不得整体放行
- `93`：`Wave B` 仍未完成，不触发总验收

### 14.5 下一轮继续做什么

1. 继续停留在 `Wave B / Step 04`
2. 继续只围绕 `CP04-2`
3. 优先转向 `resume / reconnect` 或真实 `queue/backpressure` 执行 owner
4. 每完成一个 slice，继续同步补 `docs/review` 与 `docs/架构` 回写，再重新评估 `91 / 95 / 97`

## 15. 2026-04-07 增量推进（runtime-link resume owner 回收）

### 15.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过后，`CP04-2` 仍是唯一主阻塞
- `docs/架构/131` 与 `docs/架构/136` 都明确把 `resume / reconnect` 归为 `runtime-link` 的 owner 范围
- 当前 `services/session-gateway/src/presence.rs` 仍在本地计算：
  - `resume_required`
  - `resume_from_sync_seq`
  这与 Step 04 的热路径收口目标不一致

### 15.2 本轮实际完成

- 先补 fail-first 测试：
  - `crates/craw-chat-runtime-link/src/lib.rs`
    - `test_runtime_link_decides_incremental_resume_window`
    - `test_runtime_link_decides_no_backfill_when_latest_sync_is_empty`
- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `ResumeDecision`
  - 新增 `decide_resume(last_seen_sync_seq, latest_sync_seq)`
  - 把 `resume_required / resume_from_sync_seq / latest_sync_seq` 的纯语义 owner 收口到 `runtime-link`
- `services/session-gateway/src/presence.rs`
  - `SessionPresenceRuntime::resume(...)` 不再本地计算 resume 结果
  - 改为直接消费 `craw_chat_runtime_link::decide_resume(...)`
- `services/session-gateway/tests/presence_runtime_persistence_test.rs`
  - 新增集成回归：
    - `test_presence_runtime_resume_returns_incremental_sync_window_from_runtime_link_owner`

### 15.3 本轮验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-resume-owner-red'; cargo test -p craw-chat-runtime-link test_runtime_link_decides_incremental_resume_window --offline`
  - 失败原因：`runtime-link` 尚未提供 `ResumeDecision / decide_resume(...)`
- Green：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-resume-owner-green'; cargo test -p craw-chat-runtime-link test_runtime_link_decides_incremental_resume_window --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-resume-owner-green'; cargo test -p session-gateway test_presence_runtime_resume_returns_incremental_sync_window_from_runtime_link_owner --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-resume-owner-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-resume-owner-full'; cargo test -p session-gateway --offline`

### 15.4 当前判断更新

- `CP04-2`：仍未通过，但继续前推
  - `resume` 结果计算 owner 已开始从 `session-gateway` 收口到 `runtime-link`
  - `runtime-link` 现在已同时持有：
    - 默认队列策略 owner
    - `auth_bind` 身份匹配 owner
    - `hello` 协商 owner
    - `resume` 纯语义 owner
  - 但以下仍未完整迁出 `session-gateway`：
    - HTTP/WebSocket accept / upgrade 接入层
    - 真实 `queue/backpressure` 执行路径
    - 完整 `resume / reconnect` 控制帧与重连时序
- `CP04-3`：保持通过
- `CP04-4`：保持通过
- `91 / 95 / 97`：仍不得整体放行
- `93`：`Wave B` 仍未完成，不触发总验收

### 15.5 下一轮继续做什么

1. 继续停留在 `Wave B / Step 04`
2. 继续只围绕 `CP04-2`
3. 优先把 `session_resume / session_resumed` 控制帧时序或 `accept / upgrade` owner 继续迁入 `runtime-link`
4. 每完成一个 slice，都同步补 `docs/review` 与 `docs/架构` 回写，再重新评估 `91 / 95 / 97`

## 16. 2026-04-07 增量推进（runtime-link goaway owner 回收）

### 16.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-2` 仍未通过，且 `docs/step/04`、`docs/架构/136`、`docs/架构/147` 都把 `goaway` 归为 Link Plane 的 Step 04 运行时语义
- 当前 CCP websocket 断连路径只发送 transport close，不发送 `goaway` 控制帧，说明 `goaway/close` owner 仍滞留在 `session-gateway`

### 16.2 本轮实际完成

- 先补 fail-first 测试：
  - `crates/craw-chat-runtime-link/src/lib.rs`
    - `test_runtime_link_builds_session_disconnect_goaway_owner_contract`
  - `services/session-gateway/tests/websocket_smoke_test.rs`
    - `test_realtime_websocket_sends_ccp_goaway_before_disconnect_close`
- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `SESSION_DISCONNECT_CLOSE_CODE`
  - 新增 `SESSION_DISCONNECT_CLOSE_REASON`
  - 新增 `SESSION_DISCONNECT_GOAWAY_CODE`
  - 新增 `SESSION_DISCONNECT_GOAWAY_MESSAGE`
  - 新增 `LinkGoAwayDirective`
  - 新增 `session_disconnect_goaway()`
- `services/session-gateway/src/websocket.rs`
  - `SESSION_DISCONNECT_CLOSE_CODE / REASON` 改为对齐 `runtime-link` owner 常量
  - 新增 `send_session_disconnect_signal(...)`
  - CCP 模式下设备被断连时，先发送 `cc.control.goaway.v1`，再发送 websocket close
- `services/session-gateway/tests/websocket_smoke_test.rs`
  - 新增 CCP 回归，验证 `goaway -> close` 顺序与负载

### 16.3 本轮验证

- Red：
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-goaway-owner-red'; cargo test -p craw-chat-runtime-link test_runtime_link_builds_session_disconnect_goaway_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='target/local-minimal-step04-goaway-owner-red'; cargo test -p session-gateway test_realtime_websocket_sends_ccp_goaway_before_disconnect_close --offline`
- Green：
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-goaway-owner'; cargo test -p craw-chat-runtime-link test_runtime_link_builds_session_disconnect_goaway_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-goaway-owner'; cargo test -p session-gateway test_realtime_websocket_sends_ccp_goaway_before_disconnect_close --offline`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-goaway-owner'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-goaway-owner'; cargo test -p session-gateway --offline`
- 说明：
  - 工作区 `D:` 盘的 `target/local-minimal-step04-*` 验证产物已接近磁盘上限
  - 因此本轮完整绿灯改走可写根 `C:\\Users\\admin\\.codex\\memories`

### 16.4 当前判断更新

- `CP04-2`：仍未通过，但继续前推
  - CCP 断连路径的 `goaway/close` 语义已开始从 `session-gateway` 收口到 `runtime-link`
  - `runtime-link` 现在已同时持有：
    - 默认队列策略 owner
    - `auth_bind` 身份匹配 owner
    - `hello` 协商 owner
    - `resume` 纯语义 owner
    - `goaway/close` 断连语义 owner
  - 但以下仍未完整迁出：
    - HTTP/WebSocket accept / upgrade 接入层
    - 完整 `session_resume / session_resumed` 控制帧时序
    - 真实 `queue/backpressure` 执行路径
- `CP04-3`：保持通过
- `CP04-4`：保持通过
- `91 / 95 / 97`：仍不得整体放行
- `93`：`Wave B` 仍未完成，不触发总验收

### 16.5 下一轮继续做什么

1. 继续停留在 `Wave B / Step 04`
2. 继续只围绕 `CP04-2`
3. 优先把 `session_resume / session_resumed` 控制帧时序继续迁入 `runtime-link`
4. 如果恢复时序过宽，再转向 `accept / upgrade` owner 收口

## 17. 2026-04-07 增量推进（runtime-link session_resume 控制帧 owner 回收）

### 17.1 本轮为什么继续做 Step 04

- 当前真实状态仍是 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过后，`CP04-2` 的主阻塞继续收敛到 Link Plane hot path
- `docs/step/04-link-plane与route-plane运行时重构.md`、`docs/架构/08-安全-多租户-SaaS-私有化-部署设计.md` 都要求 CCP 长连接遵循：
  - `hello -> auth_bind -> session_resume`
- 但本轮开始前，`session-gateway/src/websocket.rs` 仍然在 `auth_ok` 后直接进入 `connected` 业务帧，说明：
  - 完整 `session_resume / session_resumed` 控制帧时序还没有真正迁到 `runtime-link`

### 17.2 本轮实际完成

- 先补 fail-first 测试：
  - `crates/craw-chat-runtime-link/src/lib.rs`
    - 新增 `test_runtime_link_builds_session_resumed_owner_contract`
  - `services/session-gateway/tests/websocket_smoke_test.rs`
    - 将 CCP smoke 握手更新为：
      - `auth_ok -> session_resumed -> realtime.connected`
- `runtime-link` 已新增 resume 控制帧 owner：
  - `LinkSessionResumeDirective`
  - `LinkSessionResumeError`
  - `LinkSession::negotiate_session_resume(...)`
- `session-gateway/src/websocket.rs` 已改为真实消费该 owner：
  - CCP 握手现在要求在 `auth_ok` 之后接收 `ControlFrame::SessionResume`
  - `runtime-link` 负责：
    - 校验 `session_id`
    - 计算 `catchup_after_seq`
    - 产出 `SessionResumedFrame`
  - websocket 热路径现在负责：
    - 发送 `cc.control.session_resumed.v1`
    - 再进入 `cc.realtime.connected.v1`
    - 以 runtime owner 产出的 `catchup_after_seq` 作为后续 catchup 起点

### 17.3 本轮新增改动文件

- `crates/craw-chat-runtime-link/src/lib.rs`
- `services/session-gateway/src/websocket.rs`
- `services/session-gateway/tests/websocket_smoke_test.rs`
- `docs/review/step-04-执行卡-2026-04-07.md`
- `docs/review/step-04-质量审计与复盘-2026-04-07.md`
- `docs/review/step-04-架构兑现与回写决议-2026-04-07.md`
- `docs/架构/09-实施计划.md`
- `docs/架构/131-连接管理与分层弹性扩容架构设计-2026-04-06.md`
- `docs/架构/136-关键业务链路与跨Plane时序设计-2026-04-06.md`
- `docs/架构/147-CCP到Crate与接口模块落地映射设计-2026-04-06.md`

### 17.4 本轮增量验证

- Red：
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-resume-red'; cargo test -p session-gateway test_realtime_websocket_negotiates_ccp_subprotocol_and_wraps_business_frames --offline`
    - 失败点：服务端仍在 `auth_ok` 后直接发送 `connected`，未发送 `session_resumed`
- Green：
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-resume-green'; cargo test -p craw-chat-runtime-link test_runtime_link_builds_session_resumed_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-resume-green'; cargo test -p session-gateway test_realtime_websocket_negotiates_ccp_subprotocol_and_wraps_business_frames --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-resume-green'; cargo test -p session-gateway test_realtime_websocket_sends_ccp_goaway_before_disconnect_close --offline`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-resume-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-resume-full'; cargo test -p session-gateway --offline`

### 17.5 当前判断再更新

- `CP04-2`：仍未通过，但继续前推
  - 已新增并接入的 runtime owner：
    - 默认队列策略 owner
    - `hello` 协商 owner
    - `auth_bind` 身份匹配 owner
    - `resume` 纯语义 owner
    - `session_resume / session_resumed` 控制帧 owner
    - `goaway/close` 断连语义 owner
  - 仍未完整迁出的部分：
    - HTTP/WebSocket accept / upgrade 接入层
    - 真实 `queue/backpressure` 执行路径
- `CP04-3`：保持通过
- `CP04-4`：保持通过
- `91 / 95 / 97`：仍不允许整体放行
- `93`：`Wave B` 仍未完成，不触发总验收

### 17.6 下一轮继续做什么

1. 继续停留在 `Wave B / Step 04`
2. 继续只围绕 `CP04-2`
3. 优先转向 `accept / upgrade` owner 或真实 `queue/backpressure` 执行 owner
4. 完成上述两项前，不允许宣称 `Step 04` 已闭环
## 18. 2026-04-07 增量推进（runtime-link accept/upgrade owner 回收）

### 18.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- `session_resume / session_resumed` 与 `goaway/close` owner 回收后，剩余最小 owner 缺口集中在 `accept / upgrade` 与真实 `queue/backpressure`
- 本轮选择更小且不会大面积扰动热路径的 `accept / upgrade` contract 切片，继续按 TDD 推进 `CP04-2`

### 18.2 本轮实际完成

- `crates/craw-chat-runtime-link/Cargo.toml`
  - 新增 `craw-chat-ccp-binding-ws` 依赖，让 Link Plane owner 直接声明 WebSocket 子协议契约
- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `LINK_WEBSOCKET_SUBPROTOCOL`
  - 新增 `LinkWebsocketMode`
  - 新增 `supported_websocket_subprotocols()`
  - 新增 `select_websocket_mode(...)`
  - 新增测试 `test_runtime_link_exposes_websocket_upgrade_owner_contract`
- `services/session-gateway/src/lib.rs`
  - 新增 `realtime_websocket_subprotocols()`
  - 新增 `select_realtime_websocket_mode(...)`
  - `realtime_websocket(...)` 不再直接硬编码 `CCP_WS_SUBPROTOCOL` 与 `selected_protocol().is_some()`
  - 改为通过 `runtime-link` owner contract 决定 WebSocket 子协议暴露与 wire mode 选择
  - 新增测试 `test_realtime_websocket_upgrade_uses_runtime_link_owner_contract`
- `services/session-gateway/src/websocket.rs`
  - `CCP_WEBSOCKET_SUBPROTOCOL` 改为跟随 `runtime-link::LINK_WEBSOCKET_SUBPROTOCOL`

### 18.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-owner-red'; cargo test -p session-gateway test_realtime_websocket_upgrade_uses_runtime_link_owner_contract --offline`
  - 失败原因为 `realtime_websocket_subprotocols / select_realtime_websocket_mode` 缺失，符合 owner API 尚未落地的预期
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-owner-green'; cargo test -p session-gateway test_realtime_websocket_upgrade_uses_runtime_link_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-owner-green'; cargo test -p craw-chat-runtime-link test_runtime_link_exposes_websocket_upgrade_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-owner-green'; cargo test -p session-gateway test_realtime_websocket_negotiates_ccp_subprotocol_and_wraps_business_frames --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-owner-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-owner-session-full'; cargo test -p session-gateway --offline`

### 18.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已回收 owner
    - 默认队列策略
    - `auth_bind`
    - `hello`
    - `resume`
    - `session_resume / session_resumed`
    - `goaway/close`
    - `accept / upgrade` 的 WS 子协议声明与 wire mode 选择 contract
  - 仍未回收
    - Axum `on_upgrade(...)` 执行动作本身仍在 `session-gateway`
    - 真实 `queue/backpressure` 执行 owner 仍在 `session-gateway`
- `CP04-3` 已通过
- `CP04-4` 已通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体通过
- `Wave B / 93` 仍未触发

### 18.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先处理真实 `queue/backpressure` owner；若切片过大，再拆 `on_upgrade` 执行 owner
4. 未形成 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环
## 19. 2026-04-07 增量推进（runtime-link queue/backpressure batch owner 回收）

### 19.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- `accept / upgrade` 的最小 owner contract 已回收后，剩余最小热路径缺口集中在 live `queue/backpressure` 与 `on_upgrade`
- 本轮选择先把 `runtime-link` 的 soft/hard queue owner 接回真实 catchup / push / pull 执行路径，继续按 TDD 收敛 `CP04-2`

### 19.2 本轮实际完成

- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `LinkOutboundBatchPlan`
  - 新增 `OutboundQueuePolicy::plan_stream_batch(...)`
  - 新增 `OutboundQueuePolicy::plan_pull_batch(...)`
  - 新增 `LinkSession::plan_stream_batch(...)`
  - 新增 `LinkSession::plan_pull_batch(...)`
  - 新增测试 `test_runtime_link_plans_live_outbound_queue_batches_from_owner_limits`
- `services/session-gateway/src/websocket.rs`
  - websocket catchup 不再硬编码 `100`，改为消费 `runtime-link` 的 stream batch owner
  - websocket push 不再硬编码 `100`，改为消费 `runtime-link` 的 stream batch owner
  - `events.pull` 不再直接接受任意客户端 `limit`，改为通过 `runtime-link` hard limit 做 batch owner 裁决
  - 新增 `outbound_pending_events(...)` 作为热路径 backlog 适配函数
- `services/session-gateway/tests/websocket_smoke_test.rs`
  - 新增 `test_realtime_websocket_uses_runtime_link_queue_owner_limits_for_catchup_and_pull`
  - 验证 catchup 首批窗口按 soft limit=`128`
  - 验证 pull 请求按 hard limit=`512` 裁剪

### 19.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-owner-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_plans_live_outbound_queue_batches_from_owner_limits --offline`
    - 失败原因：`LinkSession` 尚不存在 `plan_stream_batch / plan_pull_batch`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-owner-red-session'; cargo test -p session-gateway test_realtime_websocket_uses_runtime_link_queue_owner_limits_for_catchup_and_pull --offline`
    - 失败原因：实际 catchup 窗口仍返回 `100`，而不是 owner 规定的 `128`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-owner-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_plans_live_outbound_queue_batches_from_owner_limits --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-owner-green-session'; cargo test -p session-gateway test_realtime_websocket_uses_runtime_link_queue_owner_limits_for_catchup_and_pull --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-owner-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-owner-session-full'; cargo test -p session-gateway --offline`

### 19.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已新增并接入的 runtime owner：
    - 默认队列策略 owner
    - live catchup batch owner
    - live push batch owner
    - live pull limit clamp owner
  - 仍未完整回收的部分：
    - Axum `on_upgrade(...)` 执行动作本身仍在 `session-gateway`
    - 真正的异步 outbound queue / buffer / drop / degrade / backpressure controller 全路径仍未完整回收
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 19.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先回收 `on_upgrade` 执行 owner；若切片不合适，则继续下沉完整 outbound queue / buffer / drop / degrade owner
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环

### 20. 2026-04-07 增量推进（runtime-link websocket upgrade handoff execute owner 回收）

### 20.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- 在 live batch/backpressure owner 已进入真实热路径后，剩余最小热路径缺口继续收敛到 websocket upgrade handoff execute owner 与完整 async queue controller
- 本轮选择先把 `on_upgrade` 闭包内部的 handoff execute owner 回收到 `runtime-link`，同时不误报 Axum adapter 已整体迁移

### 20.2 本轮实际完成

- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `LinkWebsocketUpgradeHandoff<TContext>`
  - 新增 `prepare_websocket_upgrade(selected_subprotocol, context)`
  - 新增 `LinkWebsocketUpgradeHandoff::mode()`
  - 新增 `LinkWebsocketUpgradeHandoff::context()`
  - 新增 `LinkWebsocketUpgradeHandoff::execute(...)`
  - 新增测试辅助 `poll_ready(...)`
  - 新增测试 `test_runtime_link_prepares_websocket_upgrade_handoff_owner_contract`
- `services/session-gateway/src/lib.rs`
  - 新增 `RealtimeWebsocketUpgradeContext`
  - 新增 `prepare_realtime_websocket_upgrade(...)`
  - 新增 `serve_realtime_websocket_upgrade(...)`
  - 新增 `map_runtime_link_websocket_mode(...)`
  - `realtime_websocket(...)` 改为先通过 `runtime-link` 构造 handoff，再调用 `.on_upgrade(move |socket| upgrade.execute(socket, serve_realtime_websocket_upgrade))`
  - 新增测试 `test_realtime_websocket_upgrade_prepares_runtime_link_handoff_owner`
- 本轮未改 `services/session-gateway/src/websocket.rs`

### 20.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-handoff-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_prepares_websocket_upgrade_handoff_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-handoff-red-session'; cargo test -p session-gateway test_realtime_websocket_upgrade_prepares_runtime_link_handoff_owner --offline`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-handoff-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-handoff-session-full'; cargo test -p session-gateway --offline`
  - `cargo test -p craw-chat-runtime-link test_runtime_link_prepares_websocket_upgrade_handoff_owner_contract --offline`
  - `cargo test -p session-gateway test_realtime_websocket_upgrade_prepares_runtime_link_handoff_owner --offline`
  - `cargo test -p session-gateway test_realtime_websocket_upgrade_uses_runtime_link_owner_contract --offline`
  - `cargo test -p session-gateway test_realtime_websocket_negotiates_ccp_subprotocol_and_wraps_business_frames --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`

### 20.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已新增并接入的 runtime owner：
    - websocket upgrade context owner
    - websocket wire mode owner
    - websocket handoff execute owner
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍在 `session-gateway`
    - 完整 async outbound queue / buffer / drop / degrade / overload controller 仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 20.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先回收完整 async outbound queue / buffer / drop / degrade / overload controller owner
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`

### 21. 2026-04-07 增量推进（runtime-link live push degrade-to-pull owner 回收）

### 21.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- websocket upgrade handoff execute owner 回收后，剩余可直接实证的最小 hot-path 缺口继续收敛到 backlog 过大时的 live push 降级裁决与完整 async queue controller
- 本轮选择先把“超 hard limit 时停止自动 push、退化为 pull-only”这段 owner 语义回收到 `runtime-link`，避免凭空发明新的外部协议

### 21.2 本轮实际完成

- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `LinkPushMode`
  - 新增 `LinkPushPlan`
  - 新增 `OutboundQueuePolicy::plan_push_batch(...)`
  - 新增 `LinkSession::plan_push_batch(...)`
  - 新增测试 `test_runtime_link_degrades_live_push_to_pull_only_when_backlog_exceeds_hard_limit`
- `services/session-gateway/src/websocket.rs`
  - websocket live push 路径不再直接按 watch 更新后立刻发送窗口
  - 改为先消费 `runtime-link` 的 `plan_push_batch(...)`
  - 当 owner 裁决为 `PullOnly` 时，停止自动 push，保留客户端 `events.pull`
- `services/session-gateway/tests/websocket_smoke_test.rs`
  - 新增 `test_realtime_websocket_degrades_live_push_to_pull_only_when_runtime_link_detects_overload`
  - 验证 backlog 超过 hard limit 时服务端不再自动 push，但 pull 仍可按 hard limit=512 拉取

### 21.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-degrade-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_degrades_live_push_to_pull_only_when_backlog_exceeds_hard_limit --offline`
    - 失败原因为缺少 `plan_push_batch / LinkPushMode`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-degrade-red-session'; cargo test -p session-gateway test_realtime_websocket_degrades_live_push_to_pull_only_when_runtime_link_detects_overload --offline`
    - 失败原因为 backlog 过大时 gateway 仍会继续自动 push
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-degrade-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_degrades_live_push_to_pull_only_when_backlog_exceeds_hard_limit --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-degrade-green-session'; cargo test -p session-gateway test_realtime_websocket_degrades_live_push_to_pull_only_when_runtime_link_detects_overload --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-degrade-runtime-full-2'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-degrade-session-full-2'; cargo test -p session-gateway --offline`

### 21.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已新增并接入的 runtime owner：
    - websocket upgrade contract / handoff execute owner
    - live catchup batch owner
    - live pull hard clamp owner
    - live push degrade-to-pull owner
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍在 `session-gateway`
    - 完整 async outbound queue / buffer / drop / overload controller 仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 21.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先回收完整 async outbound queue / buffer / drop / overload controller owner
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`

### 22. 2026-04-07 增量推进（runtime-link buffered push recovery after pull owner 回收）

### 22.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- live push degrade-to-pull owner 已回收到 `runtime-link` 后，下一处真实缺口是：
  - 连接在 backlog 超过 hard limit 后退化为 `pull-only`
  - 客户端随后通过 `events.pull` 把 backlog 压回 hard limit 以下
  - 服务器此前仍要等待“下一条新发布事件”才会恢复自动 push
- 本轮选择先把“pull 降压后恢复 buffered push”的 owner 语义回收到 `runtime-link`，避免 gateway 继续私有持有 backlog 恢复判断

### 22.2 本轮实际完成

- `crates/craw-chat-runtime-link/src/lib.rs`
  - 新增 `LinkBufferedPushPlan`
  - 新增 `LinkPushCursor`
  - 新增私有辅助 `outbound_pending_events(...)`
  - 新增 `LinkPushCursor::{new, delivered_after_seq, latest_realtime_seq, observe_latest_realtime_seq, acknowledge_window}`
  - 新增 `LinkSession::start_push_cursor(...)`
  - 新增测试 `test_runtime_link_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit`
- `services/session-gateway/src/websocket.rs`
  - websocket runtime hot path 开始接入 `LinkPushCursor`
  - initial catchup 完成后，会基于 `last_sent_seq` 与 `latest_realtime_seq` 初始化 runtime owner 的 push cursor
  - watch 驱动的 live push 改为先调用 `push_cursor.observe_latest_realtime_seq(...)`
  - 新增 `flush_buffered_push_windows(...)`，在 `Immediate` 模式下持续消费 runtime owner 的 buffered push plan
  - `events.pull` 在发送 pull 窗口后，会调用 `push_cursor.acknowledge_window(...)`，允许 backlog 降到 hard limit 以下后立即恢复 buffered push，而不是等待下一条新发布事件
- `services/session-gateway/tests/websocket_smoke_test.rs`
  - 新增 `test_realtime_websocket_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit`

### 22.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit --offline`
    - 失败原因：`LinkSession::start_push_cursor` 尚不存在
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-red-session'; cargo test -p session-gateway test_realtime_websocket_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit --offline`
    - 失败原因：pull 降压后没有恢复 buffered push
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-green-session-2'; cargo test -p session-gateway test_realtime_websocket_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-green-session-2'; cargo test -p session-gateway test_realtime_websocket_degrades_live_push_to_pull_only_when_runtime_link_detects_overload --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-push-buffer-session-full'; cargo test -p session-gateway --offline`

### 22.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已新增并接入的 runtime owner：
    - websocket upgrade contract / handoff execute owner
    - live catchup batch owner
    - live pull hard clamp owner
    - live push degrade-to-pull owner
    - pull 降压后恢复 buffered push owner
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍在 `session-gateway`
    - 完整 async outbound queue / buffer / drop / overload close controller 仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 22.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先回收 backlog drop 或 overload close owner，补齐完整 async queue / buffer / drop / overload controller 的最后缺口
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`

### 23. 2026-04-07 增量推进（runtime-link extreme overload close owner 回收）

### 23.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- `runtime-link` 已具备：
  - live batch owner
  - pull clamp owner
  - degrade-to-pull owner
  - buffered push recovery owner
- 依据 `docs/step/04` 与 `docs/架构/131` 的约束，慢消费者在极端情况下必须主动断开异常连接
- 本轮选择先把“extreme overload close owner”回收到 `runtime-link`，因为它比“backlog drop”更贴近当前真实热路径，也更少引入伪语义

### 23.2 本轮实际完成

- `crates/craw-chat-runtime-link/src/lib.rs`
  - `LinkPushMode` 新增 `Disconnect`
  - `LinkPushPlan` 新增 `disconnect: Option<LinkGoAwayDirective>`
  - 新增过载关闭常量：
    - `REALTIME_OVERLOAD_CLOSE_CODE`
    - `REALTIME_OVERLOAD_CLOSE_REASON`
    - `REALTIME_OVERLOAD_GOAWAY_CODE`
    - `REALTIME_OVERLOAD_GOAWAY_MESSAGE`
  - `OutboundQueuePolicy::plan_push_batch(...)` 新增 extreme overload close 裁决
  - 新增 `realtime_overload_goaway()`
  - 新增测试 `test_runtime_link_closes_connection_when_backlog_exceeds_overload_disconnect_limit`
- `services/session-gateway/src/websocket.rs`
  - 新增通用 helper `send_link_goaway_and_close(...)`
  - session disconnect 也改为复用上述 helper
  - `flush_buffered_push_windows(...)` 在 runtime owner 裁决为 `Disconnect` 时，发送 close 信号并结束连接
  - 对外新增 `REALTIME_OVERLOAD_CLOSE_CODE / REALTIME_OVERLOAD_CLOSE_REASON`
- `services/session-gateway/src/lib.rs`
  - 导出 `REALTIME_OVERLOAD_CLOSE_CODE / REALTIME_OVERLOAD_CLOSE_REASON`
- `services/session-gateway/tests/websocket_smoke_test.rs`
  - 新增 `test_realtime_websocket_closes_when_runtime_link_detects_extreme_overload_backlog`

### 23.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_closes_connection_when_backlog_exceeds_overload_disconnect_limit --offline`
    - 失败原因：缺少 `Disconnect` 裁决、过载关闭常量和 `disconnect` 指令字段
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-red-session'; cargo test -p session-gateway test_realtime_websocket_closes_when_runtime_link_detects_extreme_overload_backlog --offline`
    - 失败原因：缺少 `REALTIME_OVERLOAD_CLOSE_CODE / REALTIME_OVERLOAD_CLOSE_REASON` 导出
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-green-runtime-2'; cargo test -p craw-chat-runtime-link test_runtime_link_closes_connection_when_backlog_exceeds_overload_disconnect_limit --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-green-session-2'; cargo test -p session-gateway test_realtime_websocket_closes_when_runtime_link_detects_extreme_overload_backlog --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-green-runtime-recovery'; cargo test -p craw-chat-runtime-link test_runtime_link_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-green-session-recovery'; cargo test -p session-gateway test_realtime_websocket_recovers_buffered_push_after_pull_reduces_backlog_under_hard_limit --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-overload-close-session-full'; cargo test -p session-gateway --offline`

### 23.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已新增并接入的 runtime owner：
    - websocket upgrade contract / handoff execute owner
    - live catchup batch owner
    - live pull hard clamp owner
    - live push degrade-to-pull owner
    - pull 降压后恢复 buffered push owner
    - extreme overload close owner
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍在 `session-gateway`
    - backlog drop 语义仍未回收到 `runtime-link`
    - 完整 async outbound queue / buffer 实体仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 23.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先回收 backlog drop owner，或在证据充分时明确把 Axum adapter 保留为 transport seam 的最终边界
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`

### 24. 2026-04-07 增量推进（session-gateway websocket upgrade transport seam 收敛）

### 24.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- websocket upgrade contract / handoff execute 已回收到 `runtime-link` 后，`services/session-gateway/src/lib.rs` 里仍散落保留：
  - subprotocol 选择辅助
  - `LinkWebsocketMode -> RealtimeWebsocketMode` 映射
  - upgrade prepare / serve 辅助
- 这会继续放大 `lib.rs`，也会让 Axum transport seam 的边界不够清晰
- 本轮选择先把 Axum `WebSocketUpgrade` 相关 helper 收敛到单一 transport seam 模块，避免把“服务层保留 transport adapter”误写成“服务层继续私有持有 upgrade 细节”

### 24.2 本轮实际完成

- `services/session-gateway/src/websocket_upgrade.rs`
  - 新增单独的 websocket upgrade transport seam 模块
  - 移入：
    - `realtime_websocket_subprotocols()`
    - `select_realtime_websocket_mode(...)`
    - `map_runtime_link_websocket_mode(...)`
    - `prepare_realtime_websocket_upgrade(...)`
    - `serve_realtime_websocket_upgrade(...)`
    - `upgrade_realtime_websocket(...)`
- `services/session-gateway/src/lib.rs`
  - `realtime_websocket(...)` 现在只保留：
    - 认证上下文解析
    - route / session fence 校验
    - device 注册
    - 调用 `websocket_upgrade::upgrade_realtime_websocket(...)`
  - 不再直接承载 websocket upgrade seam helper
- `services/session-gateway/tests/lib_structure_test.rs`
  - 新增 `test_session_gateway_websocket_upgrade_transport_seam_moves_out_of_lib_impl`
  - 用结构测试约束 websocket upgrade seam 不得重新漂回 `lib.rs`

### 24.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-seam-red'; cargo test -p session-gateway test_session_gateway_websocket_upgrade_transport_seam_moves_out_of_lib_impl --offline`
    - 失败原因：websocket upgrade helper 仍位于 `services/session-gateway/src/lib.rs`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-seam-green-2'; cargo test -p session-gateway test_session_gateway_websocket_upgrade_transport_seam_moves_out_of_lib_impl --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-seam-green-2'; cargo test -p session-gateway test_realtime_websocket_upgrade_uses_runtime_link_owner_contract --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-seam-green-2'; cargo test -p session-gateway test_realtime_websocket_upgrade_prepares_runtime_link_handoff_owner --offline`
  - `cargo fmt --package session-gateway`
  - `cargo fmt --check --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-upgrade-seam-session-full'; cargo test -p session-gateway --offline`

### 24.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但缺口继续收敛
  - 已新增并接入的 runtime / seam 边界：
    - websocket upgrade contract owner
    - websocket upgrade handoff execute owner
    - 单一 `session-gateway` transport seam 模块
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍保留在 `session-gateway` 的 transport seam
    - backlog drop 语义仍未回收到 `runtime-link`
    - 完整 async outbound queue / buffer 实体仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 24.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 在“Axum transport seam 保留为最终边界”与“继续回收 backlog drop / async queue 实体”之间，优先选择可被 fail-first 证明的最小真实切片
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`

### 25. 2026-04-07 增量推进（session-gateway websocket route handler 收敛到 transport seam 模块）

### 25.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- 上一轮虽然已经把 websocket upgrade helper 从 `lib.rs` 收敛到 `websocket_upgrade.rs`，但：
  - `/api/v1/realtime/ws` 的路由 handler 仍在 `services/session-gateway/src/lib.rs`
  - `lib.rs` 仍直接导入 `WebSocketUpgrade`
  - `lib.rs` 仍直接调用 `websocket_upgrade::upgrade_realtime_websocket(...)`
- 本轮继续把 websocket 路由入口整体移到单一 transport seam 模块，避免 `lib.rs` 继续保留热路径入口

### 25.2 本轮实际完成

- `services/session-gateway/tests/lib_structure_test.rs`
  - 新增 `test_session_gateway_websocket_route_handler_moves_out_of_lib_impl`
  - fail-first 约束：
    - `lib.rs` 不得继续保留 `WebSocketUpgrade` 导入
    - `lib.rs` 不得继续定义 `async fn realtime_websocket(...)`
    - `lib.rs` 不得继续直接调用 `websocket_upgrade::upgrade_realtime_websocket(...)`
    - `websocket_upgrade.rs` 必须承接 websocket route handler
- `services/session-gateway/src/websocket_upgrade.rs`
  - 新增 `pub(crate) async fn realtime_websocket(...)`
  - route handler 现在在单一 transport seam 模块内完成：
    - auth 解析
    - device id 解析
    - route/session fence 校验
    - websocket register_device
    - 调用 `upgrade_realtime_websocket(...)`
- `services/session-gateway/src/lib.rs`
  - `/api/v1/realtime/ws` 路由改为直接挂到 `websocket_upgrade::realtime_websocket`
  - 删除本地 `realtime_websocket(...)` handler
  - 删除 `WebSocketUpgrade` 直接导入

### 25.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-route-seam-red'; cargo test -p session-gateway test_session_gateway_websocket_route_handler_moves_out_of_lib_impl --offline`
    - 失败原因：`services/session-gateway/src/lib.rs` 仍直接保留 `WebSocketUpgrade` 导入和 websocket route handler
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-route-seam-green'; cargo test -p session-gateway test_session_gateway_websocket_route_handler_moves_out_of_lib_impl --offline`
  - `cargo fmt --package session-gateway`
  - `cargo fmt --check --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-route-seam-session-full'; cargo test -p session-gateway --offline`

### 25.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但服务侧 websocket 边界继续收窄
  - 已新增并接入的 service-side seam 收敛：
    - websocket upgrade helper 集中到 `websocket_upgrade.rs`
    - websocket route handler 也集中到 `websocket_upgrade.rs`
    - `lib.rs` 仅保留路由装配，不再直接持有 websocket 热路径入口
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍保留在 `session-gateway` 的 transport seam 模块
    - backlog drop 语义仍未回收到 `runtime-link`
    - 完整 async outbound queue / buffer 实体仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 25.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先判断“单一 Axum transport seam 模块是否已是可接受最终边界”，若仍不足，则继续寻找可 fail-first 的 backlog drop / queue 实体切片
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`

### 26. 2026-04-07 增量推进（runtime-link pending backlog 数学 owner 回收）

### 26.1 本轮为什么继续做 Step 04

- 当前仍处于 `Wave B / Step 04`
- `CP04-3 / CP04-4` 已通过，但 `CP04-2` 仍未闭环
- 在 websocket route / upgrade seam 已集中后，`session-gateway/src/websocket.rs` 仍保留一份本地 backlog 公式：
  - `fn outbound_pending_events(after_seq, latest_realtime_seq)`
- 这会让 queue/controller 的输入数学继续在服务层复制，和 `runtime-link` 形成重复 authority
- 本轮选择先把 pending backlog 计算收回 `runtime-link`，继续压缩服务层对 queue 输入的私有持有

### 26.2 本轮实际完成

- `crates/craw-chat-runtime-link/src/lib.rs`
  - 将 backlog 数学导出为 `pending_outbound_events(...)`
  - `LinkPushCursor` 内部也统一改用该 owner 公式
  - 新增测试 `test_runtime_link_counts_pending_outbound_events_with_saturating_math`
- `services/session-gateway/src/websocket.rs`
  - catchup / pull 路径改为直接调用 `runtime-link::pending_outbound_events(...)`
  - 删除本地 `outbound_pending_events(...)`
- `services/session-gateway/tests/lib_structure_test.rs`
  - 新增 `test_session_gateway_websocket_pending_backlog_math_moves_out_of_service_impl`
  - 用结构门禁约束服务层不得重新持有 backlog 数学 helper

### 26.3 验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-pending-math-red'; cargo test -p session-gateway test_session_gateway_websocket_pending_backlog_math_moves_out_of_service_impl --offline`
    - 失败原因：`services/session-gateway/src/websocket.rs` 仍保留本地 backlog helper
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-pending-math-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_counts_pending_outbound_events_with_saturating_math --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-pending-math-green-session'; cargo test -p session-gateway test_session_gateway_websocket_pending_backlog_math_moves_out_of_service_impl --offline`
  - `cargo fmt --package craw-chat-runtime-link --package session-gateway`
  - `cargo fmt --check --package craw-chat-runtime-link --package session-gateway`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-pending-math-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-pending-math-session-full'; cargo test -p session-gateway --offline`

### 26.4 对 Step 04 判定影响

- `CP04-2` 仍未通过，但 queue/controller 输入 authority 继续收敛
  - 已新增并接入的 runtime owner：
    - pending backlog 数学 `pending_outbound_events(...)`
  - 服务层不再私有持有该公式
  - 仍未完整回收的部分：
    - Axum `WebSocketUpgrade` adapter 与 `.on_upgrade(...)` 入口调用仍保留在 `session-gateway` transport seam
    - backlog drop 语义仍未回收到 `runtime-link`
    - 完整 async outbound queue / buffer 实体仍未回收到 `runtime-link`
- `CP04-3` 保持通过
- `CP04-4` 保持通过
- `91 / 95 / 97` 对 `Step 04` 仍未整体放行
- `Wave B / 93` 仍未触发

### 26.5 下一轮动作

1. 继续停留在 `Wave B / Step 04`
2. 继续只推进 `CP04-2`
3. 优先在“单一 Axum transport seam 是否足够”与“backlog drop / async queue 实体”之间继续选择可 fail-first 的真实切片
4. 在 `91 / 95 / 97` 整体通过前，不得宣布 `Step 04` 闭环或触发 `Wave B / 93`
## 27. 2026-04-07 runtime-link outbound queue state owner

### 27.1 why this slice

- current state stays at `Wave B / Step 04`
- `CP04-3` and `CP04-4` are already passed, but `CP04-2` is still open
- `services/session-gateway/src/websocket.rs` still kept two local mutable outbound queue state holders:
  - `last_sent_seq`
  - `push_cursor`
- because the combined outbound queue controller still lived in the service hot path, `Step 04` could not truthfully claim that the Link Plane outbound queue state had moved into the runtime owner

### 27.2 actual work

- `crates/craw-chat-runtime-link/src/lib.rs`
  - added `LinkOutboundWindowPlan`
  - added `LinkOutboundQueueState`
  - added `LinkSession::start_outbound_queue(...)`
  - moved the combined outbound queue state into `runtime-link`:
    - `last_sent_after_seq`
    - `delivered_after_seq`
    - `latest_realtime_seq`
    - catchup planning
    - pull planning
    - buffered push recovery
    - client ack bookkeeping
  - kept `LinkPushCursor` as a compatibility wrapper over the new owner so previously passed `CP04-2` slices do not regress
- `services/session-gateway/src/websocket.rs`
  - replaced local `last_sent_seq + push_cursor` with one `outbound_queue`
  - catchup now consumes `outbound_queue.plan_catchup()`
  - realtime watch path now consumes `outbound_queue.observe_latest_realtime_seq(...)`
  - pull path now consumes `outbound_queue.plan_pull(...)`
  - post-send advancement now consumes `outbound_queue.record_window_sent(...)`
  - ack path now consumes `outbound_queue.record_client_ack(...)`
- `services/session-gateway/tests/lib_structure_test.rs`
  - kept and greened `test_session_gateway_websocket_outbound_queue_state_moves_out_of_service_impl`

### 27.3 verification

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-state-red-session'; cargo test -p session-gateway test_session_gateway_websocket_outbound_queue_state_moves_out_of_service_impl --offline`
    - failed as expected because `services/session-gateway/src/websocket.rs` still contained `let mut last_sent_seq =`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-state-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_outbound_queue_state_owns_last_sent_seq_and_buffered_push_recovery --offline`
    - failed as expected because `LinkSession` did not yet provide `start_outbound_queue(...)`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-state-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_outbound_queue_state_owns_last_sent_seq_and_buffered_push_recovery --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-queue-state-green-session'; cargo test -p session-gateway test_session_gateway_websocket_outbound_queue_state_moves_out_of_service_impl --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-full'; cargo test -p session-gateway --offline`
  - `rustfmt --edition 2024 crates/craw-chat-runtime-link/src/lib.rs services/session-gateway/src/websocket.rs services/session-gateway/tests/lib_structure_test.rs`
  - `rustfmt --edition 2024 --check crates/craw-chat-runtime-link/src/lib.rs services/session-gateway/src/websocket.rs services/session-gateway/tests/lib_structure_test.rs`

### 27.4 impact on Step 04

- `CP04-2` is still not passed overall, but one remaining gap is now removed:
  - the combined outbound queue state owner is no longer local to `session-gateway`
- remaining truthful gaps for `CP04-2`:
  - Axum `WebSocketUpgrade` / `.on_upgrade(...)` still remains in the gateway transport seam
  - backlog drop semantics are still not owned by `runtime-link`
- `CP04-3` stays passed
- `CP04-4` stays passed
- `Step 04` is still not closed
- `91 / 95 / 97` are still not passed overall for `Step 04`
- `Wave B / 93` is still not triggered

### 27.5 next slice

1. stay in `Wave B / Step 04`
2. continue only `CP04-2`
3. choose the next fail-first slice between:
   - transport adapter boundary (`WebSocketUpgrade` / `.on_upgrade(...)`)
   - backlog drop owner
4. do not claim `Step 04` closure and do not trigger `Wave B / 93` before `91 / 95 / 97` pass overall

## 29. 2026-04-07 runtime-link buffered push drain loop owner

### 29.1 why this slice

- current state stays at `Wave B / Step 04`
- `CP04-3` and `CP04-4` are already passed, but `CP04-2` is still open
- after the combined outbound queue state and stale replay clamp moved into `runtime-link`, one still-truthful remaining gap was the service-local async drain loop:
  - `services/session-gateway/src/websocket.rs::flush_buffered_push_windows(...)`
- if that async loop stayed in the service hot path, the Link Plane still could not truthfully claim that buffered push drain execution belonged to the runtime owner

### 29.2 actual work

- `crates/craw-chat-runtime-link/src/lib.rs`
  - added `LinkBufferedPushFetchedWindow<TWindow>`
  - added `LinkBufferedPushDrainStatus`
  - added `LinkBufferedPushDrainDriver`
  - added `LinkOutboundQueueState::drain_buffered_push_windows(...)`
  - moved the buffered push async drain loop into `runtime-link`:
    - mode gate (`Immediate / PullOnly / Disconnect`)
    - fetch-next-window loop
    - send-next-window loop
    - post-send frontier advancement
- `services/session-gateway/src/websocket.rs`
  - removed service-local `flush_buffered_push_windows(...)`
  - added `BufferedPushDrainDriver` as a transport/runtime adapter:
    - fetches event windows from `RealtimeDeliveryRuntime`
    - sends websocket `event.window` payloads
    - maps runtime/send failures back into gateway error handling
  - the gateway no longer owns the buffered push loop itself
- `services/session-gateway/tests/lib_structure_test.rs`
  - added `test_session_gateway_websocket_buffered_push_drain_loop_moves_out_of_service_impl`
- `crates/craw-chat-runtime-link/src/lib.rs`
  - added `test_runtime_link_drains_buffered_push_windows_via_owner_async_loop`

### 29.3 verification

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-buffer-drain-red-session'; cargo test -p session-gateway test_session_gateway_websocket_buffered_push_drain_loop_moves_out_of_service_impl --offline`
    - failed as expected because `services/session-gateway/src/websocket.rs` still contained `async fn flush_buffered_push_windows(...)`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-buffer-drain-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_drains_buffered_push_windows_via_owner_async_loop --offline`
    - failed as expected because `LinkOutboundQueueState` did not yet expose `drain_buffered_push_windows(...)`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-buffer-drain-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_drains_buffered_push_windows_via_owner_async_loop --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-buffer-drain-green-session'; cargo test -p session-gateway test_session_gateway_websocket_buffered_push_drain_loop_moves_out_of_service_impl --offline`
  - `rustfmt --edition 2024 crates/craw-chat-runtime-link/src/lib.rs services/session-gateway/src/websocket.rs services/session-gateway/tests/lib_structure_test.rs`
  - `rustfmt --edition 2024 --check crates/craw-chat-runtime-link/src/lib.rs services/session-gateway/src/websocket.rs services/session-gateway/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-buffer-drain-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-buffer-drain-session-full'; cargo test -p session-gateway --offline`

### 29.4 impact on Step 04

- `CP04-2` is still not passed overall, but one more truthful gap is now removed:
  - the buffered push async drain loop is no longer local to `session-gateway`
- remaining truthful gaps for `CP04-2` are now concentrated to:
  - whether Axum `WebSocketUpgrade` / `.on_upgrade(...)` is the final acceptable transport seam boundary
- `CP04-3` stays passed
- `CP04-4` stays passed
- `Step 04` is still not closed
- `91 / 95 / 97` are still not passed overall for `Step 04`
- `Wave B / 93` is still not triggered

### 29.5 next slice

1. stay in `Wave B / Step 04`
2. continue only `CP04-2`
3. prefer the next fail-first slice that proves whether the Axum transport seam is the final allowed boundary for Step 04
4. do not claim `Step 04` closure and do not trigger `Wave B / 93` before `91 / 95 / 97` pass overall

## 28. 2026-04-07 runtime-link stale pull replay clamp owner

### 28.1 why this slice

- current state stays at `Wave B / Step 04`
- `CP04-3` and `CP04-4` are already passed, but `CP04-2` is still open
- after the combined outbound queue state moved into `runtime-link`, one truthful remaining gap was stale replay under overload
- if a client could still send `events.pull { afterSeq: 0 }` while backlog from the current `last_sent_after_seq` frontier was still above `hard_limit`, the gateway would still effectively own overload-time replay rewind semantics

### 28.2 actual work

- `crates/craw-chat-runtime-link/src/lib.rs`
  - `LinkOutboundQueueState::plan_pull(...)` now clamps a stale requested `after_seq` up to `last_sent_after_seq` when backlog from the current sent frontier is still above `hard_limit`
  - this keeps non-overloaded replay rewind behavior unchanged
- `services/session-gateway/tests/websocket_smoke_test.rs`
  - added `test_realtime_websocket_clamps_stale_pull_replay_when_backlog_is_still_over_hard_limit`
  - proves that after catchup sends the first `128` events, a stale `afterSeq: 0` pull under `900` pending events starts replay from seq `129`, not seq `1`

### 28.3 verification

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-stale-pull-red-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_drops_stale_pull_replay_when_backlog_is_still_over_hard_limit --offline`
    - failed as expected because `plan_pull(...)` still returned `after_seq = 0`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-stale-pull-red-session'; cargo test -p session-gateway test_realtime_websocket_clamps_stale_pull_replay_when_backlog_is_still_over_hard_limit --offline`
    - failed as expected because the first replayed event still started at seq `1`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-stale-pull-green-runtime'; cargo test -p craw-chat-runtime-link test_runtime_link_drops_stale_pull_replay_when_backlog_is_still_over_hard_limit --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-stale-pull-green-session'; cargo test -p session-gateway test_realtime_websocket_clamps_stale_pull_replay_when_backlog_is_still_over_hard_limit --offline`
  - `rustfmt --edition 2024 crates/craw-chat-runtime-link/src/lib.rs services/session-gateway/tests/websocket_smoke_test.rs`
  - `rustfmt --edition 2024 --check crates/craw-chat-runtime-link/src/lib.rs services/session-gateway/tests/websocket_smoke_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-stale-pull-runtime-full'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-stale-pull-session-full'; cargo test -p session-gateway --offline`

### 28.4 impact on Step 04

- `CP04-2` is still not passed overall, but one more truthful gap is now removed:
  - `runtime-link` now owns the overload-time stale replay clamp instead of the gateway implicitly allowing replay rewind under pressure
- remaining truthful gaps for `CP04-2`:
  - Axum `WebSocketUpgrade` / `.on_upgrade(...)` still remains in the gateway transport seam
  - a complete runtime-owned async outbound queue / buffer entity still does not exist
- `CP04-3` stays passed
- `CP04-4` stays passed
- `Step 04` is still not closed
- `91 / 95 / 97` are still not passed overall for `Step 04`
- `Wave B / 93` is still not triggered

### 28.5 next slice

1. stay in `Wave B / Step 04`
2. continue only `CP04-2`
3. choose the next fail-first slice between:
   - proving that the Axum transport seam is the final acceptable boundary
   - pulling one more truthful async queue / buffer entity into `runtime-link`
4. do not claim `Step 04` closure and do not trigger `Wave B / 93` before `91 / 95 / 97` pass overall

## 30. 2026-04-07 Axum transport seam final-boundary acceptance

### 30.1 why this slice

- current state still started at `Wave B / Step 04`
- `CP04-3` and `CP04-4` were already passed, but `CP04-2` was still open
- after the combined outbound queue state, stale replay clamp, and buffered push drain loop moved into `runtime-link`, the only truthful Step 04 gap was whether Axum `WebSocketUpgrade` / `.on_upgrade(...)` should remain as the final retained transport seam
- if `services/session-gateway/src/websocket_upgrade.rs` still mixed auth/device/fence/register preflight with the framework adapter, the gateway would still keep hot-path business ownership instead of a pure transport seam

### 30.2 actual work

- `services/session-gateway/src/websocket_route.rs`
  - added `RealtimeWebsocketRouteContext`
  - added `prepare_realtime_websocket_route(...)`
  - moved websocket route preflight into that module:
    - auth resolve
    - device id resolve
    - route/session fence check
    - websocket `register_device(...)`
- `services/session-gateway/src/websocket_upgrade.rs`
  - kept only Axum-facing adapter responsibilities:
    - `WebSocketUpgrade` extractor entrypoint
    - subprotocol offer
    - runtime-link handoff prepare
    - `.on_upgrade(...)` execute
    - transport-to-runtime socket bridge
  - delegated route preflight to `websocket_route::prepare_realtime_websocket_route(...)`
- `services/session-gateway/src/lib.rs`
  - registered `mod websocket_route;`
  - kept `/api/v1/realtime/ws` routing on `websocket_upgrade::realtime_websocket`
- `services/session-gateway/tests/lib_structure_test.rs`
  - added `test_session_gateway_websocket_upgrade_module_stays_pure_axum_adapter`
  - locked the final Step 04 seam rule:
    - `websocket_upgrade.rs` must not keep auth/device/fence/register preflight
    - `websocket_route.rs` must own the route preflight
    - `websocket_upgrade.rs` remains the single Axum `WebSocketUpgrade` / `.on_upgrade(...)` seam

### 30.3 verification

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-axum-adapter-red-2'; cargo test -p session-gateway test_session_gateway_websocket_upgrade_module_stays_pure_axum_adapter --offline`
    - failed as expected because `services/session-gateway/src/websocket_route.rs` did not yet exist
- Green
  - `rustfmt --edition 2024 services/session-gateway/src/websocket_route.rs services/session-gateway/src/websocket_upgrade.rs services/session-gateway/src/lib.rs services/session-gateway/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-axum-adapter-green-2'; cargo test -p session-gateway test_session_gateway_websocket_upgrade_module_stays_pure_axum_adapter --offline`
  - `rustfmt --edition 2024 --check services/session-gateway/src/websocket_route.rs services/session-gateway/src/websocket_upgrade.rs services/session-gateway/src/lib.rs services/session-gateway/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-session-full-axum-boundary'; cargo test -p session-gateway --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-runtime-link-full-axum-boundary'; cargo test -p craw-chat-runtime-link --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-runtime-route-full-axum-boundary'; cargo test -p craw-chat-runtime-route --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step04-local-node-full-axum-boundary'; cargo test -p local-minimal-node --offline`

### 30.4 impact on Step 04

- `CP04-2` now passes
  - the retained gateway boundary is now a pure Axum transport seam instead of a mixed hot-path business owner
  - route preflight is explicitly separated from the Axum adapter
  - `runtime-link` remains the owner of websocket contract, handoff execute, queue/backpressure, replay clamp, and buffered drain semantics
- `CP04-3` stays passed
- `CP04-4` stays passed
- `Step 04` is now closed
- `91 / 95 / 97` now pass overall for `Step 04`
- `Wave B / 93` is still not triggered because `Wave B` still has open `Step 05` and `Step 06`

### 30.5 next slice

1. advance to `Wave B / Step 05`
2. keep `Step 04` closed unless later work exposes a regression
3. re-check `Step 05` code, tests, review evidence, and architecture gaps before writing new business-path code
