# Step 08 执行卡 - 2026-04-08

## 当前上下文
- 当前波次：`Wave C`
- 当前 step：`Step 08`
- 当前状态：
  - `CP08-1` 已完成统一 Agent / Device 主体模型
  - `CP08-2` 已完成 Agent 响应流与 tool call 生命周期
  - `CP08-3` 已完成 device telemetry / command 主路径与 gateway/capability/sender 收口
  - 当前需要判定的是：`Step 08` 是否已满足整步 `91 / 95 / 97`

## 本轮为什么做整步审计
- `docs/step/95-架构能力闭环验收标准.md` 对 `Step 08` 的闭环要求是：
  - Agent 输出与 Device 数据都接入统一 sender / actor / scope
  - 至少一条 Agent 路径和一条 Device 路径可跑通
  - 扩展能力不破坏普通 IM 路径
- 当前仓库在 `CP08-1 / 2 / 3` 的组合状态下，已经具备做整步闭环判断的基础。

## 整步实际完成

### 1. Agent 路径已形成最小闭环
- `AgentSubject::sender(...)` 已冻结 Agent 主体 sender
- `automation-service` 已把 Agent 响应流与 tool call 生命周期接到统一 `StreamSession / StreamFrame`
- `agent_response_lifecycle_test` 已持续验证这条最小主路径

### 2. Device 路径已形成最小闭环
- `device.telemetry` 已通过 `local-minimal-node -> streaming-runtime` 进入统一 stream 主链路
- `device.command` 已通过统一 stream 主链路下发，并受 `device.command.send` 能力控制
- `device.telemetry.read` 已成为真实读能力边界
- `device_scope_guard_test` 已锁死 standalone streaming-service 不允许绕过 gateway

### 3. 普通 IM / stream 主路径未被扩展能力破坏
- `stream_lifecycle_test` 继续通过
- `test_local_minimal_profile_exposes_generic_stream_frame_transport` 继续通过
- `test_conversation_bound_stream_writes_reject_bearer_actor_kind_mismatch` 继续通过

## Step 08 验证证据
- `cargo test -p im-platform-contracts --test agent_device_subject_model_test --offline`
- `cargo test -p automation-service --test agent_response_lifecycle_test --offline`
- `cargo test -p local-minimal-node --test device_stream_e2e_test --offline`
- `cargo test -p streaming-service --test device_scope_guard_test --offline`
- `cargo test -p streaming-service --test stream_lifecycle_test --offline`
- `cargo test -p local-minimal-node --test access_control_e2e_test test_conversation_bound_stream_writes_reject_bearer_actor_kind_mismatch --offline -- --exact`
- `cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_exposes_generic_stream_frame_transport --offline -- --exact`
- `cargo fmt --check`

## 结论
- `91`：通过
- `95`：通过
- `97`：已完成
- `Step 08`：闭环完成

## 当前还差什么
- `Wave C` 仍未闭环，因为 `Step 09` 的存储 / 投影 / 可观测治理尚未完成
- `Wave C / 93` 仍不允许提前执行

## 下一轮继续做什么
1. 自动进入 `Wave C / Step 09`
2. 严格围绕 storage / projection / observability / backup / restore / repair 推进
3. 不回退 `Step 08` 刚建立的 unified sender / actor / capability 边界
