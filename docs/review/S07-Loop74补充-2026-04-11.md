# S07 Loop74补充 - 2026-04-11

## 1. 本轮唯一主缺口
- Loop74 固定只做 `dead-letter inventory metadata parity assertion lock`
- `claim richer batch remediation / suggested-action contract`、`scheduler / stale-aware repair / exactly-once / https public runtime target` 继续 deferred，不与本轮并行混做

## 2. 本轮交付
- dead-letter inventory lifecycle 当前新增 direct parity assertions
- dead-letter inventory item 当前显式锁定：
  - `leaseStatus = unclaimed`
  - `takeoverEligible = false`
  - `legacyTakeoverRequired = false`
  - `ownerActorId / ownerActorKind / claimedAt / leaseExpiresAt = null`
- 本轮复核后的真实情况是：pending inventory 与 dead-letter inventory 已通过共享 helper 返回同一套 lease/takeover metadata；本轮补的是 dedicated regression lock，而不是新的 runtime policy

## 3. 运行时边界
- 本轮没有：
  - 改变 pending / dead-letter inventory response shape
  - 改变 claim / release / takeover allow/deny 语义
  - 实现 richer claim batch remediation / suggested-action contract
  - 实现 automatic stale detection / timeout reclaim
  - 实现 stale-aware `repair-shared-channel-sync`
  - 引入 release-ready exactly-once semantics

## 4. Fresh Evidence
- green：
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_inventory_and_targeted_requeue_only_restores_selected_actor -- --nocapture`
- green 证据：
  - dead-letter inventory item 当前显式返回 `leaseStatus = unclaimed`
  - dead-letter inventory item 当前显式返回 `takeoverEligible = false`
  - dead-letter inventory item 当前显式返回 `legacyTakeoverRequired = false`
  - dead-letter inventory item 当前显式返回 `ownerActorId / ownerActorKind / claimedAt / leaseExpiresAt = null`

## 5. 结论
- `S07` 仍只可标记为 `not_closed / local_closure`
- Loop74 已把 shared-channel sync 从“dead-letter metadata parity 主要依赖共享 helper 的隐式事实”推进到“dead-letter metadata parity 被 dedicated test 直接锁死”
- 下一单点主缺口继续收敛为 `claim richer batch remediation / suggested-action contract`
