# S12 执行卡 - 2026-04-10

## 1. 架构对齐
- 对齐能力：`device actor / device_twin / session access / provider health / IoT uplink-downlink / external_connection / external_member_link / shared_channel_policy`
- 对齐文档：
  - `docs/prompts/反复执行Step指令.md`
  - `docs/step/100-面向DDD的IM重构升级总实施计划-2026-04-09.md`
  - `docs/step/95-架构能力闭环验收标准.md`
  - `docs/step/97-Step完成后的架构回写与能力兑现清单.md`
  - `docs/架构/134-AI-Agent-IoT统一实时通信模型设计-2026-04-06.md`
  - `docs/架构/150CJ-im-social-space-conversation-ddd-design-2026-04-09.md`
  - `docs/架构/151CJ-im-benchmark-model-alignment-2026-04-09.md`
  - `docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
- 当前目标：基于已落地的 `device_twin` runtime/mainline，把 `S12` 从 `local_closure` 升级为 `step_closure`。
- 当前不宣称：
  - `shared` republish/runtime
  - shared history runtime / projection
  - durable persistence / tx / outbox / replay

## 2. 冻结目标
- `CPR12-1`：device actor 模型进入统一 sender、session、stream 主链。
- `CPR12-2`：IoT access/protocol 与 `device_twin` 进入 provider health、uplink、downlink、runtime 主链。
- `CPR12-3`：external connection/member link/shared channel policy 保持最小可审计 truth write path。

## 3. 当前事实
- `crates/im-platform-contracts/tests/agent_device_subject_model_test.rs`
  - `AgentSubject / DeviceSubject` 已统一映射到 `Sender`。
  - `DeviceSubject::sender(...)` 真实产出 `kind = device`。
- `services/session-gateway/tests/device_access_provider_mainline_test.rs`
  - `/api/v1/sessions/resume` 与 `/api/v1/presence/heartbeat` 只注入一次 device access provider。
  - 同时落地 register 与 owner binding。
- `services/local-minimal-node/tests/device_access_provider_mainline_test.rs`
  - `/api/v1/devices/register` 已走 injected device access provider 主链。
- `services/local-minimal-node/tests/device_stream_e2e_test.rs`
  - `device.telemetry`、`device.command` 已进入统一 stream 主链。
  - `device.telemetry.read`、`device.command.send` 权限边界已验证。
- `services/local-minimal-node/tests/iot_provider_http_test.rs`
  - `/api/v1/iot/access/provider-health`
  - `/api/v1/iot/protocol/provider-health`
- `services/local-minimal-node/tests/iot_protocol_adapter_mainline_test.rs`
  - uplink -> `device.telemetry`
  - downlink -> `device.command`
  - injected adapter path、known-device preflight、request-device mismatch、decoded-device mismatch 均已验证。
- `services/local-minimal-node/src/node/twin.rs`
  - 已提供 `GET /api/v1/devices/{device_id}/twin`
  - 已提供 `POST /api/v1/devices/{device_id}/twin/desired`
  - 已提供 `POST /api/v1/devices/{device_id}/twin/reported`
  - twin payload 进入 runtime 前会做 JSON 校验与规范化。
- `services/local-minimal-node/src/node/access.rs`
  - twin read：bound device 或 registered owner side 可读。
  - desired write：仅 non-device bound owner side 可写。
  - reported write：仅 bound device actor 可写。
- `services/local-minimal-node/tests/device_twin_mainline_test.rs`
  - register -> desired -> reported -> get 主链已跑通。
- `services/local-minimal-node/tests/device_twin_persistence_test.rs`
  - runtime-dir 模式下重启后可恢复 desired/reported twin state。
- `services/local-minimal-node/src/node/build.rs`、`adapters/local-memory/src/lib.rs`、`adapters/local-disk/src/iot.rs`
  - twin store 已进入 runtime 装配。
  - non-runtime-dir 使用 `MemoryDeviceTwinStore`。
  - runtime-dir 使用 `FileDeviceTwinStore`。
- `services/local-minimal-node/src/node/runtime_dir.rs`
  - `device-twin-state.json` 已进入 managed runtime state 文件清单与巡检口径。
- `CPR12-3` 继续沿用 `Loop-23` 已验证的 external collaboration truth。

## 4. 非目标
- 本轮不把 `shared_channel_policy` 误写为 shared history runtime 已实现。
- 本轮不把 durable repo / tx / outbox / replay 误写为已到位。
- 本轮不把 `S12` 误写为 `wave_closure / release_closure`。

## 5. 本轮主写入范围
- `docs/review/S12-执行卡-2026-04-10.md`
- `docs/review/S12-Loop25补充-2026-04-10.md`
- `docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
- `docs/架构/152CJ-Loop25补充-2026-04-10.md`
- `docs/release/CHANGELOG.md`
- `docs/release/2026-04-10-v0.0.25-loop-25.md`

## 6. 并行与串行
- 串行主路径：`truth recheck -> twin closure判定 -> review backwrite -> architecture backwrite -> release backwrite -> fresh verification`
- 可并行证据面：
  - `CPR12-1`：subject/session/device stream
  - `CPR12-2`：provider health/uplink/downlink/device_twin/runtime-dir
  - `CPR12-3`：external collaboration
- 必须串行收口：
  - `S12` 是否可升级为 `step_closure`
  - `shared` 是否被误写为 runtime truth
  - deferred owner 是否保持清晰

## 7. 当前准确口径
- `CPR12-1 = local_closure`
- `CPR12-2 = local_closure`
- `CPR12-3 = local_closure`
- `S12 = step_closure`
- 当前可以升级为 `step_closure`，原因：
  - `device actor / IoT access-protocol / external collaboration` 已全部具备主链证据。
  - `device_twin` 已进入 `local-minimal-node` runtime/mainline，并具备 auth、store、restart-restore 证据。
  - `shared` republish/runtime 已明确归属 `S09 / S07 / S08`，不再错误阻塞 `S12` owner scope。

## 8. Deferred owner
- `shared` vocabulary republish：`S09 + S12`
- shared history runtime / projection：`S07 / S08`
- durable persistence / tx / outbox / replay：后续 social/policy step

## 9. 验证与证据
- 主命令：
  - `cargo test -p im-platform-contracts --offline --test agent_device_subject_model_test -- --nocapture`
  - `cargo test -p session-gateway --offline --test device_access_provider_mainline_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test device_access_provider_mainline_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test device_stream_e2e_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test iot_provider_http_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test iot_protocol_adapter_mainline_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test device_twin_mainline_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test device_twin_persistence_test -- --nocapture`
  - `cargo test -p local-minimal-node --offline --test runtime_dir_inspection_test -- --nocapture`
- 主证据：
  - `crates/im-platform-contracts/tests/agent_device_subject_model_test.rs`
  - `services/session-gateway/tests/device_access_provider_mainline_test.rs`
  - `services/local-minimal-node/tests/device_access_provider_mainline_test.rs`
  - `services/local-minimal-node/tests/device_stream_e2e_test.rs`
  - `services/local-minimal-node/tests/iot_provider_http_test.rs`
  - `services/local-minimal-node/tests/iot_protocol_adapter_mainline_test.rs`
  - `services/local-minimal-node/tests/device_twin_mainline_test.rs`
  - `services/local-minimal-node/tests/device_twin_persistence_test.rs`
- 辅证据：
  - `services/local-minimal-node/src/node/twin.rs`
  - `services/local-minimal-node/src/node/access.rs`
  - `services/local-minimal-node/src/node/build.rs`
  - `services/local-minimal-node/src/node/runtime_dir.rs`
  - `adapters/local-memory/src/lib.rs`
  - `adapters/local-disk/src/iot.rs`
  - `docs/review/S12-Loop23补充-2026-04-10.md`

## 10. 回写要求
- review：把 `device_twin` runtime/mainline 与 `S12 = step_closure` 回写到 `S12` 主执行卡与 `Loop-25` 补充。
- 架构：把 twin routes、twin store、runtime-dir managed state 回写到 `152CJ` 当前真相。
- release：新增 `Loop-25`，并在 `CHANGELOG` 记录 `S12` 从 `local_closure` 升级到 `step_closure`。
- step：本轮不改 `100-*` 规划，只更新当前执行事实与出口结论。

## 11. 迁移 / 降级 / 回退
- 数据迁移：无。
- 降级策略：
  - 若 `device_twin` mainline 或 persistence 证据失真，立即把 `S12` 降回 `local_closure`。
  - 若发现 `shared` 被错误提升为 runtime truth，立即回退文档口径。
- 回退触发：
  - 任一主验证失败
  - review / 架构 / release 未同步回写
  - 文档口径再次高于仓库真相

## 12. 检查点
- `CPR12-1`：device subject、device sender、device session access、device-scoped stream 均已可证。
- `CPR12-2`：access provider、protocol adapter、provider health、uplink/downlink、device twin runtime/mainline、twin store 与 runtime-dir persistence 均已可证。
- `CPR12-3`：external collaboration 最小 write path 继续有效。
- `S12`：已达到 step 级闭环，主脊可切换到 `S13`。

## 13. 下一步准入
- `S12` 当前已收口，默认不再继续在本 step 内扩张 claim。
- 主脊下一步进入 `S13`，优先核对 observability / perf / HA/DR / deploy evidence。
- 若后续需要扩展 `shared` republish/runtime，必须按 `S09 / S07 / S08` 协同入口单独推进，不回写为当前 `S12` 缺口。

## 14. 出口 6 问
- Q1 `95` 是否已判定本 step 对应能力闭环，而非局部补丁：`yes`
- Q2 `97` 是否已写清本轮已兑现与 deferred 的架构结论：`yes`
- Q3 是否已给出“无须回写 / 实现更具体 / 实现有偏移”三选一结论：`yes`
- Q4 未完成能力是否都已明确归属到后续 `Sxx` 或 backlog owner：`yes`
- Q5 是否同时具备 `1` 个主证据和 `1` 组辅证据：`yes`
- Q6 回退、降级、阻塞升级条件是否已写入并经 review：`yes`
- 结论：`S12 = step_closure`
