# Step 05 执行卡 - 2026-04-07

## 1. 当前判断

- 当前波次：`Wave B`
- 当前 Step：`05`
- 当前状态：`step-wide 91 / 95 / 97 已完成，Step 05 已闭环`

## 2. 本轮为什么做 Step 05

`Step 04` 已闭环并通过 `91 / 95 / 97`，但 `Wave B` 仍未完成，因为 `Step 05 / Step 06` 还未闭环。
按 `docs/step/05-消息与会话主链路重构.md`，本轮必须从 `CP05-1` 开始，不能跳到 `CP05-2 / CP05-3 / CP05-4`。

本轮选择的最小真实 slice 是：

- 把 conversation member/read-cursor 的纯领域构造与成员 episode 命名规则，从 `conversation-runtime` 的 service support 模块移到 `im-domain-core`
- 用结构测试锁住“纯领域 builder 不得回流 service support”
- 先推进 `CP05-1` 的领域 owner 收口，不提前宣称 `sender / tenant` 权威边界已闭环

## 3. 本轮实际完成

### 3.1 先写 fail 的 Step 05 测试

新增：

- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`

Red 结论：

- `im-domain-core` 侧失败，原因是 `conversation` 模块还没有：
  - `build_conversation_member(...)`
  - `build_conversation_member_with_attributes(...)`
  - `build_default_read_cursor(...)`
  - `member_id(...)`
  - `member_episode_id(...)`
- `conversation-runtime` 侧失败，原因是这些纯领域 builder 仍然保留在 `services/conversation-runtime/src/runtime/support.rs`

### 3.2 把纯领域 helper 下沉到 `im-domain-core`

修改：

- `crates/im-domain-core/src/conversation.rs`
  - 新增：
    - `build_conversation_member(...)`
    - `build_conversation_member_with_attributes(...)`
    - `build_default_read_cursor(...)`
    - `member_id(...)`
    - `member_episode_id(...)`
- `services/conversation-runtime/src/runtime.rs`
  - 改为从 `im_domain_core::conversation` 导入上述 helper
- `services/conversation-runtime/src/runtime/support.rs`
  - 删除 service 本地持有的纯领域 builder 与成员 episode 命名规则
  - 保留 runtime 私有的 envelope、timestamp、scope/message key、状态索引辅助逻辑

### 3.3 本轮验证

- Red
  - `cargo test -p im-domain-core conversation_domain_builder_test --offline`
  - `cargo test -p conversation-runtime test_conversation_runtime_support_module_no_longer_owns_member_and_cursor_domain_builders --offline`
- Green
  - `rustfmt --edition 2024 crates/im-domain-core/src/conversation.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/support.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `cargo test -p im-domain-core --test conversation_domain_builder_test --offline`
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test --offline`
  - `rustfmt --edition 2024 --check crates/im-domain-core/src/conversation.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/support.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `cargo test -p im-domain-core --offline`
  - `cargo test -p conversation-runtime --offline`
  - `cargo test -p projection-service --offline`
  - `cargo test -p local-minimal-node --offline`
    - 首次直接使用默认 `target` 目录运行时，因 Windows 锁住 `target\\debug\\local-minimal-node.exe` 出现 `os error 5`
    - 已改用独立 `CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1-local-node'` 重跑并通过

## 4. 本轮对 Step 05 的影响

- `CP05-1`：`部分推进，未通过`
  - 已兑现：
    - conversation member/read-cursor 的纯领域 builder 与成员 episode 命名规则，不再由 `conversation-runtime` 的 service support 持有
    - `im-domain-core` 开始承接 Step 05 所要求的会话主链路核心领域语义
  - 未兑现：
    - `ConversationState` 中的成员活跃态解析、membership/read-cursor 状态机、message core 状态仍主要在 `conversation-runtime`
    - `message` 核心领域 owner 还没有进一步从 service runtime 里收口
- `CP05-2`：未通过
  - `tenantId` 仍未全面收口到 auth context
  - `sender` 权威注入还未完成
- `CP05-3`：未通过
  - direct / group / channel 现有测试仍是旧 owner 结构下的覆盖，不代表新模型已闭环验收
- `CP05-4`：未通过
  - projection / notification / multi-device sync 尚未围绕新领域 owner 重新收口
- `Step 05`：未闭环
- `Wave B / 93`：不触发

## 5. 下一轮继续项

1. 继续停留在 `Wave B / Step 05`
2. 继续只推进 `CP05-1`
3. 下一真实 slice 优先顺序：
   - 把 active member / read-cursor / membership episode 的状态聚合 owner 继续从 `conversation-runtime` 收口到 `im-domain-core`
   - 再处理 message core owner 与 `sender / tenant` 权威对齐
4. 在 `CP05-1` 未闭环前，不进入 `CP05-2`

## 6. 2026-04-07 ConversationRoster 聚合 owner 下沉

### 6.1 为什么继续这个 slice

- 第一轮 helper 下沉之后，`CP05-1` 仍然没有真实闭环
- 根因是 `services/conversation-runtime/src/runtime.rs` 里的 `ConversationState` 还直接持有：
  - `members`
  - `principal_members`
  - `read_cursors`
- 这意味着 membership/read-cursor 聚合状态仍然是 service 私有状态，而不是 domain owner

### 6.2 实际完成

- `crates/im-domain-core/src/conversation.rs`
  - 新增 `ConversationRoster`
  - 落地方法：
    - `upsert_member(...)`
    - `deactivate_member(...)`
    - `next_member_episode(...)`
    - `resolve_active_member_id(...)`
    - `resolve_active_member(...)`
    - `upsert_read_cursor(...)`
    - `ensure_default_read_cursor(...)`
    - `member(...)`
    - `read_cursor(...)`
- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - 新增 `ConversationRoster` 行为测试
- `services/conversation-runtime/src/runtime.rs`
  - `ConversationState` 改为持有 `roster: ConversationRoster`
- `services/conversation-runtime/src/runtime/support.rs`
  - active member / membership episode / read-cursor helper 全部改为委托 `ConversationRoster`
- `services/conversation-runtime/src/runtime/membership.rs`
  - member add / remove / leave / owner transfer / role change / list / read-cursor 更新逻辑切到 `ConversationRoster`
- `services/conversation-runtime/src/runtime/recovery.rs`
  - replay 路径切到 `ConversationRoster`
- `services/conversation-runtime/src/runtime/policy.rs`
  - direct conversation participant 限额与 active member target 检查切到 `ConversationRoster`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 新增 `test_conversation_runtime_state_uses_domain_roster_for_member_and_cursor_state`

### 6.3 验证

- Red
  - `cargo test -p im-domain-core --test conversation_domain_builder_test --offline`
    - 失败原因符合预期：`ConversationRoster` 不存在
  - `cargo test -p conversation-runtime test_conversation_runtime_state_uses_domain_roster_for_member_and_cursor_state --offline`
    - 失败原因符合预期：`ConversationState` 仍直接持有三个 roster map
- Green
  - `rustfmt --edition 2024 crates/im-domain-core/src/conversation.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/support.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/src/runtime/policy.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `rustfmt --edition 2024 --check crates/im-domain-core/src/conversation.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/support.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/src/runtime/policy.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `cargo test -p im-domain-core --offline`
  - `cargo test -p conversation-runtime --offline`
  - `cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1b-local-node'; cargo test -p local-minimal-node --offline`

### 6.4 对 Step 05 的影响

- `CP05-1`：继续推进，但仍未整体通过
  - 新增已兑现：
    - membership/read-cursor 三张核心状态表不再零散挂在 service `ConversationState`
    - `ConversationRoster` 成为这部分领域状态的共享 owner
  - 仍未兑现：
    - message core 仍主要在 `conversation-runtime`
    - `conversation_type / handoff_state / message_seq / message_index` 仍由 service runtime 聚合
    - `sender / tenant` 权威边界尚未进入 `CP05-2` 闭环
- `Step 05` 仍未闭环

## 7. 2026-04-07 ConversationMessageLog 聚合 owner 下沉

### 7.1 为什么继续这个 slice

- `ConversationRoster` 下沉后，`CP05-1` 仍未闭环
- `services/conversation-runtime/src/runtime.rs` 的 `ConversationState` 还直接持有：
  - `message_seq`
  - `messages`
- `services/conversation-runtime/src/runtime/recovery.rs` 与 `services/conversation-runtime/src/runtime/membership.rs` 也仍然直接依赖 service-local message high-watermark 与 message store
- 这说明 message core 仍然没有形成 domain owner

### 7.2 实际完成

- `crates/im-domain-core/src/message.rs`
  - 新增 `StoredMessage`
  - 新增 `ConversationMessageLog`
  - 落地方法：
    - `high_watermark(...)`
    - `next_message_seq(...)`
    - `unread_count_since(...)`
    - `message(...)`
    - `store_posted(...)`
    - `apply_edited(...)`
    - `apply_recalled(...)`
- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - 新增 `ConversationMessageLog` 高水位、edit/recall 行为测试
- `services/conversation-runtime/src/runtime.rs`
  - `ConversationState` 改为持有 `message_log: ConversationMessageLog`
  - post/edit/recall 主路径改为消费 `ConversationMessageLog`
- `services/conversation-runtime/src/runtime/recovery.rs`
  - replay posted/edited/recalled 路径改为消费 `ConversationMessageLog`
- `services/conversation-runtime/src/runtime/membership.rs`
  - read cursor high-watermark 校验与 unread 计算改为消费 `ConversationMessageLog`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 新增 `test_conversation_runtime_state_uses_domain_message_log_for_message_state`

### 7.3 验证

- Red
  - `cargo test -p im-domain-core --test conversation_domain_builder_test test_conversation_message_log_owns_high_watermark_and_posted_messages --offline`
    - 失败原因符合预期：`ConversationMessageLog` 尚不存在
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test test_conversation_runtime_state_uses_domain_message_log_for_message_state --offline`
    - 失败原因符合预期：`ConversationState` 仍直接持有 `messages`
- Green
  - `rustfmt --edition 2024 crates/im-domain-core/src/message.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `rustfmt --edition 2024 --check crates/im-domain-core/src/message.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-im-domain-core'; cargo test -p im-domain-core --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-conversation-runtime'; cargo test -p conversation-runtime --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-projection-service'; cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-local-node'; cargo test -p local-minimal-node --offline`

### 7.4 对 Step 05 的影响

- `CP05-1`：继续推进，但仍未整体通过
  - 新增已兑现：
    - message high-watermark 与 stored message 状态不再直接挂在 service `ConversationState`
    - post/edit/recall/replay/read-cursor unread 计算开始围绕同一个 domain message owner 收口
  - 仍未兑现：
    - `message_index` 仍是 runtime 级索引
    - `conversation_type / handoff_state` 仍由 service runtime 聚合
    - `sender / tenant` 权威边界尚未进入 `CP05-2`
- `Step 05` 仍未闭环
- `Wave B / 93` 仍不触发
## 10. 2026-04-07 ConversationAggregateState 元数据 owner 收口

### 10.1 本轮目标

- 继续推进 `Wave B / Step 05 / CP05-1`
- 不跨到 `CP05-2`，先把 `conversation_type / member_epoch / handoff_status_epoch / handoff_state` 从 `conversation-runtime` 收口到 `im-domain-core`

### 10.2 实际改动

- `crates/im-domain-core/src/conversation.rs`
  - 新增 `ConversationAggregateState`
  - 新增 `ConversationHandoffLifecycle`
  - 新增 `ConversationHandoffStatusTransition`
  - 新增 `new(...)`
  - 新增 `new_agent_handoff(...)`
  - 新增 `next_member_epoch(...)`
  - 新增 `observe_member_epoch(...)`
  - 新增 `observe_handoff_status_epoch(...)`
  - 新增 `replace_handoff_state(...)`
  - 新增 `has_closed_handoff(...)`
  - 新增 `transition_handoff_status(...)`
- `services/conversation-runtime/src/runtime.rs`
  - `ConversationState` 改为持有 `aggregate: ConversationAggregateState`
  - 移除 runtime 直接持有的 `conversation_type / member_epoch / handoff_status_epoch / handoff_state`
- `services/conversation-runtime/src/runtime/creation.rs`
  - 创建会话时改为初始化 `ConversationAggregateState`
  - 成员排序号改为通过 domain aggregate 的 `next_member_epoch(...)` 生成
- `services/conversation-runtime/src/runtime/membership.rs`
  - member add/remove/leave/transfer/role-change 改为通过 domain aggregate 推进 member epoch
- `services/conversation-runtime/src/runtime/handoff.rs`
  - handoff 生命周期改为通过 domain aggregate 的 `transition_handoff_status(...)` 推进
- `services/conversation-runtime/src/runtime/recovery.rs`
  - replay 改为通过 domain aggregate 恢复 conversation type / member epoch / handoff epoch / handoff state
- `services/conversation-runtime/src/runtime/policy.rs`
  - 权限判断改为读取 `conversation.aggregate.conversation_type()` 与 `conversation.aggregate.has_closed_handoff()`
- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - 新增 `test_conversation_aggregate_state_owns_type_epochs_and_handoff_status`
  - 新增 `test_conversation_aggregate_state_tracks_recovered_epoch_floor`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 新增 `test_conversation_runtime_state_uses_domain_aggregate_for_metadata_fields`
  - 新增 `test_conversation_runtime_modules_use_domain_aggregate_owner_for_type_and_epochs`

### 10.3 TDD 与验证证据

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-red-domain'; cargo test -p im-domain-core --test conversation_domain_builder_test test_conversation_aggregate_state_owns_type_epochs_and_handoff_status --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-red-runtime'; cargo test -p conversation-runtime --test conversation_domain_structure_test test_conversation_runtime_state_uses_domain_aggregate_for_metadata_fields --offline`
- Green
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-green-domain'; cargo test -p im-domain-core --test conversation_domain_builder_test test_conversation_aggregate_state_owns_type_epochs_and_handoff_status --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-green-runtime-2'; cargo test -p conversation-runtime --test conversation_domain_structure_test test_conversation_runtime_state_uses_domain_aggregate_for_metadata_fields --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-green-runtime-3'; cargo test -p conversation-runtime --test conversation_domain_structure_test test_conversation_runtime_modules_use_domain_aggregate_owner_for_type_and_epochs --offline`
  - `rustfmt --edition 2024 --check crates/im-domain-core/src/conversation.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/creation.rs services/conversation-runtime/src/runtime/handoff.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/src/runtime/policy.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-im-domain-core-full'; cargo test -p im-domain-core --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-conversation-runtime-full'; cargo test -p conversation-runtime --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-projection-service-full'; cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1e-local-node-full'; cargo test -p local-minimal-node --offline`
  - 说明：`local-minimal-node` 全量测试输出中仍会出现预期的启动失败日志样例，但命令退出码为 `0`，suite 实际通过

### 10.4 当前判断

- `CP05-1` 再次推进，但仍未通过
- 本轮已兑现
  - 会话聚合元数据 owner 从 service runtime 收口到 domain crate
  - handoff state storage 与 handoff status epoch 不再由 runtime 直接字段持有
  - member epoch 不再由 runtime 直接字段持有
- 本轮未兑现
  - `message_index` 仍为 runtime 级索引
  - `sender / tenant` authority closure 仍未推进到 `CP05-2`
  - direct / group / channel 的再收口仍未推进到 `CP05-3`
  - projection / notification / multi-device sync 的最终 owner 收口仍未推进到 `CP05-4`
- 因此
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍未通过 `Step 05`
- `Wave B / 93` 仍阻塞

## 17. 2026-04-07 CP05-4 message notification fanout owner 收口

### 17.1 本轮为什么做这个子项

- 前两轮 `CP05-4` 增量完成后，当前最小且真实未闭环 seam 收敛到 message notification side-effect orchestration
- `services/local-minimal-node/src/node/effects.rs` 仍自己做 sender filter、recipient loop 和 notification id 拼装
- 这会让 notification side-effect owner 继续停留在 service edge，因此本轮继续只推进这条 seam

### 17.2 本轮实际完成

- `services/notification-service/src/lib.rs`
  - 新增 `RequestNotificationFanout`
  - 新增 `request_notification_fanout(...)`
- `services/local-minimal-node/src/node/effects.rs`
  - message notification side-effect 改为消费 notification-service owner seam
- 测试新增/更新
  - `services/notification-service/tests/lib_structure_test.rs`
  - `services/notification-service/tests/notification_pipeline_test.rs`
  - `services/local-minimal-node/tests/lib_structure_test.rs`

### 17.3 本轮验证

- Red
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_notification_fanout_owner_seam --offline`
  - `cargo test -p notification-service --test notification_pipeline_test test_request_notification_fanout_skips_actor_and_creates_notifications_for_other_recipients --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054c-red-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_notification_service_fanout_owner_seam --offline`
- Green / 结构与行为
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_notification_fanout_owner_seam --offline`
  - `cargo test -p notification-service --test notification_pipeline_test test_request_notification_fanout_skips_actor_and_creates_notifications_for_other_recipients --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054c-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_notification_service_fanout_owner_seam --offline`
- Green / 回归
  - `$env:CARGO_TARGET_DIR='target-cp054c-green-local'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only --offline`
  - `cargo test -p notification-service --test notification_pipeline_test test_duplicate_request_notification_is_idempotent_when_payload_matches --offline`
  - `cargo fmt --all`
  - `cargo fmt --all --check`

### 17.4 当前判断

- 这是 `CP05-4` 的第三个真实增量
- notification side-effect fanout owner 已开始收口到 `notification-service`
- `CP05-4` 仍未闭环
- `Step 05` 仍未闭环
- `91 / 95 / 97` 仍不能整体判定 `Step 05` 通过
- `Wave B / 93` 仍阻塞

## 18. 2026-04-07 CP05-4 automation result notification owner 收口

### 18.1 本轮为什么继续做这个子项

- 完成前三个 `CP05-4` 增量后，`services/local-minimal-node/src/node/platform.rs` 仍在 automation 执行成功后本地拼装 `automation.result` 通知：
  - 本地生成 notification id
  - 本地生成 source event id / type
  - 本地指定 recipient 为当前 actor
- 这说明 notification owner 在 automation result 这条链路上仍停留在 service edge，不能判定 `CP05-4` 进入闭环。

### 18.2 本轮实际完成

- `services/notification-service/src/lib.rs`
  - 新增 `RequestAutomationResultNotification`
  - 新增 `request_automation_result_notification(...)`
- `services/local-minimal-node/src/node/platform.rs`
  - `request_automation_execution(...)` 改为直接消费 notification owner seam
  - 删除本地 `automation.result` 通知字段拼装
- 测试新增 / 更新
  - `services/notification-service/tests/lib_structure_test.rs`
  - `services/notification-service/tests/notification_pipeline_test.rs`
  - `services/local-minimal-node/tests/lib_structure_test.rs`

### 18.3 本轮验证

- Red
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_automation_result_notification_owner_seam --offline`
  - `cargo test -p notification-service --test notification_pipeline_test test_request_automation_result_notification_targets_requesting_actor_idempotently --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054d-red-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_automation_path_uses_notification_runtime_automation_result_owner --offline`
- Green / 结构与行为
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_automation_result_notification_owner_seam --offline`
  - `cargo test -p notification-service --test notification_pipeline_test test_request_automation_result_notification_targets_requesting_actor_idempotently --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054d-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_automation_path_uses_notification_runtime_automation_result_owner --offline`
- Green / 回归
- `$env:CARGO_TARGET_DIR='target-cp054d-green-local'; cargo test -p local-minimal-node --test task10_capabilities_e2e_test test_local_minimal_profile_exposes_notification_automation_audit_and_ops_capabilities --offline`
- `$env:CARGO_TARGET_DIR='target-cp054d-green-local'; cargo test -p local-minimal-node --test task10_capabilities_e2e_test test_local_minimal_profile_treats_duplicate_automation_request_as_idempotent --offline`
- `cargo fmt --all`
- `cargo fmt --all --check`

## 19. 2026-04-07 CP05-4 projection device-sync target owner 执行补充

### 19.1 本轮实际完成

- `projection-service` 新增 `device_sync_fanout_targets_for_conversation(...)`，统一解析 conversation active members、fallback principal 与 registered device target。
- `message / mutation / handoff / member governance` 四类 device-sync handler 改为消费同一 seam。
- `read_cursor` fanout 改为复用 `realtime_fanout_targets_for_principals(...)`，移除本地 raw device loop。

### 19.2 测试与验证

- Red
  - `cargo test -p projection-service --test lib_structure_test test_projection_service_exposes_conversation_device_sync_target_owner_seam --offline`
  - `cargo test -p projection-service --test timeline_projection_test test_device_sync_fanout_targets_for_conversation_include_active_members_and_fallback_devices --offline`
- Green / 回归
  - `$env:CARGO_TARGET_DIR='target-cp054e-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_exposes_conversation_device_sync_target_owner_seam --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054e-behavior'; cargo test -p projection-service --test timeline_projection_test test_device_sync_fanout_targets_for_conversation_include_active_members_and_fallback_devices --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054e-full'; cargo test -p projection-service --offline`
  - `rustfmt --edition 2024 --check services/projection-service/src/lib.rs services/projection-service/tests/lib_structure_test.rs services/projection-service/tests/timeline_projection_test.rs`

### 19.3 当前状态

- `CP05-4` 仍未完成
- `Step 05` 仍未完成
- `91 / 95 / 97` 针对 `Step 05` 仍未整体通过
- `Wave B / 93` 继续阻塞

### 18.4 对 Step 05 的影响

- 这是 `CP05-4` 的第四个真实增量
- automation result notification owner 已开始收口到 `notification-service`
- 但：
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定 `Step 05` 通过
  - `Wave B / 93` 仍阻塞

## 16. 2026-04-07 CP05-4 projection realtime fanout target owner 收口

### 16.1 本轮为什么做这个子项

- `CP05-4` 的 notification public access owner 增量完成后，当前最小且真实未闭环 seam 仍是 `local-minimal-node` side-effect 路径里的 raw projection device fanout 拼装
- `services/local-minimal-node/src/node/effects.rs` 仍直接调用 `projection_service.registered_devices(...)`，这会让 principal -> device realtime target owner 继续停留在 service edge
- 这与 `CP05-4` 要求的 projection / notification / multi-device sync 收口方向不一致，因此本轮继续只推进这个 seam

### 16.2 本轮实际完成

- `services/projection-service/src/model.rs`
  - 新增 `RealtimeFanoutTarget`
- `services/projection-service/src/lib.rs`
  - 新增 `realtime_fanout_targets_for_principals(...)`
- `services/local-minimal-node/src/node/effects.rs`
  - realtime publish 路径改为消费 projection-owned fanout target seam
- 测试新增/更新
  - `services/projection-service/tests/lib_structure_test.rs`
  - `services/projection-service/tests/timeline_projection_test.rs`
  - `services/local-minimal-node/tests/lib_structure_test.rs`

### 16.3 本轮验证

- Red
  - `cargo test -p projection-service --test lib_structure_test test_projection_service_exposes_realtime_fanout_target_owner_seam --offline`
  - `cargo test -p projection-service --test timeline_projection_test test_realtime_fanout_targets_for_principals_return_registered_principal_device_pairs --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054b-red-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_projection_owned_realtime_fanout_target_seam --offline`
- Green / 结构与行为
  - `cargo test -p projection-service --test lib_structure_test test_projection_service_exposes_realtime_fanout_target_owner_seam --offline`
  - `cargo test -p projection-service --test timeline_projection_test test_realtime_fanout_targets_for_principals_return_registered_principal_device_pairs --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054b-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_projection_owned_realtime_fanout_target_seam --offline`
- Green / 回归
  - `cargo test -p projection-service --test timeline_projection_test test_registered_devices_and_latest_sync_seq_are_queryable --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054b-green-local'; cargo test -p local-minimal-node --test cluster_realtime_routing_e2e_test test_local_minimal_profile_routes_realtime_events_to_remote_owner_node --offline`
  - `cargo fmt --all`
  - `cargo fmt --all --check`

### 16.4 当前判断

- 这是 `CP05-4` 的第二个真实增量
- projection-side principal -> device realtime fanout target owner 已开始收口到 `projection-service`
- `CP05-4` 仍未闭环
- `Step 05` 仍未闭环
- `91 / 95 / 97` 仍不能整体判定 `Step 05` 通过
- `Wave B / 93` 仍阻塞

## 11. 2026-04-07 CP05-2 读查询 authority owner 收口

### 11.1 本轮为什么做

- `CP05-2` 前三轮已经把 message mutation、non-message command field capture、non-message runtime auth-context entrypoint 做完。
- 但 runtime 自己持有的读查询 surface 仍然在 `http` 和 `local-minimal-node` 中直接线程化 `auth.tenant_id / auth.actor_id`。
- 所以本轮继续停留在 `Wave B / Step 05 / CP05-2`，补 runtime-owned read query authority owner。

### 11.2 本轮实际完成

- `services/conversation-runtime/src/runtime.rs`
  - 新增 `require_active_member_from_auth_context(...)`
- `services/conversation-runtime/src/runtime/membership.rs`
  - 新增 `list_members_from_auth_context(...)`
  - 新增 `read_cursor_view_from_auth_context(...)`
- `services/conversation-runtime/src/runtime/handoff.rs`
  - 新增 `get_agent_handoff_state_from_auth_context(...)`
- `services/conversation-runtime/src/runtime/http.rs`
  - handoff/member/read-cursor 读路径改为使用上述 wrapper
- `services/local-minimal-node/src/node/membership.rs`
- `services/local-minimal-node/src/node/handoff.rs`
- `services/local-minimal-node/src/node/access.rs`
  - 改为使用上述 wrapper
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 新增 `test_runtime_exposes_read_query_auth_context_entrypoints`
  - 新增 `test_http_read_query_surface_uses_runtime_auth_context_entrypoints`
- `services/local-minimal-node/tests/lib_structure_test.rs`
  - 新增 `test_local_minimal_node_read_query_paths_use_runtime_auth_context_entrypoints`

### 11.3 本轮验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2d-red-runtime'; cargo test -p conversation-runtime --test conversation_domain_structure_test test_runtime_exposes_read_query_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2d-red-local-node'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_read_query_paths_use_runtime_auth_context_entrypoints --offline`
- Green / fresh verification
  - `rustfmt --edition 2024 --check services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/src/runtime/handoff.rs services/conversation-runtime/src/runtime/http.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs services/local-minimal-node/src/node/membership.rs services/local-minimal-node/src/node/handoff.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/tests/lib_structure_test.rs`
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test --offline`
  - `cargo test -p conversation-runtime --test authority_command_test --offline`
  - `cargo test -p conversation-runtime --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2d-local-node-structure'; cargo test -p local-minimal-node --test lib_structure_test --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2d-local-node-full'; cargo test -p local-minimal-node --offline`
  - `cargo test -p projection-service --offline`

### 11.4 当前判断

- 本轮是 `CP05-2` 的第四个真实增量。
- 已兑现:
  - runtime-owned read query surface 开始收口到 runtime auth-context boundary
- 未兑现:
  - projection-service 读查询 authority 仍未收口
  - `CP05-2` 仍未闭环
  - `Step 05` 仍未闭环
  - `Wave B / 93` 仍不能触发

## 12. 2026-04-07 CP05-2 projection/read-model authority owner 收口

### 12.1 本轮为什么继续做这一项

- 当前仓库真实状态仍位于 `Wave B / Step 05 / CP05-2`
- `CP05-2` 之前已经完成四个真实增量，但 projection/read-model authority owner 仍残留在 adapter / service consumer 侧
- 这会继续放大 `tenant_id / actor_id / device_id` 在多入口重复拼装的漂移面，无法通过 `91 / 95 / 97`

### 12.2 本轮实际完成

- 将 projection/read-model authority owner 进一步收口到 `projection-service`
- 新增 `services/projection-service/src/access.rs`
  - `ProjectionAccessError`
  - `ensure_active_member_from_auth_context(...)`
  - `register_device_from_auth_context(...)`
  - `registered_devices_from_auth_context(...)`
  - `latest_device_sync_seq_from_auth_context(...)`
  - `device_sync_feed_from_auth_context(...)`
  - `inbox_from_auth_context(...)`
  - `timeline_from_auth_context(...)`
  - `conversation_summary_from_auth_context(...)`
  - `read_cursor_from_auth_context(...)`
- `services/projection-service/src/http.rs` 改为统一消费这些 auth-context entrypoint
- `services/local-minimal-node/src/node/projection.rs` 与 `services/local-minimal-node/src/node/session.rs` 改为消费 projection-service 新边界
- 删除 HTTP / adapter 侧重复的 device scope 与 member access helper

### 12.3 本轮验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-red-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-red-local-node-structure'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_projection_paths_use_projection_service_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-red-local-node-session'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_session_projection_paths_use_projection_service_auth_context_entrypoints --offline`
- Green / fresh verification
  - `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/src/lib.rs services/projection-service/src/http.rs services/projection-service/tests/lib_structure_test.rs services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/src/node/projection.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-green-projection-structure'; cargo test -p projection-service --test lib_structure_test --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-green-local-node-structure'; cargo test -p local-minimal-node --test lib_structure_test --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-projection-full'; cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2e-local-node-full'; cargo test -p local-minimal-node --offline`

### 12.4 当前判断

- 这是 `CP05-2` 的第五个真实增量
- 已兑现能力
  - projection/read-model runtime-owned authority capture 开始集中到 `projection-service`
  - `projection-service/http` 与 `local-minimal-node` projection/session 路径不再重复拼装原始 authority 字段
- 未兑现能力
  - downstream Step 05 authority consumer 尚未全部收口
  - `services/local-minimal-node/src/node/effects.rs` 仍保留 runtime raw read：`.list_members(...)`
- 结论
  - `CP05-2` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 不能判定通过
  - `Wave B / 93` 仍不能启动

## 13. 2026-04-07 CP05-2 downstream authority consumer seam 收口

### 13.1 本轮为什么继续做这个子项

- 在完成 read-query 与 projection/read-model owner 收口后，`CP05-2` 仍剩两类 downstream authority consumer seam：
  - `effects.rs` 的 member fanout / realtime recipient raw read
  - `access.rs` 的 conversation-bound write raw actor_kind threading
- 这两处不收，`sender / tenant` authority closure 仍不能判定闭环。

### 13.2 本轮实际完成

- `services/local-minimal-node/src/node/effects.rs`
  - 改为消费 `list_members_from_auth_context(...)`
  - 增加 `conversation_member_principal_ids_from_auth_context(...)`
- `services/local-minimal-node/src/node/membership.rs`
  - membership mutation 前解析 `base_principals`，避免 post-mutation raw roster read
- `services/conversation-runtime/src/runtime.rs`
  - 增加 `ensure_conversation_bound_write_allowed_from_auth_context(...)`
- `services/local-minimal-node/src/node/access.rs`
  - 改为消费 runtime write-access auth-context seam
- `services/local-minimal-node/src/node/message.rs`
  - conversation message realtime publish 改为透传 `AuthContext`
- 新增结构测试：
  - `test_local_minimal_node_effects_member_fanout_uses_runtime_auth_context_entrypoints`
  - `test_runtime_exposes_conversation_bound_write_access_auth_context_entrypoint`
  - `test_local_minimal_node_access_paths_use_runtime_write_access_auth_context_entrypoint`

### 13.3 本轮验证

- Red
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2f-red-local-node-effects'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_member_fanout_uses_runtime_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2g-red-runtime-write-access'; cargo test -p conversation-runtime --test conversation_domain_structure_test test_runtime_exposes_conversation_bound_write_access_auth_context_entrypoint --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2g-red-local-node-write-access'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_access_paths_use_runtime_write_access_auth_context_entrypoint --offline`
- 回归修复
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2g-fix-rtc-mismatch'; cargo test -p local-minimal-node --test access_control_e2e_test test_conversation_bound_rtc_writes_reject_bearer_actor_kind_mismatch --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2g-fix-stream-mismatch'; cargo test -p local-minimal-node --test access_control_e2e_test test_conversation_bound_stream_writes_reject_bearer_actor_kind_mismatch --offline`
- Fresh verification
  - `rustfmt --edition 2024 --check services/conversation-runtime/src/runtime.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/src/node/message.rs services/local-minimal-node/src/node/membership.rs services/local-minimal-node/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2g-runtime-full-rerun'; cargo test -p conversation-runtime --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2g-local-node-full-rerun'; cargo test -p local-minimal-node --offline`
- 搜索证据
  - `rg -n "\\.list_members\\(|\\.get_agent_handoff_state\\(|\\.require_active_member\\(|ensure_conversation_bound_write_allowed_with_actor_kind\\(|\\.ensure_conversation_bound_write_allowed\\(" services/local-minimal-node/src/node services/conversation-runtime/src/runtime/http.rs`
  - `rg -n "projection_service\\.(register_device|registered_devices|latest_device_sync_seq|device_sync_feed|timeline|inbox|conversation_summary|read_cursor)\\(" services/local-minimal-node/src/node services/projection-service/src/http.rs`
  - 当前均无匹配

### 13.4 当前判断

- `CP05-2` 现在进入闭环态
- `Step 05` 仍未闭环
  - `CP05-3` 未完成
  - `CP05-4` 未完成
- `91 / 95 / 97` 不能整体判定 `Step 05` 通过
- `Wave B / 93` 仍阻塞

## 14. 2026-04-07 CP05-3 direct/group/channel mainline scenario owner 收口

### 14.1 本轮为什么继续做这个子项

- `CP05-2` 已闭环后，当前仓库顺序推进位置进入 `Wave B / Step 05 / CP05-3`
- `docs/step/05-消息与会话主链路重构.md` 要求 direct / group / channel 主链路必须在统一 owner 下跑通
- 当前 runtime policy 仍残留 raw `conversation_type` 主链路分支，不继续收口就无法把 `CP05-3` 判定为通过

### 14.2 本轮实际完成

- `crates/im-domain-core/src/conversation.rs`
  - 新增 `ConversationScenario`
  - 新增 `ConversationScenario::from_conversation_type(...)`
  - 新增 `ConversationAggregateState::scenario()`
- `services/conversation-runtime/src/runtime/policy.rs`
  - direct / group / channel 主链路能力分支改为统一消费 `conversation.aggregate.scenario()`
  - edit / recall policy helper 也改为消费 `ConversationScenario`
- `services/conversation-runtime/src/runtime.rs`
  - edit / recall 路径改为透传 aggregate `scenario()`
- 测试新增
  - `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - `services/conversation-runtime/tests/conversation_domain_structure_test.rs`

### 14.3 本轮验证

- Red
  - `cargo test -p im-domain-core --test conversation_domain_builder_test test_conversation_aggregate_state_projects_direct_group_and_channel_scenarios --offline`
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test test_runtime_policy_uses_domain_scenario_owner_for_direct_group_channel_paths --offline`
- Green / 主链路
  - `cargo fmt --all`
  - `cargo fmt --all --check`
  - `cargo test -p im-domain-core --test conversation_domain_builder_test --offline`
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test --offline`
  - `cargo test -p conversation-runtime --test conversation_flow_test test_direct_conversation_owner_can_add_only_single_non_elevated_peer --offline`
  - `cargo test -p conversation-runtime --test conversation_flow_test test_group_owner_can_transfer_ownership_and_then_leave --offline`
  - `cargo test -p conversation-runtime --test conversation_flow_test test_system_channel_requires_dedicated_publish_command_and_allows_only_publisher --offline`
- local profile e2e
  - 默认 `target\\debug\\local-minimal-node.exe` 被常驻进程锁定，因此使用隔离 target：
  - `$env:CARGO_TARGET_DIR='target-cp053'; cargo test -p local-minimal-node --test access_control_e2e_test test_direct_conversation_member_management_is_restricted --offline`
  - `$env:CARGO_TARGET_DIR='target-cp053'; cargo test -p local-minimal-node --test access_control_e2e_test test_group_owner_transfer_allows_safe_handoff_and_leave --offline`
  - `$env:CARGO_TARGET_DIR='target-cp053'; cargo test -p local-minimal-node --test access_control_e2e_test test_system_channel_dedicated_publish_allows_only_publisher_in_local_profile --offline`

### 14.4 当前判断

- `CP05-3` 可判定通过
- `Step 05` 仍未闭环
  - `CP05-4` 未完成
- `91 / 95 / 97` 不能整体判定 `Step 05` 通过
- `Wave B / 93` 仍阻塞

## 15. 2026-04-07 CP05-4 notification public access owner 收口

### 15.1 本轮为什么做这个子项

- `CP05-3` 通过后，当前顺序推进位置进入 `Wave B / Step 05 / CP05-4`
- `notification-service` 与 `local-minimal-node` 仍各自维护一份 cross-recipient `notification.write` 判断
- 这会让 notification owner 继续停留在 duplicated service-edge 规则里，无法继续收口 `CP05-4`

### 15.2 本轮实际完成

- `services/notification-service/src/lib.rs`
  - 新增 `NotificationRuntime::request_notification_from_public_api(...)`
  - HTTP `/api/v1/notifications/requests` 改为消费该 owner seam
- `services/local-minimal-node/src/node/platform.rs`
  - app-facing notification request 路径改为消费 `notification_runtime.request_notification_from_public_api(...)`
- `services/local-minimal-node/src/node/access.rs`
  - 删除本地重复的 `ensure_notification_request_access(...)`
- 测试新增
  - `services/notification-service/tests/lib_structure_test.rs`
  - `services/local-minimal-node/tests/lib_structure_test.rs`

### 15.3 本轮验证

- Red
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_public_request_access_owner --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054-red-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_notification_request_path_uses_notification_runtime_public_access_owner --offline`
- Green / 结构
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_public_request_access_owner --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_notification_request_path_uses_notification_runtime_public_access_owner --offline`
- Green / 行为
  - `cargo test -p notification-service --test public_auth_test test_public_app_rejects_cross_recipient_notification_request_without_permission --offline`
  - `cargo test -p notification-service --test public_auth_test test_public_app_accepts_self_notification_request --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054-green-local'; cargo test -p local-minimal-node --test public_auth_e2e_test test_public_app_rejects_cross_recipient_notification_request_without_permission --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054-green-local'; cargo test -p local-minimal-node --test public_auth_e2e_test test_public_app_accepts_self_notification_request --offline`
  - `cargo fmt --all --check`

### 15.4 当前判断

- 这是 `CP05-4` 的第一个真实增量
- notification public access owner 已开始收口到 `notification-service`
- `CP05-4` 仍未闭环
- `Step 05` 仍未闭环
- `91 / 95 / 97` 仍不能整体判定 `Step 05` 通过
- `Wave B / 93` 仍阻塞

## 20. 2026-04-07 CP05-4 message-posted notification request owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮完成
  - `notification-service` 新增 `RequestMessagePostedNotifications`
  - `notification-service` 新增 `request_message_posted_notifications(...)`
  - `local-minimal-node` effects 改为消费 notification-owned message-posted request seam
- 本轮验证
  - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_message_posted_notification_owner_seam --offline`
  - `cargo test -p notification-service --test notification_pipeline_test test_request_message_posted_notifications_own_message_notification_defaults --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054f-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_notification_service_message_posted_owner_seam --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054f-reg-notification'; cargo test -p notification-service --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054f-reg-local'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only --offline`
- 当前判断
  - 本轮为 `CP05-4` 的有效增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 24. 2026-04-07 CP05-4 projection active-principal auth-context owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么继续做
  - Step 05 执行偏长的直接原因不是单点代码难，而是 `CP05-4` 的剩余 owner seam 分散在 `projection / notification / realtime` 交界处，之前逐 seam 推进带来了频繁测试与文档切换成本。
  - 本轮按批量化思路直接收掉当前最明显的 edge-owned recipient principal 解析点：`effects.rs` 仍通过 member roster read 解析 notification / realtime recipients。
- 本轮完成
  - `projection-service::access` 新增 `active_conversation_principal_ids_from_auth_context(...)`
  - `projection-service/src/lib.rs` 允许 access 模块复用 `active_conversation_principal_ids(...)`
  - `local-minimal-node/effects.rs` 的 `conversation_member_principal_ids_from_auth_context(...)` 改为统一消费 projection-owned active-principal seam
  - `projection-service/tests/timeline_projection_test.rs` 新增 active-principal 行为测试
  - `local-minimal-node/tests/lib_structure_test.rs` 改为锁定 projection-owned recipient principal seam
- 本轮验证
  - Red
    - `$env:CARGO_TARGET_DIR='target-cp054j-red-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-red-projection-behavior'; cargo test -p projection-service --test timeline_projection_test test_active_conversation_principal_ids_from_auth_context_returns_current_active_members --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-red-local-structure'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_member_fanout_uses_projection_auth_context_entrypoints --offline`
  - Green / Regression
    - `$env:CARGO_TARGET_DIR='target-cp054j-green-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-green-projection-behavior'; cargo test -p projection-service --test timeline_projection_test test_active_conversation_principal_ids_from_auth_context_returns_current_active_members --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-green-local-structure'; cargo test -p local-minimal-node --test lib_structure_test --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-reg-message-notification'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-reg-message-realtime'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_realtime_message_events_to_other_conversation_member --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-reg-membership-realtime'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_member_governance_realtime_events_to_registered_owner_device --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-reg-handoff-realtime'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_agent_handoff_lifecycle_realtime_events_to_other_device --offline`
    - `$env:CARGO_TARGET_DIR='target-cp054j-reg-projection-full'; cargo test -p projection-service --offline`
    - `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/src/lib.rs services/projection-service/tests/lib_structure_test.rs services/projection-service/tests/timeline_projection_test.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/tests/lib_structure_test.rs`
- 当前判断
  - 本轮为 `CP05-4` 的第十个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 23. 2026-04-07 CP05-4 projection realtime fanout auth-context owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮完成
  - `projection-service` access 新增 `realtime_fanout_targets_from_auth_context(...)`
  - `local-minimal-node` effects 的 `publish_realtime_event_to_principals(...)` 改为统一消费 projection-owned auth-context seam
  - message / membership / handoff / stream realtime caller 统一经由这条 seam 完成 tenant capture
  - 同步修正 `services/local-minimal-node/tests/lib_structure_test.rs` 中与已落地 message-posted owner seam 不一致的旧 fanout 断言
- 本轮验证
  - `$env:CARGO_TARGET_DIR='target-cp054i-red-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-red-local-structure'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_projection_owned_realtime_fanout_target_seam --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-green-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-green-local-structure'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_projection_owned_realtime_fanout_target_seam --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-realtime-message'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_realtime_message_events_to_other_conversation_member --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-realtime-membership'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_member_governance_realtime_events_to_registered_owner_device --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-realtime-handoff'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_agent_handoff_lifecycle_realtime_events_to_other_device --offline`
  - `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/tests/lib_structure_test.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-projection-full'; cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-local-structure-full'; cargo test -p local-minimal-node --test lib_structure_test --offline`
- 当前判断
  - 本轮为 `CP05-4` 的有效增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 21. 2026-04-07 CP05-4 projection device-sync entry owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮完成
  - `projection-service` 新增 `services/projection-service/src/device_sync.rs`
  - 新增 `DeviceSyncEntryDraft`
  - `lib.rs` 新增 `append_device_sync_draft(...)`
  - message / mutation / read-cursor / handoff / member-governance 五条 device-sync fanout 路径改为共享 draft/build seam
- 本轮验证
  - `$env:CARGO_TARGET_DIR='target-cp054g-red-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_device_sync_entry_assembly_moves_out_of_lib_impl --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054g-green-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_device_sync_entry_assembly_moves_out_of_lib_impl --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054g-green-message-read'; cargo test -p projection-service --test timeline_projection_test test_device_sync_feed_projects_registered_devices_for_message_and_read_cursor_events --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054g-green-governance'; cargo test -p projection-service --test timeline_projection_test test_member_governance_events_project_typed_sync_feed_deltas --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054g-green-handoff'; cargo test -p projection-service --test timeline_projection_test test_agent_handoff_status_change_projects_device_sync_entries_for_active_members --offline`
  - `rustfmt --edition 2024 --check services/projection-service/src/lib.rs services/projection-service/src/device_sync.rs services/projection-service/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='target-cp054g-reg-full'; cargo test -p projection-service --offline`
- 当前判断
  - 本轮为 `CP05-4` 的有效增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 22. 2026-04-07 CP05-4 projection device-sync session state owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮完成
  - `projection-service` access 新增 `DeviceSyncSessionState`
  - `projection-service` access 新增 `device_sync_session_state_from_auth_context(...)`
  - `local-minimal-node` session 改为在 `resume / get_presence_me / heartbeat / disconnect` 路径统一消费这条 seam
  - 删除本地 `registered_devices(...)` helper
- 本轮验证
  - `$env:CARGO_TARGET_DIR='target-cp054h-red-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054h-red-local-structure'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_session_projection_paths_use_projection_service_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054h-green-projection-structure'; cargo test -p projection-service --test lib_structure_test test_projection_service_access_module_exposes_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054h-green-local-structure'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_session_projection_paths_use_projection_service_auth_context_entrypoints --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054h-reg-resume'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_resumes_session_and_returns_presence_snapshot --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054h-reg-sync'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_exposes_device_sync_feed_for_multi_device_resume --offline`
  - `$env:CARGO_TARGET_DIR='target-cp054h-reg-disconnect'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_requires_fresh_resume_after_disconnect --offline`
  - `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/src/lib.rs services/projection-service/tests/lib_structure_test.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/tests/lib_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='target-cp054h-reg-projection-full'; cargo test -p projection-service --offline`
- 当前判断
  - 本轮为 `CP05-4` 的有效增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞
## 25. 2026-04-07 CP05-4 message-posted projection recipient owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么继续做这个子项
  - 上一轮虽然把 conversation active principal mapping 收到了 `projection-service::access`，但 `notification-service` 仍依赖 caller 透传 `recipient_ids`。
  - 这意味着 projection owner 与 notification owner 之间仍有一条真实未闭环连接点，`CP05-4` 不能结束。
- 本轮完成
  - `notification-service` 新增共享 projection runtime 注入能力。
  - `RequestMessagePostedNotifications` 删除 `recipient_ids`。
  - `request_message_posted_notifications(...)` 改为在 owner 边界内部通过 `active_conversation_principal_ids_from_auth_context(...)` 解析 recipients。
  - `local-minimal-node` build 路径改为给 `NotificationRuntime` 注入共享 `projection_service`。
  - `local-minimal-node/effects.rs` 不再透传 `recipient_ids`。
- 本轮验证
  - Red
    - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_message_posted_notification_owner_seam --offline`
    - `cargo test -p notification-service --test notification_pipeline_test test_request_message_posted_notifications_resolves_current_active_recipients_from_projection_auth_context --offline`
  - Green / Regression
    - `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_message_posted_notification_owner_seam --offline --target-dir target-cp054k-green-notification-structure`
    - `cargo test -p notification-service --test notification_pipeline_test test_request_message_posted_notifications_resolves_current_active_recipients_from_projection_auth_context --offline --target-dir target-cp054k-green-notification-behavior`
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_do_not_thread_message_posted_recipient_ids --offline --target-dir target-cp054k-green-local-structure`
    - `cargo test -p notification-service --offline --target-dir target-cp054k-reg-notification-full`
    - `cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only --offline --target-dir target-cp054k-reg-message-notification`
    - `cargo test -p local-minimal-node --test lib_structure_test --offline --target-dir target-cp054k-reg-local-structure-full`
    - `rustfmt --edition 2024 --check services/notification-service/src/lib.rs services/notification-service/tests/lib_structure_test.rs services/notification-service/tests/notification_pipeline_test.rs services/local-minimal-node/src/node/build.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/tests/lib_structure_test.rs`
- 当前判断
  - 本轮为 `CP05-4` 的第十一个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 26. 2026-04-07 CP05-4 session-gateway session sync-state owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么继续做这个子项
  - 上一轮 review 已明确，剩余主 seam 已收敛到 multi-device sync final closure。
  - `services/session-gateway/src/lib.rs` 的 `resume / get_presence_me / heartbeat / disconnect` 仍在 handler 边界直接读取 `registered_devices(...)` 与 `latest_device_sync_seq(...)`。
  - 这说明 session sync-state 还停留在 edge glue，而不是单一 owner seam，`CP05-4` 不能结束。
- 本轮完成
  - 新增 `services/session-gateway/src/session.rs`。
  - 新增 `DeviceSyncSessionState` 与 `device_sync_session_state(...)`。
  - `AppState` 新增 `device_sync_session_state(&AuthContext, requested_device_id)` owner seam。
  - `resume_session(...)`、`get_presence_me(...)`、`heartbeat_presence(...)`、`disconnect_session(...)` 从 `lib.rs` 迁移到 `session.rs`。
  - `build_app_with_state(...)` 改为统一路由到 `session::` handler。
- 本轮验证
  - Red
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_session_surface_moves_out_of_lib_impl --offline --target-dir target-cp054l-red-session-structure`
  - Green / Regression
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_session_surface_moves_out_of_lib_impl --offline --target-dir target-cp054l-green-session-structure`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_session_paths_use_device_sync_session_state_owner_seam --offline --target-dir target-cp054l-green-session-owner`
    - `cargo test -p session-gateway --offline --target-dir target-cp054l-reg-session-gateway`
    - `rustfmt --edition 2024 services/session-gateway/src/lib.rs services/session-gateway/src/session.rs services/session-gateway/tests/lib_structure_test.rs`
    - `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/session.rs services/session-gateway/tests/lib_structure_test.rs`
- 当前判断
  - 本轮为 `CP05-4` 的第十二个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞
  - 剩余高概率 blocker 已从 handler 级 raw sync-state 读取，收敛到 `session-gateway` 内部底层 raw device roster / latest sync seq owner 仍是本地 `AppState`

## 27. 2026-04-07 CP05-4 session-gateway sync-state store owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么继续做这个子项
  - 上一轮只完成 handler 收口，`AppState` 仍直接持有 `registered_devices / latest_sync_sequences` 与 raw read helper。
  - 如果底层 storage owner 仍留在 `lib.rs`，`CP05-4` 不能视为真正推进。
- 本轮实际完成
  - 新增 `services/session-gateway/src/session_state.rs`
  - `SessionSyncState` 接管 sync-state storage owner
  - `AppState::register_device(...)` 与 `AppState::device_sync_session_state(...)` 改为委托 `SessionSyncState`
  - `lib.rs` 移除 raw storage field 与 raw read helper
  - `services/session-gateway/tests/lib_structure_test.rs` 新增 `test_session_gateway_sync_state_owner_moves_out_of_lib_impl`
- 本轮验证
  - Red
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_sync_state_owner_moves_out_of_lib_impl --offline --target-dir target-cp054m-red-sync-owner`
  - Green / Regression
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_sync_state_owner_moves_out_of_lib_impl --offline --target-dir target-cp054m-green-sync-owner`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_session_paths_use_device_sync_session_state_owner_seam --offline --target-dir target-cp054m-green-session-owner`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_session_surface_moves_out_of_lib_impl --offline --target-dir target-cp054m-green-session-structure`
    - `cargo test -p session-gateway --offline --target-dir target-cp054m-reg-session-gateway`
    - `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/session.rs services/session-gateway/src/session_state.rs services/session-gateway/tests/lib_structure_test.rs`
- 当前判断
  - 本轮为 `CP05-4` 的第十三个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 28. 2026-04-07 CP05-4 session-gateway device registration owner 执行补充

- 本轮上下文
  - `Wave B / Step 05 / CP05-4`
- 为什么继续做
  - `session-gateway` 虽然已经把 session sync-state storage owner 从 `AppState` 收进 `session_state.rs`，但 `AppState::register_device(...)` 仍直接编排 `presence_runtime / realtime_runtime / session_state / realtime_cluster`。
  - 这说明 device registration 装配 owner 仍停留在 edge glue，`CP05-4` 不能算完成。
- 本轮实际完成
  - 新增 `services/session-gateway/src/device_registration.rs`
    - `SessionDeviceRegistration`
    - `new(...)`
    - `register_device(...)`
  - `services/session-gateway/src/lib.rs`
    - 新增 `mod device_registration;`
    - `AppState` 持有 `device_registration: SessionDeviceRegistration`
    - `AppState::register_device(...)` 改为委托 `self.device_registration.register_device(...)`
    - 移除 `lib.rs` 内联 device registration 编排细节
  - `services/session-gateway/tests/lib_structure_test.rs`
    - 新增 `test_session_gateway_device_registration_owner_moves_out_of_lib_impl`
    - 同步更新 `test_session_gateway_sync_state_owner_moves_out_of_lib_impl` 的结构基线
- 本轮验证
  - Red
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_device_registration_owner_moves_out_of_lib_impl --offline --target-dir target-cp054n-red-device-registration`
  - Green / Regression
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_device_registration_owner_moves_out_of_lib_impl --offline --target-dir target-cp054n-green-device-registration`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_sync_state_owner_moves_out_of_lib_impl --offline --target-dir target-cp054n-green-sync-owner`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_session_paths_use_device_sync_session_state_owner_seam --offline --target-dir target-cp054n-green-session-owner`
    - `cargo test -p session-gateway --offline --target-dir target-cp054n-reg-session-gateway`
    - `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/device_registration.rs services/session-gateway/tests/lib_structure_test.rs`
- 当前判断
  - 本轮是 `CP05-4` 的第十四个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞

## 29. 2026-04-07 CP05-4 local-minimal-node device registration owner 执行补充

- 本轮上下文
  - `Wave B / Step 05 / CP05-4`
- 为什么继续做
  - `session-gateway` 的 device registration seam 已完成收口，但 repo 级 `CP05-4` 仍有一条真实残余 seam 留在 `services/local-minimal-node/src/node/access.rs`。
  - 只要 `access.rs` 还握有 route/session current 与 register/bind orchestration owner，`local-minimal-node` 的 device/session 路径就仍然停留在 edge glue。
- 本轮实际完成
  - 新增 `services/local-minimal-node/src/node/device_registration.rs`
  - `AppState` 增持 `device_registration: LocalNodeDeviceRegistration`
  - `build.rs` 改为在装配期注入共享 `node_id / realtime_cluster / session_presence_runtime / realtime_runtime / projection_service`
  - `access.rs` 移除 `ensure_route_session_current(...)` 与 `bind_registered_device(...)` 的事实 owner，实现仅保留薄委托
  - `session.rs` 改为消费 `AppState::bind_device_registration(...)`
- 本轮验证
  - Red
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_device_registration_owner_moves_out_of_access_impl --offline --target-dir target-cp054o-red-device-registration`
  - Green / Regression
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_device_registration_owner_moves_out_of_access_impl --offline --target-dir target-cp054o-green-device-registration`
    - `cargo test -p local-minimal-node --test lib_structure_test --offline --target-dir target-cp054o-green-local-structure-full`
    - `cargo test -p local-minimal-node --offline --target-dir target-cp054o-reg-local-minimal-node`
    - `rustfmt --edition 2024 --check services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/build.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/src/node/device_registration.rs`
- 当前判断
  - 本轮是 `CP05-4` 的第十五个真实增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞
  - repo 级搜索仍显示 `services/session-gateway/src/lib.rs`、`services/session-gateway/src/session.rs`、`services/session-gateway/src/websocket_route.rs` 保留重复 `ensure_route_session_current(...) + register_device(...)` 入口 glue，疑似下一条高概率 blocker

## 30. 2026-04-07 CP05-4 session-gateway route preflight owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么做
  - 上一轮 review 已明确指出 `session-gateway` 三条入口还保留重复 `ensure_route_session_current(...) + register_device(...)` glue。
  - 如果这个组合 preflight 仍散落在入口层，`CP05-4` 不能视为继续实质推进。
- 本轮实际完成
  - 新增 `test_session_gateway_route_preflight_owner_moves_out_of_entrypoints`
  - `SessionDeviceRegistration` 新增 `prepare_active_device_route(...)`
  - `SessionDeviceRegistration` 新增私有 `ensure_route_session_current(...)`
  - `AppState` 新增 `prepare_active_device_route(...)`
  - `lib.rs / session.rs / websocket_route.rs` 改为统一消费该 seam
- 验证
  - Red
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_route_preflight_owner_moves_out_of_entrypoints --offline --target-dir target-cp054p-red-route-preflight`
  - Green / Regression
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_route_preflight_owner_moves_out_of_entrypoints --offline --target-dir target-cp054p-green-route-preflight`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_websocket_upgrade_module_stays_pure_axum_adapter --offline --target-dir target-cp054p-green-websocket-structure`
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_device_registration_owner_moves_out_of_lib_impl --offline --target-dir target-cp054p-green-device-registration-structure`
    - `cargo test -p session-gateway --offline --target-dir target-cp054p-reg-session-gateway`
    - `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/device_registration.rs services/session-gateway/src/session.rs services/session-gateway/src/websocket_route.rs services/session-gateway/tests/lib_structure_test.rs`
    - `rg -n "ensure_route_session_current\\(" services crates adapters tools -g "*.rs"`
- 执行补充结论
  - 本轮是 `CP05-4` 的第十六个真实增量
  - `session-gateway` 的 route preflight owner 已从入口胶水收口到单一 seam
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞
## 31. 2026-04-07 CP05-4 local-minimal-node route preflight owner 执行补充

- 当前定位
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么继续做
  - `local-minimal-node` 虽然已经把 device registration owner 收到 `node/device_registration.rs`，但 [`services/local-minimal-node/src/node/session.rs`](D:\javasource\spring-ai-plus\spring-ai-plus-business\apps\craw-chat\services\local-minimal-node\src\node\session.rs) 仍在多个 session / realtime / websocket 入口保留本地 `bind_device(...)` preflight 胶水。
  - 只要普通 preflight 还停在 session entrypoint，本轮 `CP05-4` 就还没有继续向 repo 级 closure 推进。
- 本轮实际完成
  - 新增 `LocalNodeDeviceRegistration::prepare_active_device_route(...)`
  - 新增 `AppState::prepare_active_device_route(...)`
  - `session.rs` 删除本地 `bind_device(...)`
  - `heartbeat_presence(...)`、`disconnect_session(...)`、`register_device(...)`、`sync_realtime_subscriptions(...)`、`list_realtime_events(...)`、`ack_realtime_events(...)`、`realtime_websocket(...)` 统一改为消费 `state.prepare_active_device_route(...)`
  - `resume_session(...)` 保留 `bind_device_registration(..., true)` takeover 语义
  - 新增 `test_local_minimal_node_route_preflight_owner_moves_out_of_session_entrypoints`
- 本轮验证
  - Red
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_route_preflight_owner_moves_out_of_session_entrypoints --offline --target-dir target-cp054q-red-route-preflight`
  - Green / Regression
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_route_preflight_owner_moves_out_of_session_entrypoints --offline --target-dir target-cp054q-green-route-preflight`
    - `cargo test -p local-minimal-node --offline --target-dir target-cp054q-reg-local-minimal-node`
    - `rustfmt --edition 2024 --check services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/device_registration.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/tests/lib_structure_test.rs`
- 当前判断
  - 本轮是 `CP05-4` 的有效增量
  - `CP05-4` 仍未闭环
  - `Step 05` 仍未闭环
  - `91 / 95 / 97` 仍不能整体判定通过
  - `Wave B / 93` 仍阻塞
## 32. 2026-04-07 CP05-4 session-gateway disconnect lifecycle owner 执行补充

- 执行上下文
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么做
  - 上一轮完成 route preflight owner 后，`services/session-gateway/src/session.rs` 仍残留 disconnect lifecycle raw glue，`CP05-4` 还不能宣告 repo 级 closure。
- 本轮实际完成
  - `services/session-gateway/src/device_registration.rs`
    - 新增 `DisconnectActiveDeviceRouteOutcome`
    - 新增 `disconnect_active_device_route(...)`
    - 收敛 disconnect fence 判断、活动路由预处理、订阅清理、route release、disconnect 标记与断开信号发送
  - `services/session-gateway/src/lib.rs`
    - 新增 `AppState::disconnect_active_device_route(...)` delegate
    - 清理下沉后不再需要的 `node_id` / `realtime_cluster` 状态字段
  - `services/session-gateway/src/session.rs`
    - `disconnect_session(...)` 改为只消费 owner outcome，不再直连 raw disconnect glue
  - `services/session-gateway/tests/lib_structure_test.rs`
    - 新增 `test_session_gateway_disconnect_lifecycle_owner_moves_out_of_session_entrypoints`
- 做了哪些验证
  - Red
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_disconnect_lifecycle_owner_moves_out_of_session_entrypoints --offline --target-dir target-cp054r-red-disconnect-owner`
  - Green
    - `cargo test -p session-gateway --test lib_structure_test test_session_gateway_disconnect_lifecycle_owner_moves_out_of_session_entrypoints --offline --target-dir target-cp054r-green-disconnect-owner`
  - Structure regression
    - `cargo test -p session-gateway --test lib_structure_test --offline --target-dir target-cp054r-structure`
  - Package regression
    - `cargo test -p session-gateway --offline --target-dir target-cp054r-reg-session-gateway`
  - Format
    - `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/device_registration.rs services/session-gateway/src/session.rs services/session-gateway/tests/lib_structure_test.rs`
- 当前还差什么
  - `local-minimal-node` 对等 disconnect lifecycle seam 仍未下沉
  - `CP05-4` 仍未完成
  - `Step 05` 仍未完成
  - `91 / 95 / 97` 仍未完成
  - `Wave B / 93` 仍未完成
## 33. 2026-04-07 CP05-4 local-minimal-node disconnect lifecycle owner 执行补充

- 执行上下文
  - `Wave B / Step 05 / CP05-4`
- 本轮为什么做
  - `session-gateway` 侧 disconnect lifecycle owner 完成后，`local-minimal-node` 的 `session.rs` 仍保留对等 raw glue，`CP05-4` 还没有真正收口。
- 本轮实际完成
  - `services/local-minimal-node/src/node/device_registration.rs`
    - 新增 `DisconnectActiveDeviceRouteOutcome`
    - 新增 `disconnect_active_device_route(...)`
    - 收敛 disconnect fence 判断、活动路由预处理、订阅清理、route release、disconnect 标记、disconnect signal 与 `platform::refresh_node_operational_view(...)`
  - `services/local-minimal-node/src/node.rs`
    - 新增 `AppState::disconnect_active_device_route(...)` delegate
  - `services/local-minimal-node/src/node/session.rs`
    - `disconnect_session(...)` 改为只消费 owner outcome，不再直连 raw disconnect glue
  - `services/local-minimal-node/tests/lib_structure_test.rs`
    - 新增 `test_local_minimal_node_disconnect_lifecycle_owner_moves_out_of_session_entrypoints`
- 做了哪些验证
  - Red
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_disconnect_lifecycle_owner_moves_out_of_session_entrypoints --offline --target-dir target-cp054s-red-local-disconnect`
  - Green
    - `cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_disconnect_lifecycle_owner_moves_out_of_session_entrypoints --offline --target-dir target-cp054s-green-local-disconnect`
  - Structure regression
    - `cargo test -p local-minimal-node --test lib_structure_test --offline --target-dir target-cp054s-structure`
  - Package regression
    - `cargo test -p local-minimal-node --offline --target-dir target-cp054s-reg-local-minimal-node`
  - Format
    - `rustfmt --edition 2024 --check services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/device_registration.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/tests/lib_structure_test.rs`
  - 补充扫描
    - `rg -n "disconnect_fence_matches_session\\(|clear_device_subscriptions\\(|release_device_route\\(|mark_device_disconnected\\(|platform::refresh_node_operational_view\\(&state\\)" services/session-gateway/src/session.rs services/local-minimal-node/src/node/session.rs -g "*.rs"`
    - 结果：无命中
- 当前还差什么
  - `CP05-4` 已收口，但 `Step 05` 还缺整步 `91 / 95 / 97` 闭环审计
  - `Wave B / 93` 仍未完成

## 34. 2026-04-07 Step 05 step-wide 91 / 95 / 97 闭环审计

- 执行上下文
  - `Wave B / Step 05`
- 本轮为什么做
  - `CP05-4` 的 owner seam 收口代码与局部回归已完成，但 `Step 05` 仍缺整步 `91 / 95 / 97` 闭环证据，不能直接进入 `Step 06`。
- 本轮实际完成
  - 按最新仓库真实状态重新执行 `Step 05` 范围包级回归与格式校验，确认 `message / conversation` 主链路重构没有新增回归。
  - 复核 `session-gateway` 与 `local-minimal-node` 的 disconnect lifecycle raw glue 扫描结果，确认 `CP05-4` 不再残留已知 owner seam blocker。
  - 将整步闭环结论回写到 `docs/review` 与 `docs/架构`，作为 `91 / 95 / 97` 的收口证据。
- 做了哪些验证
  - Package regression
    - `cargo test -p im-auth-context --offline --target-dir target-step05-audit-im-auth-context`
    - `cargo test -p im-domain-core --offline --target-dir target-step05-audit-im-domain-core`
    - `cargo test -p im-platform-contracts --offline --target-dir target-step05-audit-im-platform-contracts`
    - `cargo test -p conversation-runtime --offline --target-dir target-step05-audit-conversation-runtime`
    - `cargo test -p projection-service --offline --target-dir target-step05-audit-projection-service`
    - `cargo test -p notification-service --offline --target-dir target-step05-audit-notification-service`
    - `cargo test -p session-gateway --offline --target-dir target-step05-audit-session-gateway`
    - `cargo test -p local-minimal-node --offline --target-dir target-step05-audit-local-minimal-node`
  - Format
    - `cargo fmt -p im-auth-context -p im-domain-core -p im-platform-contracts -p conversation-runtime -p projection-service -p notification-service -p session-gateway -p local-minimal-node -- --check`
  - Raw glue scan
    - `rg -n "disconnect_fence_matches_session\\(|clear_device_subscriptions\\(|release_device_route\\(|mark_device_disconnected\\(|platform::refresh_node_operational_view\\(&state\\)" services/session-gateway/src/session.rs services/local-minimal-node/src/node/session.rs -g "*.rs"`
    - 结果：无命中
- 本轮结论
  - `Step 05` 已通过整步 `91 / 95 / 97` 审计，可以闭环。
  - `Wave B / 93` 仍未触发，因为 `Step 06` 尚未闭环，不是 `Step 05` 阻塞。
- 当前还差什么
  - `Step 05` 无剩余实现缺口。
  - 下一闭环目标转入 `Wave B / Step 06`。
- 下一轮继续做什么
  - 进入 `Step 06`，按 `00 -> 13` 顺序继续推进 `stream / rtc` 相关能力，并在 `Step 06` 闭环后再判断是否满足 `Wave B / 93` 总验收条件。
