# S09 Loop-18 补充 - 2026-04-10

## As-Built 增量
- runtime 已新增 `list_messages_from_auth_context` 与 `MessageHistoryResult`
- `GET /im/v3/api/chat/conversations/{conversation_id}/messages` 已进入正式 HTTP surface
- `ConversationMessageLog` 已暴露 `messages_in_order()`
- `history_visibility` 已真实影响历史读取：
  - `joined`：非成员 `403`
  - `world_readable`：同租户已鉴权非成员 `200`

## Fresh Evidence
- `cargo test -p im-domain-core --offline`
- `cargo test -p conversation-runtime --offline --tests`
- `cargo test -p control-plane-api --offline --tests`
- `cargo test -p craw-chat-ccp-registry --offline`
- `cargo test -p im-platform-contracts --offline`

## 闭环判断
- `S09.status = not_closed`
- `S09.local_closure = runtime history visibility partial read consumption`
- `S10` 继续阻塞；当前阻塞已收敛为：
  - `invited/shared` 未形成独立 runtime 语义
  - `retention_policy_ref` 仍无 owner 闭环

## 下一轮输入
- 优先补齐 `invited/shared` 的最小 artifact 与读权限判定。
- 若当前架构窗口不允许补齐 artifact，必须在 control-plane/runtime 明确阻断未支持模式。
- `retention_policy_ref` 继续保持 `S13` owner，不在 `S09` 虚报闭环。
