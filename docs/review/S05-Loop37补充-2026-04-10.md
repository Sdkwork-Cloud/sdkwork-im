# S05 Loop37补充 - 2026-04-10

## 1. 本轮主任务
- 关闭 `snapshot save fail` 仍缺稳定 failpoint 和 operator repair 证据的缺口。

## 2. 已兑现事实
- `SocialControlRuntime` 新增一次性 `failNextSnapshotSave` 注入能力，落点为 runtime-dir 下的 `state/social-failpoints.json`。
- `persist_state_transition` 现在会在 journal 已持久化后消费 failpoint，并稳定返回 `social_state_unavailable`，不再依赖只读文件权限制造偶发失败。
- 控制面新增 `POST /api/v1/control/social/runtime/repair-derived-snapshot`，可显式把当前 live state 回刷为 `social-state.json`。
- 新增并转绿：
  - `test_control_plane_social_file_runtime_failpoint_forces_next_snapshot_save_failure_once`
  - `test_control_plane_social_file_runtime_operator_repair_rebuilds_snapshot_after_failpoint`
- fresh 包级回归：`cargo test -p control-plane-api --offline --tests -- --nocapture` = `58 passed`

## 3. 仍未关闭
- 仍无真正 `atomic tx boundary`
- operator repair 仍以当前 runtime live-state 为准，尚未沉淀为独立的 journal-only replay/repair 工具
- `S05 = not_closed / local_closure`

## 4. 当前结论
- Loop37 已把 `S05` 从“same-event retry 可确认 truth，但 failpoint/repair 证据不足”推进到“failpoint 可稳定注入，operator 可显式 repair derived snapshot，并可在重启后读回 repaired truth”；下一主缺口收敛为 `atomic tx boundary + full durable step closure`。
