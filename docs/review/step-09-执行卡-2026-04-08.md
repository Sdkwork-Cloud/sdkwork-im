# Step 09 执行卡 - 2026-04-08

## 当前上下文
- 当前波次：`Wave C`
- 当前 step：`Step 09`
- 当前状态：
  - `CP09-1` 已把 local-disk metadata / projection store port 落地，并在后续增量中被真实 consumer 接线
  - `CP09-2` 已完成 projection snapshot rebuild / runtime-dir recovery 收口
  - `CP09-3` 已完成 projection plane observability 收口
  - `CP09-4` 本轮已完成 archive lifecycle 闭环
- 当前需要判定的是：`Step 09` 是否已满足整步 `91 / 95 / 97`

## 本轮为什么做整步审计
- `docs/step/95-架构能力闭环验收标准.md` 对 `Step 09` 的闭环要求是：
  - 主链路已接到统一存储抽象
  - 投影可重建
  - 恢复与诊断能力可重复执行
  - 关键指标和 tracing 已按 plane 收口
- 当前仓库已经具备相应代码、测试、脚本与 review 证据，因此需要从子任务状态提升到整步闭环判断。

## 整步实际完成

### 1. 统一存储抽象已经不再停留在接口层
- `adapters/local-disk` 的 metadata / projection store 已成为真实 consumer 使用的 file-backed seam
- `projection-service` 已持续读写同一份 snapshot store
- `local-minimal-node` runtime-dir recovery 已真实消费这套 snapshot 恢复合同

### 2. projection rebuild / recovery 已可重复执行
- conversation read-side 与 device-sync read-side 都已可从 projection snapshot 恢复
- runtime-dir inspection / repair / preview / restore / backup catalog 都已纳入 projection snapshot managed files
- stale snapshot restart、snapshot-only recovery、runtime-dir restore 都已有自动化验证

### 3. plane 级 observability 已完成最小收口
- `projection-service` / `ops-service` / `local-minimal-node` 已公开：
  - snapshot persist / restore metrics
  - replay backlog / replay duration / throughput
  - live projection lag
  - update delay
  - rebuild duration
- 这些指标都已进入统一 `Projection Plane` owner，而不是散落日志。

### 4. backup / restore / repair / archive 已形成可执行工具链
- runtime-dir 已具备连续操作面：
  - inspect
  - repair
  - list backups
  - archive
  - preview restore
  - restore
  - prune archived backups
- archive lifecycle 已具备：
  - retention metadata
  - legal hold
  - prune action
  - restore path

## Step 09 验证证据
- `cargo fmt --all --check`
- `cargo test -p im-adapters-local-disk --offline`
- `cargo test -p projection-service --offline`
- `cargo test -p ops-service --offline`
- `cargo test -p local-minimal-node --offline`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/archive-runtime-backup-local.ps1 -Help`
- `powershell -NoProfile -ExecutionPolicy Bypass -File bin/prune-runtime-archives-local.ps1 -Help`

## 结论
- `91`：通过
- `95`：通过
- `97`：已完成
- `Step 09`：闭环完成

## 当前还差什么
- `Wave C / 93` 仍需执行总验收
- `Wave D / Step 10` 还未进入真实代码推进

## 下一轮继续做什么
1. 立刻执行 `Wave C / 93` 总验收
2. 若 93 通过，自动进入 `Wave D / Step 10`
3. 不回退 `Step 09` 刚冻结的 storage / projection / recovery / observability / lifecycle 边界
