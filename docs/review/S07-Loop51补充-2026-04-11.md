# S07 Loop51补充 - 2026-04-11

## 1. 本轮唯一主缺口
- Loop51 固定只做 `shared_channel_policy durable sync seam`
- `control-plane -> conversation-runtime automatic sync trigger / auto-projection`、独立 `im_thread_subscription` durable model、per-user unread / notification level 均保持 deferred，不与本轮并行混写

## 2. 本轮交付
- `conversation-runtime` 新增 `SyncSharedChannelLinkedMemberCommand` 与 `sync_shared_channel_linked_member_*`
- 该 seam 只允许 `system` actor 调用，并把 shared durable truth 派生 payload materialize 成 `role = guest / state = linked` 的 runtime member
- sync path 会持久化 `sharedChannelPolicyId / externalConnectionId / externalMemberId` 三元锚点，并初始化默认 read cursor
- 新增 `POST /im/v3/api/chat/conversations/shared-channel-links/sync`
- recovery replay 后，同一 linked member truth 仍保留，shared history 继续可读

## 3. 运行时边界
- 本轮只闭合 `shared_channel_policy / external_member_link -> runtime linked member materialization -> shared history read -> replay recovery` 最小链路
- 没有让 `control-plane-api` 直接依赖 `conversation-runtime`
- 没有实现跨服务自动事件总线 / outbox / republish
- 没有解决 `external_member_link` durable truth 缺少 principal kind 的建模问题；当前由 sync request 显式桥接 `localActorKind`
- 没有实现 per-user unread / notification level / follow-unfollow

## 4. Fresh Evidence
- red：`cargo test -p conversation-runtime --offline --test http_smoke_test test_sync_shared_channel_linked_member_over_http_materializes_linked_history_reader -- --nocapture`
- red 证据：初始失败断言为 `left: 404`、`right: 200`
- `cargo test -p conversation-runtime --offline --test conversation_flow_test test_sync_shared_channel_linked_member_materializes_runtime_truth_and_survives_recovery_replay -- --nocapture` = `passed`
- `cargo fmt -p conversation-runtime -p im-domain-core` = `passed`
- `cargo test -p conversation-runtime --offline --tests -- --nocapture` = `128 passed`

## 5. 结论
- `S07` 仍只能标记为 `not_closed / local_closure`
- `shared_channel_policy durable sync seam` 已进入最小可运行闭环
- 下一单点主缺口切换为 `control-plane -> conversation-runtime automatic sync trigger / auto-projection`
