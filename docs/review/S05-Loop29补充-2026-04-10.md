# S05 Loop29 补充 - 2026-04-10

## 1. 本轮目标
- 把 `user_block` 从“仅有领域模型/事件契约”推进到“控制面最小 truth 写链路”。

## 2. 已落地事实
- `services/control-plane-api` 新增：
  - `POST /api/v1/control/social/user-blocks`
  - `GET /api/v1/control/social/user-blocks/{blockId}`
- 控制面已产出：
  - `user_block.blocked`
  - `social.user_block.blocked.v1`
  - `control.user_block_blocked`
- 已加约束：
  - `blocker_user_id -> blocked_user_id` 为有向关系
  - 同租户、同方向、同 `scope` 的 active block 冲突
  - `scope = direct_chat` 时要求 `directChatId`

## 3. 测试证据
- TDD red：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test -- --nocapture`
  - 结果：`404 != 200`
- TDD green：
  - `cargo test -p control-plane-api --offline --test social_friend_request_test -- --nocapture`
  - 结果：`8 passed`
- 包级回归：
  - `cargo test -p control-plane-api --offline --tests -- --nocapture`
  - 结果：`48 passed`

## 4. 当前结论
- `CPR05-3 = done`
- `S05 = not_closed / local_closure`

## 5. 剩余 blocker
- `control-plane-api` 仍是进程内内存态，不是 durable repo / tx / outbox / replay
- `direct_chat -> conversation` 仍只是 control-plane bind fact，不是 runtime 真绑定
- `contacts` 对 `user_block` 的消费语义尚未落地

## 6. 下一刀建议
- 优先级 1：把 social truth 抽成 durable repo / tx / outbox
- 优先级 2：补 `direct_chat -> conversation` runtime binding
