# S07 Loop50补充 - 2026-04-11

## 1. 本轮唯一主缺口
- Loop50 固定只做 `shared fanout / rebuild`
- `shared_channel_policy auto-projection / durable sync`、独立 `im_thread_subscription` durable model、per-user unread / notification level 均保持 deferred，不与本轮并行混写

## 2. 本轮交付
- `projection-service` 新增 `message_posted_notification_principal_ids_from_auth_context(...)`
- 该 seam 在调用者仍需是 active member 的前提下，返回 `joined active members + shared-history-visible linked members`
- `notification-service::request_message_posted_notifications(...)` 已切换到该 seam，shared linked principal 现会进入 message-posted 通知目标集合，actor 仍在 fanout 阶段被排除
- 由于 projection 继续完整持有 `ConversationMember` 的 `state = linked` 与 shared/external 锚点 attributes，事件重放 / rebuild 后仍能恢复相同 recipient truth

## 3. 运行时边界
- 本轮只闭合 `shared linked history visibility -> projection recipient resolution -> message-posted notification fanout -> rebuild replay` 最小链路
- 没有引入 `shared_channel_policy` durable truth 的自动投影 / materialize
- 没有实现 per-user unread / notification level / follow-unfollow
- 没有引入独立 `im_thread_subscription` durable model

## 4. Fresh Evidence
- red：`cargo test -p notification-service --offline --test notification_pipeline_test test_request_message_posted_notifications_includes_shared_linked_recipients_from_projection -- --nocapture`
- red 证据：初始失败断言为 `left: 1`、`right: 2`
- red：`cargo test -p projection-service --offline --test timeline_projection_test test_message_posted_notification_principal_ids_from_auth_context_includes_shared_linked_members -- --nocapture`
- red 证据：初始失败为缺少 `message_posted_notification_principal_ids_from_auth_context`
- `cargo fmt -p notification-service -p projection-service` = `passed`
- `cargo test -p projection-service --offline --tests -- --nocapture` = `47 passed`
- `cargo test -p notification-service --offline --tests -- --nocapture` = `19 passed`

## 5. 结论
- `S07` 仍只能标记为 `not_closed / local_closure`
- `shared fanout / rebuild` 已进入最小可运行闭环
- 下一单点主缺口切换为 `shared_channel_policy auto-projection / durable sync`
