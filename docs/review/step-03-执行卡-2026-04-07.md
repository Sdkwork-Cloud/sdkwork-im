# Step 03 执行卡 - 2026-04-07

## 1. 当前判断

- 当前波次：`Wave A`
- 当前 Step：`03`
- 当前状态：`已完成，已闭环`
- 门禁判定：
  - `91`：通过
  - `95`：通过
  - `97`：通过
  - `Wave A / 93`：可执行，且已具备通过条件

## 2. 本轮为什么继续做 Step 03

`Step 02` 已闭环后，`Wave A` 的唯一剩余阻塞一直是 `Step 03`。

本轮继续停留在 `Step 03` 的原因不是重复做 crate 命名工作，而是基于当前仓库真实状态补两个收口缺口：

- 代码侧仍缺少 `contract-admin / contract-iot` 两个真实 crate owner
- 文档侧仍沿用“主入口尚未接线”的旧结论，而仓库代码实际上已经完成 `session-gateway + local-minimal-node + chat-cli` 的 CCP WebSocket 主入口闭环

因此本轮没有跳到 `Step 04`，而是先把 `Step 03` 的代码、测试、`docs/review` 与 `docs/架构` 一次收口。

## 3. 本轮实际完成

### 3.1 补齐真实 `contract-admin / contract-iot` crate

- 新增 `crates/craw-chat-contract-admin`
- 新增 `crates/craw-chat-contract-iot`
- `im-platform-contracts` compatibility facade 已同步 re-export 新类型与 trait
- `contract_split_smoke_test` 已覆盖这两个新增契约 owner，避免它们继续停留在文档名词层

### 3.2 基于当前代码确认主入口已经完成统一 CCP 运行时接线

当前仓库中的真实实现已经满足 Step 03 对“主入口不再各写一套协议细节”的要求：

- `session-gateway` WebSocket 主入口已支持：
  - 子协议 `craw-chat.ccp.ws.v1`
  - `LegacyJson / CcpJson` 双模式兼容
  - 服务端 `hello -> hello_ack -> auth_bind -> auth_ok`
  - CCP-wrapped `realtime.connected / subscriptions.sync / events.pull / events.ack`
- `local-minimal-node` 的 realtime WebSocket 入口已透传同一子协议和同一 `RealtimeWebsocketMode`
- `tools/chat-cli` 已通过 `src/realtime.rs` 真实消费 `craw-chat-ccp-binding-ws + craw-chat-ccp-control + JsonEnvelopeCodec`，内部完成握手，对外维持旧 JSON 业务输出

### 3.3 完成 Step 03 的 review 证据与架构回写

- `docs/review/step-03-*` 三份文档已改为闭环结论
- `docs/review/wave-a-93-总验收阻塞审计-2026-04-07.md` 已从“阻塞视角”更新为可执行总验收结论
- 已回写 `docs/架构/09 / 144 / 145 / 146 / 147`

## 4. 本轮改动文件

### 4.1 代码

- `Cargo.toml`
- `crates/craw-chat-contract-admin/*`
- `crates/craw-chat-contract-iot/*`
- `crates/im-platform-contracts/Cargo.toml`
- `crates/im-platform-contracts/src/lib.rs`
- `crates/im-platform-contracts/tests/contract_split_smoke_test.rs`

### 4.2 文档

- `docs/review/step-03-执行卡-2026-04-07.md`
- `docs/review/step-03-质量审计与复盘-2026-04-07.md`
- `docs/review/step-03-架构兑现与回写决议-2026-04-07.md`
- `docs/review/wave-a-93-总验收阻塞审计-2026-04-07.md`
- `docs/架构/09-实施计划.md`
- `docs/架构/144-CCP传输绑定与握手协商设计-2026-04-06.md`
- `docs/架构/145-CCP数据协议与版本兼容安全设计-2026-04-06.md`
- `docs/架构/146-CCP协议注册表与多端SDK兼容治理设计-2026-04-06.md`
- `docs/架构/147-CCP到Crate与接口模块落地映射设计-2026-04-06.md`

## 5. 本轮验证

### 5.1 格式检查

- `cargo fmt --check --package craw-chat-contract-admin --package craw-chat-contract-iot --package im-platform-contracts --package session-gateway --package local-minimal-node --package craw-chat-cli`

结果：通过

### 5.2 契约与协议基础验证

- `$env:CARGO_TARGET_DIR='target/local-minimal-step03'; cargo test -p craw-chat-contract-admin -p craw-chat-contract-iot -p im-platform-contracts --offline`

结果：通过，关键验证包括：

- `ccp_foundation_smoke_test`：`3/3`
- `contract_split_smoke_test`：`1/1`
- `contracts_smoke_test`：`1/1`
- `craw-chat-contract-admin / craw-chat-contract-iot`：编译通过

### 5.3 主入口 WebSocket 验证

- `$env:CARGO_TARGET_DIR='target/local-minimal-step03'; cargo test -p session-gateway --test websocket_smoke_test --offline`

结果：通过，`3/3`

- `test_realtime_websocket_binds_http_control_semantics`
- `test_realtime_websocket_negotiates_ccp_subprotocol_and_wraps_business_frames`
- `test_realtime_websocket_closes_when_session_disconnects`

### 5.4 CLI 入口验证

- `$env:CARGO_TARGET_DIR='target/local-minimal-step03'; cargo test -p craw-chat-cli test_connect_realtime_socket_negotiates_ccp_subprotocol --offline`
- `$env:CARGO_TARGET_DIR='target/local-minimal-step03'; cargo test -p craw-chat-cli --test chat_cli_e2e_test --offline`

结果：通过

- CCP 子协议协商测试：`1/1`
- CLI E2E：`3/3`

### 5.5 相关包 fresh 回归

- `$env:CARGO_TARGET_DIR='target/local-minimal-step03'; cargo test -p session-gateway -p local-minimal-node -p craw-chat-cli -p im-platform-contracts --offline`

结果：通过。关键点：

- `session-gateway` 全包测试通过
- `local-minimal-node` 全包测试通过
- `craw-chat-cli` 全包测试通过
- `im-platform-contracts` 全包测试通过

## 6. 本轮对应架构能力

- `docs/架构/143-统一协议总纲与分层设计-2026-04-06.md`
- `docs/架构/144-CCP传输绑定与握手协商设计-2026-04-06.md`
- `docs/架构/145-CCP数据协议与版本兼容安全设计-2026-04-06.md`
- `docs/架构/146-CCP协议注册表与多端SDK兼容治理设计-2026-04-06.md`
- `docs/架构/147-CCP到Crate与接口模块落地映射设计-2026-04-06.md`
- `docs/架构/148-CCP控制面注册表与协议发布治理设计-2026-04-06.md`

本轮已兑现：

- `CCP` 协议骨架已具备真实 crate、真实 binding、真实 codec、真实 registry
- WebSocket 主入口已映射到统一 CCP 运行时，不再是旧 JSON 与新 CCP 各走一套实现
- `tenant / actor / sender` 权威边界已经在握手与连接事件上稳定收口
- `contract-*` owner 已完整冻结，包括 `admin / iot`
- `im-platform-contracts` 已明确退为 compatibility facade

本轮明确不纳入 `Step 03`、但已标记归属的能力：

- `session_resume / routeEpoch / drain / goaway` 运行时语义：`Step 04`
- rollout / kill switch / release channel：`Step 07`
- MQTT / IoT 真实运行时接入：`Step 08`
- stable SDK facade 与兼容矩阵产品化：`Step 12`

## 7. 当前 Step 03 还差什么

- `Step 03` 内已无阻塞项
- `docs/review`：已完成
- `docs/架构` 回写：已完成
- 当前剩余事项已明确属于后续 step，不再计入 `Step 03`

## 8. 下一轮动作

1. 以当前 review 证据执行并归档 `Wave A / 93` 总验收结论
2. 自动进入 `Step 04`
3. 严格按 `Step 04` 边界继续推进 `Link Plane / Route Plane / resume / drain / fencing`
