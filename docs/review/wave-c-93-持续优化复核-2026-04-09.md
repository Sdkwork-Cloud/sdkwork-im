# Wave C / 93 持续优化复核 - 2026-04-09

## 1. 结论

- 波次：`Wave C / 93`
- 当前状态：`持续通过`
- 本轮结论：`Step 08` 在 2026-04-09 新增的 IoT protocol 连续闭环属于 Wave C 已通过门禁内的增量收敛，不重新打开 Wave C 门禁
- 下一阶段入口：仍然是 `Wave D / Step 10+`，但允许继续对 `Step 08` 做持续优化

## 2. 为什么需要这次复核

历史总验收文档 [`wave-c-93-总验收-2026-04-08.md`](./wave-c-93-总验收-2026-04-08.md) 已判定 `Wave C / 93` 通过。

但 2026-04-09 在 `Step 08` 上又新增了三条真实 IoT protocol mainline 闭环：

- `08-H`：`/api/v1/iot/protocol/provider-health`
- `08-I`：`/api/v1/iot/protocol/uplink`
- `08-J`：`/api/v1/iot/protocol/downlink`
- `08-K`：known-device uplink preflight
- `08-L`：actor-only uplink preflight
- `08-M`：request-device mismatch preflight
- `08-N`：decoded-device mismatch bad-request boundary

如果不补本次复核，`Wave C / 93` 的总验收证据无法覆盖这些新增 as-built 事实。

## 3. 本次增量收敛覆盖

### 3.1 Step 08 新增闭环

- `08-H / 09AK / 150AK`
  - `local-minimal-node`
  - `GET /api/v1/iot/protocol/provider-health`
  - `IotProtocolAdapter::provider_health_snapshot()`
- `08-I / 09AL / 150AL`
  - `local-minimal-node`
  - `POST /api/v1/iot/protocol/uplink`
  - `decode_uplink() -> device.telemetry`
- `08-J / 09AM / 150AM`
  - `local-minimal-node`
  - `POST /api/v1/iot/protocol/downlink`
  - `encode_downlink() -> device.command`
- `08-K / 09AN / 150AN`
  - `local-minimal-node`
  - `POST /api/v1/iot/protocol/uplink`
  - known-device preflight before `decode_uplink()`
- `08-L / 09AO / 150AO`
  - `local-minimal-node`
  - `POST /api/v1/iot/protocol/uplink`
  - actor preflight before `decode_uplink()`
- `08-M / 09AP / 150AP`
  - `local-minimal-node`
  - `POST /api/v1/iot/protocol/uplink`
  - request/auth device mismatch returns `device_id_mismatch` before `decode_uplink()`
- `08-N / 09AQ / 150AQ`
  - `local-minimal-node`
  - `POST /api/v1/iot/protocol/uplink`
  - decoded/preflight device mismatch returns `device_id_mismatch` after `decode_uplink()`

### 3.2 为什么不重新打开 Wave C 门禁

根据 [`docs/step/93-波次里程碑与阶段总验收矩阵.md`](../step/93-波次里程碑与阶段总验收矩阵.md)，`Wave C` 关注的是：

- `07-09` 的检查点是否通过
- control plane、Agent / Device、projection / rebuild / backup / restore 是否形成稳定闭环

本次新增内容没有削弱这些门禁，反而补强了 `Step 08` 在 IoT protocol 方向上的真实主链路证据：

- `Step 07`：未被回退
- `Step 08`：IoT protocol 从 provider-health 推进到 `uplink / downlink` 双向 mainline
- `Step 09`：未被回退，projection / rebuild / backup / restore 基线仍然成立

因此本轮的正确结论是：

- `Wave C / 93` 保持通过
- `Step 08` 的持续优化不重新打开 Wave C 门禁

## 4. 文档总验收复核

- 新增 `docs/step`：
  - `08-H-IoT-protocol-provider-health-http-surface-2026-04-09.md`
  - `08-I-IoT-protocol-uplink接入device-telemetry-mainline-2026-04-09.md`
  - `08-J-IoT-protocol-downlink接入device-command-mainline-2026-04-09.md`
  - `08-K-IoT-protocol-uplink-known-device预检鉴权-2026-04-09.md`
  - `08-L-IoT-protocol-uplink-actor-preflight-2026-04-09.md`
  - `08-M-IoT-protocol-uplink-request-device-mismatch-2026-04-09.md`
  - `08-N-IoT-protocol-uplink-decoded-device-mismatch-2026-04-09.md`
- 新增 `docs/架构`：
  - `09AK`
  - `09AL`
  - `09AM`
  - `09AN`
  - `09AO`
  - `09AP`
  - `09AQ`
  - `150AK`
  - `150AL`
  - `150AM`
  - `150AN`
  - `150AO`
  - `150AP`
  - `150AQ`
- 新增 `docs/review`：
  - `continuous-optimization-iot-protocol-provider-http-surface-2026-04-09.md`
  - `continuous-optimization-iot-protocol-uplink-device-telemetry-mainline-2026-04-09.md`
  - `continuous-optimization-iot-protocol-downlink-device-command-mainline-2026-04-09.md`
  - `continuous-optimization-iot-protocol-uplink-known-device-preflight-2026-04-09.md`
  - `continuous-optimization-iot-protocol-uplink-actor-preflight-2026-04-09.md`
  - `continuous-optimization-iot-protocol-uplink-request-device-mismatch-2026-04-09.md`
  - `continuous-optimization-iot-protocol-uplink-decoded-device-mismatch-2026-04-09.md`

## 5. 工程总验收复核

- `local-minimal-node` 已具备：
  - `IotProtocolAdapter` 注入式 runtime seam
  - `provider-health / uplink / downlink` 三条真实 route
  - `device.telemetry / device.command` 双向统一 stream mainline
- 本轮没有引入新的跨服务隐藏默认值，仍通过：
  - `build_default_app_with_iot_protocol_adapter`

## 6. 能力总验收复核

本轮新增能力证明：

- IoT protocol external HTTP surface 不再只有观察路径，还具备真实主链路消费
- `local-minimal-node` 已把 `decode_uplink` 与 `encode_downlink` 都接进统一业务主链
- `/api/v1/iot/protocol/uplink` 已具备 actor preflight 与 known-device preflight 两层收敛
- `/api/v1/iot/protocol/uplink` 的 request/auth 设备不一致现在会在 decode 前返回 `device_id_mismatch`
- `/api/v1/iot/protocol/uplink` 的 decoded/preflight 设备不一致现在会在 decode 后返回 `device_id_mismatch`
- `Step 08` 在 Agent / Device / IoT 的统一实时模型上更接近完整，但还没有声称真实设备投递确认已完成

## 7. fresh evidence

- `cargo fmt --all --check`
- `cargo test -p local-minimal-node --offline --test iot_protocol_adapter_mainline_test --test device_stream_e2e_test --test iot_provider_docs_test --test iot_provider_http_test --test provider_plugin_docs_test --test device_access_provider_mainline_test --test lib_structure_test -- --nocapture`

## 8. 本轮边界

- 本次是 `Wave C / 93` 的 as-built 持续优化复核，不是重新做一次 Step 07-09 全量验收
- 不把 `Step 08` 的增量收敛误写成 `Step 09` 或 `Wave D` 的新完成项
- 不把 `iot-xiaozhi`、真实设备投递确认、ACK / retry / timeout 写成已完成

## 9. 下一轮动作

1. 继续在 `Step 08` 内收敛 IoT protocol 剩余真实差距
2. 优先检查 `Step 08` 是否还存在新的真实代码缺口；若无，则按下一 step / wave 继续推进
3. 保持 `Wave C / 93` 为持续通过状态，不因为局部优化而回退总验收结论
