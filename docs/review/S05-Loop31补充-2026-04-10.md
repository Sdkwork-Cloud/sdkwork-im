# S05 Loop31补充 - 2026-04-10

## 1. 本轮新增

- `control-plane-api` social runtime 改为统一 `SocialControlState`。
- 新增 `social-state.json` durable snapshot。
- 新增 `social-commit-journal.json` social outbox。
- 新增 runtime-dir 显式 builder 与 `sdkwork_im_RUNTIME_DIR` 自动装配。

## 2. 已落地事实

- file-backed runtime 重建后可恢复 `friend_request` snapshot。
- file-backed runtime 重建后仍能拒绝重复 `direct_chat` active pair。
- 现有 `friend_request / friendship / user_block / direct_chat / external_* / shared_*` 写链路全部复用同一持久化骨架。

## 3. 对 S05 的真实影响

- 已消除缺口：纯内存态 social runtime。
- 已新增能力：durable snapshot 样板、social outbox 样板、restart recovery 样板。
- 仍未消除：真正 tx boundary、journal replay、step closure。

## 4. 当前结论

- `S05 = not_closed / local_closure`
- 下一个主刀应继续留在 `S05`，优先完成 `social-commit-journal` replay 与事务边界硬化。
