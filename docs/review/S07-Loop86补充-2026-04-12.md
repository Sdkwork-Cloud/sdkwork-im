# S07 Loop86 Addendum - 2026-04-12

## 1. Main Conclusion
- Loop86 extends stale-claim normalization into the ordinary next-write path.
- A stale claimed pending shared-channel sync request no longer has to wait for explicit operator reclaim / repair / republish before its owner metadata is normalized.
- No `deployment_profile`, script entry, or operator surface changes were introduced in this loop.

## 2. What Changed
- `services/control-plane-api/src/lib.rs`
  - `dispatch_shared_channel_sync_requests(...)` now proactively reclaims stale pending claim metadata before system dispatch / backlog queue evaluation
  - the same normalization now runs before the `trigger_unconfigured` branch persists new failed requests
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_next_write_reclaims_stale_claim_metadata_when_trigger_is_unconfigured`
  - re-ran the full `social_external_collaboration_test` regression file

## 3. TDD Notes
- Red started with one pending request claimed by `u_operator_a`, then forced `leaseExpiresAt` into the past.
- The test performed a second ordinary ready-pair write while no trigger was configured.
- Failure proved the gap directly:
  - the second write succeeded
  - the original pending request stayed in backlog
  - stale owner metadata stayed serialized on that older pending item
- Green after the fix:
  - next-write dispatch first reclaims stale pending claim metadata
  - the older item stays pending but returns to the `unclaimed` pool
  - inventory reports `leaseStatus = unclaimed`

## 4. Fresh Evidence
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_write_reclaims_stale_claim_metadata_when_trigger_is_unconfigured -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test -- --nocapture` = `31 passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `87 passed`

## 5. Remaining Gap
- `S07` remains `not_closed / local_closure`.
- The next remaining gap is idle-time stale claim normalization: when no operator action and no subsequent writes arrive, stale claim metadata still persists until something touches the backlog.
- Release-ready exactly-once delivery semantics are still deferred.
