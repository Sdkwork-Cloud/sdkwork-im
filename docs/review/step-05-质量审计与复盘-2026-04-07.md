# Step 05 质量审计与复盘 - 2026-04-07

## 1. 基本信息

- Step 编号：`05`
- 波次：`Wave B`
- 执行日期：`2026-04-07`
- 关联 step 文档：
  - `docs/step/05-消息与会话主链路重构.md`
  - `docs/step/91-Step质量审计清单与复盘模板.md`
  - `docs/step/95-架构能力闭环验收标准.md`
  - `docs/step/97-Step完成后的架构回写与能力兑现清单.md`
- 关联架构文档：
  - `docs/架构/130-连接优先的AI时代即时通讯架构蓝图-2026-04-06.md`
  - `docs/架构/134-AI-Agent-IoT统一实时通信模型设计-2026-04-06.md`
  - `docs/架构/136-关键业务链路与跨Plane时序设计-2026-04-06.md`
  - `docs/架构/139-权限能力模型与协议演进设计-2026-04-06.md`
  - `docs/架构/147-CCP到Crate与接口模块落地映射设计-2026-04-06.md`

## 2. 本轮完成项

- 用 TDD 先写出 `im-domain-core` 的 conversation domain builder 测试
- 用结构测试锁住 `conversation-runtime` support 模块不得继续持有纯领域 builder
- 把以下纯领域 helper 下沉到 `im-domain-core::conversation`
  - `build_conversation_member(...)`
  - `build_conversation_member_with_attributes(...)`
  - `build_default_read_cursor(...)`
  - `member_id(...)`
  - `member_episode_id(...)`
- 让 `conversation-runtime` 改为消费领域 crate，而不是继续自带这套构造逻辑

## 3. 检查点结果

- `CP05-1`：未通过
  - 通过项
    - member/read-cursor 的纯领域构造规则已从 service support 移到 `im-domain-core`
    - 成员 episode 命名规则不再由 `conversation-runtime` 私有持有
  - 未通过项
    - membership/read-cursor/message 的核心状态聚合 owner 仍主要留在 `conversation-runtime`
    - `conversation` 与 `message` 的主状态机还没有完成 domain/app/interface/runtime 边界收口
- `CP05-2`：未通过
  - `tenantId` 未全面从 auth context 注入
  - `sender` 尚未完成结构化权威注入闭环
- `CP05-3`：未通过
  - direct / group / channel 主链路还没有基于新 owner 重新判定闭环
- `CP05-4`：未通过
  - projection / notification / multi-device sync 还没有按新领域 owner 重新固化

## 4. 测试结果

### 4.1 Red / Green 证据

- Red
  - `cargo test -p im-domain-core conversation_domain_builder_test --offline`
    - 失败原因符合预期：`im-domain-core::conversation` 缺少目标 helper 导出
  - `cargo test -p conversation-runtime test_conversation_runtime_support_module_no_longer_owns_member_and_cursor_domain_builders --offline`
    - 失败原因符合预期：`services/conversation-runtime/src/runtime/support.rs` 仍包含旧 builder
- Green
  - `cargo test -p im-domain-core --test conversation_domain_builder_test --offline`
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test --offline`
  - `cargo test -p im-domain-core --offline`
  - `cargo test -p conversation-runtime --offline`
  - `cargo test -p projection-service --offline`
  - `cargo test -p local-minimal-node --offline`
    - 默认 `target` 目录首跑被 Windows 锁文件阻塞
    - 改用独立 `CARGO_TARGET_DIR` 后通过

### 4.2 结构与格式

- `rustfmt --edition 2024 ...`
- `rustfmt --edition 2024 --check ...`

## 5. 残余风险

- 本轮只移动了纯领域 helper owner，没有完成 membership/message 聚合状态机收口
- `sender` 与 `tenantId` 权威边界仍未落到 `CP05-2`
- projection / notification / device sync 仍然消费旧的 service 主 owner，后续需要同步改造
- 因此当前还不能把局部 helper 下沉误判为 `Step 05` 闭环

## 6. 审计决议

- `91`：本轮证据完整，但 `Step 05` 整体未通过
- `95`：未通过
- `97`：本轮增量回写完整，但 `Step 05` 整体未通过
- `Step 05`：未闭环
- `Wave B / 93`：不触发

## 7. 2026-04-07 ConversationRoster 聚合审计更新

### 7.1 审计范围

- `crates/im-domain-core/src/conversation.rs`
- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
- `services/conversation-runtime/src/runtime.rs`
- `services/conversation-runtime/src/runtime/support.rs`
- `services/conversation-runtime/src/runtime/membership.rs`
- `services/conversation-runtime/src/runtime/recovery.rs`
- `services/conversation-runtime/src/runtime/policy.rs`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`

### 7.2 审计结果

- 通过项
  - `ConversationState` 不再直接持有 `members / principal_members / read_cursors`
  - `ConversationRoster` 已成为 membership/read-cursor 聚合 owner
  - support / membership / recovery / policy 路径都已切到新 roster owner
  - `im-domain-core`、`conversation-runtime`、`projection-service`、`local-minimal-node` 的 fresh 验证均通过
- 未通过项
  - `CP05-1` 仍未整体通过，因为 message core 与剩余 conversation aggregate 仍在 service runtime
  - `CP05-2 / CP05-3 / CP05-4` 仍未进入通过态

### 7.3 fresh evidence

- Red
  - `cargo test -p im-domain-core --test conversation_domain_builder_test --offline`
  - `cargo test -p conversation-runtime test_conversation_runtime_state_uses_domain_roster_for_member_and_cursor_state --offline`
- Green
  - `rustfmt --edition 2024 --check crates/im-domain-core/src/conversation.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/support.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/src/runtime/policy.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `cargo test -p im-domain-core --offline`
  - `cargo test -p conversation-runtime --offline`
  - `cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1b-local-node'; cargo test -p local-minimal-node --offline`

### 7.4 审计决议

- `CP05-1`：未通过，但较上一增量更接近闭环
- `91`：本轮证据完整，但 `Step 05` 整体未通过
- `95`：未通过
- `97`：本轮增量回写完整，但 `Step 05` 整体未通过
- `Wave B / 93`：不触发

## 8. 2026-04-07 ConversationMessageLog 聚合审计更新

### 8.1 审计范围

- `crates/im-domain-core/src/message.rs`
- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
- `services/conversation-runtime/src/runtime.rs`
- `services/conversation-runtime/src/runtime/recovery.rs`
- `services/conversation-runtime/src/runtime/membership.rs`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`

### 8.2 审计结果

- 通过项
  - `ConversationState` 不再直接持有 message high-watermark 与 stored message map
  - `ConversationMessageLog` 已成为 post/edit/recall/replay/read-cursor unread 的共享 message owner
  - `conversation-runtime`、`projection-service`、`local-minimal-node` 的 fresh 验证均通过，说明主链路没有被这次 owner shift 打坏
- 未通过项
  - `CP05-1` 仍未整体通过，因为 `message_index` 以及剩余 conversation aggregate 仍在 service runtime
  - `CP05-2 / CP05-3 / CP05-4` 仍未进入通过态

### 8.3 fresh evidence

- Red
  - `cargo test -p im-domain-core --test conversation_domain_builder_test test_conversation_message_log_owns_high_watermark_and_posted_messages --offline`
  - `cargo test -p conversation-runtime --test conversation_domain_structure_test test_conversation_runtime_state_uses_domain_message_log_for_message_state --offline`
- Green
  - `rustfmt --edition 2024 --check crates/im-domain-core/src/message.rs crates/im-domain-core/tests/conversation_domain_builder_test.rs services/conversation-runtime/src/runtime.rs services/conversation-runtime/src/runtime/recovery.rs services/conversation-runtime/src/runtime/membership.rs services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-im-domain-core'; cargo test -p im-domain-core --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-conversation-runtime'; cargo test -p conversation-runtime --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-projection-service'; cargo test -p projection-service --offline`
  - `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-1c-local-node'; cargo test -p local-minimal-node --offline`

### 8.4 审计决议

- `CP05-1`：未通过，但继续逼近 message core owner 收口
- `91`：本轮证据完整，但 `Step 05` 整体未通过
- `95`：未通过
- `97`：本轮增量回写完整，但 `Step 05` 整体未通过
- `Wave B / 93`：不触发
## 10. 2026-04-07 ConversationAggregateState 质量审计与复盘

### 10.1 审计结论

- 本轮改动符合 `Wave B / Step 05 / CP05-1` 的真实边界
- 代码、测试、验证、`docs/review`、`docs/架构` 都已同步推进
- 没有把 `CP05-1` 的阶段推进误报成 `Step 05` 完成

### 10.2 本轮新增质量护栏

- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - 用 domain 测试锁定 aggregate metadata owner 与 replay epoch floor
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 用结构测试锁定 runtime 不再直接持有：
    - `conversation_type`
    - `member_epoch`
    - `handoff_status_epoch`
    - `handoff_state`
  - 同时锁定 creation / membership / handoff / recovery 不再直接修改这些字段

### 10.3 本轮发现与处理

- 发现 1
  - 结构测试最初使用 `conversation_type: String,` 这种过宽字符串，误命中了 command DTO
  - 处理
    - 先保持生产代码不动
    - 收窄测试匹配为 `ConversationState` 字段级字符串
    - 再继续验证红绿
  - 结论
    - 红测最终失败原因回到目标缺口本身，TDD 证据有效
- 发现 2
  - `local-minimal-node` 全量测试会输出若干预期的启动失败日志样例
  - 处理
    - 继续以 fresh `CARGO_TARGET_DIR` 独立跑整包
    - 以进程退出码和 suite 汇总结果作为最终证据
  - 结论
    - 命令退出 `0`，suite 通过，无新增回归

### 10.4 残余风险

- `message_index` 仍在 runtime，`CP05-1` 尚未完全收口
- `sender / tenant` authority closure 尚未进入实现，`CP05-2` 仍未启动
- direct / group / channel 再收口以及 projection/notification/multi-device sync 收口仍未开始

### 10.5 复盘结论

- 本轮切片大小合适：一次只收口 conversation aggregate metadata owner，没有把 `message_index` 和 `sender / tenant` 混在同一轮
- 这样做的收益是：
  - handoff 生命周期、member epoch、recovery floor 现在共享一个 domain aggregate owner
  - 下一轮可以继续在 `CP05-1` 内独立判断是否还要处理 `message_index`
  - 或在确认 `CP05-1` 余项边界后再推进 `CP05-2`

## 11. 2026-04-07 CP05-2 读查询 authority owner 审计补充

### 11.1 审计结论

- 本轮改动符合 `Wave B / Step 05 / CP05-2` 的真实边界。
- runtime-owned read query surface 的 authority capture 已开始从 service entrypoint 收口到 runtime。
- 代码、测试、验证、`docs/review`、`docs/架构` 已同步推进。
- 本轮没有把局部收口误报成 `CP05-2` 或 `Step 05` 完成。

### 11.2 本轮新增质量护栏

- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 锁定 runtime 必须暴露:
    - `require_active_member_from_auth_context(...)`
    - `list_members_from_auth_context(...)`
    - `read_cursor_view_from_auth_context(...)`
    - `get_agent_handoff_state_from_auth_context(...)`
  - 锁定 `http` 读路径必须消费这些 wrapper
- `services/local-minimal-node/tests/lib_structure_test.rs`
  - 锁定 membership / handoff / access 必须消费这些 wrapper

### 11.3 本轮发现与处理

- 发现
  - `conversation-runtime/http` 与 `local-minimal-node` 的 runtime-owned read surface 仍在直接传 `tenant_id / actor_id`
- 处理
  - 先写结构红测
  - 再在 runtime 增加读查询 auth-context wrapper
  - 再切换 `http` 与 `local-minimal-node` 消费面
  - 最后跑 fresh verification

### 11.4 残余风险

- `services/local-minimal-node/src/node/projection.rs` 里的 projection-service 查询仍未收口。
- 读查询 authority owner 还没有覆盖到 projection/read model 全部 surface。
- 因此:
  - `CP05-2` 仍未闭环
  - `91 / 95 / 97` 仍不能判定 `Step 05` 通过

### 11.5 fresh evidence

- `cargo test -p conversation-runtime --test conversation_domain_structure_test --offline`
- `cargo test -p conversation-runtime --test authority_command_test --offline`
- `cargo test -p conversation-runtime --offline`
- `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2d-local-node-structure'; cargo test -p local-minimal-node --test lib_structure_test --offline`
- `$env:CARGO_TARGET_DIR='C:\\Users\\admin\\.codex\\memories\\target-step05-cp05-2d-local-node-full'; cargo test -p local-minimal-node --offline`
- `cargo test -p projection-service --offline`

## 12. 2026-04-07 CP05-2 projection/read-model authority owner 审计补充

### 12.1 审计结论

- 本轮把 projection/read-model authority owner 从 adapter / HTTP consumer 继续收口到 `projection-service`
- 审计通过项
  - projection-service 已提供 auth-context-backed read / device query entrypoint
  - `services/projection-service/src/http.rs` 不再保留旧的 device/member scope helper
  - `services/local-minimal-node/src/node/projection.rs` 与 `services/local-minimal-node/src/node/session.rs` 不再直接调用 raw projection read surface
- 审计未通过项
  - `services/local-minimal-node/src/node/effects.rs` 仍存在 runtime read raw call
  - Step 05 authority consumer 尚未整体收口，仍不足以证明 `CP05-2` 通过

### 12.2 风险与偏差判断

- 未发现偏离 `Step 05` 架构方向的实现
- 当前实现仍符合“authority owner 下沉到真正拥有业务边界的 service/runtime”这一方向
- 风险点在于下游 side-effect / realtime fanout 路径仍能绕过 auth-context seam 直接读取 member roster

### 12.3 复盘

- 本轮策略有效
  - 先让 `projection-service` 自己拥有 auth boundary，再让 HTTP/local node 统一消费
- 本轮仍不能宣称完成
  - `CP05-2` 未闭环
  - `Step 05` 未闭环
  - `91 / 95 / 97` 未通过
  - `Wave B / 93` 未就绪
- 下一焦点
  - 继续清理 `services/local-minimal-node/src/node/effects.rs`
  - 把 runtime-owned member read 再收口到 runtime auth-context entrypoint

## 13. 2026-04-07 CP05-2 downstream authority consumer seam 审计补充

### 13.1 审计结论

- downstream authority consumer seam 已继续向 owner boundary 收口：
  - `effects.rs` 不再保留 raw runtime member read
  - `access.rs` 不再保留 raw `actor_kind` threading
- 本轮存在一次真实回归，但已被 fresh verification 抓出并修复：
  - actor_kind mismatch 安全语义曾被放宽
  - 修复后相关 e2e 已恢复为 `403`

### 13.2 审计裁定

- `CP05-2`：通过
- `CP05-3`：未通过
- `CP05-4`：未通过
- `Step 05`：未闭环

### 13.3 下一焦点

- 严格按顺序进入 `CP05-3`
- 保持 `CP05-4` 与 `Wave B / 93` 阻塞，直到 `Step 05` 整体闭环

## 14. 2026-04-07 CP05-3 direct/group/channel mainline scenario owner 审计补充

### 14.1 审计结论

- 本轮改动符合 `Wave B / Step 05 / CP05-3` 的真实边界
- direct / group / channel 主链路场景 owner 已下沉到 `im-domain-core`
- `conversation-runtime` policy 已改为消费 aggregate `scenario()` 而非 raw `conversation_type` 分支
- `CP05-3` 可判定通过
- `Step 05` 仍未闭环，原因是 `CP05-4` 未完成

### 14.2 本轮新增质量护栏

- `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - 锁定 aggregate 必须暴露 `ConversationScenario` 和 `scenario()`
- `services/conversation-runtime/tests/conversation_domain_structure_test.rs`
  - 锁定 runtime policy 必须消费 domain scenario owner
  - 锁定 direct / group / channel 主链路不得退回 raw `conversation_type` 字符串分支
- 现有 `conversation_flow_test.rs` 与 `access_control_e2e_test.rs`
  - 继续对 direct / group / system channel 行为做主链路回归覆盖

### 14.3 本轮发现与处理

- 发现
  - runtime policy 中 direct / group / channel 主链路能力仍依赖 raw `conversation_type`
- 处理
  - 先用结构红测锁定 domain owner 缺口
  - 再将 scenario owner 下沉到 `im-domain-core`
  - 再让 policy 统一消费 aggregate `scenario()`
  - 最后用 flow test 和 local profile e2e 复验主链路行为

### 14.4 残余风险

- `CP05-3` 范围内未发现新的架构偏离
- 当前剩余风险已转移到：
  - `CP05-4` projection / notification / multi-device sync owner 收口
  - `Step 05` 整体 `91 / 95 / 97` 闭环

### 14.5 fresh evidence

- `cargo test -p im-domain-core --test conversation_domain_builder_test --offline`
- `cargo test -p conversation-runtime --test conversation_domain_structure_test --offline`
- `cargo test -p conversation-runtime --test conversation_flow_test test_direct_conversation_owner_can_add_only_single_non_elevated_peer --offline`
- `cargo test -p conversation-runtime --test conversation_flow_test test_group_owner_can_transfer_ownership_and_then_leave --offline`
- `cargo test -p conversation-runtime --test conversation_flow_test test_system_channel_requires_dedicated_publish_command_and_allows_only_publisher --offline`
- `$env:CARGO_TARGET_DIR='target-cp053'; cargo test -p local-minimal-node --test access_control_e2e_test test_direct_conversation_member_management_is_restricted --offline`
- `$env:CARGO_TARGET_DIR='target-cp053'; cargo test -p local-minimal-node --test access_control_e2e_test test_group_owner_transfer_allows_safe_handoff_and_leave --offline`
- `$env:CARGO_TARGET_DIR='target-cp053'; cargo test -p local-minimal-node --test access_control_e2e_test test_system_channel_dedicated_publish_allows_only_publisher_in_local_profile --offline`

## 15. 2026-04-07 CP05-4 notification public access owner 审计补充

### 15.1 审计结论

- `notification-service` 与 `local-minimal-node` 之间重复的 notification public-access 判断已开始收口
- `NotificationRuntime` 现在拥有 public request access owner seam
- cross-recipient `403` 与 self-request `200` 行为未回归
- 本轮推进了 `CP05-4`，但不构成 `CP05-4` 整体闭环

### 15.2 本轮新增质量护栏

- `services/notification-service/tests/lib_structure_test.rs`
  - 锁定 runtime 必须暴露 `request_notification_from_public_api(...)`
  - 锁定 notification-service HTTP 必须消费该 owner seam
- `services/local-minimal-node/tests/lib_structure_test.rs`
  - 锁定 local-minimal-node platform 必须消费 `notification_runtime.request_notification_from_public_api(...)`
  - 锁定 local node 不得继续保留本地 notification public-access enforcement

### 15.3 本轮发现与处理

- 发现
  - `notification-service` HTTP 与 `local-minimal-node` platform 都在单独做 cross-recipient `notification.write` 判断
- 处理
  - 先写结构红测
  - 再把 public request access seam 下沉到 `NotificationRuntime`
  - 再切换两个 service edge 共同消费
  - 最后用 notification-service public auth 与 local node e2e 回归行为

### 15.4 残余风险

- `CP05-4` 仍有其他 owner seam 待收口：
  - projection / device fanout
  - notification side-effect 编排
  - multi-device sync 最终衔接
- 因此 `Step 05` 仍不能判定通过

### 15.5 fresh evidence

- `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_public_request_access_owner --offline`
- `$env:CARGO_TARGET_DIR='target-cp054-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_notification_request_path_uses_notification_runtime_public_access_owner --offline`
- `cargo test -p notification-service --test public_auth_test test_public_app_rejects_cross_recipient_notification_request_without_permission --offline`
- `cargo test -p notification-service --test public_auth_test test_public_app_accepts_self_notification_request --offline`
- `$env:CARGO_TARGET_DIR='target-cp054-green-local'; cargo test -p local-minimal-node --test public_auth_e2e_test test_public_app_rejects_cross_recipient_notification_request_without_permission --offline`
- `$env:CARGO_TARGET_DIR='target-cp054-green-local'; cargo test -p local-minimal-node --test public_auth_e2e_test test_public_app_accepts_self_notification_request --offline`
- `cargo fmt --all --check`

## 17. 2026-04-07 CP05-4 message notification fanout owner 审计补充

### 17.1 审计结论

- `notification-service` 已开始拥有 message notification fanout 的统一 owner seam
- `local-minimal-node` effects 路径不再本地维护 sender filter / recipient loop / notification id 规则
- 现有“只 fanout 给其他 active member”的 e2e 行为未回归
- 本轮推进了 `CP05-4`，但不构成 `CP05-4` 整体闭环

### 17.2 本轮新增质量护栏

- `services/notification-service/tests/lib_structure_test.rs`
  - 锁定 `notification-service` 必须暴露 `request_notification_fanout(...)`
- `services/notification-service/tests/notification_pipeline_test.rs`
  - 锁定 runtime fanout 行为必须跳过 actor 自己，并给其他 recipient 建 task
- `services/local-minimal-node/tests/lib_structure_test.rs`
  - 锁定 effects 路径必须消费 notification-service owner seam
  - 锁定 effects 路径不得继续直接 per-recipient 调 `request_notification(...)`

### 17.3 本轮发现与处理

- 发现
  - message notification fanout side-effect 仍在 `local-minimal-node` 自己编排
- 处理
  - 先写 fanout owner seam 与 consumer seam 的 fail 测试
  - 再把 self-filter / recipient loop / notification id 规则下沉到 `notification-service`
  - 最后用 message notification fanout e2e 回归主链路行为

### 17.4 残余风险

- `CP05-4` 仍有其他 owner seam 待收口：
  - projection / sync 与 notification side-effect 的剩余衔接点
  - multi-device sync 最终收口
- 因此 `Step 05` 仍不能判定通过

### 17.5 fresh evidence

- `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_notification_fanout_owner_seam --offline`
- `cargo test -p notification-service --test notification_pipeline_test test_request_notification_fanout_skips_actor_and_creates_notifications_for_other_recipients --offline`
- `cargo test -p notification-service --test notification_pipeline_test test_duplicate_request_notification_is_idempotent_when_payload_matches --offline`
- `$env:CARGO_TARGET_DIR='target-cp054c-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_notification_service_fanout_owner_seam --offline`
- `$env:CARGO_TARGET_DIR='target-cp054c-green-local'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only --offline`
- `cargo fmt --all`
- `cargo fmt --all --check`

## 18. 2026-04-07 CP05-4 automation result notification owner 审计补充

### 18.1 审计结论

- `notification-service` 已开始拥有 automation result notification 的统一 owner seam
- `local-minimal-node` platform 路径不再本地拼装 automation result notification id / source event / recipient
- 现有 automation notification 可见性与幂等行为未回归
- 本轮推进了 `CP05-4`，但不构成 `CP05-4` 整体闭环

### 18.2 本轮新增质量护栏

- `services/notification-service/tests/lib_structure_test.rs`
  - 锁定 `notification-service` 必须暴露 `request_automation_result_notification(...)`
- `services/notification-service/tests/notification_pipeline_test.rs`
  - 锁定 automation result notification seam 必须把通知投递给当前 actor，并保持幂等
- `services/local-minimal-node/tests/lib_structure_test.rs`
  - 锁定 platform 路径必须消费 notification owner seam
  - 锁定 platform 路径不得继续直接拼装 `request_notification(...)`

### 18.3 本轮发现与处理

- 发现
  - automation 完成后的 `automation.result` 通知仍在 `local-minimal-node` platform 里自行拼装
- 处理
  - 先写 owner seam 与 consumer seam 的 fail 测试
  - 再把 automation result notification 组装规则下沉到 `notification-service`
  - 最后用 task10 capability 与 duplicate automation e2e 回归可见行为

### 18.4 残余风险

- `CP05-4` 仍有其他 owner seam 待收口：
  - projection / sync 与 notification 的剩余衔接点
  - multi-device sync 最终收口
- 因此 `Step 05` 仍不能判定通过

### 18.5 fresh evidence

- `cargo test -p notification-service --test lib_structure_test test_notification_runtime_exposes_automation_result_notification_owner_seam --offline`
- `cargo test -p notification-service --test notification_pipeline_test test_request_automation_result_notification_targets_requesting_actor_idempotently --offline`
- `$env:CARGO_TARGET_DIR='target-cp054d-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_automation_path_uses_notification_runtime_automation_result_owner --offline`
- `$env:CARGO_TARGET_DIR='target-cp054d-green-local'; cargo test -p local-minimal-node --test task10_capabilities_e2e_test test_local_minimal_profile_exposes_notification_automation_audit_and_ops_capabilities --offline`
- `$env:CARGO_TARGET_DIR='target-cp054d-green-local'; cargo test -p local-minimal-node --test task10_capabilities_e2e_test test_local_minimal_profile_treats_duplicate_automation_request_as_idempotent --offline`
- `cargo fmt --all`
- `cargo fmt --all --check`

## 16. 2026-04-07 CP05-4 projection realtime fanout target owner 审计补充

### 16.1 审计结论

- `projection-service` 已开始拥有 principal -> device realtime fanout target 的统一 owner seam
- `local-minimal-node` realtime side-effect 路径不再本地重建 fanout target
- cross-node realtime routing 行为未回归
- 本轮推进了 `CP05-4`，但不构成 `CP05-4` 整体闭环

### 16.2 本轮新增质量护栏

- `services/projection-service/tests/lib_structure_test.rs`
  - 锁定 `projection-service` 必须暴露 `realtime_fanout_targets_for_principals(...)`
- `services/projection-service/tests/timeline_projection_test.rs`
  - 锁定 principal -> device pair 输出行为与排序
- `services/local-minimal-node/tests/lib_structure_test.rs`
  - 锁定 effects 路径必须消费 projection-owned fanout target seam
  - 锁定 effects 路径不得继续直接从 raw `registered_devices(...)` 组装目标

### 16.3 本轮发现与处理

- 发现
  - realtime publish 路径仍在 `local-minimal-node` 自己解析 principal -> device fanout target
- 处理
  - 先写 projection owner seam 与 consumer seam 的 fail 测试
  - 再把 target 解析下沉到 `projection-service`
  - 最后用 cluster realtime routing e2e 回归跨节点行为

### 16.4 残余风险

- `CP05-4` 仍有其他 owner seam 待收口：
  - notification side-effect 与 projection / sync 的剩余衔接点
  - multi-device sync 最终收口
- 因此 `Step 05` 仍不能判定通过

### 16.5 fresh evidence

- `cargo test -p projection-service --test lib_structure_test test_projection_service_exposes_realtime_fanout_target_owner_seam --offline`
- `cargo test -p projection-service --test timeline_projection_test test_realtime_fanout_targets_for_principals_return_registered_principal_device_pairs --offline`
- `cargo test -p projection-service --test timeline_projection_test test_registered_devices_and_latest_sync_seq_are_queryable --offline`
- `$env:CARGO_TARGET_DIR='target-cp054b-green-local'; cargo test -p local-minimal-node --test lib_structure_test test_local_minimal_node_effects_use_projection_owned_realtime_fanout_target_seam --offline`
- `$env:CARGO_TARGET_DIR='target-cp054b-green-local'; cargo test -p local-minimal-node --test cluster_realtime_routing_e2e_test test_local_minimal_profile_routes_realtime_events_to_remote_owner_node --offline`
- `cargo fmt --all`
- `cargo fmt --all --check`

## 19. 2026-04-07 CP05-4 projection device-sync target owner 质量审计

### 19.1 审计结论

- 本轮增量通过。
- `projection-service` conversation 级 device-sync target 解析已收归单一 owner seam，减少多处 handler 分别维护 active principal / registered device loop 的 drift 风险。

### 19.2 证据

- `test_projection_service_exposes_conversation_device_sync_target_owner_seam`
- `test_device_sync_fanout_targets_for_conversation_include_active_members_and_fallback_devices`
- `$env:CARGO_TARGET_DIR='target-cp054e-full'; cargo test -p projection-service --offline`
- `rustfmt --edition 2024 --check services/projection-service/src/lib.rs services/projection-service/tests/lib_structure_test.rs services/projection-service/tests/timeline_projection_test.rs`

### 19.3 剩余问题

- 该增量只解决 `CP05-4` 中一个真实 projection seam，不能据此结束 `CP05-4`。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 20. 2026-04-07 CP05-4 message-posted notification request owner 质量审计

### 20.1 审计结论

- 本轮增量通过。
- `message.posted` notification 默认字段已收回 `notification-service` 单一 owner seam。

### 20.2 证据

- `test_notification_runtime_exposes_message_posted_notification_owner_seam`
- `test_request_message_posted_notifications_own_message_notification_defaults`
- `test_local_minimal_node_effects_use_notification_service_message_posted_owner_seam`
- `$env:CARGO_TARGET_DIR='target-cp054f-reg-notification'; cargo test -p notification-service --offline`
- `$env:CARGO_TARGET_DIR='target-cp054f-reg-local'; cargo test -p local-minimal-node --test http_e2e_test test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only --offline`

### 20.3 剩余问题

- 该增量只解决 `CP05-4` 中一个 notification 连接点，不构成整体闭环。
- `projection / sync` 剩余连接点与 multi-device sync final closure 仍待推进。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 21. 2026-04-07 CP05-4 projection device-sync entry owner 质量审计

### 21.1 审计结论

- 本轮增量通过。
- `projection-service` 已把 device-sync feed entry 的公共字段装配收口到单一 owner module。

### 21.2 证据

- `test_projection_service_device_sync_entry_assembly_moves_out_of_lib_impl`
- `test_device_sync_feed_projects_registered_devices_for_message_and_read_cursor_events`
- `test_member_governance_events_project_typed_sync_feed_deltas`
- `test_agent_handoff_status_change_projects_device_sync_entries_for_active_members`
- `$env:CARGO_TARGET_DIR='target-cp054g-reg-full'; cargo test -p projection-service --offline`
- `rustfmt --edition 2024 --check services/projection-service/src/lib.rs services/projection-service/src/device_sync.rs services/projection-service/tests/lib_structure_test.rs`

### 21.3 剩余问题

- 该增量只解决 `CP05-4` 中一个真实 projection / sync seam，不能据此结束 `CP05-4`。
- `projection / sync` 与 notification 之间的剩余连接点、multi-device sync final closure 仍待继续推进。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 22. 2026-04-07 CP05-4 projection device-sync session state owner 质量审计

### 22.1 审计结论

- 本轮增量通过。
- `projection-service` 已把 session 级 `registered_devices + latest_sync_seq` 装配收口到单一 access seam。

### 22.2 证据

- `test_projection_service_access_module_exposes_auth_context_entrypoints`
- `test_local_minimal_node_session_projection_paths_use_projection_service_auth_context_entrypoints`
- `test_local_minimal_profile_resumes_session_and_returns_presence_snapshot`
- `test_local_minimal_profile_exposes_device_sync_feed_for_multi_device_resume`
- `test_local_minimal_profile_requires_fresh_resume_after_disconnect`
- `$env:CARGO_TARGET_DIR='target-cp054h-reg-projection-full'; cargo test -p projection-service --offline`
- `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/src/lib.rs services/projection-service/tests/lib_structure_test.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/tests/lib_structure_test.rs`

### 22.3 剩余问题

- 该增量只解决 `CP05-4` 中一个真实 projection / sync seam，不能据此结束 `CP05-4`。
- `projection / sync` 与 notification 之间的剩余连接点、multi-device sync final closure 仍待继续推进。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 23. 2026-04-07 CP05-4 projection realtime fanout auth-context owner 质量审计

### 23.1 审计结论

- 本轮增量通过。
- `projection-service::access` 已开始统一拥有 realtime fanout target 的 auth-context capture。
- `local-minimal-node/effects.rs` 不再在公共 realtime publish helper 里手工透传 `tenant_id` 调用 raw projection fanout target seam。

### 23.2 证据

- 结构证据
  - `test_projection_service_access_module_exposes_auth_context_entrypoints`
  - `test_local_minimal_node_effects_use_projection_owned_realtime_fanout_target_seam`
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-local-structure-full'; cargo test -p local-minimal-node --test lib_structure_test --offline`
- 行为证据
  - `test_local_minimal_profile_fanouts_realtime_message_events_to_other_conversation_member`
  - `test_local_minimal_profile_fanouts_member_governance_realtime_events_to_registered_owner_device`
  - `test_local_minimal_profile_fanouts_agent_handoff_lifecycle_realtime_events_to_other_device`
- 回归证据
  - `$env:CARGO_TARGET_DIR='target-cp054i-reg-projection-full'; cargo test -p projection-service --offline`
  - `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/tests/lib_structure_test.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/tests/lib_structure_test.rs`

### 23.3 剩余问题

- 该增量只解决 `CP05-4` 中一个 projection realtime seam，不能据此结束 `CP05-4`。
- `projection / sync` 与 notification 的剩余连接点、multi-device sync final closure 仍待继续推进。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 24. 2026-04-07 CP05-4 projection active-principal auth-context owner 质量审计

### 24.1 审计结论

- 本轮增量通过。
- `projection-service::access` 已开始统一拥有 conversation active principal mapping 的 auth-context capture。
- `local-minimal-node/effects.rs` 不再在 notification / realtime recipient 解析时直接读取 runtime member roster。

### 24.2 证据

- 结构证据
  - `test_projection_service_access_module_exposes_auth_context_entrypoints`
  - `test_local_minimal_node_effects_member_fanout_uses_projection_auth_context_entrypoints`
  - `$env:CARGO_TARGET_DIR='target-cp054j-green-local-structure'; cargo test -p local-minimal-node --test lib_structure_test --offline`
- 行为证据
  - `test_active_conversation_principal_ids_from_auth_context_returns_current_active_members`
  - `test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only`
  - `test_local_minimal_profile_fanouts_realtime_message_events_to_other_conversation_member`
  - `test_local_minimal_profile_fanouts_member_governance_realtime_events_to_registered_owner_device`
  - `test_local_minimal_profile_fanouts_agent_handoff_lifecycle_realtime_events_to_other_device`
- 回归证据
  - `$env:CARGO_TARGET_DIR='target-cp054j-reg-projection-full'; cargo test -p projection-service --offline`
  - `rustfmt --edition 2024 --check services/projection-service/src/access.rs services/projection-service/src/lib.rs services/projection-service/tests/lib_structure_test.rs services/projection-service/tests/timeline_projection_test.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/tests/lib_structure_test.rs`

### 24.3 剩余问题

- 该增量只解决 `CP05-4` 中一个 projection / notification 连接点，不能据此结束 `CP05-4`。
- `projection / sync` 与 notification 的剩余连接点、multi-device sync final closure 仍待继续推进。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。
## 25. 2026-04-07 CP05-4 message-posted projection recipient owner 质量审计

### 25.1 审计结论

- 本轮增量通过。
- `notification-service` 已开始统一拥有 message-posted recipient authority。
- `local-minimal-node` message side-effect 不再线程化 `recipient_ids`。

### 25.2 证据

- 结构证据
  - `test_notification_runtime_exposes_message_posted_notification_owner_seam`
  - `test_local_minimal_node_effects_do_not_thread_message_posted_recipient_ids`
  - `cargo test -p local-minimal-node --test lib_structure_test --offline --target-dir target-cp054k-reg-local-structure-full`
- 行为证据
  - `test_request_message_posted_notifications_resolves_current_active_recipients_from_projection_auth_context`
  - `test_local_minimal_profile_fanouts_message_notifications_to_other_active_members_only`
- 回归证据
  - `cargo test -p notification-service --offline --target-dir target-cp054k-reg-notification-full`
  - `rustfmt --edition 2024 --check services/notification-service/src/lib.rs services/notification-service/tests/lib_structure_test.rs services/notification-service/tests/notification_pipeline_test.rs services/local-minimal-node/src/node/build.rs services/local-minimal-node/src/node/effects.rs services/local-minimal-node/tests/lib_structure_test.rs`

### 25.3 剩余问题

- 该增量只解决 `CP05-4` 中一条 projection -> notification 剩余连接点，不能据此结束 `CP05-4`。
- 从当前代码搜索看，剩余主 seam 已收敛到 multi-device sync final closure。
- 可见的下一处高概率 blocker 是 `services/session-gateway/src/lib.rs` 的 `resume / get_presence_me / heartbeat / disconnect` 路径仍在本地读取 `registered_devices(...)` 与 `latest_device_sync_seq(...)`。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 26. 2026-04-07 CP05-4 session-gateway session sync-state owner 质量审计

### 26.1 审计结论

- 本轮增量通过。
- `session-gateway` 的四条 session/presence handler 已不再各自直接拼装 session sync state。
- session sync-state 开始通过单一 owner seam 进入 `SessionPresenceRuntime`。

### 26.2 证据

- 结构证据
  - `test_session_gateway_session_surface_moves_out_of_lib_impl`
  - `test_session_gateway_session_paths_use_device_sync_session_state_owner_seam`
- 行为证据
  - `test_session_resume_returns_presence_snapshot_for_current_device`
  - `test_presence_heartbeat_and_disconnect_drive_device_offline_transition`
  - `test_session_gateway_requires_fresh_resume_after_disconnect`
  - `test_session_gateway_treats_duplicate_disconnect_as_idempotent_for_same_session`
  - `test_session_gateway_rebuild_preserves_reconnect_required_fence_until_fresh_resume`
- 回归证据
  - `cargo test -p session-gateway --offline --target-dir target-cp054l-reg-session-gateway`
  - `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/session.rs services/session-gateway/tests/lib_structure_test.rs`

### 26.3 剩余问题

- 该增量只解决 `CP05-4` 中一个 session-gateway multi-device sync seam，不能据此结束 `CP05-4`。
- 当前 repo 级搜索表明，底层 raw `registered_devices / latest_device_sync_seq` owner 仍在 `AppState`，说明 multi-device sync final closure 还未完全达成。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未闭环。

## 27. 2026-04-07 CP05-4 session-gateway sync-state store owner 质量复盘

### 27.1 复盘结论

- 本轮增量在 `session-gateway` 范围内通过结构测试、格式检查和包级回归。
- 这次通过的是增量级质量门，不是 `Step 05` 总体质量门。

### 27.2 主要证据

- `test_session_gateway_sync_state_owner_moves_out_of_lib_impl`
- `test_session_gateway_session_paths_use_device_sync_session_state_owner_seam`
- `test_session_gateway_session_surface_moves_out_of_lib_impl`
- `cargo test -p session-gateway --offline --target-dir target-cp054m-reg-session-gateway`
- `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/session.rs services/session-gateway/src/session_state.rs services/session-gateway/tests/lib_structure_test.rs`

### 27.3 风险变化

- 已消除
  - `AppState` 继续兼任 sync-state storage owner 的混合职责
  - handler 收口后底层 raw storage 仍留在 `lib.rs` 的半收口状态
- 仍保留
  - `CP05-4` 的 repo 级收口尚未整体完成
  - `Step 05 / 91 / 95 / 97 / Wave B / 93` 仍未通过

## 28. 2026-04-07 CP05-4 session-gateway device registration owner 质量审计

### 28.1 审计范围

- `services/session-gateway/src/lib.rs`
- `services/session-gateway/src/device_registration.rs`
- `services/session-gateway/tests/lib_structure_test.rs`

### 28.2 审计结论

- 本轮增量在 `session-gateway` 范围内通过结构约束、格式检查和包级回归。
- 未发现新的阻断级行为回退。
- 这次通过的是增量级质量门，不是 `Step 05` 总体质量门。

### 28.3 已验证证据

- `test_session_gateway_device_registration_owner_moves_out_of_lib_impl`
- `test_session_gateway_sync_state_owner_moves_out_of_lib_impl`
- `test_session_gateway_session_paths_use_device_sync_session_state_owner_seam`
- `cargo test -p session-gateway --offline --target-dir target-cp054n-reg-session-gateway`
- `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/device_registration.rs services/session-gateway/tests/lib_structure_test.rs`

### 28.4 审计裁定

- 本轮增量：通过。
- `CP05-4`：未闭环。
- `Step 05`：未闭环。
- `91 / 95 / 97 / Wave B / 93`：仍未整体通过。

## 29. 2026-04-07 CP05-4 local-minimal-node device registration owner 质量审计

### 29.1 审计范围

- `services/local-minimal-node/src/node.rs`
- `services/local-minimal-node/src/node/build.rs`
- `services/local-minimal-node/src/node/access.rs`
- `services/local-minimal-node/src/node/session.rs`
- `services/local-minimal-node/src/node/device_registration.rs`
- `services/local-minimal-node/tests/lib_structure_test.rs`

### 29.2 审计结论

- 本轮增量在 `local-minimal-node` 范围内通过结构验证、包级回归和格式检查。
- 未发现新的阻断级行为回退。
- 这次通过的是增量级质量门，不是 `Step 05` 总体质量门。

### 29.3 已验证证据

- `test_local_minimal_node_device_registration_owner_moves_out_of_access_impl`
- `cargo test -p local-minimal-node --test lib_structure_test --offline --target-dir target-cp054o-green-local-structure-full`
- `cargo test -p local-minimal-node --offline --target-dir target-cp054o-reg-local-minimal-node`
- `rustfmt --edition 2024 --check services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/build.rs services/local-minimal-node/src/node/access.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/src/node/device_registration.rs`

### 29.4 审计裁定

- 本轮增量：通过。
- `CP05-4`：未闭环。
- `Step 05`：未闭环。
- `91 / 95 / 97 / Wave B / 93`：仍未整体通过。

## 30. 2026-04-07 CP05-4 session-gateway route preflight owner 质量审计

### 30.1 审计对象

- `services/session-gateway/src/device_registration.rs`
- `services/session-gateway/src/lib.rs`
- `services/session-gateway/src/session.rs`
- `services/session-gateway/src/websocket_route.rs`
- `services/session-gateway/tests/lib_structure_test.rs`

### 30.2 审计结论

- 红绿顺序成立，测试先失败后成功。
- owner seam 沿着既有 `SessionDeviceRegistration` 继续收口，没有偏离现有架构方向。
- 结构测试、全量 `session-gateway` 回归、格式校验全部通过。

### 30.3 风险与复盘

- `resume_session(...)` 继续直接走 `register_device(...)` 是有意保留，因为它不是重复 route preflight glue 的一部分，而是 session takeover 语义入口。
- 这轮消掉了上一轮 review 指向的高概率 blocker，但不等于 `CP05-4` repo 级 closure 已完成。

### 30.4 审计证据

- `cargo test -p session-gateway --test lib_structure_test test_session_gateway_route_preflight_owner_moves_out_of_entrypoints --offline --target-dir target-cp054p-red-route-preflight`
- `cargo test -p session-gateway --test lib_structure_test test_session_gateway_route_preflight_owner_moves_out_of_entrypoints --offline --target-dir target-cp054p-green-route-preflight`
- `cargo test -p session-gateway --offline --target-dir target-cp054p-reg-session-gateway`
- `rustfmt --edition 2024 --check services/session-gateway/src/lib.rs services/session-gateway/src/device_registration.rs services/session-gateway/src/session.rs services/session-gateway/src/websocket_route.rs services/session-gateway/tests/lib_structure_test.rs`

### 30.5 执行补充结论

- 认定本轮为 `CP05-4` 的有效质量增量
- 不认定 `CP05-4`、`Step 05`、`91 / 95 / 97`、`Wave B / 93` 已整体通过
## 31. 2026-04-07 CP05-4 local-minimal-node route preflight owner 质量审计

### 31.1 审计结论

- 红绿顺序成立，新增结构测试先失败后通过。
- `local-minimal-node` 的普通 route preflight 已通过 `device_registration` owner seam 收口，session 入口层只保留 delegate。
- `resume_session(...)` 的 takeover 语义保持独立，未被普通 preflight seam 稀释。

### 31.2 审计证据

- 结构证据
  - `test_local_minimal_node_route_preflight_owner_moves_out_of_session_entrypoints`
  - `test_local_minimal_node_device_registration_owner_moves_out_of_access_impl`
- 回归证据
  - `cargo test -p local-minimal-node --offline --target-dir target-cp054q-reg-local-minimal-node`
- 格式证据
  - `rustfmt --edition 2024 --check services/local-minimal-node/src/node.rs services/local-minimal-node/src/node/device_registration.rs services/local-minimal-node/src/node/session.rs services/local-minimal-node/tests/lib_structure_test.rs`

### 31.3 风险与复盘

- 本轮通过的是 `local-minimal-node` 范围内的增量质量门，不是 `Step 05` 总体质量门。
- 当前没有证据表明本轮改动引入行为回退，但 `CP05-4` 仍存在 repo 级剩余 owner seam，不能据此判定整体闭环。
- `Step 05 / 91 / 95 / 97 / Wave B / 93` 继续阻塞。
## 32. 2026-04-07 CP05-4 session-gateway disconnect lifecycle owner 质量审计

### 32.1 审计结论

- `session-gateway` 的 disconnect 生命周期 raw glue 已从 session entrypoint 下沉到 owner seam。
- disconnect 语义保持不变：
  - fence 命中时保持幂等断开分支
  - fence 未命中时继续执行 route preflight、订阅清理、route release、disconnect 标记与 disconnect signal
- `AppState` 没有留下新增死字段，主状态继续收缩成 delegate surface。

### 32.2 证据

- 结构红绿测试
  - `test_session_gateway_disconnect_lifecycle_owner_moves_out_of_session_entrypoints`
  - `target-cp054r-red-disconnect-owner`
  - `target-cp054r-green-disconnect-owner`
- 结构回归
  - `target-cp054r-structure`
- 包级回归
  - `target-cp054r-reg-session-gateway`

### 32.3 剩余问题

- `local-minimal-node` 仍保留同类 raw disconnect lifecycle glue，说明 `CP05-4` 还没完成 repo 级收口。
- `Step 05` 仍未满足 `91 / 95 / 97` 的整体通过条件。
- `Wave B / 93` 仍然阻塞。
## 33. 2026-04-07 CP05-4 local-minimal-node disconnect lifecycle owner 质量审计

### 33.1 审计结论

- `local-minimal-node` 的 disconnect 生命周期已与 `session-gateway` 对齐，下沉到 `device_registration` owner seam。
- 两个 session entry 模块的 raw disconnect lifecycle glue 已全部清空。
- `CP05-4` 的已知 owner seam blocker 已完成收口。

### 33.2 证据

- 结构红绿测试
  - `test_local_minimal_node_disconnect_lifecycle_owner_moves_out_of_session_entrypoints`
  - `target-cp054s-red-local-disconnect`
  - `target-cp054s-green-local-disconnect`
- 结构回归
  - `target-cp054s-structure`
- 包级回归
  - `target-cp054s-reg-local-minimal-node`
- 代码扫描
  - `rg -n "disconnect_fence_matches_session\\(|clear_device_subscriptions\\(|release_device_route\\(|mark_device_disconnected\\(|platform::refresh_node_operational_view\\(&state\\)" services/session-gateway/src/session.rs services/local-minimal-node/src/node/session.rs -g "*.rs"`

### 33.3 剩余问题

- `Step 05` 还缺最新仓库状态下的 step-wide `91 / 95 / 97` 闭环审计。
- `Wave B / 93` 仍然阻塞。

## 34. 2026-04-07 Step 05 step-wide 91 / 95 / 97 质量审计

### 34.1 审计结论

- `Step 05` 在最新仓库状态下已完成 step-wide `91 / 95 / 97` 闭环审计。
- 本轮未发现新的 `message / conversation` 主链路回归，也未发现新的 owner seam 回退。
- `CP05-4` 已知的 disconnect lifecycle owner blocker 仍保持收口状态。
- `Wave B / 93` 仍未进入执行条件，原因是 `Step 06` 尚未闭环，而不是 `Step 05` 存在未解问题。

### 34.2 证据

- 包级回归
  - `cargo test -p im-auth-context --offline --target-dir target-step05-audit-im-auth-context`
  - `cargo test -p im-domain-core --offline --target-dir target-step05-audit-im-domain-core`
  - `cargo test -p im-platform-contracts --offline --target-dir target-step05-audit-im-platform-contracts`
  - `cargo test -p conversation-runtime --offline --target-dir target-step05-audit-conversation-runtime`
  - `cargo test -p projection-service --offline --target-dir target-step05-audit-projection-service`
  - `cargo test -p notification-service --offline --target-dir target-step05-audit-notification-service`
  - `cargo test -p session-gateway --offline --target-dir target-step05-audit-session-gateway`
  - `cargo test -p local-minimal-node --offline --target-dir target-step05-audit-local-minimal-node`
- 格式校验
  - `cargo fmt -p im-auth-context -p im-domain-core -p im-platform-contracts -p conversation-runtime -p projection-service -p notification-service -p session-gateway -p local-minimal-node -- --check`
- Raw glue 扫描
  - `rg -n "disconnect_fence_matches_session\\(|clear_device_subscriptions\\(|release_device_route\\(|mark_device_disconnected\\(|platform::refresh_node_operational_view\\(&state\\)" services/session-gateway/src/session.rs services/local-minimal-node/src/node/session.rs -g "*.rs"`
  - 结果：无命中

### 34.3 复盘

- `Step 05` 耗时较长的根因不是单点代码实现，而是需要沿 `91 / 95 / 97` 把 step 级证据、质量审计和架构回写全部补齐后才能算真正闭环。
- 为了提升后续迭代速度，应继续保持“先集中实现 owner seam，再做 step-wide 一次性回归”的节奏，但不能省略最终整步验证与文档收口。
