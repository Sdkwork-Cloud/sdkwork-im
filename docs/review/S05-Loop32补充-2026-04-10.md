# S05 Loop32补充 - 2026-04-10

## 1. 本轮主任务
- 回写 `152CJ` 主架构真相文档，消除 Loop31 durable social truth 已落地但主文档未同步的漂移。

## 2. 已回写事实
- `S05` social runtime 当前分为两种真实运行形态：
  - 默认 builder：内存态 `SocialControlState + MemoryCommitJournal`
  - runtime-dir builder：`state/social-state.json + state/social-commit-journal.json`
- `friend_request / friendship / user_block / direct_chat / external_* / shared_*` 当前写链路均已复用同一 social 持久化骨架。
- 启动恢复当前以 snapshot 为准；journal 目前仅承担出站/审计谱系，不承担 replay truth。

## 3. 本轮未改变
- 未新增代码行为。
- 未改变 `S05` 结论：`not_closed / local_closure`
- 未改变下一主缺口：`tx boundary + journal replay + replay-based repair`

## 4. 当前结论
- Loop32 属于 `S05` 收口批次的文档真相补齐，而非新的 step closure。
- `S05` 仍应继续留在主刀序列，优先完成 `social-commit-journal` replay 与事务边界硬化。
