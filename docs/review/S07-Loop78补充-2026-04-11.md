# S07 Loop78 Addendum - 2026-04-11

## 1. Main Conclusion
- Loop78 closed the next-write retry persistence gap left after Loop77: stale claimed backlog no longer keeps owner metadata when the retry fails again.
- The ownership model is now consistent across repair retry and next-write retry failure persistence.
- This is still not a scheduler; it only fixes stale metadata consistency when a retry path already executes.

## 2. What Changed
- `services/control-plane-api/src/lib.rs`
  - `record_failed_shared_channel_sync_requests(...)` now accepts `now`
  - stale pending items clear owner metadata before failure count / error metadata are rewritten back into backlog
  - the same reclaim behavior now applies through runtime failure persistence, repair retry failure, and targeted republish retry failure
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_next_ready_pair_retry_failure_reclaims_stale_claim_metadata`
  - re-ran the Loop77 ownership guard and unclaimed auto-retry baselines to prove no regression

## 3. TDD Notes
- Red started with the new stale retry-failure regression test.
- Failure proved the bug directly: after the next healthy ready-pair write retried the stale claimed backlog and failed again, the original pending item still serialized `ownerActorId`.
- Green after the persistence fix:
  - the original stale request stays pending
  - stale owner metadata is reclaimed before re-persistence
  - adjacent next-write queue behavior remains unchanged

## 4. Fresh Evidence
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_ready_pair_retry_failure_reclaims_stale_claim_metadata -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_healthy_ready_pair_write_respects_active_pending_claim_ownership -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_backlog_retries_on_next_healthy_ready_pair_write -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `79 passed`

## 5. Remaining Gap
- `S07` remains `not_closed / local_closure`.
- The main remaining gap is `automatic stale detection / timeout reclaim / scheduler`.
- Release-ready exactly-once shared-channel sync semantics remain deferred.
