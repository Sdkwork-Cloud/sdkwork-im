# S09 Loop-17 补充 - 2026-04-10

## As-Built 增量

- runtime 已落地 `ConversationPolicy`、`policy_epoch`、`conversation.policy_applied`
- `POST /im/v3/api/chat/conversations` 已支持：
  - `policyVersion`
  - `capabilityFlags`
  - `historyVisibility`
  - `retentionPolicyRef`
- `capability_flags` 已真实作用于
  - `message.reaction`
  - `message.pin`

## Fresh Evidence

- `cargo test -p im-domain-core --offline`
- `cargo test -p conversation-runtime --offline --tests`
- `cargo test -p control-plane-api --offline --tests`
- `cargo test -p sdkwork-im-ccp-registry --offline`
- `cargo test -p im-platform-contracts --offline`

## 闭环判断

- `S09.status = not_closed`
- `S09.local_closure = runtime capability policy consumption`
- `S10` 继续阻塞；阻塞原因已从笼统 `S09 step_closure` 收敛为“读路径 retention owner 仍缺闭环”

## 下一轮输出

- 先冻结最小 history read surface
- 再把 `history_visibility` 接入真实读路径
- `retention_policy_ref` 继续保持 `S13` owner，不在 S09 虚报闭环
