# S07 Loop80 Addendum - 2026-04-11

## 1. Main Conclusion
- Loop80 closes a dead-letter consistency hole in the stale-claim lifecycle.
- Dead-letter requeue no longer restores stale owner metadata back into pending backlog.
- This keeps requeue semantics aligned with the rest of the reclaim hardening work from Loops 76-79.

## 2. What Changed
- `services/control-plane-api/src/lib.rs`
  - `requeue_selected_dead_letter_shared_channel_sync_requests(...)` now clears owner metadata before inserting the item back into pending backlog
  - failure budget rearm remains unchanged
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_dead_letter_requeue_reclaims_stale_claim_metadata`
  - re-ran the dead-letter rearm and targeted requeue baselines

## 3. TDD Notes
- Red started with the new dead-letter stale requeue regression test.
- Failure proved the bug directly: after requeue, the restored pending item still serialized `ownerActorId`.
- Green after the requeue fix:
  - owner metadata is cleared
  - failure budget still resets
  - requeue isolation semantics still hold

## 4. Fresh Evidence
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_requeue_reclaims_stale_claim_metadata -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_requeue_rearms_failure_budget_before_next_repair_attempt -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_inventory_and_targeted_requeue_only_restores_selected_actor -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `81 passed`

## 5. Remaining Gap
- `S07` remains `not_closed / local_closure`.
- The main remaining gap is still `automatic stale detection / timeout reclaim / scheduler`.
- Release-ready exactly-once shared-channel sync semantics remain deferred.
