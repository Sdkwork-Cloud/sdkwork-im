# 152CJ Loop-20 补充 - 2026-04-10

## As-Built 增量
- `streaming-service` 与 `local-minimal-node` 已形成：
  - standalone stream lifecycle surface
  - conversation-bound stream assembled auth / fanout / rebuild
- `im-call-runtime` 与 `local-minimal-node` 已形成：
  - provider routing / credential / callback / health / recording artifact surface
  - conversation-bound RTC assembled auth / rebuild / timeline side effects
- `media-service` 已形成：
  - provider-selected upload / complete / download-url / provider-health surface
  - RTC recording playback URL 通过选中的 `ObjectStorageProvider` 重签名

## 架构结论
- `S10 = step_closure`
- standalone service 与 assembled gateway 的 ownership 已清晰分离，不再需要在独立 provider 服务里重复承载 conversation-bound 授权语义。
- `S11 || S12` 现已具备真实准入条件。

## Deferred
- multi-provider failover / HA / DR / release-grade hardening：`S13`
- Agent actor 扩展：`S11`
- Device / external access 扩展：`S12`
