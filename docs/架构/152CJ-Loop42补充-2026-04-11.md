# 152CJ Loop42补充 - 2026-04-11

- `S05` runtime-dir social durable truth 再向前收敛一格：
  - social 写路径新增 `state/social-transaction-marker.json`，把 `journal append` 与 `snapshot save` 之间的 pending repair 窗口显式物化
  - 当前写入骨架已收敛为 `append journal -> write marker -> save snapshot -> clear marker`
  - `snapshot save` 失败后，marker 会保留在 runtime-dir 中；startup journal replay、same-event retry、HTTP repair 与 standalone CLI repair 成功后都会清 marker
- 当前架构语义应表述为：
  - journal 仍是 authority，snapshot 仍是 derived cache
  - `repair-marker` 现在承担最小的跨文件事务边界职责
  - 当前已具备 `atomic multi-file tx` 的最小可恢复边界，但尚未提升到 staged/manifest 级更强事务
- 因 `S05` 仍缺完整 `step_closure` 证据，Loop42 结束后 `S05` 继续只能标记为 `not_closed / local_closure`。
