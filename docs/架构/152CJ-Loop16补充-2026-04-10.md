# 152CJ Loop-16 补充 - 2026-04-10

## As-Built 增量
- `craw-chat-ccp-registry` 的 `ProtocolGovernanceSnapshot` 已新增 `business_policy_vocabulary`
- `control-plane-api` 的 `GET /backend/v3/api/control/protocol-governance` 已新增 `businessPolicyVocabulary`
- 当前冻结词汇：
  - `policy_version`
  - `capability_flags`
  - `history_visibility`
  - `retention_policy_ref`
- 当前冻结取值：
  - `historyVisibilityModes = invited / joined / shared / world_readable`
  - `retentionPolicyScopes = tenant / space / group / channel / thread`

## 架构结论
- `S09` 已在 protocol/control-plane 发布面补齐 `CPR09-4` 的词汇快照
- 该能力当前属于协议治理发布面，不等价于 `S07` runtime enforcement 或 `S13` archive/restore drill 已闭环
- `S10` 的前置阻塞不变：仍需 `S09 step_closure`
