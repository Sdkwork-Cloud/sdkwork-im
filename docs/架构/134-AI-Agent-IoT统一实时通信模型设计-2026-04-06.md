# AI / Agent / IoT 统一实时通信模型设计

## 1. 文档目标

本文档用于定义 `craw-chat` 在 AI、Agent、IoT 三类新场景下的统一通信模型，确保系统不是“传统 IM + 若干外挂功能”，而是从协议和领域模型层面支持：

- 人与人
- 人与 Agent
- 人与设备
- Agent 与设备
- Agent 与 Agent

之间的统一实时通信。

## 2. 基本判断

AI 与 IoT 场景之所以容易把 IM 系统做乱，根本原因是：

- 把 Agent 当成普通用户补丁
- 把设备当成外部旁路系统
- 把流式输出当成文本消息补丁

`craw-chat` 必须避免这种设计。

系统应统一采用：

- 一等主体模型
- 一等流模型
- 一等扩展事件模型

## 3. 一等主体模型

系统统一主体：

- `user`
- `agent`
- `device`
- `bot`
- `system`

统一 actor / sender 结构必须至少具备：

- `id`
- `kind`
- `member_id`
- `device_id`
- `session_id`
- `metadata`

这样才能支持：

- 普通用户消息
- Agent 输出
- 设备遥测
- 系统广播
- Bot / workflow 执行结果

## 4. Message 与 Stream 的统一模型

### 4.1 Message

适合：

- 离散消息
- 卡片
- 文件引用
- 结构化通知
- 最终物化结果

### 4.2 Stream

适合：

- token streaming
- delta patch
- telemetry frame
- 长任务进度
- 音频转写增量
- 模型推理过程输出

### 4.3 统一原则

- Message 与 Stream 并列
- Stream 结束后可物化为 Message
- 同一会话中两者共存

## 5. AI / Agent 模型

### 5.1 Agent 作为会话主体

Agent 应可直接：

- 加入 conversation
- 接收历史上下文
- 输出普通消息
- 输出流式消息
- 发起工具调用
- 产出结构化结果

### 5.2 Agent 事件类型

建议标准化：

- `agent.response.started`
- `agent.response.delta`
- `agent.response.completed`
- `agent.tool_call.requested`
- `agent.tool_call.completed`
- `agent.handoff.started`
- `agent.handoff.accepted`
- `agent.handoff.closed`

### 5.3 Agent 结果物化

Agent 输出可物化为：

- 普通消息
- 卡片
- 文件
- 知识条目引用
- 工作流状态更新

## 6. IoT / 智能硬件模型

### 6.1 设备作为一等主体

设备必须具备：

- `device_session`
- `device_presence`
- `device_twin`
- `device_command_stream`
- `device_uplink_stream`

### 6.2 典型交互

#### 用户 -> 设备

- 控制消息
- 配置变更
- 音视频呼叫
- command stream

#### 设备 -> 用户

- telemetry stream
- 告警通知
- 结构化状态消息
- 媒体上报

#### Agent -> 设备

- 指令编排
- 任务执行
- 自动化反馈

#### 设备 -> Agent

- 状态上报
- 感知结果
- 异常事件

### 6.3 接入协议

建议支持：

- WebSocket
- MQTT
- HTTP / SSE
- 预留 QUIC / WebRTC DataChannel

### 6.4 协议插件与设备管理

IoT 不能只停留在“协议接进来”，还必须冻结成统一的设备管理与接入体系：

- `device product / model`
- `device registry`
- `device credential / certificate / token`
- `device session / presence`
- `device twin / shadow`
- `telemetry uplink`
- `command downlink`
- `command ack / error`

在此基础上，协议插件首批固定为：

- `MQTT`
- 开源 `小智协议`

两者都只能通过统一 `IotProtocolAdapter` 接入，不能另造一套设备事件模型。

## 7. 协作上下文与知识沉淀

系统必须支持：

- topic
- thread
- document reference
- task reference
- workflow reference
- AI summary
- context anchor

原则：

- 聊天不是终点
- 会话上下文是协作容器
- AI、任务、文档、设备都挂在会话上下文上

## 8. 流式事件标准建议

建议统一 frame：

- `message.publish`
- `message.edit`
- `message.recall`
- `stream.open`
- `stream.delta`
- `stream.patch`
- `stream.checkpoint`
- `stream.finalize`
- `stream.abort`
- `presence.update`
- `rtc.signal`
- `device.telemetry`
- `agent.tool_call`
- `agent.tool_result`

这套标准必须同时覆盖：

- 普通聊天
- AI 对话
- 设备上行
- RTC 信令
- 长任务流

## 9. 安全边界

### 9.1 Agent

Agent 默认不应自动拥有所有上下文权限。

必须明确：

- 可见上下文范围
- 可调用工具范围
- 可访问资源范围
- 可向哪些 conversation 输出

### 9.2 Device

设备必须明确：

- device identity
- owner binding
- tenant binding
- session binding
- command authorization

### 9.3 流式输出

流式输出必须支持：

- 中止
- 限流
- resume
- checkpoint
- 数据类型约束

## 10. 对存储与连接层的影响

### 10.1 对连接层的影响

连接层必须原生支持：

- 大量低带宽 IoT 设备
- 高频短帧 AI delta
- 多设备并发会话

### 10.2 对存储层的影响

存储层必须支持：

- stream checkpoint
- materialization
- large blob reference
- telemetry retention policy
- agent artifact retention

## 11. 推荐的实施优先级

### 第一阶段

- Message / Stream 统一模型冻结
- actor / sender 统一模型冻结
- conversation / member / message / stream 基线落地

### 第二阶段

- agent 作为一等主体落地
- tool call / tool result 标准落地
- AI 流式对话基线落地

### 第三阶段

- device 作为一等主体落地
- `MQTT` 与 `小智协议` 适配落地
- 设备控制与 telemetry 统一接入

## 12. 结论

`craw-chat` 的 AI 与 IoT 设计必须从第一天统一在一个实时通信模型中，而不是分别做两套外挂系统。只有这样，系统才能真正成为 AI 时代的专业通信底座。

## 2026-04-09 增补：统一主体模型标准与当前实现边界

### A. 当前实现真相

- 当前仓库已存在 `AI / stream / rtc / device / agent` 的部分真实模型与服务链路，但并不等于本文描述的一体化主体模型已全部 durable truth 化。
- 文末 `As-Built` 才是当前已落地部分；前文不能直接当作现状清单。

### B. 本文哪些是目标态

- 第 2-12 节中的一等主体模型、Message/Stream 统一、AI/Agent/IoT 模型与协作上下文，属于标准目标面。
- 这些章节定义的是统一实时通信母模型，不等于当前仓库所有主体边界都已完整实现。

### C. 文档口径规则

- 写当前主体模型现状时，以本文 `As-Built`、`150CJ / 151CJ / 152CJ` 与当前相关 service / crate 为准。
- 写未来统一模型时，必须显式标记 `目标态`、`标准目标面` 或 `后续阶段`。

## 2026-04-07 As-Built 1

- `Step 05 / CP05-1` now begins to consolidate member identity and read-cursor construction into the shared domain model instead of the service runtime
- landed owner shift
  - `im-domain-core::conversation`
    - `build_conversation_member(...)`
    - `build_conversation_member_with_attributes(...)`
    - `build_default_read_cursor(...)`
    - `member_id(...)`
    - `member_episode_id(...)`
- why this matters for the unified actor model
  - later `sender` authority cannot be stabilized while member identity and read-cursor identity are still invented ad hoc inside one service
  - this increment makes the member/read-cursor identity baseline shared before `sender` and auth-context authority are tightened
- still not closed
  - `sender` is not yet fully injected from authority context
  - agent/device/user actor convergence is still incomplete at Step 05 overall

## 2026-04-07 As-Built 2

- the shared actor baseline now also owns the membership/read-cursor roster that later `sender` resolution depends on
- landed owner shift
  - `im-domain-core::conversation::ConversationRoster`
    - active member lookup
    - member episode continuity
    - default read-cursor continuity
- this matters because:
  - actor identity convergence cannot be made trustworthy if active member resolution is still service-private state
  - this increment makes user/agent/device participation state more shareable before `sender` authority is tightened
- still not closed
  - `sender` snapshot generation is still not fully server-owned across Step 05

## 2026-04-07 As-Built 3

- the shared actor baseline now also owns message sequence and mutation continuity that later structured `sender` snapshots depend on
- landed owner shift
  - `im-domain-core::message::ConversationMessageLog`
    - message sequence continuity
    - edit / recall continuity
    - unread high-watermark continuity
- this matters because:
  - user/agent/device sender convergence cannot be made trustworthy if message sequence and unread continuity are still service-private state
  - this increment makes later `sender` authority tightening depend on one shared message owner instead of scattered runtime fields
- still not closed
  - `sender` snapshot generation is still not fully server-owned across Step 05
## 2026-04-07 As-Built 4

- the shared actor baseline now also owns handoff conversation aggregate metadata instead of leaving it in service-local runtime fields
- landed owner shift
  - `im-domain-core::conversation::ConversationAggregateState`
    - `conversation_type`
    - `member_epoch`
    - `handoff_status_epoch`
    - `handoff_state`
    - `transition_handoff_status(...)`
- this matters because:
  - user / agent / device convergence cannot be made trustworthy if handoff lifecycle ordering and state storage are still private service fields
  - this increment makes later sender-authority tightening depend on one shared aggregate owner for handoff conversations
- still not closed
  - structured `sender` authority is still not fully server-owned across Step 05

## 2026-04-07 As-Built 5

- the shared actor/session baseline now also owns one device-scoped session sync-state seam before presence transitions run
- landed owner shift
  - `services/session-gateway/src/session.rs`
    - `DeviceSyncSessionState`
    - `device_sync_session_state(...)`
    - `resume_session(...)`
    - `get_presence_me(...)`
    - `heartbeat_presence(...)`
    - `disconnect_session(...)`
  - `services/session-gateway/src/lib.rs`
    - `AppState::device_sync_session_state(...)`
- this matters because:
  - user / agent / device convergence becomes fragile if device roster and latest sync continuity are still assembled independently inside each session HTTP handler
  - this increment makes later session/device authority tightening depend on one shared seam instead of four duplicated handler-local reads
- still not closed
  - the underlying raw device roster / latest sync storage is still local to `session-gateway`, so `CP05-4` remains open

## 2026-04-07 As-Built 6

- the shared actor/session baseline now removes one more device-scoped sync-state ambiguity by moving storage ownership out of `AppState`
- landed owner shift
  - `services/session-gateway/src/session_state.rs`
    - `SessionSyncState`
    - `DeviceSyncSessionState`
    - `register_device(...)`
    - `device_sync_session_state(...)`
  - `services/session-gateway/src/lib.rs`
    - `AppState::register_device(...)`
    - `AppState::device_sync_session_state(...)`
- this matters because:
  - user / agent / device convergence becomes fragile if app glue still owns device roster and latest sync continuity
  - this increment makes later session/device authority tightening depend on one stable storage owner seam instead of an app-level holder plus handler seam
- still not closed
  - `session-gateway` is cleaner, but `CP05-4` and `Step 05` remain open at repo scope

## 2026-04-07 As-Built 7

- device-scoped registration / route / sync-state 装配已在 `session-gateway` 内部落到稳定 seam：
  - `device_registration.rs` 负责注册装配 owner
  - `session_state.rs` 负责 sync-state owner
- 入口层不再直接持有 device registration 细节。

## 2026-04-07 As-Built 8

- `local-minimal-node` 现在也把 device-scoped registration / route / projection register 装配收口到 `LocalNodeDeviceRegistration`。
- 本地 node 入口层不再直接持有 device registration 细节，只消费共享 owner seam。

## 2026-04-07 As-Built 9

- `session-gateway` 现在把 device-scoped route-current 校验与 device registration 组合预检收口到同一个 owner seam。
- 已落地
  - `SessionDeviceRegistration::prepare_active_device_route(...)`
  - `SessionDeviceRegistration::ensure_route_session_current(...)`
- 这让 device-scoped registration / route / sync-state 的共享 actor/session 基线进一步稳定。
## 2026-04-07 As-Built 10

- `local-minimal-node` 的普通 device-scoped route preflight 现已进入 `LocalNodeDeviceRegistration`
- 普通 preflight 与 takeover 语义已拆开，避免 device-scoped authority seam 被 session 特例污染
- `device-scoped registration / route / sync-state` 在 node 侧与 gateway 侧开始对齐到同类 owner seam
## 2026-04-07 As-Built 11

- `session-gateway` 的 device-scoped lifecycle owner 已从 register/route preflight 继续扩展到 disconnect。
- `disconnect_active_device_route(...)` 保持同一组权威输入：
  - `tenant_id`
  - `principal_id`
  - `device_id`
  - `session_id`
- device route / realtime subscription / disconnect fence 的生命周期现在继续在同一 owner seam 内部协同。
## 2026-04-07 As-Built 12

- `local-minimal-node` 的 device-scoped lifecycle owner 已从 register/route preflight 扩展到 disconnect。
- `platform::refresh_node_operational_view(...)` 也被收回 owner seam，使本地 profile 的设备生命周期副作用不再散落在 session entrypoint。

## 2026-04-07 As-Built 13

- 对应范围：`Wave B / Step 06`
- 统一实时模型在当前仓库里已经补上 stream / rtc 两条关键能力证据：
  - stream 以 generic frame transport、completion/abort fanout、runtime persistence 的形式独立存在
  - rtc 以 signaling service、signal message bridge、runtime persistence 的形式独立存在
- 这保证了后续 `user / agent / device` 扩展不会依赖“把流式能力塞回 message payload”或“把 RTC 混成媒体内部逻辑”。
- 当前边界保持正确：
  - stream 是一等实时能力
  - rtc 是一等 signaling 能力
  - 媒体面与更复杂基础设施仍然留在后续阶段处理
## 2026-04-07 As-Built 14

- 对应范围：`Wave C / Step 08 / CP08-1`
- 一等主体模型已开始在真实代码中落地：
  - `AgentSubject` 进入 `craw-chat-contract-agent`
  - `DeviceSubject` 进入 `craw-chat-contract-iot`
  - 两者都可以物化为统一 `Sender`
- 已落地的 sender 收敛规则：
  - `agent` 使用 `kind = "agent"`，保留 `member_id / session_id / metadata`
  - `device` 使用 `kind = "device"`，保留 `member_id / device_id / session_id / metadata`
  - 两者都复用 `im_domain_core::message::Sender`，没有另造 AI / IoT 私有 sender 结构
- 这一步为什么重要：
  - 后续 `Agent` 流式输出、tool call、handoff 与 `device telemetry / command` 只有建立在统一主体模型上，才能继续复用同一套 message / stream 主链路
  - 它避免 Step 08 继续退化成“IM 主链路之外的 AI / IoT 旁路协议栈”
- 仍未闭环：
  - `agent.response.*`、`agent.tool_call.*` 等生命周期还未落地
  - `device session / presence / telemetry / command / twin` 还未全部接入统一实时主链路
  - `Step 08` 仍需继续完成 `CP08-2 / CP08-3 / CP08-4`
## 2026-04-07 As-Built 15

- 对应范围：`Wave C / Step 08 / CP08-2`
- Agent 行为主链路已开始落地到真实运行时：
  - `automation-service` 可以启动 Agent 响应流
  - Agent delta 可以被追加到统一 `StreamFrame`
  - tool call request / complete 可以被记录为结构化生命周期
  - Agent 响应流完成后可产出 `result_message_id`
- 当前已落实的统一规则：
  - Agent delta frame 的 sender 直接来自 `AgentSubject::sender(...)`
  - Agent 响应流复用 `StreamSession / StreamFrame`，没有单独造一套 AI stream 协议
  - tool call 生命周期复用同一 execution 作用域和 commit journal 事件排序
- 这一步为什么重要：
  - Step 08 不再只是“agent 是一等主体”的静态建模，而是开始具备真实的流式输出行为
  - 它为后续把 Agent 输出继续接到 projection / audit / node 主链路提供了最小运行时基础
- 仍未闭环：
  - 设备 telemetry / command 还未打通
  - Agent 生命周期还未通过 HTTP / local-minimal-node 暴露为主链路入口
  - `Step 08` 仍需继续完成 `CP08-3 / CP08-4`

## 2026-04-08 As-Built 16

- 对应范围：`Wave C / Step 08 / CP08-3`
- 设备主链路已开始真实接入统一实时模型：
  - `device.telemetry` 已通过统一 stream 主链路写入
  - `device.command` 已通过统一 stream 主链路下发
  - `device.telemetry` sender 由 `DeviceSubject::sender(...)` 生成，稳定表现为：
    - `id = device_id`
    - `kind = "device"`
    - `device_id = device_id`
    - `session_id = auth.session_id`
- 当前已兑现的统一规则：
  - 设备路径没有单独创建私有 telemetry runtime 或私有 command protocol
  - 设备命令和遥测继续建立在 `StreamSession / StreamFrame` 之上
  - 设备扩展继续复用统一 capability 名称，而不是在 handler 内继续散落条件分支

## 2026-04-08 As-Built 17

- 对应范围：`Wave C / Step 08`
- Agent 与 Device 现在都已具备最小真实主路径：
  - Agent 输出复用统一 `StreamSession / StreamFrame + AgentSubject::sender(...)`
  - Device 数据复用统一 `StreamSession / StreamFrame + DeviceSubject::sender(...)`
- 这意味着 Step 08 已不再只是“主体建模”，而是：
  - Agent / Device 都开始通过统一 sender / actor / scope 进入真实运行时
  - AI / IoT 没有再造一套独立实时协议面
- 因此 `Step 08` 可以从“局部能力推进”进入“整步闭环完成”状态。
