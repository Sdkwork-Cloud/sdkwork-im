# 152CJ Loop-15 补充 - 2026-04-10

## As-Built 增量
- `projection-service` 已补齐 `reaction/pin summary` 派生面：
  - `GET /api/v1/conversations/{conversationId}/messages/{messageId}/interaction-summary`
  - `GET /api/v1/conversations/{conversationId}/pins`
- 事件消费已覆盖：`message.reaction_added`、`message.reaction_removed`、`message.pin_added`、`message.pin_removed`
- snapshot persist/restore 已覆盖 interaction summary，device-sync feed 已接入 reaction/pin 投影增量

## 架构结论
- `reaction/pin` truth 继续归属 `conversation-runtime`
- `reaction/pin summary` 归属 `projection-service`，且保持可重建读模型，不反写 truth
- `S08` 不再被 `reaction/pin summary` 阻塞
- `S10` 当前仅剩 `S09 step_closure` 前置阻塞
