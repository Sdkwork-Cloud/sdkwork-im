# 152CJ Loop45补充 - 2026-04-11

- `conversation-runtime` 现已支持 `thread minimal runtime truth`：
  - thread 必须锚定到 `group conversation + root message`
  - dedicated create path 由 `POST /api/v1/conversations/threads` 暴露
  - thread business binding 当前写成 `thread/rootMessageId`
  - recovery replay 可以恢复 thread binding 与 owner metadata
- Loop45 的关键架构决议：
  - thread 不是 message reply 字段的派生影子，而是独立子会话
  - 当前 thread 先按 `group-like` 最小权限语义接入
  - `thread subscription / notification` 继续 deferred，不阻塞本轮 local closure
- 主架构文档应同步反映：
  - `thread` 已从“仍未物化能力”列表中移出
  - `S07` 的剩余主缺口收敛为 `invited history visibility / shared-external history / retention enforcement`
