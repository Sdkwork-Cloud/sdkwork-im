# 高可用与灾备恢复设计

## 1. 文档目标

本文档用于定义 `craw-chat` 在高可用、故障隔离、恢复和灾备方面的架构原则，确保系统不仅能运行，还能在异常情况下：

- 快速止损
- 正确恢复
- 保持权威数据不被破坏
- 支撑 SaaS 与私有化交付

## 2. 高可用设计总原则

- 连接可中断，但真相不能错乱。
- route 可以重建，但 fencing 不能失效。
- projection 可以滞后，但 durable truth 不能缺失。
- 控制面可以降级，但消息热链不能依赖控制面强事务。
- 恢复必须以权威数据源为准，而不是以内存态为准。

## 3. 故障域模型

系统至少区分以下故障域：

- 进程级故障
- 节点级故障
- plane 级故障
- 存储级故障
- cell 级故障
- region 级故障

设计目标是让故障尽可能停留在小故障域内，不向整个系统扩散。

从协议角度，还必须显式考虑：

- protocol profile 配置错误
- compatibility matrix 漂移
- close / error contract 不一致
- capability rollout 失控

## 4. 各平面的高可用要求

### 4.1 Link Plane

故障表现：

- 连接中断
- 心跳异常
- 出站队列丢失

恢复策略：

- 客户端主动重连
- route epoch 重新协商
- `sync feed + stream checkpoint` 补偿恢复

### 4.2 Route Plane

故障表现：

- route lookup 超时
- owner 不明确
- drain 过程卡住

恢复策略：

- 以权威 route store 重建
- 使用 `epoch + fencing` 防止陈旧 owner 继续工作
- 支持显式 takeover

### 4.3 Messaging / Stream Plane

故障表现：

- durable commit 延迟升高
- write path 不稳定
- AI / stream backlog 堆积

恢复策略：

- 优先保护 durable write
- 非关键副链降级
- 按需暂停部分低优先级 stream delta 下发

### 4.4 Projection Plane

故障表现：

- timeline / inbox / summary 滞后
- 未读数更新延迟

恢复策略：

- 投影可滞后但必须可重建
- lag 必须可观测
- 支持 replay 和 targeted rebuild

## 5. 典型故障场景

## 5.1 连接节点故障

处理原则：

1. 连接断开
2. 客户端重连到新 link node
3. route plane 判断当前 owner
4. 旧 route 通过 fencing 失效
5. 客户端补拉 `sync feed` 和 `stream checkpoint`

## 5.2 Redis 短暂不可用

处理原则：

- route、presence、resume hint 读写会受影响
- 允许退化到更慢的权威存储和恢复路径
- 不允许因为 Redis 不可用就宣告消息 durable truth 丢失

## 5.3 PostgreSQL 压力或故障

处理原则：

- 优先保护消息与流的提交真相
- 暂停部分非关键投影和后台任务
- 结合备份、PITR、replay 恢复

## 5.4 Projection Worker 故障

处理原则：

- 不影响消息提交结果
- 通过 lag 和 backlog 指标发现问题
- 恢复后按事件重放重建视图

## 5.5 对象存储异常

处理原则：

- 不影响纯文本消息链路
- 媒体上传、附件物化和导出可降级
- 引用一致性必须后续补偿校验

## 5.6 协议升级事故

处理原则：

- 优先回退 capability 或 protocol profile，而不是直接全量回滚系统
- 以 cell 为最小止损域
- 如果 close / resume 语义已受影响，则使用 `goaway + session_resume` 进行受控迁移
- 需要保留至少一个 stable cell 作为回流目标

## 6. RTO / RPO 设计建议

不同 profile 的目标可以不同，但原则建议如下：

- `Local Minimal`：以开发闭环为目标，不承诺严格 RTO/RPO
- `Private Standard`：要求支持标准备份恢复和 projection rebuild
- `SaaS Shared / Dedicated`：要求具备节点级快速恢复、存储级恢复和租户级恢复能力

更重要的是恢复顺序必须被文档化和脚本化，而不是只写一个抽象指标。

## 7. 恢复流程标准

推荐恢复顺序：

1. 恢复配置与控制面最小能力
2. 恢复 metadata
3. 恢复 `MessageLog / Stream checkpoint`
4. 重建 route 热状态
5. 重放 projection
6. 对齐对象存储引用和审计状态
7. 恢复对外服务

若故障与协议治理相关，还必须追加：

8. 校验 protocol registry / compatibility matrix / effective snapshot
9. 确认 close contract 与 resume contract 在目标 cell / region 一致

## 8. 备份与重建能力

必须具备：

- metadata snapshot
- message log replay
- stream checkpoint restore
- object storage backup
- tenant-scoped recovery
- projection rebuild

## 9. 演练与观测

建议固定纳入运维演练：

- link node 故障演练
- route takeover 演练
- projection rebuild 演练
- object storage 引用一致性校验演练
- tenant 级恢复演练
- protocol profile 回滚演练
- canary cell 协议升级失败止损演练
- region 灾备切换下的协议兼容演练

必须观察的关键指标：

- reconnect success rate
- resume success rate
- route migration success rate
- projection lag
- replay duration
- drain completion time
- protocol negotiation success rate
- capability rollback success rate
- close contract mismatch rate

## 10. 结论

`craw-chat` 的高可用不是“所有组件都双机热备”这么简单，而是要把连接恢复、route fencing、durable truth、projection rebuild、tenant 级恢复和协议升级止损这六件事设计成完整闭环。

## 2026-04-09 增补：高可用标准与当前实现边界

### A. 当前实现真相

- 当前仓库已具备部分排空、恢复、脚本化验证、protocol governance baseline 等真实资产，但并不等于完整多 region 高可用与灾备平台已落地。
- 文末 `As-Built` 才是当前真相；前文更多是高可用标准与演练目标。

### B. 本文哪些是目标态

- 第 2-10 节中的故障域、高可用要求、RTO/RPO、恢复流程、备份重建与演练标准，属于标准目标面。
- 这些章节定义的是商用 IM 的终局韧性标准，不等于当前仓库已经具备完整生产级灾备体系。

### C. 文档口径规则

- 写当前 HA/DR 现状时，以本文 `As-Built`、`137 / 149 / 152CJ` 与当前脚本/服务事实为准。
- 写未来灾备标准时，必须显式标记 `目标态`、`标准目标面` 或 `演练目标`。

## 2026-04-08 As-Built 1

- `Wave C / Step 09 / CP09-2` 已在 `Local Minimal` profile 上补出第一条真实的 projection snapshot recovery 路径。
- 当前真实已落地的恢复语义包括：
  - managed runtime-dir 会持续写出 conversation projection snapshot
  - startup recovery 会先从 metadata snapshot 发现 conversation scope，再恢复 projection read-side
  - 即使 `commit-journal.json` 被替换成空数组，`inbox / summary / timeline / read-cursor` 仍可恢复
- 这让本文中的恢复顺序开始有代码证据：
  - 先恢复 metadata snapshot
  - 再恢复 projection read-side 查询能力
  - 并把 `commit-journal replay` 从“唯一恢复入口”降为“domain/runtime 仍需的恢复手段之一”
- 当前仍未兑现的部分：
  - runtime-dir `backup / restore / preview / repair` 还没把新 snapshot 文件纳入同一份受管清单
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - projection snapshot persist/restore 的观测指标仍未建立

## 2026-04-08 As-Built 2

- `Wave C / Step 09 / CP09-2` 已继续把 projection snapshot recovery 证据从“启动恢复”推进到“运维恢复工具链”。
- 当前真实已落地的灾备恢复语义包括：
  - runtime-dir `inspection` 已能把 `projection-metadata.json / projection-timeline.json` 作为正式受管文件检查
  - runtime-dir `backup catalog` 已会把 projection snapshot 文件计入 full/partial snapshot 质量判定
  - runtime-dir `restore preview` 已会暴露 projection snapshot 的 `content_differs / target_missing` 差异
  - runtime-dir `restore` 与 `repair` 已会真实恢复或重建 projection snapshot 文件
  - pre-restore / repair backup 现在会保留 projection snapshot 文件，便于回退与审计
- 这让本文中的恢复顺序拥有更完整的本地证据：
  - 先检查与保护 metadata / projection snapshot
  - 再执行 restore / repair
  - 再对外恢复 projection read-side 查询能力
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - `registered_devices / device_sync_feeds / device_sync_sequences` 是否进入同一恢复合同仍待收口
  - projection snapshot persist/restore 的 metrics / tracing / structured logging 仍未建立

## 2026-04-08 As-Built 3

- `Wave C / Step 09 / CP09-2` 已继续把灾备恢复证据从“conversation read-side”推进到“session/device-sync read-side”。
- 当前真实已落地的恢复语义包括：
  - `projection snapshot` 现在可恢复：
    - `registered_devices`
    - `device_sync_feeds`
    - `device_sync_sequences`
  - startup recovery 会先恢复 device-sync snapshot，再恢复 conversation snapshot checkpoint
  - 即使 `commit-journal.json` 被替换成空数组，`resume / sync-feed` 仍能在 `Local Minimal` profile 下恢复
  - device registration 也会把快照写回同一份 projection snapshot 文件，使恢复不依赖下一次 conversation 事件
- 这让本文中的恢复顺序拥有更完整的证据：
  - 先恢复 metadata / projection snapshot
  - 再恢复 session/device-sync read-side
  - 再对外恢复 `resume / sync-feed / inbox / summary / timeline / read-cursor`
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - projection snapshot persist/restore 的 metrics / tracing / structured logging 仍未建立
  - 更完整的 archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 4

- `Wave C / Step 09 / CP09-3` 已继续把灾备恢复证据推进到公开 `ops` 诊断面。
- 当前真实已落地的恢复观测语义包括：
  - `ops/health` 现在可以看到 projection snapshot persist 的成功计数
  - `ops/diagnostics` 现在可以看到 projection snapshot restore 的：
    - success counter
    - recent trace
    - structured log
  - 在 `Local Minimal` profile 下，即使 `commit-journal.json` 被替换为空数组，runtime-dir 恢复后仍能通过 `ops/diagnostics` 证明 restore 确实发生过
- 这让本文中的恢复顺序拥有新的运维证据：
  - 不只是“恢复结果可读”
  - 还可以证明“恢复动作已执行并成功”
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - 更广泛的 restore drill / tenant 级恢复 / region 级灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 5

- `Wave C / Step 09 / CP09-3` 已继续把灾备恢复证据推进到 replay gap 可解释的级别。
- 当前真实已落地的恢复观测语义包括：
  - `ops/health` 与 `ops/diagnostics` 现在可以看到：
    - replay backlog
    - replayed event count
    - replay duration
  - `ops/lag` 现在可以看到按 scope 暴露的 `projection_replay` lag 项
  - stale snapshot restart 演练已经有自动化测试证明：
    - snapshot 先落后于 journal
    - 重启后 replay 补齐缺失消息
    - lag/backlog/duration 证据与恢复结果一致
- 这让本文中的恢复要求继续具备代码证据：
  - `Projection Plane` 的 replay/backlog 不再只能靠日志猜测
  - restore 后的 replay gap 已能在公开 ops 面被读到
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 6

- `Wave C / Step 09 / CP09-3` 已继续把灾备恢复证据推进到公开 replay drill 读面。
- 当前真实已落地的恢复观测语义包括：
  - `ops/replay-status` 现在可以直接公开：
    - `idle / replayed` 状态
    - replay backlog
    - replayed event count
    - replay duration
    - replay throughput
    - 对应 `projection_replay` lag
  - 极短 replay 路径现在不会因为毫秒取整变成 `0ms` 而丢失吞吐率语义
  - stale snapshot restart 演练已证明：
    - replay-status 读面会从 `idle` 进入 `replayed`
    - backlog / duration / throughput / lag 证据与真实 replay 结果一致
- 这让本文中的恢复要求继续具备更可操作的代码证据：
  - 不只是“恢复动作发生过”
  - 还可以通过单一公开读面快速判断“这次恢复 replay 到底处于什么状态、补了多少、速度如何”
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 7

- `Wave C / Step 09 / CP09-3` 已继续把恢复后的 steady-state 运维证据推进到 live projection update delay。
- 当前真实已落地的恢复观测语义包括：
  - `ops/health` 与 `ops/diagnostics` 现在可公开：
    - `timelineMs`
    - `inboxMs`
    - `sourceEventType`
    - `scopeId`
    - `recordedAt`
  - 这意味着运维判断不只停留在“replay 完成没有”，还开始具备“恢复后 projection read-side 更新是否已经回到稳定延迟”的辅助证据
  - 本轮也通过结构回归修复，保持 `projection-service` 继续满足既有模块边界约束
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 8

- `Wave C / Step 09 / CP09-3` 已继续把恢复后的 steady-state 运维证据推进到 live projection lag。
- 当前真实已落地的恢复观测语义包括：
  - `ops/lag` 与 `diagnostics.lag` 现在可公开 `projection_live`
  - 这份证据直接来自真实 projection apply 主路径，而不是 startup replay 或外部脚本估算
  - 这让运维判断不只停留在“replay 是否补完”，还开始具备“steady-state projection 是否继续追平”的辅助证据
  - 本轮也通过结构回归修复，保持 `projection-service` 继续满足既有模块边界约束
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 9

- `Wave C / Step 09 / CP09-3` 已继续把恢复语义推进到 total projection rebuild duration。
- 当前真实已落地的恢复观测语义包括：
  - `ops/health` 与 `ops/diagnostics` 现在可公开：
    - `projectionPlane.rebuildDurationMs`
  - 这份时长直接来自 startup projection recovery 主路径，而不是外部脚本或 replay-only 估算
  - snapshot-only recovery 现在也能稳定证明：
    - replay 没发生
    - rebuild 仍真实发生过
- 这让本文中的恢复要求继续具备代码证据：
  - 不只是“replay 花了多久”
  - 还可以判断“整次 projection recovery 到底花了多久”
  - 从而更准确地区分 snapshot restore 与 replay backlog 补偿两类恢复形态
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 10

- `Wave C / Step 09 / CP09-4` 已开始把灾备恢复语义推进到 archive snapshot restore path。
- 当前真实已落地的恢复语义包括：
  - runtime-dir managed backup snapshot 现在可以被正式 archive，而不是只能停留在 active backup 目录
  - archived snapshot 仍可直接作为：
    - restore preview
    - restore
    的输入
  - archive 动作会留下 `archive-report.json`，使“先 archive、后按需恢复”开始具备最小审计证据
- 这让本文中的恢复要求开始拥有新的代码证据：
  - 归档不再天然等于“从恢复路径中消失”
  - archive 后若需要 replay / restore，已经有清晰的本地 restore path
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - archive / lifecycle / tenant 级灾备治理仍未建立

## 2026-04-08 As-Built 11

- `Wave C / Step 09 / CP09-4` 已继续把灾备恢复语义推进到 archive lifecycle governance。
- 当前真实已落地的恢复语义包括：
  - archived runtime backup 现在不只保留 restore path，还会记录：
    - `storageClass`
    - `retentionPolicy`
    - `retentionDays`
    - `restoreStatus`
    - `legalHold`
    - `archivedAt`
  - archive prune 已具备正式 owner seam 与脚本入口：
    - retention 已到期、且未被 legal hold 保护的 archived snapshot 会被删除
    - legal hold archived snapshot 会被明确保留
  - 这意味着 runtime-dir 恢复链路现在已经覆盖：
    - active backup
    - archived backup
    - hold-aware archive retention
- 这让本文中的恢复要求继续具备代码证据：
  - 归档不只“还能恢复”
  - 还开始具备“什么情况下允许清理、什么情况下必须保留”的最小恢复治理语义
- 当前仍未兑现的部分：
  - `conversation_runtime` 在 journal 丢失时仍不能恢复 domain owner 状态
  - tenant 级恢复、跨 cell / region 灾备演练仍未建立
  - 更高阶的 object storage / multi-cell archive 恢复治理仍未建立

## 2026-04-08 As-Built 12

- `Wave D / Step 10 / CP10-1` 已把本地灾备/恢复相关入口正式纳入标准命令面，而不再要求运维人员自行阅读源码或拼装子命令。
- 当前已经被统一公开到命令面与快启文档中的恢复治理入口包括：
  - `inspect-runtime-local`
  - `repair-runtime-local`
  - `list-runtime-backups-local`
  - `archive-runtime-backup-local`
  - `prune-runtime-archives-local`
  - `preview-runtime-restore-local`
  - `restore-runtime-local`
- `status-local` 的 help 语义现在也已明确把以上动作公开为标准 follow-up loop，这意味着：
  - 健康检查
  - 运行目录检查
  - 缺失文件修复
  - 备份盘点
  - 归档与归档清理
  - 恢复预演与显式恢复
  已进入同一套本地运维口径。
- 这属于对本文件“恢复链路必须具备可操作入口”的一次真实落地，但仍未覆盖：
  - tenant 级恢复编排
  - 跨 cell / region 灾备演练
  - object storage / multi-cell archive 的更高阶恢复治理

## 2026-04-08 As-Built 13

- `Wave D / Step 11 / CP11-1` 已把本文件中的高可用与恢复验证入口冻结成正式场景族，而不再只依赖零散测试名称。
- 当前已被冻结的 HA / DR 场景包括：
  - `drain-rebalance`
  - `restore-recovery`
  - `upgrade-rollback`
- 对应种子资产已明确指向：
  - `services/local-minimal-node/tests/cluster_drain_rebalance_e2e_test.rs`
  - `services/local-minimal-node/tests/runtime_dir_restore_test.rs`
  - `services/local-minimal-node/tests/runtime_dir_restore_preview_test.rs`
  - `services/control-plane-api/tests/protocol_governance_test.rs`
  - `services/control-plane-api/tests/protocol_registry_test.rs`
- 这让本文中的恢复要求首次拥有一份统一的 Step 11 演练目录：
  - 先冻结演练路径
  - 再在后续 checkpoint 填入 RTO / RPO / drain duration / rollback timing
- 当前仍未兑现的部分：
  - `failover` 的真实演练结果
  - tenant 级与跨 cell / region 的灾备编排
  - object storage / multi-cell archive 的更高阶恢复治理

## 2026-04-08 As-Built 14

- `Wave D / Step 11 / CP11-3` 已把本文件中的三条最小 HA / DR 路径推进到真实演练结果：
  - `drain-rebalance`
  - `restore-recovery`
  - `failover`
- 当前真实结果包括：
  - `drain-rebalance`
    - `migratedRouteCount = 1`
    - `deliveryPreserved = true`
    - `drillDurationMs = 0.661`
  - `restore-recovery`
    - `restoredFileCount = 11`
    - `previewDurationMs = 2.159`
    - `restoreDurationMs = 18.313`
  - `failover`
    - `activeOwnerNodeId = node_b`
    - `staleDisconnectRejected = true`
    - `takeoverDurationMs = 0.438`
- 这意味着本文件中的恢复要求现在已经拥有：
  - 一轮 route owner 排空与迁移演练
  - 一轮 runtime-dir 恢复演练
  - 一轮跨节点 session owner failover 演练
- 当前仍未兑现的部分：
  - object storage / archive / cross-region 的更高阶恢复治理
  - 多 cell / region 的真实灾备切换
  - `upgrade-rollback` 的 Step 11 演练证据

## 2026-04-08 As-Built 15

- `Wave D / Step 11 / CP11-4` 已把本文件中最后缺失的 `upgrade-rollback` 演练证据补齐到最小可信路径。
- 当前新增结果为：
  - `compatibleClientCount = 4 / 4`
  - `compatibilityMatrixPassRate = 1.0`
  - `rollbackActivationMs = 0.007`
  - `killSwitchPropagationSuccessRate = 1.0`
  - `postRollbackProtocolErrorRate = 0.0`
- 这意味着本文件中的恢复要求现在已经同时拥有：
  - route drain / rebalance 演练
  - runtime-dir restore 演练
  - cross-node session owner failover 演练
  - kill switch rollback 与 safe path preserved 演练
- 当前仍未兑现的部分：
  - object storage / archive / cross-region 的更高阶恢复治理
  - 多 cell / region 的真实灾备切换
  - 多 cell / 多 region 的升级编排自动化
