# 09Z - 实施计划 - SDK 容器 README 版本占位收敛

## 目标

让 app/admin 两层 SDK 容器 README 与 bundle 级版本占位 contract 保持一致，补齐根 README 与叶子 README 之间的中间层断层。

## 最小实施面

1. 先在 `tools/chat-cli/tests/chat_cli_contract_test.rs` 写红测
2. 更新：
   - `sdks/sdkwork-im-sdk/README.md`
   - `sdks/sdkwork-control-plane-sdk/README.md`
3. 每个 README 至少补齐：
   - `plannedVersion = null`
   - `versionStatus = version_unassigned_pending_freeze`
4. 回归 contract test
5. 回写 `docs/step`、`docs/架构`、`docs/review`

## 约束

- 不伪造真实版本号
- 不伪造 freeze 决议来源
- 中间层 README 只公开 bundle 真源，不维护独立版本状态

## 放行标准

- 两个容器 README 全部显式表达版本占位状态
- fresh `fmt --check` 与 `chat_cli_contract_test` 保持通过
