# 152CJ Loop33补充 - 2026-04-10

- `S05` runtime-dir social truth 已从 `snapshot-only recovery` 推进到 `snapshot + idempotent journal replay + snapshot self-heal`。
- startup recovery 当前顺序为：`load social-state.json -> replay social-commit-journal.json -> 若有增量则回写 social-state.json`。
- replay 当前覆盖 `friend_request / friendship / user_block / direct_chat / external_connection / external_member_link / shared_channel_policy`。
- `friend_request.requestMessage` 与 `user_block.expiresAt` 已进入新 journal payload；旧 journal 缺字段时按 `None` 降级恢复。
- 因仍缺 `tx boundary + crash consistency + replay-based repair`，`S05` 仍只能标记为 `not_closed / local_closure`。

