# 152CJ - Loop29补充 - 2026-04-10

## 1. 新增 As-Built
- `S05` 已新增 `user_block` minimal control-plane write path：
  - `POST /api/v1/control/social/user-blocks`
  - `GET /api/v1/control/social/user-blocks/{blockId}`
  - `user_block.blocked` commit
  - `control.user_block_blocked` audit
- `user_block` 当前语义与领域模型一致：
  - 有向关系：`blocker_user_id -> blocked_user_id`
  - 冲突键：`tenant + blocker + blocked + scope + active`
  - `scope = direct_chat` 需要 `directChatId`

## 2. 仍未夸大为 step_closure 的原因
- `S05` 仍缺：
  - durable repo / tx / outbox / replay
  - `direct_chat -> conversation` runtime binding
- `S07` 现状更精确地应表述为：
  - 已有 `reaction/pin`
  - 已有 `joined / world_readable`
  - 已有 `retentionPolicyRef`
  - 仍缺 `thread / shared / external / direct_chat -> conversation`

## 3. 全局门禁
- `release_closure = no`
- 阻塞项：
  - `S05` 未到 `step_closure`
  - `S07` 未到 `step_closure`
