# 线�?�?��? PostgreSQL �?�据�?�?�置�??�?

�?��??说�?? SDKWork Chat server �??�?��?�?�?��?�?PostgreSQL�??线�?�?��?不要使�?�根�?��? `.env.postgres`�?�?不要�??�?�据�?�?码�??�?�?Git �?�?�?��?�?�??�??�?�?启�?��?�令�??

�?�?��?�?��?�??�?

- app code: `chat`
- public route: `/sdkwork/chat`
- package name: `sdkwork-chat`
- server database default: PostgreSQL
- desktop database default: SQLite

## 1. �?�??�?��?

Linux server/service/container 使�?��?

```text
/opt/sdkwork/chat                 # archive install root
/etc/sdkwork/chat                 # config and secret root
/var/lib/sdkwork/chat             # durable data
/var/log/sdkwork/chat             # file logs
/run/sdkwork/chat                 # runtime state
```

核�?�?�置�??件�?

```text
/etc/sdkwork/chat/
  chat.toml
  server.env
  postgresql.yaml
  database.secret
  redis.secret
```

Windows Service 使�?�?`%ProgramFiles%/sdkwork/chat` �??`%ProgramData%/sdkwork/chat`�??macOS service 使�?�?`/usr/lib/sdkwork/chat`�?�`/Library/Application Support/sdkwork/chat`�?�`/Library/Logs/sdkwork/chat`�??

## 2. �?�据�?�?�??

建议线�?�?SDKWork Chat �??建�?��?�?�据�?�?��?��?�?�?�账号�?��?��?schema�?

```text
database: sdkwork
schema: public
username: sdkwork
password: generated by password manager
```

�?�?�账号只�??�?�?�?��??�??�?�?��?不使�?�?PostgreSQL �?级�?��?��??

## 3. �??建�?�据�?�??账号示�?

以�? SQL �??DBA �??�?�署管�?�??�?��?�?�??�?�??�?�?��?�?��?�?��?

```sql
CREATE ROLE "sdkwork" LOGIN PASSWORD 'replace-with-generated-password';
CREATE DATABASE sdkwork OWNER "sdkwork";
\connect sdkwork
CREATE SCHEMA IF NOT EXISTS public AUTHORIZATION "sdkwork";
GRANT CONNECT ON DATABASE sdkwork TO "sdkwork";
GRANT USAGE, CREATE ON SCHEMA public TO "sdkwork";
ALTER DEFAULT PRIVILEGES IN SCHEMA public_chat_prod
  GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO "sdkwork";
ALTER DEFAULT PRIVILEGES IN SCHEMA public_chat_prod
  GRANT USAGE, SELECT, UPDATE ON SEQUENCES TO "sdkwork";
ALTER ROLE "sdkwork" SET search_path TO public;
```

�?�??�?�据�?�?? schema 已�?�平台提�?��??建�?只�??要确认�?�?�账号�?��?读�??�??迁移�??�??�?�?��??

## 4. chat.toml

`chat.toml` �??server 运�?�?�置�?�口�?

```toml
[runtime]
environment = "production"
deployment_mode = "server"
app_code = "chat"

[server]
bind_address = "0.0.0.0:18080"
trust_forwarded_headers = true

[public_endpoints]
base_url = "https://chat.example.com/sdkwork/chat"
api_base_url = "https://chat.example.com/sdkwork/chat"
websocket_base_url = "wss://chat.example.com"
docs_base_url = "https://chat.example.com/sdkwork/chat/docs"

[paths]
config_directory = "/etc/sdkwork/chat"
config_file = "/etc/sdkwork/chat/chat.toml"
data_directory = "/var/lib/sdkwork/chat"
log_directory = "/var/log/sdkwork/chat"
cache_directory = "/var/cache/sdkwork/chat"
runtime_directory = "/run/sdkwork/chat"

[database]
engine = "postgresql"
host = "postgres.internal.example.com"
port = 5432
database = "sdkwork"
schema = "public"
username = "sdkwork"
password_file = "/etc/sdkwork/chat/database.secret"
ssl_mode = "require"
max_connections = 20

[redis]
enabled = true
host = "redis.internal.example.com"
port = 6379
database = 0
password_file = "/etc/sdkwork/chat/redis.secret"
key_prefix = "chat"
tls = false
max_connections = 16
```

## 5. server.env

`server.env` 维�?��?�?级�?�??项�?不保�?�??�??�?�据�?�?码�?

```env
SDKWORK_IM_DEPLOYMENT_MODE=server
SDKWORK_IM_CONFIG_FILE=/etc/sdkwork/chat/chat.toml
SDKWORK_IM_DATA_DIR=/var/lib/sdkwork/chat
SDKWORK_IM_LOG_DIR=/var/log/sdkwork/chat
SDKWORK_IM_RUN_DIR=/run/sdkwork/chat
SDKWORK_IM_APPLICATION_PUBLIC_INGRESS_BIND=0.0.0.0:18080
SDKWORK_IM_APPLICATION_PUBLIC_HTTP_URL=https://im.sdkwork.com
SDKWORK_IM_APPLICATION_PUBLIC_WEBSOCKET_URL=wss://im.sdkwork.com
SDKWORK_IM_PLATFORM_API_GATEWAY_HTTP_URL=https://api.sdkwork.com
SDKWORK_IM_DATABASE_ENGINE=postgresql
SDKWORK_IM_DATABASE_HOST=postgres.internal.example.com
SDKWORK_IM_DATABASE_PORT=5432
SDKWORK_CLAW_DATABASE_NAME=sdkwork
SDKWORK_CLAW_DATABASE_SCHEMA=public_chat_prod
SDKWORK_CLAW_DATABASE_USERNAME=sdkwork
SDKWORK_IM_DATABASE_PASSWORD_FILE=/etc/sdkwork/chat/database.secret
SDKWORK_IM_DATABASE_SSL_MODE=require
SDKWORK_IM_DATABASE_MAX_CONNECTIONS=20
SDKWORK_IM_LOG_LEVEL=info
SDKWORK_IM_BROWSER_ORIGINS=https://im.sdkwork.com
```

## 6. postgresql.yaml

`postgresql.yaml` �?��?�?�??�?��?�??�?�?��?�置�?保�?��??�??�?段�?

```yaml
provider: postgresql

connection:
  host: postgres.internal.example.com
  port: 5432
  database: sdkwork
  username: sdkwork
  passwordFile: /etc/sdkwork/chat/database.secret
  sslmode: require
  applicationName: sdkwork-chat-server
  connectTimeoutSeconds: 10

schema:
  name: sdkwork
  provisioningMode: none
  migrationMode: apply
  expectedVersion: latest

pool:
  minConnections: 5
  maxConnections: 20
  idleTimeoutSeconds: 300
  maxLifetimeSeconds: 1800
```

## 7. database.secret

�?码�??件只�??含�?码�?�身�?

```text
replace-with-generated-password
```

�?�?�建议�?

```bash
sudo chown root:sdkwork /etc/sdkwork/chat/database.secret
sudo chmod 0640 /etc/sdkwork/chat/database.secret
```

Windows Service �?��?��?�?�?确保�?��?�运�?账号�?`%ProgramData%/sdkwork/chat/database.secret` �??读�?�?�?��?�?��?�?��??�?��?�没�??读�?�?�?��??

## 8. �?证�?�置

Windows Service 示�?�?

```powershell
Test-Path "$env:ProgramData\sdkwork\chat\chat.toml"
Test-Path "$env:ProgramData\sdkwork\chat\server.env"
Test-Path "$env:ProgramData\sdkwork\chat\postgresql.yaml"
Test-Path "$env:ProgramData\sdkwork\chat\database.secret"
```

Linux 示�?�?

```bash
test -f /etc/sdkwork/chat/chat.toml
test -f /etc/sdkwork/chat/server.env
test -f /etc/sdkwork/chat/postgresql.yaml
test -f /etc/sdkwork/chat/database.secret
```

�?�据�?客�?�端�?证�?

```bash
PGPASSWORD="$(cat /etc/sdkwork/chat/database.secret)" \
psql "postgresql://sdkwork@postgres.internal.example.com:5432/sdkwork?sslmode=require"
```

## 9. 启�?��??�??�?

线�?�?��?使�?�?server �??�?��?��??�??�?��??�?��?�管�?�?��??管�?

```bash
bash bin/install-server.sh --instance default
bash bin/init-config-server.sh --instance default
bash bin/init-storage-server.sh --instance default --mode verify-only
bash bin/verify-server.sh --instance default
bash bin/install-service-server.sh --instance default
bash bin/start-server.sh --instance default --release
```

Windows Service 使�?�?`install-service-server.ps1` �??�?�并注�??`SdkworkImServer`�??�?�台启�?��?�systemd�?�launchd �??Windows Service �?�使�?��?�?�?�置�?�口�?`sdkwork-im-server --config <config-root>/chat.toml`�??

��������Ҫʹ�ñ��ؿ������`pnpm dev`��`pnpm dev:browser`��`pnpm dev:desktop`��`pnpm dev:browser:sqlite`��`pnpm dev:desktop:sqlite` �����ڿ������ţ����� PostgreSQL ���б���ʹ�÷���������� `/etc/sdkwork/chat/chat.toml`��

## 10. �?�?�?��?�??

- PostgreSQL 主�?�?��?份�?�恢复�?�?已�?�?��??
- �?�?�账号不�?��?级�?��?��??
- �?码�??件�?��?�?��??�?��?�Git�?��?�?�??�??�?��?�??
- �?�?�池�?�置�? PostgreSQL `max_connections` �??�?�?容�?��?��?��??
- `SDKWORK_IM_SERVER_BASE_URL` 使�?�?`/sdkwork/chat` 路�?��??
- `SDKWORK_IM_LOG_DIR` �??�? `/var/log/sdkwork/chat` �??平台�?价�?��?�?��?�??
- `sdkwork_im_BROWSER_ORIGINS` 只�??含可信�?�端�??名�??
- API �??WebSocket 使�?�?HTTPS/WSS�??

�?�?�?��?�??线�?�?��?不要�?��?��?�?个�?�置�??件�??线�?�?不要�?��?�复�?�?`.env.postgres` �?为�?��?��?�置�??
