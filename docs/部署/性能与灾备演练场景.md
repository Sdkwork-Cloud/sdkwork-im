# 性能与灾备演练场�?

## 1. 文档定位

本文件用于冻�?`Step 11 / CP11-1` 的执行基线，避免后续压测、排空、恢复和升级演练各自使用不同口径�?

- 机器可读清单：`tools/perf/step-11-scenario-catalog.json`
- 场景清单 schema：`tools/perf/schemas/step-11-scenario-catalog.schema.json`
- 当前高阶门禁 schema：`tools/perf/schemas/step-11-tier-gate.schema.json`
- 当前本地量化基线：`tools/perf/step-11-cp11-2-local-baseline.json`
- 当前本地演练基线：`tools/perf/step-11-cp11-3-local-drill-baseline.json`
- 当前 IM realtime core 商用门禁基线：`tools/perf/step-11-cp11-4-im-realtime-core-baseline.json`
- 当前 IM websocket E2E 商用门禁基线：`tools/perf/step-11-cp11-5-websocket-e2e-baseline.json`
- 当前 CP11-6 WebSocket E2E 预发布补充证据包：`artifacts/perf/step-11/pre-release/im-websocket-e2e/metrics.json`
- 当前预发布门禁模板：`tools/perf/step-11-pre-release-tier-gate.json`
- 当前容量门禁模板：`tools/perf/step-11-capacity-tier-gate.json`
- 当前高阶 gate 证据模板字段：`artifactRoot`、`collectionSummary`、`evidenceSlots`
- 当前 catalog 直接暴露的高阶证据根目录�?
  - `artifacts/perf/step-11/pre-release`
  - `artifacts/perf/step-11/capacity`
- 当前冻结目标：先统一场景、层级、指标和仓库资产映射，再进入 `CP11-2` �?`CP11-3`
- 当前不做的事：不在本轮新建独立压测框架，不把单次手工跑分冒充正式基线

## 2. 执行层级

| Tier | 用�?| 当前基线 | 何时算进入下一�?|
| --- | --- | --- | --- |
| `CI Smoke Tier` | 仓库内快速回归连接、消息、流、排空、恢复语�?| `self-hosted.split-services.development` 与定�?`cargo test` / smoke 脚本 | 形成稳定可重复的定量命令后进入预发布�?|
| `Pre-Release Tier` | 发布前验证中等规模并发、吞吐和故障演练 | `self-hosted.split-services.development` 或受控预发布拓扑 | 形成至少一轮定量结果与故障复盘后进入容量层 |
| `Capacity Tier` | 专用环境验证连接密度、吞吐上限和尾延�?| 独立容量环境或后�?cell/region 演练环境 | 输出容量报告与恢复报�?|

## 3. 场景�?

| 场景�?| 目标 | 主要指标 | 当前种子资产 |
| --- | --- | --- | --- |
| `connection` | 冻结握手、连接建立、多客户端命令流�?resume 基线 | 握手成功率、活跃连接数、resume 成功率、连�?p95 | `services/sdkwork-im-gateway/tests/websocket_e2e_test.rs`、`tools/chat-cli/tests/chat_cli_e2e_test.rs`、`tools/smoke/local_stack_smoke.ps1`、`tools/smoke/local_stack_smoke.sh` |
| `message` | 冻结路由消息投递与多客户端会话扇出吞吐基线 | 消息 TPS、投递成功率、扇�?p95、route transfer 成功�?| `services/sdkwork-im-gateway/tests/cluster_realtime_routing_e2e_test.rs`、`tools/chat-cli/tests/chat_cli_e2e_test.rs` |
| `stream` | 冻结流帧持久化、checkpoint �?replay-safe 吞吐基线 | 帧吞吐、checkpoint 成功率、stream replay 成功率、帧 p95 | `services/sdkwork-im-gateway/tests/stream_runtime_persistence_test.rs` |
| `im-realtime-core` | 冻结 IM 核心实时链路 fanout、窗口裁剪、checkpoint、补偿回滚与集群 handoff 商用门禁 | fanout success permille、publish p95/p99、publish TPS、ack p95、restore duration、compensation rollback、cluster handoff、capacity trimmed events | `services/session-gateway/tests/performance_realtime_core_baseline_test.rs`、`services/session-gateway/tests/commercial_realtime_acceptance_test.rs`、`tools/perf/step-11-cp11-4-im-realtime-core-baseline.json`；执行后输出 `STEP11_REALTIME_CORE` |
| `im-websocket-e2e` | 冻结真实 TCP WebSocket 长连接、订阅、live push、断线恢复、backlog restore、checkpoint �?route handoff 商用门禁 | connect success permille、live fanout success permille、connect p95、subscribe p95、live push p95、ack p95、disconnect recovery、backlog restore、cluster handoff、capacity trimmed events | `services/session-gateway/tests/performance_websocket_e2e_baseline_test.rs`、`services/session-gateway/tests/websocket_smoke_test.rs`、`tools/perf/step-11-cp11-5-websocket-e2e-baseline.json`；执行后输出 `STEP11_WEBSOCKET_E2E` |
| `drain-rebalance` | 冻结排空、路由迁移和 rebalance 演练基线 | 排空完成时间、route 迁移成功率、rebalance p95、route loss | `services/sdkwork-im-gateway/tests/cluster_drain_rebalance_e2e_test.rs`、`services/control-plane-api/tests/governance_loop_test.rs` |
| `restore-recovery` | 冻结备份预演、恢复和运行目录恢复时间基线 | restore 成功率、RTO、RPO、preview diff 准确�?| `services/sdkwork-im-gateway/tests/runtime_dir_restore_test.rs`、`services/sdkwork-im-gateway/tests/runtime_dir_restore_preview_test.rs` |
| `failover` | 冻结跨节�?`resume takeover` �?stale session 拒绝演练基线 | takeover 时延、owner 切换准确率、stale session 拒绝率、resume takeover 成功�?| `services/sdkwork-im-gateway/tests/performance_ha_dr_drill_test.rs`、`services/sdkwork-im-gateway/tests/cluster_realtime_routing_e2e_test.rs`、`tools/perf/step-11-cp11-3-local-drill-baseline.json` |
| `upgrade-rollback` | 冻结协议兼容、kill switch 和回滚演练基�?| compatibility 通过率、rollback 激活时间、kill switch 成功率、回滚后协议错误�?| `crates/sdkwork-im-ccp-registry/tests/compatibility_matrix_test.rs`、`services/control-plane-api/tests/protocol_registry_test.rs`、`services/control-plane-api/tests/protocol_governance_test.rs`、`services/sdkwork-im-gateway/tests/performance_ha_dr_drill_test.rs`、`tools/perf/step-11-cp11-3-local-drill-baseline.json` |

## 4. 当前推荐执行顺序

1. �?`CI Smoke Tier` 冻结六类场景的命令入口和结果字段�?
2. �?`Pre-Release Tier` 至少完成一�?`connection`、`message`、`stream` 的定量结果�?
3. 在同一部署基线上完�?`drain-rebalance`、`restore-recovery` �?`failover`�?
4. 最后执�?`upgrade-rollback`，避免兼容策略变动影响前两轮结果口径�?
5. 如果还没有真实结果，先使�?`tools/perf/step-11-pre-release-tier-gate.json` �?`tools/perf/step-11-capacity-tier-gate.json` 冻结所需证据字段，不把模板误写成结果�?
6. 高阶 gate 的模板态证据口径统一为：
   - `artifactRoot` 指向未来归档根目�?
   - `collectionSummary` 表达当前缺少多少槽位
   - `evidenceSlots[*].status = pending_collection`
   - `evidenceSlots[*].artifactPath / collectedAt / sizeBytes / checksumSha256 = null`
   - `evidenceSlots[*].suggestedRelativePath` 作为默认归档相对路径
7. 如果只读�?`tools/perf/step-11-scenario-catalog.json`，当前也必须能直接拿到高�?tier 的目录入口：
   - `Pre-Release Tier -> artifacts/perf/step-11/pre-release`
   - `Capacity Tier -> artifacts/perf/step-11/capacity`

## 5. 与后续检查点的关�?

- `CP11-1`
  - 完成本文件与 `tools/perf/step-11-scenario-catalog.json` 的冻�?
  - 明确层级、场景族、指标与仓库资产映射
- `CP11-2`
  - 基于本文件执�?`connection`、`message`、`stream` 三类至少一轮定量结�?
  - 当前最小可重复执行入口为：
    - `tools/perf/step-11-cp11-2-local-baseline.json`
    - `services/sdkwork-im-gateway/tests/performance_quant_baseline_test.rs`
- `CP11-3`
  - 基于同一部署基线执行 `drain-rebalance`、`restore-recovery`、`failover`、`upgrade-rollback`
  - 当前最小可重复执行入口为：
    - `tools/perf/step-11-cp11-3-local-drill-baseline.json`
    - `services/sdkwork-im-gateway/tests/performance_ha_dr_drill_test.rs`
  - 当前 `failover` 先以跨节�?`resume takeover` 路径验证 owner 切换�?stale owner 拒绝语义
  - 当前 `upgrade-rollback` 先以 control-plane governance snapshot �?runtime hello 协商验证 kill switch rollback�?
    覆盖 MQTT binding 回滚拒绝、`payload.cbor` capability 剥离�?safe binding 零协议错误率
- `CP11-4`
  - 基于 `tools/perf/step-11-cp11-4-im-realtime-core-baseline.json` 执行 `im-realtime-core` 商用门禁
  - 当前最小可重复执行入口为：
    - `services/session-gateway/tests/performance_realtime_core_baseline_test.rs`
    - `services/session-gateway/tests/commercial_realtime_acceptance_test.rs`
  - 执行 `cargo test -p session-gateway --test performance_realtime_core_baseline_test --offline -- --nocapture` 时输�?`STEP11_REALTIME_CORE`，用于回�?metrics artifact
- `CP11-5`
  - 基于 `tools/perf/step-11-cp11-5-websocket-e2e-baseline.json` 执行 `im-websocket-e2e` 商用门禁
  - 当前最小可重复执行入口为：
    - `services/session-gateway/tests/performance_websocket_e2e_baseline_test.rs`
    - `services/session-gateway/tests/websocket_smoke_test.rs`
  - 执行 `cargo test -p session-gateway --test performance_websocket_e2e_baseline_test --offline -- --nocapture` 时输�?`STEP11_WEBSOCKET_E2E`，用于回�?metrics artifact
  - 把定量结果、异常点、改进建议和是否回写架构的结论写�?`docs/review/`
- `CP11-6`
  - �?CP11-5 `STEP11_WEBSOCKET_E2E` 输出标准化为预发布补充证据路径：`artifacts/perf/step-11/pre-release/im-websocket-e2e/metrics.json`
  - 状态固定为 `supplement_collected_gate_blocked_pending_real_pre_release_run`
  - 覆盖真实 TCP WebSocket 长连接、订�?fanout、断线恢复、消息窗口裁剪、跨客户端路�?checkpoint、一致性补偿和 cluster route handoff
  - 该证据包仅用于补充预发布追踪，not full Pre-Release Tier sign-off；真实预发布拓扑压测仍需替换�?CI Smoke 来源 artifact

## 6. 当前边界

- 本文件只冻结执行基线，不声称已经完成任何容量结论�?
- 当前 `CP11-2` 的本地量化基线只覆盖 `CI Smoke Tier`，用于先产出一轮连接、消息、流的真实数量化结果�?
- 当前 `CP11-3` 的本地演练基线只覆盖 `CI Smoke Tier`，用于先产出一�?`drain-rebalance / restore-recovery / failover / upgrade-rollback` 的真实演练结果�?
- 当前 `Pre-Release Tier` �?`Capacity Tier` 已进入“模板可执行、结果待采集”状态：
  - `template_only_pending_execution`
  - `self-hosted.split-services.development` 仅作为预发布门禁模板的当前默�?profile
  - `capacity-dedicated` 仅作为容量门禁模板的目标环境名，不代表仓库内已有容量结果
  - `artifactRoot / collectionSummary / evidenceSlots` 只表达未来证据结构，不表达真实结果已存在
  - `pending_collection` 只表达槽位待采集，不表达任何 artifact 已落�?
  - `step-11-scenario-catalog.json` 现在也直接暴露：
    - `tools/perf/step-11-pre-release-tier-gate.json`
    - `tools/perf/step-11-capacity-tier-gate.json`
    - `artifacts/perf/step-11/pre-release`
    - `artifacts/perf/step-11/capacity`
- 当前 `upgrade-rollback` 的本�?drill 只验证最小可信路径：
  - control-plane registry �?governance snapshot 一�?
  - runtime hello 协商遵从 effective snapshot
  - kill switch 能阻断高风险 binding / capability，同时保留安全降级路�?
- 当前 `self-hosted.split-services.development` 仍沿�?`Step 10` 冻结的兼容运行合同，不额外引入新的本地拓扑�?
- Git Bash 独立命令帮助在当前宿主环境可能存在兼容问题，因此 Step 11 的主要放行证据仍以仓库内测试�?PowerShell/CMD 入口为主�?
