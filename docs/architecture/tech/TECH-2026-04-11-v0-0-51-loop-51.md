> Migrated from `docs/release/2026-04-11-v0.0.51-loop-51.md` on 2026-06-24.
> Owner: SDKWork maintainers

## Loop Status
- date: `2026-04-11`
- loop_id: `51`
- current_wave: `Wave-2`
- current_mode: `实施批次`
- current_steps: `S07`
- closure_level: `local_closure`

## Delivery
- 在 `conversation-runtime` 落地 `shared_channel_policy durable sync seam` 最小 runtime truth
- 新增 `SyncSharedChannelLinkedMemberCommand`
- 新增 `POST /im/v3/api/chat/conversations/shared_channel_links/sync`
- sync seam 只允许 `system` actor 调用，并把 shared durable truth 派生 payload materialize 成 `role = guest / state = linked` 的 runtime member
- linked member 会持久化 `sharedChannelPolicyId / externalConnectionId / externalMemberId` 锚点，并在 replay 后继续保留同一 truth

## TDD
- red:
  - `cargo test -p conversation-runtime --offline --test http_smoke_test test_sync_shared_channel_linked_member_over_http_materializes_linked_history_reader -- --nocapture`
  - 初始失败点为缺少 shared-channel sync HTTP surface，断言为 `left: 404` / `right: 200`
- green:
  - shared-channel sync HTTP 回归通过
  - runtime replay 回归通过

## Verification
- `cargo test -p conversation-runtime --offline --test http_smoke_test test_sync_shared_channel_linked_member_over_http_materializes_linked_history_reader -- --nocapture` = `passed`
- `cargo test -p conversation-runtime --offline --test conversation_flow_test test_sync_shared_channel_linked_member_materializes_runtime_truth_and_survives_recovery_replay -- --nocapture` = `passed`
- `cargo fmt -p conversation-runtime -p im-domain-core` = `passed`
- `cargo test -p conversation-runtime --offline --tests -- --nocapture` = `128 passed`

## Risks
- control-plane 当前仍不会自动触发这条 sync seam
- `external_member_link` durable truth 仍不携带 principal kind；当前 sync request 仍要求显式传入 `localActorKind`
- 尚未实现 per-user unread / notification level / follow-unfollow

## Backwrite
- `docs/step/122-S07-shared-channel-policy-durable-sync-loop51-current-checkpoint-2026-04-11.md`
- `docs/review/S07-Loop51补充-2026-04-11.md`
- `docs/架构/152CJ-Loop51补充-2026-04-11.md`
- `docs/release/2026-04-11-v0.0.51-loop-51.md`

## Next Loop Input
- next_step: `S07`
- main_gap: `control-plane -> conversation-runtime automatic sync trigger / auto-projection`
- verification_focus: `shared durable truth -> runtime sync trigger` `localActorKind bridge ownership` `fresh package evidence`

