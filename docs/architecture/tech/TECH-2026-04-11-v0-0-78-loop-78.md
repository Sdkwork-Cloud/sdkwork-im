> Migrated from `docs/release/2026-04-11-v0.0.78-loop-78.md` on 2026-06-24.
> Owner: SDKWork maintainers

# v0.0.78 - Loop 78 - 2026-04-11

## Summary
- Fixed stale owner metadata persistence in the shared-channel sync next-write retry path.
- A stale claimed pending request retried by a healthy ready-pair write no longer keeps obsolete owner metadata when that retry fails again.
- Existing active-claim ownership guard and unclaimed auto-retry behavior remain intact.

## Code
- `services/control-plane-api/src/lib.rs`
  - updated `record_failed_shared_channel_sync_requests(...)` to accept `now`
  - clears stale owner metadata before rewriting failed pending backlog
  - applies the same stale reclaim rule to runtime failure persistence, repair retry failure, and targeted republish retry failure
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_next_ready_pair_retry_failure_reclaims_stale_claim_metadata`
  - revalidated the Loop77 ownership and baseline auto-retry tests

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_ready_pair_retry_failure_reclaims_stale_claim_metadata -- --nocapture`
  - failure: the original stale pending item still serialized `ownerActorId`
- green:
  - stale owner metadata is reclaimed before the failed retry is re-persisted
  - adjacent next-write ownership behavior remains unchanged

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_ready_pair_retry_failure_reclaims_stale_claim_metadata -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_healthy_ready_pair_write_respects_active_pending_claim_ownership -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_backlog_retries_on_next_healthy_ready_pair_write -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `79 passed`

## Remaining Gap
- This still is not a scheduler.
- Proactive stale reclaim and timeout-based recovery remain deferred.
- `S07` remains `not_closed / local_closure`.

