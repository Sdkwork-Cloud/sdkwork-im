> Migrated from `docs/review/S09-执行卡-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S09 执行卡 — 2026-04-10

## 1. 架构对齐
- 对齐能力：`CCP schema / registry / compatibility / protocol governance / runtime policy consumption`
- 本轮新增兑现
  - `history_visibility` 发布面已收敛到真实支持的 `joined / world_readable`
  - `invited/shared` 已在 `ConversationPolicy::normalize()`、governance snapshot、HTTP 建会、runtime 读路径边界显式阻断
  - `S09` / `S10` 的准入口径已从“补齐四模式”收敛为“已发布即真实支持，未支持即阻断”
- 本轮仍后延：
  - `invited` 依赖 invitation durable truth，归 `S06/S07`
  - `shared` 依赖 shared-channel / external truth，归 `S12`
  - `retention_policy_ref` 运行归档闭环继续归 `S13`

## 2. Current / Target / Deferred
- Current State
  - `protocol-governance` 已发布 `policy_version / capability_flags / history_visibility / retention_policy_ref`
  - `im-domain-core::conversation` 已落地 `ConversationPolicy`、`policy_epoch`、capability 校验逻辑
  - `conversation-runtime` 已落地 `ApplyConversationPolicyCommand`、`conversation.policy_applied`、恢复回放、HTTP 建会后即时绑定策略
  - 当前真实 enforcement 已覆盖：
    - `capability_flags -> message.reaction / message.pin`
    - `history_visibility -> conversation history read`
  - 当前已具备稳定最小历史消息 `GET` 读面；`joined` 非成员读历史返回 `403`，`world_readable` 同租户已鉴权非成员读历史返回 `200`
  - 当前 governance 只发布 `historyVisibilityModes = joined / world_readable`
  - 当前 `invited/shared` 已被显式拒绝，不再以假语义对外发布；`retention_policy_ref` 仍是建模/校验/快照字段，不是闭环约束
- Target State
  - 已发布词汇与 runtime 实际支持面完全一致
  - `S10` 可基于 `S08 + S09` 的真实闭环进入下一轮实现批次
- Deferred
  - `invited` 真能力待 invitation truth 闭环后由 `S06/S07 + S09` 重发
  - `shared` 真能力待 shared-channel truth 闭环后由 `S12 + S09` 重发
  - `retention_policy_ref` archive/restore drill 继续归 `S13`
  - SDK 真实生成、版本冻结、发布闭环继续归 `S14`

## 3. 本轮目标 / 非目标
- 目标
  - 让已发布 `history_visibility` 词汇与 runtime 真相完全一致
  - 显式阻断 `invited/shared`，结束“已发布但未实现”的漂移
  - 形成可验证、可回写、可准入下一步的治理结论
- 非目标：
  - 不伪造 invitation/shared-channel runtime 语义
  - 不提前宣称 `retention_policy_ref` 已生效
  - 不把未来 truth owner 混入当前 step

## 4. 主写入范围
- 代码：`crates/im-domain-core/src/conversation.rs`、`crates/sdkwork-im-ccp-registry/src/lib.rs`、`services/conversation-runtime/src/runtime/policy.rs`、相关测试
- 文档：`docs/review/S09-*`、`docs/review/S10-*`、`docs/架构/152CJ-*`、`docs/release/*`
- 禁止并写：`docs/step/100-*`、`docs/step/101-*`、共享 step 主计划、无关业务聚合

## 5. 最小交付包
- 领域：`ConversationPolicy` 只接受 `joined/world_readable`，并拒绝 `invited/shared`
- 治理：`historyVisibilityModes` 只发布真实支持模式
- 运行时：建会不再接受未实现模式；读路径若 legacy `invited/shared` 快照返回显式 unsupported
- 已有能力保持：`message.reaction / message.pin` capability guard、`joined/world_readable` history read contract
- 证据：runtime / domain / control-plane / contract 相关测试 fresh 通过
- 回写：`review + 架构 + release` 与当前代码一致，并重开 `S10` 准入

## 6. 能力覆盖声明
- 已覆盖：
  - `CPR09-1`：业务策略词汇已冻结结论
  - `CPR09-2`：registry + governance snapshot 对外发布面已与 runtime 真相一致
  - `CPR09-3`：runtime/business model 绑定已完成主线收口
  - `CPR09-4`：已发布词汇已被 runtime 真实消费到写路径与历史读路径，未支持模式已显式阻断
- 未覆盖：
  - `invited` invitation durable truth
  - `shared` shared-channel / external durable truth
  - `retention_policy_ref` 的运行时/归档联动
  - 依赖 `S09 step_closure` 后 `S10` 准入

## 7. 验证
- `cargo test -p im-domain-core --offline`
- `cargo test -p conversation-runtime --offline --tests`
- `cargo test -p control-plane-api --offline --tests`
- `cargo test -p sdkwork-im-ccp-registry --offline`
- `cargo test -p im-platform-contracts --offline`
- 主证据：
  - `crates/im-domain-core/src/conversation.rs`
  - `crates/sdkwork-im-ccp-registry/src/lib.rs`
  - `services/conversation-runtime/src/runtime/policy.rs`
  - `crates/im-domain-core/tests/conversation_domain_builder_test.rs`
  - `crates/sdkwork-im-ccp-registry/tests/governance_snapshot_test.rs`
  - `services/control-plane-api/tests/protocol_governance_test.rs`
  - `services/conversation-runtime/tests/http_smoke_test.rs`
- 辅证据：
  - `services/conversation-runtime/src/runtime/governance.rs`
  - `services/conversation-runtime/src/runtime/http.rs`
  - `docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
  - `docs/review/S10-准入判断-2026-04-10.md`

## 8. 架构回写结论
- 结论：`实现更具体`
- 原因：
  - `S09` 不再只是“发布四 mode 的静态词汇”
  - 当前实现把已发布支持面收敛为真实支持的 `joined / world_readable`，并将 `invited/shared` 退回 future truth owner
  - 这是对旧 as-built 漂移的纠正，不是新能力虚报

## 9. 迁移 / 降级 / 回退
- 数据迁移：无
- 灰度：无
- 降级路径：若 fresh tests 失败，降回 `v0.0.18` `local_closure` 口径，并恢复 `S10` 阻塞
- 回退条件：治理快照、domain normalize、runtime HTTP runtime read 任一面与测试证据不一致

## 10. 检查点
- `CPR09-1`：词汇字段与枚举值稳定
- `CPR09-2`：`conversation.policy_applied` 可生成、可回放、可恢复落盘
- `CPR09-3`：registry / control-plane / runtime / tests 词汇口径一致
- `CPR09-4`：`capability_flags` 已真实拦截 capability 写操作；`history_visibility` 已形成 honest publish + honest runtime；`retention_policy_ref` owner 明确 deferred

## 11. 下一步准入
- 允许继续：`S10`
- 下一最小切片：
  - 冻结 `S10` 执行卡
  - 进入 `stream / rtc / media artifact lifecycle` 主线实现
  - 继续保持 `retention_policy_ref` 归 `S13`，不回流为 `S10` 阻塞
- Deferred 归属结论
  - `invited` 真能力：`S06/S07 + S09`
  - `shared` 真能力：`S12 + S09`
  - `retention_policy_ref` 运行归档：`S13`

## 12. 出口 6 问
- Q1 `95` 是否已判定本 step 对应能力闭环，而非局部补丁：`yes`
- Q2 `97` 是否已写清本轮已兑现与 deferred 的架构结论：`yes`
- Q3 是否已给出“无需回写 / 实现更具体 / 实现有偏移”三选一：`yes`
- Q4 未完成能力是否都已归属到后续 `Sxx` owner：`yes`
- Q5 是否具备 `1` 个主证据 + `1` 组辅证据：`yes`
- Q6 回退、降级、阻塞升级条件是否已写入并经 review：`yes`
- 结论：`S09 = step_closure`

