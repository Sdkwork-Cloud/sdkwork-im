> Migrated from `docs/review/S08-执行卡-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S08 执行卡 - 2026-04-10

## 1. 架构对齐
- 对齐文档：`docs/step/100-面向DDD的IM重构升级总实施计划-2026-04-09.md`、`docs/step/101-Step执行卡与并行交付模板-2026-04-10.md`、`docs/step/95-架构能力闭环验收标准.md`、`docs/step/97-Step完成后的架构回写与能力兑现清单.md`、`docs/架构/150CJ-im-social-space-conversation-ddd-design-2026-04-09.md`、`docs/架构/151CJ-im-benchmark-model-alignment-2026-04-09.md`、`docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
- 对齐能力：`projection / notification / read model / rebuild-doc`
- 本轮已兑现结论：`projection-service` 已消费 `friendship.activated + direct_chat.bound`，形成可查询、可快照恢复、不可反写 truth 的 `contacts read model`。
- 本轮显式排除：旧 `step-08 / cp08-*` 不作为新 `S08` 闭环证据；Loop-15 以 fresh evidence 完成 `reaction/pin summary` 收口。

## 2. Current / Target / Deferred
- Current State：`projection-service` 已覆盖 `timeline / inbox / read cursor / unread / member snapshot / client-route sync / handoff summary / member_directory / contacts`，并具备 snapshot restore、rebuild duration、live lag、update-delay 证据；`S05` 已提供 `friendship.activated / direct_chat.bound` 上游 truth。
- Target State：补齐 `reaction/pin summary`，把 `S08` 推进到 `step_closure`，并保证 `contacts / interaction` 不越界承担 truth。
- Deferred：`user_block` 继续挂 `S05`；stranger-DM contacts 因缺 `actor -> user` 投影语义，本轮不推导。

## 3. 本轮交付
- 已落地 `contacts read model`，按 `owner_user_id -> target_user_id` 产出双向好友联系人视图。
- 已消费 `friendship.activated` 建立联系人条目；已消费 `direct_chat.bound` 回填 `directChatId / conversationId / lastInteractionAt`。
- 已补齐最小闭环：HTTP 查询、乱序绑定回填、snapshot persist/restore、鉴权入口。

## 4. 本轮非目标
- 不把 `S08` 的 `step_closure` 误写为 social truth 闭环。
- 不伪造 `reaction/pin` truth 或投影输入。
- 不把 `projection-service` 反向升级为 social truth 写入面。
- 不把 `direct_chat.bound` 单独推导成 stranger-DM contacts truth。

## 5. 主写入范围
- 主目录：`services/projection-service`、`docs/review`、`docs/架构`、`docs/release`
- 主文件：`services/projection-service/src/contacts.rs`、`services/projection-service/src/http.rs`、`services/projection-service/src/access.rs`、`services/projection-service/src/model.rs`、`services/projection-service/src/snapshot.rs`、`services/projection-service/src/update_delay.rs`、`services/projection-service/tests/http_smoke_test.rs`、`services/projection-service/tests/projection_snapshot_test.rs`、`docs/review/S08-*`、`docs/review/S10-*`、`docs/架构/152CJ-*`、`docs/release/*`
- 禁止并写文件：`services/control-plane-api`、`services/notification-service`、`crates/im-domain-events`、`docs/step/100-*`、`docs/step/101-*`

## 6. owner 与冻结边界
- owner：`L4` 主写 `projection-service`；`LV` 负责 red/green 与包级回归；`L0` 负责 review / 架构 / release 回写。
- 冻结边界：`S08` 只消费既有 `friendship.activated / direct_chat.bound` 形成读模型，不新增 social truth；`S05` 继续负责 future social truth；`S07` 继续负责 `reaction/pin` truth。
- 触发串行收口条件：触碰 shared schema、durable truth、`docs/架构/152CJ-*`、`docs/release/CHANGELOG.md` 时必须串行。

## 7. 并行车道
| 车道 | owner | 写集 | 最小交付包 | 不可触碰 |
| --- | --- | --- | --- | --- |
| `L0` | `step owner` | `docs/review/S08-*`、`docs/review/S10-*`、`docs/架构/152CJ-*`、`docs/release/*` | 执行卡、review、release 记录 | 业务实现 |
| `L4` | `projection` | `services/projection-service` | contacts read model、HTTP 读口、snapshot restore | durable truth |
| `LV` | `验证与证据` | `services/projection-service/tests/*` | red/green、包级回归、阻塞结论 | 架构和 truth 模型 |

## 8. 本轮最小交付包
- 代码/文档：`contacts read model`、HTTP 读口、鉴权入口、snapshot restore、`S08/S10/152CJ/release` 回写。
- 测试：TDD 先 red，再 green，并 fresh 重跑 `cargo test -p projection-service --offline --tests` 与 `cargo test -p notification-service --offline --tests`。
- review：补齐 `S08` 质量审计、架构兑现决议、`S10` 准入判断、新 loop release。
- evidence：contacts query、direct-chat enrich、snapshot restore、`lib.rs < 1000` 红线保持成立。
- CPR：
  - `CPR08-4A`：contacts query 闭环。
  - `CPR08-4B`：contacts direct-chat enrich 闭环。
  - `CPR08-4C`：contacts snapshot restore 闭环。

## 9. 能力覆盖声明
- 已覆盖能力：`timeline / inbox / read cursor / unread badge / member snapshot / client-route sync / handoff summary / member_directory / contacts / reaction/pin summary / pinned-message list`
- 显式后延能力：`stranger-DM contacts 推断`
- 后延原因：缺 `actor -> user` 投影语义，不能把 `direct_chat.bound` 反推为联系人 truth。
- 后续 owner：
  - `S05`：继续补 `user_block` 与更完整 social truth

## 10. 测试与验证
- 自动化测试：
  - `cargo test -p projection-service --offline --tests` -> `46 passed`
  - `cargo test -p notification-service --offline --tests` -> `18 passed`
- 主证据路径：`services/projection-service/tests/http_smoke_test.rs`、`services/projection-service/tests/projection_snapshot_test.rs`
- 辅证据路径：`services/projection-service/src/contacts.rs`、`services/projection-service/src/http.rs`、`services/projection-service/src/snapshot.rs`、`services/projection-service/src/update_delay.rs`

## 11. 架构回写结论
- 结论：`实现更具体`
- docs/架构：`docs/架构/152CJ-current-architecture-as-built-alignment-2026-04-09.md`
- docs/step：无
- docs/review：`docs/review/S08-*`、`docs/review/S10-*`
- 受影响章节：`152CJ` 的 social as-built 与 projection as-built
- 必回写细节：`contacts` 与 `reaction/pin summary` 已成为 `projection-service` 的显式 as-built 能力；`S08` 当前已满足 `step_closure`

## 12. 迁移 / 降级 / 回退
- 数据迁移：无
- 灰度或双写：无；`contacts` 必须继续保持派生态
- 降级路径：若 contacts rebuild 或绑定回填不稳定，只保留 `S05` truth 与事件出口，不放行 `S08` / `S10`
- 回退条件：一旦 `S08` 需要反向写 truth、绕过事件出口或把 stranger-DM 推断写成事实，立即阻塞本 step

## 13. 检查点
- `CPR08-2A`：member directory query 证据齐全，`done`
- `CPR08-2B`：member directory snapshot restore 证据齐全，`done`
- `CPR08-2C`：contacts / reaction-pin 阻塞与 owner 写清，`done`
- `CPR08-4A`：contacts query 证据齐全，`done`
- `CPR08-4B`：contacts direct-chat enrich 证据齐全，`done`
- `CPR08-4C`：contacts snapshot restore 证据齐全，`done`
- `CPR08-5`：`reaction/pin summary` 代码、测试、回写、验证齐全，`done`

## 14. 下一步准入
- 允许启动的下一动作：并行窗口继续推进 `S05 user_block`；主线转入 `S09` 收口并为 `S10` 准入清障
- 仍需阻塞的事项：`S10` 正式启动与收口仍待 `S09 step_closure`
- Deferred 归属结论：`S05 user_block -> S05`；`S10 准入 -> S09`

## 15. 出口 6 问
- Q1 `95` 是否已判定本 step 对应能力已闭环，而非局部补丁：`no`
- Q2 `97` 是否已写清本轮已兑现与仍 deferred 的架构结论：`yes`
- Q3 是否已给出“无需回写 / 实现更具体 / 实现有偏移”三选一结论：`yes`
- Q4 未完成能力是否都已明确归属到后续 `Sxx` 或 backlog owner：`yes`
- Q5 是否至少具备 `1` 个主证据和 `1` 组辅证据：`yes`
- Q6 回退、降级、阻塞升级条件是否已写入并可 review：`yes`
- 结论：`step_closure；CPR08-5 已完成，S08 当前 step 契约已闭环`

