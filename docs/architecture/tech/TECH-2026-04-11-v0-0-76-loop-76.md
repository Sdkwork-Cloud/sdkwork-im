> Migrated from `docs/release/2026-04-11-v0.0.76-loop-76.md` on 2026-06-24.
> Owner: SDKWork maintainers

# v0.0.76 - Loop 76 - 2026-04-11

## Summary
- Hardened `shared-channel sync` repair so stale pending claim ownership is reclaimed before repair dispatch.
- Added machine-readable `reclaimed` visibility to both the repair response and repair audit payload.
- Locked the stale-repair contract with a dedicated regression test.

## Code
- `services/control-plane-api/src/lib.rs`
  - `SocialSharedChannelSyncRepairResponse` now includes `reclaimed`
  - `repair_shared_channel_sync(...)` now reclaims stale claim ownership before dispatch
  - repair audit payload now includes `reclaimed`
  - `SocialSharedChannelSyncLeaseStatus` derives `PartialEq/Eq`
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - `test_control_plane_social_shared_channel_repair_reclaims_stale_claim_before_dispatch` now proves:
    - pre-repair inventory sees `leaseStatus = stale`
    - repair returns `reclaimed = 1`
    - repair empties the pending backlog

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_repair_reclaims_stale_claim_before_dispatch -- --nocapture`
  - initial failure: `repair_json["reclaimed"] = null`
- green:
  - stale-aware repair response now reports `reclaimed = 1`
  - repair audit payload now includes `reclaimed`

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_repair_reclaims_stale_claim_before_dispatch -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `77 passed`

## Remaining Gap
- This is not a scheduler.
- Stale claim reclaim now happens during operator repair, but automatic timeout reclaim and proactive stale detection are still deferred.
- `S07` remains `not_closed / local_closure`.

