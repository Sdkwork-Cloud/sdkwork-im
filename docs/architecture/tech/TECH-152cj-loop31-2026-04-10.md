> Migrated from `docs/架构/152CJ-Loop31补充-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 152CJ Loop31补充 - 2026-04-10

## 1. 本轮新增事实
- `control-plane-api` social runtime 已统一到单一 `SocialControlState`。
- 新增 `social-state.json` durable snapshot。
- 新增 `social-commit-journal.json` social outbox。
- `sdkwork_im_RUNTIME_DIR` 命中时自动启用 file-backed social store。

## 2. 对当前架构真相的影响
- `S05` 不再只是“control-plane 最小写链路 + 进程内内存态”。
- 当前已具备 file-backed durable 样板，可在重建后恢复 social snapshot。
- 当前仍不能把 `S05` 写成 `step_closure`，因为 tx / replay 仍未闭环。

## 3. 当前口径
- `S05 = not_closed / local_closure`
- `release_closure = no`
- 下一主刀：`social-commit-journal` replay 恢复与事务边界硬化

