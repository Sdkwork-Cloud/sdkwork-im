> Migrated from `docs/review/S07-Loop30补充-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S07 Loop30补充 - 2026-04-10

## 1. 本轮新增
- 兑现 `direct_chat -> conversation` runtime binding。

## 2. 已落地事实
- 领域新增 `ConversationBusinessBinding`。
- runtime 新增 `BindDirectChatConversationCommand`。
- HTTP 新增：
  - `POST /im/v3/api/chat/conversations/direct-chats/bindings`
  - `GET /im/v3/api/chat/conversations/{conversationId}/binding`
- binding 要求显式 `left/right actor kind`，避免把 actor 语义塌缩成固定 `user`。
- replay 后仍保持 `direct_chat` 绑定与唯一索引。

## 3. fresh verification
- `cargo test -p conversation-runtime --offline --tests -- --nocapture` -> `119 passed`
- `cargo test -p im-domain-core --offline --tests -- --nocapture` -> `36 passed`

## 4. 已关闭与未关闭
- 已关闭：`direct_chat -> conversation` runtime binding
- 仍未关闭：
  - `thread`
  - `shared / external`
  - `invited / shared` history visibility runtime path
  - retention enforcement / archive-restore owner coupling

## 5. 当前结论
- `S07 = local_closure`
- 还不能升为 `step_closure`

