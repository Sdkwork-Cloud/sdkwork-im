> Migrated from `docs/release/2026-04-11-v0.0.80-loop-80.md` on 2026-06-24.
> Owner: SDKWork maintainers

# v0.0.80 - Loop 80 - 2026-04-11

## Summary
- Fixed stale owner metadata restoration during shared-channel sync dead-letter requeue.
- Dead-letter items now return to pending backlog as `unclaimed`.
- Existing failure-budget rearm and targeted requeue isolation behavior remain intact.

## Code
- `services/control-plane-api/src/lib.rs`
  - updated `requeue_selected_dead_letter_shared_channel_sync_requests(...)` to clear owner metadata before restoring pending backlog
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_dead_letter_requeue_reclaims_stale_claim_metadata`
  - revalidated dead-letter rearm and targeted requeue baselines

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_requeue_reclaims_stale_claim_metadata -- --nocapture`
  - failure: requeued pending item still serialized `ownerActorId`
- green:
  - requeued dead-letter item is restored as `unclaimed`
  - failure budget reset and targeted requeue behavior remain intact

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_requeue_reclaims_stale_claim_metadata -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_requeue_rearms_failure_budget_before_next_repair_attempt -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_inventory_and_targeted_requeue_only_restores_selected_actor -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `81 passed`

## Remaining Gap
- This still is not automatic timeout reclaim.
- Scheduler-driven stale recovery remains deferred.
- `S07` remains `not_closed / local_closure`.

