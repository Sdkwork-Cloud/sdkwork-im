> Migrated from `docs/架构/152CJ-Loop34补充-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 152CJ Loop34补充 - 2026-04-10

- `S05` runtime-dir social durable truth 已从 `snapshot + replay` 收敛到 `journal authority + journal-first persistence。
- startup recovery 当前顺序为：
  - `if social-commit-journal.json exists -> replay from default -> rewrite social-state.json`
  - `else -> load social-state.json`
- 持久化当前顺序为：
  - `append social-commit-journal.json`
  - `save social-state.json`
- 因此 `snapshot ahead of journal` 不再能在重启后形成 phantom truth；`friend_request` 与 `direct_chat pair guard` 已有 TDD 证据。
- 当前仍缺 `atomic tx boundary + snapshot-save failure repair evidence + operator-grade replay/repair`，所以 `S05` 仍只能标记为 `not_closed / local_closure`。


