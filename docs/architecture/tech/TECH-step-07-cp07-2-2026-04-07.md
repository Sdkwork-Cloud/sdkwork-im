> Migrated from `docs/review/step-07-cp07-2-执行卡-2026-04-07.md` on 2026-06-24.
> Owner: SDKWork maintainers

# Step 07 / CP07-2 执行- 2026-04-07

## 当前上下

- 当前波次：`Wave C`
- 当前 step：`Step 07`
- 当前子任务：`CP07-2`
- 前置状态：
  - `CP07-1` 已完
  - `CP07-2 / CP07-3 / CP07-4` 未完
  - `Step 07` 还不能进`91 / 95 / 97`

## 本轮为什么做这个子任

- `docs/step/07-控制面与协议治理落地.md` 明确要求 `CP07-2` 先把 `capability profile / quota / rollout / kill switch` 落成真实治理模型
- 只有把治理模型与 `effective snapshot` 先冻结出来，`CP07-3` 才能runtime 改为只读消费控制面结果：

## 本轮实际完成

- `crates/sdkwork-im-ccp-registry` 新增控制面治理类型：
  - `ReleaseChannel`
  - `CapabilityProfile`
  - `QuotaProfile`
  - `RolloutPolicy`
  - `KillSwitchRule`
  - `EffectiveProtocolSnapshot`
  - `ProtocolGovernanceSnapshot`
- `CcpRegistry::control_plane_v1()` 现在会冻结一份治baseline
  - stable capability profile
  - default quota profile
  - global stable rollout policy
  - emergency kill switch
  - precedence 顺序
- `services/control-plane-api` 新增 `GET /backend/v3/api/control/protocol-governance`
  - `control.read / control.write` 权限保护
  - 输出 capability/quota/rollout/kill switch/effective snapshot

## 改动范围

- 代码
  - `crates/sdkwork-im-ccp-registry/src/lib.rs`
  - `services/control-plane-api/src/lib.rs`
- 测试
  - `crates/sdkwork-im-ccp-registry/tests/governance_snapshot_test.rs`
  - `services/control-plane-api/tests/protocol_governance_test.rs`

## TDD 证据

### Red

- `cargo test -p sdkwork-im-ccp-registry --test governance_snapshot_test --offline --target-dir target-step07-cp072-red-registry -- --exact test_control_plane_registry_freezes_governance_snapshot_for_runtime_consumers`
- `cargo test -p control-plane-api --test protocol_governance_test --offline --target-dir target-step07-cp072-red-api -- --exact test_control_plane_exposes_protocol_governance_snapshot_to_control_readers`

失败点与预期一致：

- registry 尚无 `governance_snapshot()`
- control-plane 尚无 `/backend/v3/api/control/protocol-governance`

### Green

- `cargo test -p sdkwork-im-ccp-registry --test governance_snapshot_test --offline --target-dir target-step07-cp072-green-registry -- --exact test_control_plane_registry_freezes_governance_snapshot_for_runtime_consumers`
- `cargo test -p control-plane-api --test protocol_governance_test --offline --target-dir target-step07-cp072-green-api -- --exact test_control_plane_exposes_protocol_governance_snapshot_to_control_readers`

## 回归验证

- `cargo test -p sdkwork-im-ccp-registry --offline --target-dir target-step07-cp072-final-registry`
- `cargo test -p control-plane-api --offline --target-dir target-step07-cp072-final-control-plane`
- `cargo test -p im-platform-contracts --test ccp_foundation_smoke_test --offline --target-dir target-step07-cp072-final-platform-smoke`
- `rustfmt --edition 2024 --check crates/sdkwork-im-ccp-registry/src/lib.rs crates/sdkwork-im-ccp-registry/tests/compatibility_matrix_test.rs crates/sdkwork-im-ccp-registry/tests/governance_snapshot_test.rs services/control-plane-api/src/lib.rs services/control-plane-api/tests/protocol_registry_test.rs services/control-plane-api/tests/protocol_governance_test.rs services/control-plane-api/tests/drain_routes_test.rs`

## 结论

- `CP07-2`：已闭环
- `Step 07`：仍未闭环

## 当前还差什

- `CP07-3`：runtime 只读消费 `effective snapshot`
- `CP07-4`：admin auth / audit / ops 治理闭环
- `91 / 95 / 97`：整步未满足
- `Wave C / 93`：尚不能启动

## 下一轮继续做什

1. 直接进入 `CP07-3`
2. 优先清理 `crates/sdkwork-im-runtime-link/src/lib.rs` 中本地硬编码hello capability baseline
3. runtime control-plane 冻结结果读取 `effective snapshot`

