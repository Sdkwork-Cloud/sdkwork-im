> Migrated from `docs/review/S07-Loop84补充-2026-04-12.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S07 Loop84 Addendum - 2026-04-12

## 1. Main Conclusion
- Loop84 extends stale-lease normalization into the targeted pending-republish failure path.
- A same-owner stale republish attempt no longer falls back to `unclaimed` just because the dispatch failed while the old lease had already expired.
- No `deployment_profile`, script entry, or operator surface changes were introduced in this loop.

## 2. What Changed
- `services/control-plane-api/src/lib.rs`
  - `republish_pending_shared_channel_sync_targeted(...)` now refreshes lease timestamps for same-owner stale pending requests before dispatch
  - failed dispatch persistence now sees an active lease and keeps owner metadata
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_pending_republish_renews_stale_lease_for_same_operator_on_failure`
  - re-ran pending-claim ownership, same-owner stale claim renewal, and dead-letter republish baselines

## 3. TDD Notes
- Red started with a pending request already claimed by `u_operator_a`.
- The test then forced `leaseExpiresAt` into the past and called `republish-pending-shared-channel-sync-targeted` as the same operator against a failing trigger.
- Failure proved the gap directly:
  - republish returned `pending`
  - the pending request lost `claimedAt / leaseExpiresAt`
  - inventory fell back to `unclaimed`
- Green after the fix:
  - stale same-owner republish renews the lease first
  - failed dispatch persistence keeps owner metadata
  - inventory returns `leaseStatus = active`

## 4. Fresh Evidence
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_republish_renews_stale_lease_for_same_operator_on_failure -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_enforces_targeted_republish_ownership -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_renews_stale_lease_for_same_operator -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_targeted_republish_dead_letter_reclaims_claim_metadata -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `85 passed`

## 5. Remaining Gap
- `S07` remains `not_closed / local_closure`.
- The next smallest remaining gap is `trigger-unconfigured targeted republish` still returning early without stale lease normalization.
- Background timeout reclaim / scheduler work is still deferred.

