> Migrated from `docs/release/2026-04-11-v0.0.74-loop-74.md` on 2026-06-24.
> Owner: SDKWork maintainers

# v0.0.74 - Loop 74 - 2026-04-11

## 本轮目标
- 把 `shared-channel sync` 从“dead-letter inventory metadata parity 主要依赖共享 helper 的隐式事实”推进到“dead-letter inventory metadata parity 被 dedicated assertion 直接锁定”的最小 contract

## 本轮实现
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - `test_control_plane_social_shared_channel_dead_letter_inventory_and_targeted_requeue_only_restores_selected_actor` 当前新增 dead-letter inventory parity assertions
  - 当前显式锁定 `leaseStatus = unclaimed`
  - 当前显式锁定 `takeoverEligible = false`
  - 当前显式锁定 `legacyTakeoverRequired = false`
  - 当前显式锁定 `ownerActorId / ownerActorKind / claimedAt / leaseExpiresAt = null`
- 本轮复核结论：
  - pending/dead-letter inventory 当前已通过共享 helper 返回同一套 lease/takeover metadata
  - 本轮没有改 runtime 行为；补的是 direct regression lock

## TDD / Verification
- green：
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_inventory_and_targeted_requeue_only_restores_selected_actor -- --nocapture`

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_dead_letter_inventory_and_targeted_requeue_only_restores_selected_actor -- --nocapture` = `passed`

## 风险与未闭环
- 当前还没有 richer claim batch remediation / suggested-action contract
- 当前没有 automatic stale detection / timeout reclaim / scheduler
- 当前 `repair-shared-channel-sync` 仍不是 stale-aware policy
- 当前没有 release-ready exactly-once semantics
- `https://` public runtime target 仍未进入本轮 truth

## 结论
- `S07` 仍是 `not_closed / local_closure`
- Loop74 已把 `dead-letter metadata parity` 从隐式事实推进成显式回归契约
- 下一主缺口继续收敛为 `claim richer batch remediation / suggested-action contract`

