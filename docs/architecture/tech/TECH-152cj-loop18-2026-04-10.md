> Migrated from `docs/架构/152CJ-Loop18补充-2026-04-10.md` on 2026-06-24.
> Owner: SDKWork maintainers

# 152CJ Loop-18 补充 - 2026-04-10

## As-Built 增量
- `conversation-runtime` 已落地最小历史读取面：`GET /im/v3/api/chat/conversations/{conversation_id}/messages`
- runtime 已新增 auth-context 历史查询入口与 `MessageHistoryResult`
- `history_visibility` 已真实进入读路径：
  - `joined`：限制为活跃成员
  - `world_readable`：放开到同租户已鉴权非成员

## 未闭环项
- `invited/shared` 仍未形成独立 artifact 与读语义
- `retention_policy_ref` 尚未进入 archive/restore/retention owner 闭环

## 架构判断
- `S09` 获得更强的 `local_closure`
- `S09` 仍不是 `step_closure`
- `S10` 阻塞不变，但阻塞原因已进一步收敛为：
  - `history_visibility` 四模式未完整闭环
  - `retention_policy_ref` 仍无 owner 闭环

