> Migrated from `docs/review/S07-Loop83补充-2026-04-12.md` on 2026-06-24.
> Owner: SDKWork maintainers

# S07 Loop83 Addendum - 2026-04-12

## 1. Main Conclusion
- Loop83 closes a stale-lease consistency gap on the targeted pending-claim surface.
- A same-owner claim that succeeds after the previous lease has already expired now renews `claimedAt / leaseExpiresAt` instead of returning success with stale lease metadata.
- No `deployment_profile`, script entry, or operator surface changes were introduced in this loop.

## 2. What Changed
- `services/control-plane-api/src/lib.rs`
  - `PendingSharedChannelSyncRequest::assign_owner(...)` now refreshes lease timestamps when the same owner re-claims a stale pending request
  - active same-owner repeat claims still preserve the existing active lease
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_pending_claim_renews_stale_lease_for_same_operator`
  - re-ran pending-claim ownership, foreign takeover, and unconfigured repair reclaim baselines

## 3. TDD Notes
- Red started with a pending request already claimed by `u_operator_a`.
- The test then forced `leaseExpiresAt` into the past and called `claim-pending-shared-channel-sync-targeted` again as the same operator.
- Failure proved the gap directly:
  - claim still returned `status = claimed`
  - `claimedAt` stayed unchanged
  - inventory still exposed `leaseStatus = stale`
- Green after the fix:
  - same-owner stale re-claim refreshes both timestamps
  - inventory returns to `leaseStatus = active`
  - foreign takeover and repair reclaim baselines remain intact

## 4. Fresh Evidence
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_renews_stale_lease_for_same_operator -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_enforces_targeted_republish_ownership -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_takeover_transfers_claim_to_foreign_operator -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_repair_reclaims_stale_claim_when_trigger_is_unconfigured -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `84 passed`

## 5. Remaining Gap
- `S07` remains `not_closed / local_closure`.
- The main remaining gap is still `automatic stale detection / timeout reclaim / scheduler`.
- Stale ownership normalization is stronger than before, but it still depends on explicit operator or path-local trigger points instead of a background timeout sweep.

