# v0.0.86 - Loop 86 - 2026-04-12

## Summary
- Fixed the proactive stale-claim normalization gap on the shared-channel sync next-write path.
- An ordinary next ready-pair write now reclaims older stale pending claim metadata before system dispatch / queue evaluation, even when no shared-channel trigger is configured.
- Existing operator-triggered reclaim, same-owner stale renewal, and retry-failure reclaim behavior remain intact.

## Code
- `services/control-plane-api/src/lib.rs`
  - added proactive stale pending-claim reclaim before `dispatch_shared_channel_sync_requests(...)` evaluates trigger availability or dispatch queue state
  - refactored stale pending reclaim persistence into a reusable helper so operator reclaim and system dispatch share the same durability path
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_next_write_reclaims_stale_claim_metadata_when_trigger_is_unconfigured`
  - revalidated the full `social_external_collaboration_test` regression file
- No `deployment_profile`, script entry, or operator route changed in this loop.

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_write_reclaims_stale_claim_metadata_when_trigger_is_unconfigured -- --nocapture`
  - failure: the second ordinary write left the original stale claimed pending item serialized with stale owner metadata
- green:
  - next-write dispatch now reclaims stale pending claim metadata before queue evaluation
  - the older backlog item stays pending but returns to the `unclaimed` pool

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_write_reclaims_stale_claim_metadata_when_trigger_is_unconfigured -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test -- --nocapture` = `31 passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `87 passed`

## Remaining Gap
- This still is not idle-time timeout reclaim.
- No background scheduler or automatic stale sweep exists when the system is idle.
- `S07` remains `not_closed / local_closure`.
