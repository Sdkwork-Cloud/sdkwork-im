# v0.0.85 - Loop 85 - 2026-04-12

## Summary
- Fixed the trigger-unconfigured stale-lease normalization gap on the shared-channel sync targeted pending-republish path.
- Same-owner stale republish now refreshes `claimedAt / leaseExpiresAt` before returning `trigger_unconfigured`, so the pending request no longer remains stale just because no trigger is installed.
- Existing dispatch-enabled renewal, owner boundary, and repair/claim stale-normalization behavior remain intact.

## Code
- `services/control-plane-api/src/lib.rs`
  - updated `republish_pending_shared_channel_sync_targeted(...)` so same-owner stale republish renews lease metadata before the `TriggerUnconfigured` early return and persists the refreshed state
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_pending_republish_renews_stale_lease_for_same_operator_when_trigger_unconfigured`
  - revalidated the full `social_external_collaboration_test` regression file
- No `deployment_profile`, script entry, or operator route changed in this loop.

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_republish_renews_stale_lease_for_same_operator_when_trigger_unconfigured -- --nocapture`
  - failure: same-owner stale republish returned `trigger_unconfigured`, but left `claimedAt / leaseExpiresAt` unchanged and inventory `leaseStatus = stale`
- green:
  - same-owner stale republish now renews lease metadata before the unconfigured early return
  - the refreshed pending state is persisted even when no trigger exists

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_republish_renews_stale_lease_for_same_operator_when_trigger_unconfigured -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test -- --nocapture` = `30 passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `passed`

## Remaining Gap
- This still is not proactive timeout reclaim.
- No background scheduler or automatic stale sweep exists yet.
- `S07` remains `not_closed / local_closure`.
