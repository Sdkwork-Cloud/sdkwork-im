# v0.0.83 - Loop 83 - 2026-04-12

## Summary
- Fixed a stale-lease renewal gap on the shared-channel sync targeted pending-claim surface.
- Same-owner claim on a stale pending request now refreshes `claimedAt / leaseExpiresAt` and returns inventory to `leaseStatus = active`.
- Existing pending-claim ownership, foreign takeover, and unconfigured repair reclaim behavior remain intact.

## Code
- `services/control-plane-api/src/lib.rs`
  - updated `PendingSharedChannelSyncRequest::assign_owner(...)` so same-owner stale re-claim refreshes lease metadata
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_pending_claim_renews_stale_lease_for_same_operator`
  - revalidated pending-claim ownership, takeover, and unconfigured repair reclaim baselines
- No `deployment_profile`, script entry, or operator route changed in this loop.

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_renews_stale_lease_for_same_operator -- --nocapture`
  - failure: claim succeeded, but `claimedAt` stayed unchanged and inventory remained `leaseStatus = stale`
- green:
  - same-owner stale claim now refreshes both lease timestamps
  - the pending inventory item becomes `active`

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_renews_stale_lease_for_same_operator -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_claim_enforces_targeted_republish_ownership -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_takeover_transfers_claim_to_foreign_operator -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_repair_reclaims_stale_claim_when_trigger_is_unconfigured -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `84 passed`

## Remaining Gap
- This still is not background timeout reclaim.
- Scheduler-driven stale recovery remains deferred.
- `S07` remains `not_closed / local_closure`.
