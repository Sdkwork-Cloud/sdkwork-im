## Loop Status
- date: `2026-04-11`
- loop_id: `50`
- current_wave: `Wave-2`
- current_mode: `实施批次`
- current_steps: `S07`
- closure_level: `local_closure`

## Delivery
- 在 `projection-service` 与 `notification-service` 落地 `shared fanout / rebuild` 最小 runtime truth
- `projection-service` 新增 message-posted recipient seam，返回 `joined active members + shared-history-visible linked members`
- `notification-service::request_message_posted_notifications(...)` 已切到该 seam，shared linked principal 会进入 message-posted 通知目标集合
- projection replay / rebuild 继续复用 `ConversationMember` 投影记录，不需要新增 rebuild 专用存储模型

## TDD
- red:
  - `cargo test -p notification-service --offline --test notification_pipeline_test test_request_message_posted_notifications_includes_shared_linked_recipients_from_projection -- --nocapture`
  - 初始失败点为 shared linked recipient 未进入 fanout，断言为 `left: 1` / `right: 2`
  - `cargo test -p projection-service --offline --test timeline_projection_test test_message_posted_notification_principal_ids_from_auth_context_includes_shared_linked_members -- --nocapture`
  - 初始失败点为缺少 `message_posted_notification_principal_ids_from_auth_context` seam
- green:
  - shared linked recipient fanout 与 projection recipient seam 回归通过

## Verification
- `cargo test -p notification-service --offline --test notification_pipeline_test test_request_message_posted_notifications_includes_shared_linked_recipients_from_projection -- --nocapture` = `passed`
- `cargo test -p projection-service --offline --test timeline_projection_test test_message_posted_notification_principal_ids_from_auth_context_includes_shared_linked_members -- --nocapture` = `passed`
- `cargo fmt -p notification-service -p projection-service` = `passed`
- `cargo test -p projection-service --offline --tests -- --nocapture` = `47 passed`
- `cargo test -p notification-service --offline --tests -- --nocapture` = `19 passed`

## Risks
- `shared_channel_policy` durable truth 仍未自动投影为 runtime linked members
- 尚未实现 per-user unread / notification level / follow-unfollow
- 尚未引入独立 `im_thread_subscription` durable model

## Backwrite
- `docs/step/121-S07-shared-fanout-rebuild-runtime-truth-loop50-current-checkpoint-2026-04-11.md`
- `docs/review/S07-Loop50补充-2026-04-11.md`
- `docs/架构/152CJ-Loop50补充-2026-04-11.md`
- `docs/release/2026-04-11-v0.0.50-loop-50.md`

## Next Loop Input
- next_step: `S07`
- main_gap: `shared_channel_policy auto-projection / durable sync`
- verification_focus: `control-plane durable truth -> runtime linked member materialization` `shared policy replay alignment` `fresh package evidence`
