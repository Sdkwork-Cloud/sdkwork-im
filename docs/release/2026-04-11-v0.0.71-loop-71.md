# v0.0.71 - Loop 71 - 2026-04-11

## 本轮目标
- 把 `shared-channel sync` 从“takeover 冲突写路径只返回 `code/message`”推进到“takeover 冲突写路径显式回写 machine-readable `details`”的最小 contract

## 本轮实现
- `services/control-plane-api/src/lib.rs`
  - `ControlPlaneError` 新增可选 `details`
  - active foreign takeover conflict 当前显式返回 machine-readable `details`
  - legacy override-required conflict 当前也显式返回 machine-readable `details`
  - `details` 当前至少覆盖 `requestKey / ownerActorId / leaseExpiresAt / leaseStatus / takeoverEligible / legacyTakeoverRequired`
  - operator 在 takeover 被拒绝时，当前可直接消费写路径冲突细节，而不必再回头读取 inventory
  - 本轮没有改变 takeover allow/deny 语义；补的是 error surface symmetry
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - 现有 takeover lifecycle 当前升级为同时断言 active conflict 与 legacy override-required conflict 的 `details`
  - 同一条 lifecycle 继续锁定 active conflict、legacy override gate、stale takeover success 与 owner republish guard 共存

## TDD
- red：
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_takeover_transfers_claim_to_foreign_operator -- --nocapture`
  - 初始失败点为 `active_takeover_json["details"]["requestKey"] = null`
- green：
  - active conflict 当前显式回写完整 `details`
  - legacy override-required conflict 当前也显式回写完整 `details`
  - control-plane package 全量回归继续通过

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_takeover_transfers_claim_to_foreign_operator -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `75 passed`

## 风险与未闭环
- dead-letter inventory 当前仍缺 dedicated metadata parity 断言
- conflict `details` 当前只覆盖 takeover path，尚未扩展到 claim/release conflict symmetry
- 当前没有 automatic stale detection / timeout reclaim / scheduler
- 当前 `repair-shared-channel-sync` 仍不是 stale-aware policy
- 当前没有 release-ready exactly-once semantics
- `https://` public runtime target 仍未进入本轮 truth

## 结论
- `S07` 仍是 `not_closed / local_closure`
- Loop71 已把 `shared-channel sync` 补齐到 `durable pending backlog + operator repair + pending inventory + targeted pending republish + pending targeted claim + republish ownership guard + pending targeted release + pending claimedAt visibility + pending targeted takeover + pending leaseExpiresAt visibility + pending stale-lease takeover guard + pending/dead-letter inventory leaseStatus visibility + pending/dead-letter inventory takeoverEligible visibility + legacy untracked takeover explicit override + takeover conflict machine-readable details symmetry + next healthy ready-pair write auto-retry + repeated-failure dead-letter + dead-letter inventory + dead-letter targeted/all requeue + failure-budget rearm`
- 下一主缺口收敛为 `pending/dead-letter response parity for lease/takeover metadata`
