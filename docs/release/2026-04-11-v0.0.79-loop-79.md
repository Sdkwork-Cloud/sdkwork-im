# v0.0.79 - Loop 79 - 2026-04-11

## Summary
- Added an explicit stale pending-claim reclaim surface for shared-channel sync.
- Operators can now clear stale ownership and return backlog items to the unclaimed pool without dispatching them.
- Existing repair reclaim, stale takeover, and next-write stale retry behavior remain intact.

## Code
- `services/control-plane-api/src/lib.rs`
  - added `SocialSharedChannelSyncPendingStaleReclaimStatus`
  - added `SocialSharedChannelSyncPendingStaleReclaimResponse`
  - added runtime reclaim method and `POST /api/v1/control/social/runtime/reclaim-stale-pending-shared-channel-sync`
  - added audit payload for explicit stale reclaim
- `services/control-plane-api/tests/social_external_collaboration_test.rs`
  - added `test_control_plane_social_shared_channel_stale_claim_reclaim_surface_clears_owner_metadata`
  - revalidated adjacent stale repair, stale takeover, and next-write stale retry tests

## TDD
- red:
  - `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_stale_claim_reclaim_surface_clears_owner_metadata -- --nocapture`
  - failure: the new operator route returned `404`
- green:
  - stale claim reclaim returns `reclaimed = 1`
  - the pending item stays in backlog as `unclaimed`

## Fresh Verification
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_stale_claim_reclaim_surface_clears_owner_metadata -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_repair_reclaims_stale_claim_before_dispatch -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_pending_takeover_transfers_claim_to_foreign_operator -- --nocapture` = `passed`
- `cargo test -p control-plane-api --offline --test social_external_collaboration_test test_control_plane_social_shared_channel_next_ready_pair_retry_failure_reclaims_stale_claim_metadata -- --nocapture` = `passed`
- `cargo fmt -p control-plane-api` = `passed`
- `cargo test -p control-plane-api --offline --tests -- --nocapture` = `80 passed`

## Remaining Gap
- This is still manual reclaim, not a scheduler.
- Automatic timeout reclaim remains deferred.
- `S07` remains `not_closed / local_closure`.
