-- IM database module baseline anchor migration (L2).
-- Canonical DDL lives in database/ddl/baseline/postgres/0001_im_legacy_baseline.sql
-- and is applied by sdkwork-database-cli bootstrap for baseline-plus-migrations profiles.
-- This versioned pair exists for lifecycle history, drift observation, and release gates.

SELECT 1;
