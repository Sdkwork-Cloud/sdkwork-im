import type { ImRealtimeModule } from './realtime-module.js';
import type { ImSdkContext } from './sdk-context.js';
import type { ImInternalRealtimeBatch, ImInternalReceiverDataEvent, ImInternalReceiverEvent, ImInternalReceiverMessageEvent, ImInternalReceiverRtcSignalEvent, ImInternalRealtimeEventsAckedFrame, ImInternalRealtimeSubscriptionsSyncedFrame, ImInternalRealtimeWebSocketReceiverOptions, ImInternalRealtimeWindowFrame } from './receiver-internal-types.js';
import type { ImRealtimeConnectedFrame, ImRealtimeErrorFrame, ImSubscription, ImWebSocketCloseEventLike, QueryParams, RealtimeAckState, RealtimeSubscriptionItemInput, RealtimeSubscriptionSnapshot, SyncRealtimeSubscriptionsRequest } from './types.js';
export declare class ImWebSocketReceiver {
    private readonly realtime;
    private readonly context;
    private readonly receiver;
    private readonly openHandlers;
    private readonly closeHandlers;
    private readonly socketErrorHandlers;
    private readonly connectedHandlers;
    private readonly windowHandlers;
    private readonly syncedHandlers;
    private readonly ackedHandlers;
    private readonly realtimeErrorHandlers;
    private readonly pendingRequests;
    private readonly options;
    private socket?;
    private cleanupSocketListeners;
    private connectPromise?;
    private isConnected;
    private requestSequence;
    private connectResolve?;
    private connectReject?;
    private ccpSessionResumeEnabled;
    constructor(realtime: ImRealtimeModule, context: ImSdkContext, options?: ImInternalRealtimeWebSocketReceiverOptions);
    onOpen(handler: (event: unknown) => void): ImSubscription;
    onClose(handler: (event: ImWebSocketCloseEventLike | unknown) => void): ImSubscription;
    onSocketError(handler: (event: unknown) => void): ImSubscription;
    onConnected(handler: (frame: ImRealtimeConnectedFrame) => void): ImSubscription;
    onWindow(handler: (frame: ImInternalRealtimeWindowFrame) => void): ImSubscription;
    onSubscriptionsSynced(handler: (frame: ImInternalRealtimeSubscriptionsSyncedFrame) => void): ImSubscription;
    onAcked(handler: (frame: ImInternalRealtimeEventsAckedFrame) => void): ImSubscription;
    onRealtimeError(handler: (frame: ImRealtimeErrorFrame) => void): ImSubscription;
    onEvent(handler: (event: ImInternalReceiverEvent) => void): ImSubscription;
    onMessageEvent(handler: (event: ImInternalReceiverMessageEvent) => void): ImSubscription;
    onDataEvent(handler: (event: ImInternalReceiverDataEvent) => void): ImSubscription;
    onRtcSignalEvent(handler: (event: ImInternalReceiverRtcSignalEvent) => void): ImSubscription;
    onScope(scopeType: string, scopeId: string | number, handler: (event: ImInternalReceiverEvent) => void): ImSubscription;
    connect(): Promise<this>;
    close(code?: number, reason?: string): void;
    syncSubscriptions(body: SyncRealtimeSubscriptionsRequest | RealtimeSubscriptionItemInput[]): Promise<RealtimeSubscriptionSnapshot>;
    pullWindow(params?: QueryParams): Promise<ImInternalRealtimeWindowFrame>;
    ackWindow(batchOrSeq: ImInternalRealtimeBatch | number): Promise<RealtimeAckState>;
    private resolveSocket;
    private attachSocket;
    private handleSocketOpen;
    private handleSocketMessage;
    private handleSocketClose;
    private handleSocketError;
    private unwrapBusinessFrame;
    private handleCcpControlFrame;
    private sendBusinessFrame;
    private sendCcpControlFrame;
    private sendJson;
    private requireSocket;
    private createPendingRequest;
    private resolvePendingRequest;
    private rejectPendingRequest;
    private cleanupPendingRequests;
    private createRequestId;
    private resolveConnect;
    private rejectConnect;
    private resetSocketState;
}
//# sourceMappingURL=websocket-receiver.d.ts.map