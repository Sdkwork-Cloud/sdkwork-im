"$schema" = "schemas/schema.json"

[[permission]]
identifier = "allow-sdkwork-chat-pc-window-control"
description = "Allows the SDKWork Chat PC auth AppHeader to minimize, maximize, hide to tray, and restore the main desktop window through the native shell."
commands.allow = ["sdkwork_chat_pc_window_control"]
